# STEP_0 CODING

task_id=4d201a98-b7a6-4029-9be8-9407c4942f50; status=COMPLETE; build=ALL_GREEN

## files_changed

- `features/ui/wishlist/src/commonMain/kotlin/ui/UserWishlistsViewModel.kt`: added `isOwnerState: StateFlow<Boolean>` (MutableRedeliverStateFlow(false), set on every load/resume via `model.getCurrentUserId() == node.config.userId`) and `onCreateWishlist()` which delegates to `interactor.onCreateWishlist(node)` via `scope.launchLoggingDropExceptions`.
- `features/ui/wishlist/src/commonMain/kotlin/ui/UserWishlistsViewInteractor.kt`: added `suspend fun onCreateWishlist(node: NavigationNode<UserWishlistsViewConfig, ViewConfig>)`.
- `client/src/commonMain/kotlin/ClientPlugin.kt`: UserWishlistsViewInteractor impl gained `onCreateWishlist` = `node.chain.push(WishlistEditViewConfig(null))` (opens create form).
- `features/ui/wishlist/src/jsMain/kotlin/ui/UserWishlistsView.kt`: header row now `BackButton` + right-aligned inner `Div(d-flex align-items-center gap-2 ms-auto)` containing (when isOwner) `btn btn-primary` create button (label `WishlistStrings.createWishlistButton`) then the existing `btn btn-outline-primary` Profile button.
- `features/ui/wishlist/src/jvmMain/kotlin/ui/UserWishlistsView.kt`: header `Row(SpaceBetween)` now has trailing inner `Row(spacedBy(8.dp))` with conditional create `Button` (same label string, existing `createWishlistButton` key reused — no new strings) before the Profile `Button`.
- `features/ui/wishlist/src/androidMain/kotlin/ui/UserWishlistsView.kt`: header `Row(SpaceBetween)` now has trailing inner `Row(spacedBy(8.dp))` with conditional create `Button` (same label string, existing `createWishlistButton` key reused — no new strings) before the Profile `Button`.

## behavior

Owner-gated "New Wishlist" button visible in the header row of the all-items screen (`UserWishlistsView`) on all three platforms (JS/JVM/Android), placed before the Profile button. Button rendered only when `isOwnerState` is true (authenticated caller == target user). Tapping the button navigates to the wishlist create form (`WishlistEditViewConfig(null)`) via the interactor.

## build_results

- `:wishlist.features.ui.wishlist:build` = SUCCESS
- `:wishlist.client:compileKotlinJs` = SUCCESS
- `:wishlist.client:compileKotlinJvm` = SUCCESS
- `:wishlist.client:compileDebugKotlinAndroid` = SUCCESS
- `ast-index rebuild` = SUCCESS (5 files indexed)

## readme

`features/ui/wishlist/README.md` updated:
- Models table, `UserWishlistsViewModel` row: appended `isOwnerState: StateFlow<Boolean>` and `onCreateWishlist()`.
- Models table, `UserWishlistsViewInteractor` row: appended `suspend fun onCreateWishlist(node)` (impl: push `WishlistEditViewConfig(null)`).
- Screens table, All Items row: added sentence describing the New Wishlist button in the header row before Profile, shown only when caller is displayed user.
- Architecture Notes: added "All-items create button" bullet explaining owner gating mirrors `WishlistsListView` pattern; navigation via interactor → `WishlistEditViewConfig(null)`.
- Operator Notes section untouched.
