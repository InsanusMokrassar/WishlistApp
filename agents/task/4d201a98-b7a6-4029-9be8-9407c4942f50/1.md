# STEP_1 CODING

task_id=4d201a98-b7a6-4029-9be8-9407c4942f50; depends_on=STEP_0; status=COMPLETE; build=ALL_GREEN

## files_changed

- `features/ui/wishlist/src/commonMain/kotlin/ui/UserWishlistsViewModel.kt`: removed `.filter { it.items.isNotEmpty() }` from sections loading via `getUserWishlists(userId).map { UserWishlistsSection(it, getWishlistItems(it.id)) }` — all wishlists now included; added `onCreateItem(wishlist: RegisteredWishlist)` which delegates to `interactor.onCreateItem(node, wishlist.id)` via `scope.launchLoggingDropExceptions`.
- `features/ui/wishlist/src/commonMain/kotlin/ui/UserWishlistsViewInteractor.kt`: added `suspend fun onCreateItem(node: NavigationNode<UserWishlistsViewConfig, ViewConfig>, wishlistId: WishlistId)`.
- `client/src/commonMain/kotlin/ClientPlugin.kt`: UserWishlistsViewInteractor impl gained `onCreateItem` = `node.chain.push(WishlistItemEditViewConfig(null, wishlistId))` (opens item create form for target wishlist).
- `features/ui/wishlist/src/jsMain/kotlin/ui/UserWishlistsView.kt`: section header row now contains title (weighted left), owner-gated `Div(d-flex align-items-center gap-2)` with `btn btn-sm btn-primary` Add Item button (label `WishlistStrings.addItemButton`, calls `viewModel.onCreateItem(section.wishlist)`) and existing `btn btn-sm btn-outline-primary` Open button.
- `features/ui/wishlist/src/jvmMain/kotlin/ui/UserWishlistsView.kt`: extracted private `@Composable SectionHeader(section: UserWishlistsSection, isOwner: Boolean)` composable (title weighted left; trailing `Row(spacedBy(8.dp))` with conditional Add Item `Button` + Open `Button`; `Divider()` after) shared by both grid and list branches. Calls on Add Item → `viewModel.onCreateItem(section.wishlist)`.
- `features/ui/wishlist/src/androidMain/kotlin/ui/UserWishlistsView.kt`: extracted private `@Composable SectionHeader(section: UserWishlistsSection, isOwner: Boolean)` composable (title weighted left; trailing `Row(spacedBy(8.dp))` with conditional Add Item `Button` + Open `Button`; `HorizontalDivider()` after) shared by both grid and list branches. Calls on Add Item → `viewModel.onCreateItem(section.wishlist)`.

## behavior

Empty wishlists now visible in all-items screen; each section renders `emptyItems` placeholder ("No items yet") when `section.items.isEmpty()`. Each section header shows owner-gated "Add Item" button (before "Open" button); button visible only when `isOwnerState` true (caller == target user). Tapping Add Item navigates to item create form (`WishlistItemEditViewConfig(null, wishlistId)`) via interactor, pre-scoped to that wishlist. JVM/Android platforms share extracted `SectionHeader` composable for grid and list modes.

## build_results

- `:wishlist.features.ui.wishlist:build` = SUCCESS
- `:wishlist.client:compileKotlinJs` = SUCCESS
- `:wishlist.client:compileKotlinJvm` = SUCCESS
- `:wishlist.client:compileDebugKotlinAndroid` = SUCCESS
- `ast-index rebuild` = SUCCESS (6 files indexed)

## readme

`features/ui/wishlist/README.md` updated:
- Screens table, All Items row: replaced "wishlists with no items omitted" with empty wishlists shown as section with `emptyItems` placeholder; added description of owner-gated Add Item button in each section header (before Open button) opening item create form for that wishlist.
- Models table, `UserWishlistsViewModel` row: fixed section loading logic (`.filter` removed, all wishlists included); appended `onCreateItem(wishlist)`.
- Models table, `UserWishlistsViewInteractor` row: appended `suspend fun onCreateItem(node, wishlistId)` (impl: push `WishlistItemEditViewConfig(null, wishlistId)`).
- Architecture Notes, "All-items view:" section: updated to state empty sections visible with `emptyItems` placeholder; added sub-bullet describing header row composition (owner-gated Add Item + Open buttons; JS flex group, JVM/Android extracted `SectionHeader` composable shared by grid/list branches; calls on Add Item → item create form).
- Operator Notes section untouched.
