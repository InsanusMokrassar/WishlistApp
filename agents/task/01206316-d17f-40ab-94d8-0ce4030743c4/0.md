# STEP_0 — Planning

task_id=01206316-d17f-40ab-94d8-0ce4030743c4; role=root/ORCHESTRATOR; branch=issue/29-book-functionality; PR=31
intent=apply 8 operator review comments (2nd review) on booking feature; code-only; no commit/push/PR.

## review_points → resolution

1. rename BookingFeature.book→tryBook. files=BookingFeature.kt(interface), KtorBookingFeature.kt(impl), ui/booking/Plugin.kt(BookingModel.bookItem call site). NOTE: server BookingService.book also rename→tryBook (point5 mentions tryBook). routing call site updated.
2. rename BookingFeature.cancel→cancelBooking. files=BookingFeature.kt, KtorBookingFeature.kt, ui/booking/Plugin.kt.
3. BookingState 2-boolean DTO → sealed interface @Serializable, 3 cases: Free, Booked, BookedByMe. update:
   - server BookingService.getState builds Free/Booked/BookedByMe (no UserId leak).
   - client BookingFeature.getState returns BookingState? unchanged.
   - ui/booking BookingViewModel + 3 BookingView (js/jvm/android) consume `when(state){Free/Booked/BookedByMe}`.
4. booking/common/jvmMain/JVMPlugin.kt:27 — split CacheBookingRepo into own single + bind BookingRepo via get<CacheBookingRepo>(). pattern: `single { CacheBookingRepo(...) }` + `single<BookingRepo> { get<CacheBookingRepo>() }`. drop singleWithBinds.
5. BookingService add SmartRWLocker field; getState under withReadAcquire; tryBook/cancel under withWriteLock. protects single-active-booking in-process.
6. extract BookingResult/BookResult/CancelResult sealed ifaces from BookingService.kt into features/booking/common/src/commonMain/kotlin/models/. import in service + routing configurator. semantics unchanged.
7. ui/booking BookingView android:49 — booking action control(s) in a Row. apply compactness js/jvm/android parity. (combined with point8: view trimmed to single button/text — see point8.)
8. ARCHITECTURAL: WishlistItemView (3 platforms) — draw provider views INLINE via InjectNavigationChain(id)+InjectNavigationNode(config), NOT button-push.
   - remove per-provider button + onAdditionalConfig path (VM.onAdditionalConfig, interactor.onAdditionalConfig, ClientPlugin impl).
   - WishlistAdditionalConfigsProvider: replace buttonLabel(StringResource) with chainId(NavigationChainId) for unique chain per provider; keep createConfig.
   - BookingConfigsProvider supplies stable chainId.
   - BookingView trimmed: VERY COMPACT, single book/cancel button or short text in a Row. remove BackButton/title/TopBarTitleProvider scaffolding (inline embed, no own screen chrome). onBack/BackButton no longer needed for inline; keep BookingViewInteractor? onBack still used by VM.onBack — inline has no back; trim onBack from VM+view but keep interactor file (still registered). DECISION: keep interactor binding to avoid Koin missing-dep; VM keeps onBack unused OR remove. SAFER: keep VM.onBack + interactor (registered in ClientPlugin) untouched; just stop rendering BackButton in inline view. Minimizes blast radius.

## constraints
- 4 issue#29 rules intact: 401 (authenticate{}), 403 (owner OwnerForbidden), booker-anonymous (BookingState no UserId; BookedByMe boolean-equivalent case), 409 (unique index + now SmartRWLocker).
- JS Bootstrap; localized EN+RU for changed labels (no new labels needed — reuse BookingStrings).
- build modules: booking.common/server/client, ui.booking, ui.wishlist, wishlist.server, client JS+JVM, android assembleDebug. rebuild ast-index.

## BookingState shape (decided)
```
@Serializable sealed interface BookingState {
  @Serializable object Free : BookingState
  @Serializable object Booked : BookingState      // booked by someone else (anonymous)
  @Serializable object BookedByMe : BookingState
}
```
server getState: booking==null→Free; booking.userId==caller→BookedByMe; else→Booked.

## inline injection (decided)
WishlistItemView per platform:
```
viewModel.additionalConfigsProviders.forEach { provider ->
    InjectNavigationChain<ViewConfig>(id = provider.chainId) {
        InjectNavigationNode(provider.createConfig(item))
    }
}
```
item = loaded RegisteredWishlistItem (non-null branch). provider.chainId unique per provider.

## open risks
- InjectNavigationNode inside a Row/Column composable — supported (ScaffoldView does inside Box/Column).
- chainId must be NavigationChainId; provider iface in commonMain of ui/wishlist; NavigationChainId from dev.inmo.navigation.core.
- BookingView inline: must still work as full node (BookingViewModel resolves node via parametersOf(view)). Provider node = BookingView. compactness via Row + drop fillMaxSize.

status=plan_complete; next=STEP_1 Architecturing
