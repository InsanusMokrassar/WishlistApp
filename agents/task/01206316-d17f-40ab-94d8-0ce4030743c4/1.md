# STEP_1 — Architecturing

task_id=01206316-d17f-40ab-94d8-0ce4030743c4

## verified facts
- sealed @Serializable iface pattern present (Booking.kt). data object cases serialize via closed polymorphism, no manual polymorphic registration needed.
- NavigationChainId = dev.inmo.navigation.core.NavigationChainId (constructor NavigationChainId("string")).
- BookingState consumers: BookingViewModel (StateFlow<BookingState?>), 3 BookingView, server BookingService.getState, client passthrough. Plugin.kt BookingModel passthrough (BookingState? not destructured).
- additionalConfigsProviders registered singleWithRandomQualifier<WishlistAdditionalConfigsProvider>{BookingConfigsProvider()}; injected via getAllDistinct() into WishlistItemViewModel.

## file-level change set

### point1+2+5 server BookingService.book→tryBook, cancel stays cancel (server method name not in review; only CLIENT iface rename). 
RE-READ point1/2: rename targets = BookingFeature (client iface) `book`→`tryBook`, `cancel`→`cancelBooking`. point5 text says "mutations (book/tryBook, cancel)". Server method names NOT mandated to rename. DECISION: rename CLIENT BookingFeature.book→tryBook, cancel→cancelBooking + KtorBookingFeature impl + BookingModel call sites (ui/booking/Plugin.kt). Server BookingService keeps book/cancel (point6 keeps semantics; point5 wraps locker around them). routing configurator server-side untouched names.

### models (point3 + point6)
- BookingState.kt: rewrite → sealed interface + Free/Booked/BookedByMe.
- NEW models/BookingResults.kt: move BookingResult, BookResult, CancelResult (extracted from BookingService.kt).

### server (point5 + point6)
- BookingService.kt: add `private val locker = SmartRWLocker()`; getState→withReadAcquire; book→withWriteLock; cancel→withWriteLock; remove inline sealed result decls (now imported from common models); getState builds Free/Booked/BookedByMe.
- BookingRoutingsConfigurator.kt: imports BookingResult/BookResult/CancelResult from common.models instead of server.services.

### booking/common jvmMain (point4)
- JVMPlugin.kt: `single { CacheBookingRepo(originalRepo=get<ExposedBookingRepo>(), scope=get()) }` + `single<BookingRepo> { get<CacheBookingRepo>() }`. remove singleWithBinds import.

### client (point1+2)
- BookingFeature.kt: book→tryBook, cancel→cancelBooking (iface + KDoc).
- KtorBookingFeature.kt: impl rename.

### ui/booking (point1+2 call sites, point3 consume, point7+8 compact view)
- Plugin.kt: bookingFeature.book→tryBook, bookingFeature.cancel→cancelBooking.
- BookingViewModel.kt: `when(state){Free/Booked/BookedByMe}` not needed in VM (VM only holds state). onBook calls model.bookItem (unchanged), onCancelBooking model.cancelBooking (unchanged). KEEP onBack (still bound).
- BookingView x3 (js/jvm/android): trim to COMPACT inline — drop BackButton, drop title H6/Text scaffolding+TopBarTitleProvider? KEEP TopBarTitleProvider iface impl (harmless; node may render in top bar elsewhere) but inline view body becomes compact Row with single button/text. Layout `when(state)`:
  - Free → Book button
  - BookedByMe → Cancel button (+ short text)
  - Booked → short text only
  Wrap in Row (android/jvm Row, js d-flex). Remove Column fillMaxSize/padding(16). Keep loading-disable.
  DECISION on back/title: inline embed has no back nav + no own top bar. REMOVE BackButton render + remove TopBarTitleProvider (since embedded inline, not a routed screen with top bar). VM.onBack becomes unused → remove onBack from VM + BookingViewInteractor + ClientPlugin binding? RISK: MyPresentsView? No, separate. BookingViewInteractor only used by BookingView. SAFER MINIMAL: remove TopBarTitleProvider+BackButton+onBack+BookingViewInteractor binding+interactor param. BookingViewModel ctor (node, model, interactor) → (node, model). Plugin factory `BookingViewModel(it.get(), get(), get())`→`(it.get(), get())`. Remove BookingViewInteractor.kt + ClientPlugin BookingViewInteractor single + imports.
  COUNTER-RISK: BookingViewInteractor referenced elsewhere? only ClientPlugin + VM. safe to remove.
  FINAL DECISION: remove BookingViewInteractor entirely (file, ClientPlugin binding, VM param, VM.onBack). View loses BackButton + TopBarTitleProvider.

### ui/wishlist (point8)
- WishlistAdditionalConfigsProvider.kt: replace `val buttonLabel: StringResource` → `val chainId: NavigationChainId`. keep createConfig.
- BookingConfigsProvider.kt: remove buttonLabel; add `override val chainId = NavigationChainId("wishlistItemAdditionalConfig_booking")`. drop BookingStrings import.
- WishlistItemViewModel.kt: remove onAdditionalConfig fn; keep additionalConfigsProviders list. remove now-unused imports.
- WishlistItemViewInteractor.kt: remove onAdditionalConfig method.
- ClientPlugin.kt: remove onAdditionalConfig override in WishlistItemViewInteractor impl.
- WishlistItemView x3: replace per-provider Button{onAdditionalConfig} with InjectNavigationChain(id=provider.chainId){ InjectNavigationNode(provider.createConfig(it)) }. imports InjectNavigationChain/InjectNavigationNode. `it` = loaded item (non-null branch).

## READMEs to update (haiku-equivalent: minimal factual edits)
- features/booking/README.md: BookingState shape (Free/Booked/BookedByMe); results moved to models; JVMPlugin own-single wiring; SmartRWLocker guard.
- features/ui/booking/README.md: BookingView compact inline (no back/title); client iface tryBook/cancelBooking; interactor removed.
- features/ui/wishlist/README.md: item view draws provider configs INLINE via InjectNavigationChain/Node; provider has chainId not buttonLabel; onAdditionalConfig removed.

## build order
booking.common → booking.server → booking.client → ui.booking → ui.wishlist → wishlist.server → client(js+jvm) → android assembleDebug. then ast-index update.

status=arch_complete; next=STEP_2 Coding
