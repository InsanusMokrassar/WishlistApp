Model: GPT-5.6 Sol (high-level planning model, preferred by agents/PLAN.md)
Changed files: agents/task/20.09.2026_07.45.16-690c5d8d-8049-4561-a685-49f067bff3d9/011-planning.md

## Task understanding and questions

Validation step 010 rejected the first implementation for three concrete defects: a partial or stale Chromium cache can bypass installation, broad console filtering can hide an unexpected authenticated 401, and server descendants can survive failure cleanup. Cycle 2 should repair those defects without changing the established Kotlin/JVM test module, isolated SQLite fixture, public `./gradlew browserTest` gate, or test intent. No unresolved requirement requires operator input, so no operator question was asked or answered.

## Findings that drive the correction

In `browser-tests.gradle`, `installBrowserChromium.onlyIf` accepts any `chromium-*` directory in `browserTests/build/playwright`, including another revision or a directory without an executable; the test task then sets `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1`. The previous passing runs in `009-verification.md` used an already complete cache. The existing `browserTests/src/test/kotlin/dev/inmo/wishlist/browser/BrowserFixture.kt` launches Playwright-managed headless Chromium, which also needs the matching headless-shell installation. Architecture step 002 already called for a version-matched launch probe and repair. Keep the cache private to the browser test module and derive expected paths or launch capability from the pinned Playwright Java dependency rather than guessing revision numbers or folder prefixes.

`browserTests/src/test/kotlin/dev/inmo/wishlist/browser/ServedWebSmokeTest.kt` currently ignores any console error containing `server responded with a status of 401` or `NoTransformationFoundException`, through the entire anonymous and registration tests. The authenticated path can therefore pass despite a different failed request. The tests have access to Playwright response URLs/statuses and can track when the page leaves the anonymous bootstrap phase; use those observations to constrain any unavoidable expected anonymous error. In `browser-tests.gradle`, the normal finalizer destroys descendant processes gracefully but waits only for the parent, while its forced kill and the `buildFinished` hook target only the parent. Readiness failure also force-kills only the parent. All paths should call the same bounded process-tree shutdown helper before deleting the generated suite directory.

## Plan for Architecture and Coding

Replace the directory-prefix `onlyIf` check with an execution-time browser availability probe in the pinned Playwright Java classpath. The probe should ask Playwright for the expected Chromium executable and attempt an actual headless launch, then close browser and Playwright. A successful probe skips install; a failed probe invokes the pinned `com.microsoft.playwright.CLI install chromium` with forced repair if the CLI supports it, then repeats the probe. Architecture should confirm the pinned CLI's force option or define a targeted deletion/reinstall of only the expected incomplete Chromium and headless-shell revisions. A probe failure after repair must fail the task with a useful cache/OS-dependency diagnostic. The Gradle task must not claim up-to-date solely because a cache directory exists; its probe/install remain inert during `help`, `build`, and other unrelated commands. Retain the private cache and `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1` for the test JVM after provisioning. Avoid deleting unrelated browser revisions or any global cache.

Exercise provisioning with an empty private cache, a deliberately incomplete expected-revision directory or missing headless-shell executable, and a cache containing only a wrong revision. For each state, the documented `./gradlew browserTest` must repair/provision and pass. Repeat once with the complete cache and verify the installer is skipped. Also verify `./gradlew help` does not probe or install. Keep these destructive cache fixtures confined to a copy or the dedicated `browserTests/build/playwright` path; never mutate the operator's global Playwright cache.

In `ServedWebSmokeTest.kt`, attach `page.onResponse` before navigation and treat unexpected API 401 responses as failures, including during the registration/login transition and after authentication. Identify the exact existing anonymous bootstrap endpoint from a browser/network observation, and allow only that URL with status 401 while the page is still in the anonymous bootstrap phase. Any console allowance should likewise be restricted to the observed message, known endpoint where attributable, and that phase; broad substring filtering throughout `withPage` must go. Flip the phase before submitting registration, so a registration or authenticated request yielding 401 fails even if the UI later appears. Preserve uncaught page error detection. Add a focused negative check using an injected or test-served unexpected authenticated 401 response, or a unit-tested error classifier driven by a representative response URL/status and phase; prove it is recorded while the known anonymous bootstrap response is allowed. Ensure a failing check still retains screenshot and trace.

Extract one process-tree shutdown routine used by readiness failure, the `finalizedBy` task, and the build-finished hook. Snapshot descendants while the parent is alive, request graceful termination from leaves toward parent, wait with a bounded deadline for every captured process, then force-kill all survivors and await them again. Resnapshot while possible to catch children spawned during shutdown; do not remove the temporary suite directory until the captured tree has exited. If a process remains after forced termination, report cleanup failure with PIDs and preserve state/logs for diagnosis rather than claiming cleanup. Make repeated calls safe because finalizer and hook may both run. Preserve the original test failure and attach cleanup failures as additional diagnostics where Gradle permits. Test the helper against a synthetic parent/child/grandchild process tree in which a descendant ignores graceful termination; cover normal finalization, startup/readiness failure, and a failing test path. Check that no captured PID survives and only the invocation-specific directory is removed.

Run the full `./gradlew build` and `./gradlew browserTest` gates after corrections. Run the browser gate twice with a complete cache and inspect per-invocation SQLite cleanup and failure artifacts. The existing `.github/workflows/build.yml` invokes the documented browser command on `ubuntu-latest`; after pushing the corrected branch, inspect a fresh CI job from a clean checkout and its browser-gate result to prove recoverable provisioning. A local empty-cache run proves download behavior but cannot substitute for a clean CI runner. If CI is inaccessible or fails for infrastructure reasons, record the exact limitation and leave the clean-CI acceptance criterion unverified rather than marking the issue fully complete. Verification must record exact commands, exits, browser/test counts, repair observations, and relevant artifacts; a required browser gate failure remains FAIL.

This is a planning handoff. No product or test source was edited in this step. Code navigation used `ast-index outline` and `ast-index file` with its index under `/tmp/issue85-ast-cache` because the default cache location is read-only in this sandbox.
