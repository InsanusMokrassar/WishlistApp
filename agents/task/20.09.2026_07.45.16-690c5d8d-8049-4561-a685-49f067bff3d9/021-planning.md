Model: GPT-5.6 Sol (high-level planning model, preferred by agents/PLAN.md)
Changed files: agents/task/20.09.2026_07.45.16-690c5d8d-8049-4561-a685-49f067bff3d9/021-planning.md

## Task understanding and questions

Validation step 020 remains FAIL because the browser classifier suppresses an unlocated `NoTransformationFoundException` after an expected anonymous 401 without attributing that exception to the request. Cycle 4 must remove the expected error at its real client source, then reject every unlocated transformation console error. Do not add another response-count or timing allowance. There is no blocking operator decision; no question was asked or answered.

## Source trace and correction target

`client/src/jsMain/kotlin/ClientJSPlugin.kt` starts on `WishlistsListViewConfig()` for every visitor. `features/ui/wishlist/src/commonMain/kotlin/ui/WishlistsListViewModel.kt` subscribes to initial load and resume, and `loadWishlists()` unconditionally calls `model.getMyWishlists()` when `config.userId == null`. `features/ui/wishlist/src/commonMain/kotlin/ui/DefaultWishlistsModel.kt` delegates that call to `features/wishlist/client/src/commonMain/kotlin/KtorWishlistFeature.kt`, whose `getMyWishlists()` calls `GET /api/wishlist/getMy` and directly decodes the body. The endpoint is bearer-protected, as documented in `features/wishlist/README.md`. On an anonymous page load the server returns 401; decoding the non-success response produces the observed `NoTransformationFoundException`, which `subscribeLoggingDropExceptions` logs to the browser console. The model already exposes `userAuthorisedState: StateFlow<Boolean>` from `AuthCredentialsStorage`; use that state in the list ViewModel rather than changing the protected API contract or catching all decode errors.

The sidebar already checks `currentUserIdFlow.value == null` in `features/ui/sidebar/src/commonMain/kotlin/ui/SidebarViewModel.kt` before requesting caller wishlists, so the main-list ViewModel is the identified bootstrap source. Architecture should verify the live request sequence once after the fix to confirm no other anonymous caller produces the same diagnostic. I read the relevant feature READMEs (`features/ui/wishlist`, `features/wishlist`, `features/ui/sidebar`); their Operator Notes contain only the template comment. Code navigation used `ast-index refs`/`search` from the temporary index because the default cache location is read-only.

## Plan for Architecture and Coding

At the narrow `WishlistsListViewModel` layer, skip `getMyWishlists()` whenever the displayed target is the caller's own list and `model.userAuthorisedState.value` is false. Set the list to empty and clear caller-derived profile/name state for the anonymous own-list view. Keep `getUserWishlists(userId)` available for public browsing of another user, regardless of auth state. Subscribe the own-list load to login/logout state changes in addition to the current initial/resume triggers, so registration or login loads the newly authenticated caller's wishlists and logout clears formerly private rows. Coalesce duplicate startup triggers if needed; preserve loading-state reset and normal errors from authenticated requests rather than swallowing them. Consider a stale in-flight authorized load completing after logout: check the current auth state before publishing results or use cancellation/latest-load semantics so private data does not reappear anonymously. Keep the bearer-protected endpoint, HTTP client behavior, and server-side 401 unchanged.

Remove the `NoTransformationFoundException` suppression branches from `browserTests/src/test/kotlin/dev/inmo/wishlist/browser/BrowserResponseClassifier.kt`. The browser gate should reject all console errors of that class, including `webpack-internal://` or missing source URL, independent of phase or previous responses. If the source fix eliminates the anonymous `GET /api/wishlist/getMy` request, also remove its response/console 401 allowance rather than preserving an unused exception; retain explicit failure on any unexpected API 401. Do not hide a remaining anonymous error by expanding the classifier. Update `BrowserResponseClassifierTest.kt` so both anonymous and authenticated unlocated transformation messages fail after an exact bootstrap 401 has been recorded; retain the existing unexpected 401 cases. The served smoke test must pass with zero unlocated transformation errors and keep screenshot/trace capture on failure.

Add focused ViewModel regression coverage in the existing wishlist UI test source set or a small accessible seam: anonymous own-list startup makes no `getMyWishlists()` call and renders an empty list; login transition invokes the protected read and publishes caller rows; logout clears rows without another protected read; public `userId` navigation remains readable while anonymous; an authenticated 401/decode failure is still surfaced by the existing logging/error path rather than silently converted into empty data. Include an in-flight logout case if the asynchronous load can publish after logout. Tests should exercise the state change path, not merely a pure predicate copied from production code. Adjust `features/ui/wishlist/README.md` architecture notes only if the change introduces behavior absent from the existing feature description, without touching Operator Notes.

Run the focused KMP tests, `./gradlew build`, and `./gradlew browserTest` on the repaired private Playwright cache. Inspect browser responses and console output to confirm anonymous startup no longer requests `/api/wishlist/getMy` and no transformation diagnostic is suppressed. Verification should report exact commands, exits, browser/test counts, and artifacts. The already closed cache-repair and descendant-cleanup findings remain regression gates; do not reopen their implementations unless the new run exposes a failure. The clean CI proof sequence remains: complete local Verification and Validation, let the Orchestrator push under `agents/GIT.md`, inspect a fresh clean-checkout CI run of the same `./gradlew browserTest` command, and report that result. Until a clean CI result exists, do not claim clean-runner provisioning verified.

This is a planning handoff only. No product or test source was edited in this step.
