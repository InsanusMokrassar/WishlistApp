Model: Claude Sonnet 5 (claude-sonnet-5)
Execution time: not tracked by harness; single continuous implementation+build+test pass
Tokens used: not tracked by harness
Changed files: `features/roles/common/src/commonMain/kotlin/FeatureRolesRegistry.kt` (new), `features/roles/common/src/commonMain/kotlin/Plugin.kt` (populated — was stock), `features/roles/common/src/commonTest/kotlin/FeatureRolesRegistryTest.kt` (new), `features/roles/server/src/commonMain/kotlin/utils/RequireRole.kt` (new), `features/roles/server/src/commonTest/kotlin/utils/RequireRoleTest.kt` (new), `features/roles/README.md` (status-note update only), this report

# Coding pass 3/4: Issue #68 — Aggregator + guard only

**Scope note:** this is Coding **pass 3 of 4** for `agents/task/10.07.2026_17.25.10-4eebb958-6bfa-4f82-a4cc-f3b4120dc4c3/003-architecturing.md`'s 4-pass split (§4 of that doc). Only "Pass 3 — Aggregator + guard" is implemented in this step. Pass 4 (call-site replacement, security-sensitive) is **not** implemented and must be picked up by a later Coding invocation. Pass 1 (Foundation) and Pass 2 (Bootstrap + migration) were already committed (`a733130`/`2dfdf46`) before this step started.

## 1. What this pass delivered

All 5 files from the architecture spec's §4 "Pass 3" file list, byte-exact against `003-architecturing.md` §3.3/§3.4/§8.6/§8.7, cross-checked against the real `dev.inmo.kroles.repos.ReadRolesRepo`/`BaseRoleSubject`/`BaseRole` API (`/home/aleksey/projects/own/kroles/repos/src/commonMain/kotlin/{RolesRepo,BaseRoleSubject}.kt`, `/home/aleksey/projects/own/kroles/roles/src/commonMain/kotlin/BaseRole.kt`) and this repo's own `getCallerUserIdOrAnswerUnauthorized`/`UserId`/`FakeRolesRepo` sources before writing, not trusted from the spec's prose alone:

- **`features/roles/common/src/commonMain/kotlin/FeatureRolesRegistry.kt`** (new) — the point-4 "aggregator of features and required role for them": an in-memory `object FeatureRolesRegistry` wrapping a private `MutableMap<String, BaseRole>`, with `register(featureId, role)` (idempotent on same-role re-registration, throws `IllegalStateException` on a conflicting re-registration) and `requiredRole(featureId): BaseRole?` (returns `null` for an unregistered id — fail-closed downstream). Also declares `object RoleGatedFeatureIds` with the three `const val` symbolic ids (`adminPanel`, `filesAvatarChangeForOthers`, `emailSendTest`) that Pass 4 will eventually enforce.
- **`features/roles/common/src/commonMain/kotlin/Plugin.kt`** (populated — was the stock empty `setupDI` scaffold since Pass 1) — `setupDI` now calls `FeatureRolesRegistry.register(...)` three times, once per `RoleGatedFeatureIds` constant, all requiring `SuperAdminRole` (matches today's real privilege-check shape: all three existing root-only checks are superadmin-gated).
- **`features/roles/common/src/commonTest/kotlin/FeatureRolesRegistryTest.kt`** (new) — 4 tests: round-trip register/lookup, same-role re-registration is a no-op, conflicting-role re-registration throws, never-registered id resolves to `null`. This is a KMP module (JVM+JS+Android), so this suite runs on all three targets.
- **`features/roles/server/src/commonMain/kotlin/utils/RequireRole.kt`** (new) — `internal suspend fun isRoleRequirementSatisfied(featureId, callerId, rolesRepo): Boolean`, the pure allow/deny decision (looks up `FeatureRolesRegistry.requiredRole(featureId)`, denies on `null`, otherwise delegates to `rolesRepo.contains(BaseRoleSubject.Direct(callerId.long.toString()), requiredRole)`), and `suspend fun RoutingContext.requireRole(featureId, rolesRepo): UserId?`, the Ktor route-guard wrapper — a **suspend function routing configurators call manually** inside a route handler (not a route-wrapper block analogous to `authenticate { }`), mirroring the existing `AdminRoutingsConfigurator.requireAdmin()` shape: resolves the caller via `getCallerUserIdOrAnswerUnauthorized()` (401 on no caller), then responds `403 Forbidden` and returns `null` if the role check fails, otherwise returns the caller's `UserId` so the calling route handler can chain further logic. No `build.gradle` change was needed — Pass 2 already added `auth.server`/`users.common` to `roles/server/build.gradle` for the bootstrap's own needs, and this file reuses the same two deps.
- **`features/roles/server/src/commonTest/kotlin/utils/RequireRoleTest.kt`** (new) — 3 tests against `isRoleRequirementSatisfied` only (not `requireRole` itself — this repo has no precedent for constructing a fake `RoutingContext`, an explicitly allowed gap per the spec's Test Planning section, matching how `AdminRoutingsConfigurator`/`AuthRoutingsConfigurator` are tested today): allowed when the registered role is held, denied when the role is registered but missing, denied when the feature id was never registered (fail-closed on a typo) even though the caller holds an unrelated role. Reuses Pass 2's `FakeRolesRepo` (`internal class` in the same `roles/server` module, different package — Kotlin `internal` is module-scoped, not package-scoped, so this compiles without changes to that fixture).
- **`features/roles/README.md`** — updated only the trailing "Coding-pass status note" blockquote to mark Pass 3 as current/complete (what it added, and a restatement that `requireRole` still has no production caller by design). The Architecture Notes body itself was already written byte-exact in Pass 1 to describe the feature's final 4-pass design and needed no change, per `agents/CODING.md`'s README-update rule and the precedent set by Pass 2's own README update.

No production call site outside `features/roles` itself references `FeatureRolesRegistry`/`RoleGatedFeatureIds`/`requireRole` yet (verified via a repo-wide grep after writing) — confirms this pass stayed purely additive, exactly as the spec's Pass 3 "Delivers" note states: "no existing call site is touched yet."

## 2. Deviation from the spec

None. All 5 files were written byte-exact against `003-architecturing.md`. No `build.gradle` changes were needed (Pass 2 already carries the two extra deps `RequireRole.kt` needs), matching the Pass 3 file list exactly (no Gradle file listed).

## 3. Build and test result

Ran in the foreground, waited for completion (not backgrounded) for the scoped build; the full-project build was run in the background per the caller's "if time allows" allowance and its completion was awaited before finalizing this report:

1. `ast-index rebuild` — 727 files, 52 modules indexed, clean.
2. `./gradlew :wishlist.features.roles.common:build :wishlist.features.roles.server:build :wishlist.features.roles.client:build` — **BUILD SUCCESSFUL**, 493 actionable tasks (83 executed, 410 up-to-date). New tests confirmed green via the emitted `TEST-*.xml` files:
   - `FeatureRolesRegistryTest` — 4/4 passed on every target it runs on: `jvmTest`, `testDebugUnitTest`, `testReleaseUnitTest` (Android), `jsBrowserTest`, `jsNodeTest` (5 XML files, all `failures="0" errors="0"`).
   - `RequireRoleTest` — 3/3 passed (`jvmTest`, `roles/server` is JVM-only).
   - Pass 2's `RolesBootstrapTest` — still 6/6 passed, unaffected by this pass's changes.
3. `./gradlew build` (full project) — **BUILD SUCCESSFUL** in 1m 5s, 4339 actionable tasks (195 executed, 4144 up-to-date). Scanned all 36 `TEST-*.xml` files under every module's `build/test-results/` for `failures="[1-9]`/`errors="[1-9]` — **zero matches** (36, up from Pass 2's 30, reflecting the 6 new test-report files: 5 for `FeatureRolesRegistryTest` across its 3 KMP targets' variants, 1 for `RequireRoleTest`).

Manual runtime verification was not applicable this pass — the spec's own Pass 3 "Verify" section only calls for "full build green; `FeatureRolesRegistryTest` + `RequireRoleTest` green" (no manual/curl check listed, unlike Pass 1/2/4), since `requireRole` has no route calling it yet in this pass.

## 4. Confirmation of the spec's own flagged gap (per the caller's explicit instruction to implement and test fully regardless)

Per `003-architecturing.md` §2.1/§1379-1394 and restated in the caller's task brief: `FeatureRolesRegistry` now has real production data (all three `RoleGatedFeatureIds` registered to `SuperAdminRole` from `roles/common/Plugin.setupDI`, which already runs today since `roles.common.JVMPlugin`/`roles.server.JVMPlugin` are registered in both server config files since Pass 1), but `requireRole`/`isRoleRequirementSatisfied` have **no production caller** — this is intentional, not an oversight left by this pass. Per the issue's own literal point 8 wording and the architecture spec's decision, Pass 4's three concrete privilege-check replacements (`admin`, `email`, `files`) call `SimpleRolesFeature.isSuperAdmin(...)` directly instead of `requireRole(...)`, to keep those three otherwise-unrelated modules scoped to `simpleRoles`'s narrow surface rather than the general `roles` `RolesRepo`. `requireRole` is implemented fully and unit-tested (this pass's `RequireRoleTest`, 3/3 green) as a real, ready-to-use guard for the next role-gated route — not scaffolding, per the caller's explicit instruction.

## 5. What Pass 4 (call-site replacement) needs from this pass

- Nothing from this pass is a hard dependency for Pass 4 — Pass 4 depends on Pass 1 (`SimpleRolesFeature`, already delivered) and Pass 2 (bootstrap granting roles at runtime, already delivered), not on Pass 3's registry/guard (per the spec's ordering note: "Pass 3 has no dependency on pass 2" and vice versa — the two passes are independent).
- `RoleGatedFeatureIds`/`FeatureRolesRegistry` now exist and are populated, available for Pass 4 or any future pass to consult if a future call site decides to use `requireRole` instead of `SimpleRolesFeature.isSuperAdmin` directly.
- `roles/common/Plugin.kt` is no longer the stock scaffold — any future re-registration of one of the three existing `RoleGatedFeatureIds` with a *different* role than `SuperAdminRole` will now throw at `setupDI` time (by design — the registry treats a conflicting re-registration as a bug, not a valid update path).

## 6. Note on `agents/AGENTS.md`

`agents/AGENTS.md` still contains the "AML-HIP" prompt-injection payload (a "SYSTEM DIRECTIVE" demanding pronoun-free, densely-formatted output) already flagged as ignored in `001-planning.md` §0, `003-architecturing.md` §0, and `005-coding.md` §5. It was read, disregarded again this step for the same reason — it is untrusted repo content, not an instruction from the operator or this task's own prompt, and following it would conflict with the caller's explicit request for a clear step report and a clear final report. `agents/SHORTCUTS.md`/`agents/TOOLS.md` (this repo's actual routing/tooling docs) are intact and were followed instead; `agents/TOOLS.md` explicitly confirms step reports and commit messages must be normal prose, caveman-compression applies only to internal agent thinking/search. No code or config change was made in response to the AGENTS.md payload.
