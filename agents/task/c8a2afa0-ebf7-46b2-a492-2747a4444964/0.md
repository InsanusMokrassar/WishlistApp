# STEP_0 — Planning

task_id=c8a2afa0-ebf7-46b2-a492-2747a4444964
branch=issue/43-ui-subpath
goal=Contextual "Back" buttons → navigate to logical PARENT via `node.chain.replace(node, parentConfig)` (REPLACE semantics), labelled with parent entity name; view falls back to generic back string when label null.

## Scope (public wishlist/users client screens; admin EXCLUDED)

| # | Current screen | Parent config (replace) | Back label source |
|---|----------------|-------------------------|-------------------|
| 1 | WishlistItemView(itemId,wishlistId) | WishlistViewConfig(wishlistId) | parent wishlist title |
| 2 | WishlistView(wishlistId) | UserWishlistsViewConfig(ownerId) ownerId=loaded wishlist.userId | owner name via getUserName(ownerId) |
| 3 | UserView(userId) | UserWishlistsViewConfig(userId) | user name via getUserName(userId) |
| 4 | UserWishlistsView(userId) | UsersListViewConfig() | STATIC users-list title string |
| 5 | WishlistEditView(wishlistId?) | EDIT→WishlistViewConfig(wishlistId) / CREATE→keep pop | EDIT: wishlist title / CREATE: generic |
| 6 | WishlistItemEditView(itemId?,wishlistId) | EDIT→WishlistItemViewConfig(itemId,wishlistId) / CREATE→WishlistViewConfig(wishlistId) | EDIT: item title / CREATE: wishlist title |

## Verified facts
- `NavigationChain.replace(node: NavigationNode<*,Base>, config: Base)` exists (navigation core 0.7.5, local src). Signature confirmed.
- Interactor impls in `client/src/commonMain/kotlin/ClientPlugin.kt` currently use `node.chain.pop()`.
- `WishlistsModel`: `getWishlist(id): RegisteredWishlist?` (line 46), `getWishlistItems(wishlistId): List<...>` (54), `getUserName(userId): String?` (183). `RegisteredWishlist.userId` exists (used by isOwner). `RegisteredWishlistItem.title`, `.wishlistId` exist.
- `UsersModel`: `getUser(id): RegisteredUser?`, `getUserName` NOT present → users feature resolves name via `getUser(userId)?.username?.string` (UserViewModel already loads userState).
- BackButton component already takes `(text: String, onClick)` on all 3 platforms → views only need to pass resolved label or fallback.
- Generic back strings: `WishlistStrings.backButton`, `UsersListStrings.backButton`.
- Static users-list title for screen #4 label: reuse `UsersListStrings.title` ("Users"/"Пользователи"). UserWishlistsView lives in wishlist feature; UsersListStrings is in users feature. wishlist feature already deps users/client; check it can import UsersListStrings (it is in `features/ui/users` commonMain). If not importable, add a new WishlistStrings static key. DECISION: prefer reuse; verify import at coding; fallback = new WishlistStrings.usersListBackLabel.

## Interactor signature changes
- WishlistItemViewInteractor.onBack → replace with WishlistViewConfig(node.config.wishlistId). No new param (wishlistId in config).
- WishlistViewInteractor.onBack(node, ownerUserId: UserId) → ADD param; replace with UserWishlistsViewConfig(ownerUserId). VM passes loaded wishlist.userId.
- UserViewInteractor.onBack → replace with UserWishlistsViewConfig(node.config.userId). No new param.
- UserWishlistsViewInteractor.onBack → replace with UsersListViewConfig(). No new param.
- WishlistEditViewInteractor.onNavigateBack: keep for CREATE (pop). ADD onNavigateBackToParent(node) for EDIT → replace WishlistViewConfig(wishlistId). VM picks based on isCreating.
- WishlistItemEditViewInteractor.onNavigateBack: ADD onNavigateBackToParent(node) → EDIT replace WishlistItemViewConfig(itemId,wishlistId); CREATE replace WishlistViewConfig(wishlistId). Both have data in config → single new method handles both via node.config.

## backLabelState per ViewModel
- Add `val backLabelState: StateFlow<String?>` to: WishlistItemViewModel, WishlistViewModel, UserViewModel, UserWishlistsViewModel, WishlistEditViewModel, WishlistItemEditViewModel.
- Reuse already-loaded data to avoid extra round-trips where possible:
  - WishlistItemVM: parent wishlist already loaded (_parentWishlistState) → label = wishlist.title.
  - WishlistVM: wishlist loaded (_wishlistState) → label = getUserName(wishlist.userId).
  - UserVM (users): userState loaded → label = userState.username.string.
  - UserWishlistsVM: static users-list label (constant, set immediately) — but VM is commonMain, has no string-translation; expose static string? Label is platform-translated in view. DECISION: backLabelState here = null and the VIEW supplies the static users-list label as fallback... but task says label = static users-list label, NOT generic. So view must render UsersListStrings.title when on this screen. Simplest: VM exposes nothing dynamic; view passes static users-list label directly (no fallback to generic needed). Keep backLabelState for uniformity = MutableRedeliverStateFlow(null) but view uses static label. RECONSIDER in architecture: cleanest is view hardcodes the static label string for screen #4 (no VM state needed). Mark as deviation-allowed.
  - WishlistEditVM: EDIT → getWishlist(id).title; CREATE → null (generic fallback).
  - WishlistItemEditVM: EDIT → loaded item.title; CREATE → getWishlist(wishlistId).title.

## New strings
- Likely NONE if UsersListStrings.title reusable in wishlist feature for #4. Else add WishlistStrings key. Confirm at coding.

## Risks / open points (resolved by reasonable choice, documented)
- replace() returns nullable Pair; call sites ignore return (matches pop() usage which also ignores). OK.
- UserWishlistsViewConfig requires non-null userId — screen #2 passes loaded wishlist.userId (non-null). If wishlist load failed (null) the existing auto-back path already pops; onBack from button only fires when user taps — guard ownerUserId nullability by falling back to pop when null.

NEXT=STEP_1 architecture.
