# STEP_2 — Coding

task_id=c8a2afa0-ebf7-46b2-a492-2747a4444964

## Implemented (REPLACE-semantics contextual Back)

### Interactor signature changes
- `WishlistViewInteractor.onBack(node, ownerUserId: UserId?)` — ADDED `ownerUserId` param; replace→`UserWishlistsViewConfig(ownerUserId)`, null→pop fallback.
- `WishlistEditViewInteractor.onNavigateBackToParent(node)` — NEW; EDIT replace→`WishlistViewConfig(wishlistId)`, null id→pop. `onNavigateBack` kept = pop (save/CREATE).
- `WishlistItemEditViewInteractor.onNavigateBackToParent(node)` — NEW; EDIT(itemId≠null) replace→`WishlistItemViewConfig(itemId,wishlistId)`, CREATE replace→`WishlistViewConfig(wishlistId)`. `onNavigateBack` kept = pop (save).
- Unchanged signatures (body-only in ClientPlugin): `WishlistItemViewInteractor.onBack`→replace `WishlistViewConfig`; `UserViewInteractor.onBack`→replace `UserWishlistsViewConfig(userId)`; `UserWishlistsViewInteractor.onBack`→replace `UsersListViewConfig()`.

### ClientPlugin.kt — 6 interactor bodies changed pop()→replace(node,parent). Admin/copy/booking untouched.

### ViewModels — added `backLabelState: StateFlow<String?>`
- WishlistItemViewModel: `_parentWishlistState.map{it?.title}` (no extra fetch).
- WishlistViewModel: set in loadWishlist `wishlist?.userId?.let{ getUserName(it) }`; `onBack()` passes `wishlistState.value?.userId`; auto-back uses `onBack(node,null)`.
- UserViewModel(users): `_userState.map{it?.username?.string}`.
- UserWishlistsViewModel: always-null flow; view uses static fallback `WishlistStrings.usersListBackLabel`.
- WishlistEditViewModel: EDIT→wishlist.title on load (null CREATE). onBack/onConfirmBack→private navigateBack(): EDIT onNavigateBackToParent / CREATE onNavigateBack.
- WishlistItemEditViewModel: EDIT→item.title / CREATE→wishlist.title on load. onBack/onConfirmBack→onNavigateBackToParent.
- onSaved/onConfirmDelete paths LEFT as onNavigateBack (pop) — not "Back" buttons, out of task scope; discard-modal flows intact.

### Strings (EN+RU)
- NEW `WishlistStrings.usersListBackLabel` = "Users"/"Пользователи" (static users-list label for screen #4). Reuse of `UsersListStrings.title` impossible: wishlist UI feature deps `features/users/client`, not `features/ui/users`.

### Views — 18 BackButton sites (label ?: fallback)
- wishlist {js,jvm,android}: WishlistItemView, WishlistView, WishlistEditView, WishlistItemEditView → fallback `WishlistStrings.backButton`; UserWishlistsView → fallback `WishlistStrings.usersListBackLabel`.
- users {js,jvm,android}: UserView → fallback `UsersListStrings.backButton`.
- Each onDraw: added `val backLabel by viewModel.backLabelState.collectAsState()`. Android uses `.translation(resources)`.

## Build results (all GREEN)
- `:wishlist.client:compileKotlinJs` — BUILD SUCCESSFUL
- `:wishlist.client:compileKotlinJvm` — BUILD SUCCESSFUL
- `:wishlist.client.android:compileDebugKotlin` — BUILD SUCCESSFUL
Feature modules (ui.wishlist, ui.users) compiled transitively. Only pre-existing `@Warning` warnings (ClientPlugin:521, platform plugins) — not from this change. ast-index rebuilt.

## Assumptions / decisions
- A1: replace() return value ignored (mirrors prior pop() usage).
- A2: edit-screen onSaved/onDelete keep pop (only the user-facing Back path rerouted) — task items 5/6 govern "back" only.
- A3: screen #4 static label = new WishlistStrings.usersListBackLabel (import isolation); VM backLabelState stays null, view supplies static fallback.
- A4: WishlistView ownerUserId null (wishlist not yet loaded) → pop fallback, prevents constructing UserWishlistsViewConfig with no owner.

## READMEs
Architecture Notes update delegated to haiku subagent (per ALL.md *.md rule). Operator Notes untouched (both empty).
