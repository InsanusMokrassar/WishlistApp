# STEP_1 ARCHITECTURE

task_id=f63f02d4-d69b-4b82-93b2-cb1194680a08; depends_on=STEP_0
intent=full file/package/signature map for booking extraction + WishlistAdditionalConfigsProvider + 2 scenario views.

## new_module: features/booking (triad)  package_root=dev.inmo.wishlist.features.booking

### booking/common  (apply mppJvmJsAndroid; android.library)
deps(commonMain): common.common, users.common, wishlist.common
files:
- models/Booking.kt  ← moved. pkg dev.inmo.wishlist.features.booking.common.models. imports WishlistItemId from wishlist.common.models, UserId from users.common.models. types: BookingId, sealed Booking{itemId,userId}, NewBooking, RegisteredBooking.
- models/BookingState.kt ← moved. pkg .booking.common.models. {booked,bookedByMe}. (R2 unchanged.)
- Constants.kt ← NEW (booking path consts only): object Constants{ bookingPrefixPathPart="wishlistItemBooking", bookingStatePathPart="state", bookingBookPathPart="book", bookingCancelPathPart="cancel", bookingMyPresentsPathPart="myPresents" }.
- repo/{ReadBookingRepo,WriteBookingRepo,BookingRepo,CacheBookingRepo}.kt ← moved. pkg .booking.common.repo. ReadBookingRepo.getByItemId + NEW getByUserId(userId):List<RegisteredBooking> (for view B route). CacheBookingRepo overrides both via originalRepo.
- jvmMain/repo/ExposedBookingRepo.kt ← moved. pkg .booking.common.repo. table wishlist_item_bookings (UNCHANGED name → preserves data + R4 uniqueIndex). add getByUserId query userIdColumn eq.
- commonMain/Plugin.kt: empty StartPlugin (delegate base).
- jvmMain/JVMPlugin.kt: with(Plugin){setupDI}; single{ExposedBookingRepo(get())}; singleWithBinds<BookingRepo>{CacheBookingRepo(get<ExposedBookingRepo>(), get())}.

### booking/server (apply mppJavaProject)
deps(commonMain): booking.common, common.server, auth.server, wishlist.common(read repos for ownerOf + RegisteredWishlistItem for view B route)
files:
- commonMain/services/BookingService.kt ← moved (+results BookResult/BookingResult/CancelResult). pkg .booking.server.services. deps BookingRepo, WishlistItemRepo, WishlistRepo (from wishlist.common). R3 ownerOf logic UNCHANGED. NEW method: suspend fun myPresents(callerId):List<RegisteredWishlistItem> = bookingRepo.getByUserId(callerId).mapNotNull{ wishlistItemRepo.getById(it.itemId) }. (caller=booker → R2 ok.)
- commonMain/configurators/BookingRoutingsConfigurator.kt ← moved. pkg .booking.server.configurators. authenticate{} tree UNCHANGED (R1). routes state/book/cancel UNCHANGED. NEW: get(myPresentsPathPart){ callerId or 401; respond bookingService.myPresents(callerId) }.
- commonMain/Plugin.kt: single{BookingService(get(),get(),get())}; singleWithRandomQualifier<ApplicationRoutingConfigurator.Element>{BookingRoutingsConfigurator(get())}.
- jvmMain/JVMPlugin.kt: with(booking.common.JVMPlugin){setupDI}; with(Plugin){setupDI}; startPlugin delegates both. (same-feature delegation legal.) Relies on config order (wishlist.server before booking.server) for WishlistItemRepo/WishlistRepo singles — Koin dedups.

### booking/client (apply mppJvmJsAndroidWithCompose? NO—no compose needed; use mppJvmJsAndroid + android.library). MIRROR wishlist/client which uses compose... booking/client has no UI → use mppJvmJsAndroid (like currency/common). Actually generate_feature stub client uses compose template. Will set build.gradle to mppJvmJsAndroid (no compose) since only Ktor HTTP.
deps(commonMain): booking.common, common.client
files:
- BookingFeature.kt ← moved. pkg .booking.client. + NEW suspend fun myPresents():List<RegisteredWishlistItem>.
- KtorBookingFeature.kt ← moved. pkg .booking.client. + myPresents() GET prefix/myPresents → body or emptyList on non-2xx.
- Plugin.kt: single{KtorBookingFeature(get())}; single<BookingFeature>{get<KtorBookingFeature>()}.

## new_module: features/ui/booking (scenario)  package_root=dev.inmo.wishlist.features.ui.booking
generate via scenario gen, title=Booking → BookingView/Config/ViewModel/Interactor/Model + strings. Then add 2nd view MyPresents manually.
deps(commonMain): common.client, booking.client, wishlist.common(WishlistItemId,RegisteredWishlistItem)
build: mppJvmJsAndroidWithCompose.

### view A = BookItem (rename generated Booking→BookItem OR keep generated `Booking` as view A). DECISION: use generated `BookingView`=view A (book button). config BookingViewConfig(itemId:WishlistItemId, wishlistId:WishlistId).
files (commonMain):
- ui/BookingViewConfig.kt: @Serializable data class BookingViewConfig(itemId,wishlistId):ViewConfig.
- ui/BookingModel.kt: interface{ suspend getState(itemId):BookingState?; suspend book(itemId):Boolean; suspend cancel(itemId):Boolean }.
- ui/BookingViewInteractor.kt: suspend fun onBack(node). (push/pop; impl in client/ClientPlugin.)
- ui/BookingViewModel.kt(node,model,interactor): state itemIdState; bookingState:StateFlow<BookingState?>; loadingState; init load getState; onBook/onCancel reload; onBack. (booking UI logic moved from WishlistItemViewModel.)
- BookingStrings.kt: bookingLabel,bookButton,cancelBookingButton,bookedByYou,bookedByOther,notBooked,backButton,myPresentsTitle,emptyPresents (EN+RU; moved/renamed from WishlistStrings).
views (js/jvm/android ui/BookingView.kt): render bookingState booked/byme/byother + book/cancel buttons + back. self-gates: if state==null show nothing/empty (covers owner/anon → R3 defense-in-depth).

### view B = MyPresents
files (commonMain):
- ui/MyPresentsViewConfig.kt: @Serializable class MyPresentsViewConfig:ViewConfig.
- ui/MyPresentsViewModel.kt(node,model,interactor): presentsState:StateFlow<List<RegisteredWishlistItem>>; loadingState; init model.myPresents(); onBack.
- MyPresentsViewInteractor.kt: suspend onBack(node). (OR reuse BookingViewInteractor — separate iface per VM per CODING rule → MyPresentsViewInteractor.)
- BookingModel add: suspend myPresents():List<RegisteredWishlistItem>.
views (js/jvm/android ui/MyPresentsView.kt): list of items (title + price) + back. point#6: NO navigation pushes MyPresentsViewConfig anywhere.

### ui/booking common Plugin.kt:
- polymorphic serializers for BookingViewConfig + MyPresentsViewConfig.
- factory{BookingViewModel(it.get(),get(),get())}; factory{MyPresentsViewModel(it.get(),get(),get())}.
- single<BookingModel>{ val f=get<BookingFeature>(); object:BookingModel{ getState/book/cancel/myPresents → f } }.
### ui/booking {JS,JVM,Android}Plugin: NavigationNodeFactory.Typed for BookingView + MyPresentsView (per platform). delegate Plugin.

## ext_point in features/ui/wishlist
- ui/WishlistAdditionalConfigsProvider.kt (commonMain): `sealed interface WishlistAdditionalConfigsProvider { val buttonLabel: StringResource; fun createConfig(item: RegisteredWishlistItem): ViewConfig }`. (StringResource = dev.inmo.micro_utils.strings.StringResource.)
- ui/BookingConfigsProvider.kt (commonMain, ui/wishlist): `class BookingConfigsProvider : WishlistAdditionalConfigsProvider { override val buttonLabel = WishlistStrings.bookButton... actually generic "Gift booking"; override fun createConfig(item)= BookingViewConfig(item.id, item.wishlistId) }`. imports BookingViewConfig from ui.booking. (sealed legal: impl in same module as iface.)
- ui/wishlist build.gradle: add `api project(":wishlist.features.ui.booking")`.
- WishlistItemViewModel: ADD ctor param `additionalConfigsProviders: List<WishlistAdditionalConfigsProvider>`; expose `val additionalConfigs = additionalConfigsProviders`; ADD `fun onAdditionalConfig(p)` → `scope.launch{ val item=_itemState.value?:return; interactor.onAdditionalConfig(node, p.createConfig(item)) }`. REMOVE bookingState/_bookingState/onBook/onCancelBooking/reloadBookingState + BookingState import + model.getBookingState call in init.
- WishlistItemViewInteractor: ADD `suspend fun onAdditionalConfig(node, config: ViewConfig)` → impl pushes config onto node.chain (client/ClientPlugin).
- ui/wishlist common Plugin.kt: `singleWithRandomQualifier<WishlistAdditionalConfigsProvider>{ BookingConfigsProvider() }`; change factory to `factory{ WishlistItemViewModel(it.get(), get(), get(), getAllDistinct()) }` (import getAllDistinct).
- WishlistsModel: REMOVE getBookingState/bookItem/cancelBooking + BookingState import. Plugin anon impl: remove those 3 overrides + bookingFeature dep + BookingFeature import.
- 3 WishlistItemView platform views: REMOVE booking section; ADD in SAME place (after priority block, before links) a row rendering one Button per `viewModel.additionalConfigs` with text buttonLabel.translation(); onClick viewModel.onAdditionalConfig(provider). JS Bootstrap btn; JVM/Android Button.
- WishlistStrings: REMOVE booking* strings now living in BookingStrings (keep a generic addtl-button label? Provider buttonLabel uses BookingStrings.bookButton → but BookingStrings in ui/booking which ui/wishlist depends on → import ok). Use BookingStrings.bookButton as BookingConfigsProvider.buttonLabel. So delete WishlistStrings booking* entries entirely.

## client/ClientPlugin.kt (commonMain) interactor impls:
- BookingViewInteractor{ onBack(node)=node.chain.pop() }.
- MyPresentsViewInteractor{ onBack(node)=node.chain.pop() }.
- WishlistItemViewInteractor: ADD onAdditionalConfig(node,config)=node.chain.push(config).

## wiring matrix
- settings.gradle includes: +":features:booking:common", +":features:booking:server", +":features:booking:client", +":features:ui:booking".
- server/build.gradle: +api project(":wishlist.features.booking.server").
- client/build.gradle commonMain: +booking.client, +ui.booking.
- client/android: transitive via :client. ok.
- sample.config.json plugins: +"dev.inmo.wishlist.features.booking.server.JVMPlugin" AFTER wishlist.server (ordering for repo singles).
- Main.kt x3: + booking.client.{JS/JVM/Android}Plugin (after wishlist.client) + ui.booking.{JS/JVM/Android}Plugin (after ui.wishlist).
- ui/wishlist build.gradle: +api project(":wishlist.features.ui.booking").

## README plan (haiku-class fill; orchestrator writes since subagents not spawned here)
- features/booking/README.md NEW (full structure; routes incl myPresents; models; R1-R4 notes).
- features/ui/booking/README.md NEW (BookingView=view A, MyPresentsView=view B; note view B unreachable by design point#6).
- features/wishlist/README.md: remove "Item Bookings" routes section + booking mentions (NOT operator notes).
- features/ui/wishlist/README.md: replace item booking description with "additional configs provider row"; document WishlistAdditionalConfigsProvider ext point.

## R1-R4 reverify post-move
R1 authenticate{} kept in moved BookingRoutingsConfigurator. R2 BookingState unchanged + myPresents returns caller-own items. R3 ownerOf checks unchanged in moved BookingService. R4 table name + uniqueIndex unchanged in moved ExposedBookingRepo + book pre-check/catch.

## risks
- sealed cross-module: handled (impl in ui/wishlist).
- booking/client compose: use non-compose template gradle.
- StringResource type name verify in CODING step.
- ordering of repo singles: config order. verify build.
next=STEP_2 coding.
