# STEP_1: Architecturing

ENTITY:
entity_id=issue#42; type=github_issue; state=architecture_done

DECISION:
- no_new_types; no_signature_changes
- change=value_substitution_only (List→Grid) at 3 default-decision sites
- storage_layer_untouched (returns null; default lives at consumers)
- enum_member_order_untouched (Grid stays second; reorder unnecessary, default ≠ declaration order)

RISK:
- low; behavior-only default flip; persisted user choice still honored (getSavedViewMode reads storage first)

VERIFICATION_PLAN:
- check=compileKotlinJvm; expected=success
- check=ast-index rebuild; expected=success
