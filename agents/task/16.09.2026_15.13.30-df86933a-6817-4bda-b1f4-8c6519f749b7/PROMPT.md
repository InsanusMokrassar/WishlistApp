Treat it as an issue :) Lets change the rule for models in MVVM UI parts which declare that models must be realized in plugins as anonymous objects. Lets change it with requirement to create classes-realizations of these models with prefix `Default` and replace all currently exists models into the classes. Models must be replaced as is and accept "outer world" dependencies they have used in their single as constructor private vals. That rule must now be the main one for models
