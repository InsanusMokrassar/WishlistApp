# STEP_2 — Coding — Issue #45 "Add deeplinks feature"

## Result: DONE. Builds green.

## Scaffold

Ran `echo "deeplinks" | bash generate_feature.sh` → created `features/deeplinks/{common,server,client}` with stub `build.gradle` + plugin objects (package substituted to `dev.inmo.wishlist.features.deeplinks.*`).

## New / edited files

### common (`features/deeplinks/common`)
- `src/commonMain/kotlin/Constants.kt` — `object DeepLinksConstants { const val linksPrefixPathPart = "links" }`.
- `src/commonMain/kotlin/models/DeepLinkId.kt` — NEW. `@Serializable @JvmInline value class DeepLinkId(val string: String)`.
- Plugin stubs untouched (empty/delegating).

### server (`features/deeplinks/server`)
- `src/commonMain/kotlin/DeepLinkHandler.kt` — NEW. `fun interface DeepLinkHandler { suspend fun tryHandle(deeplinkId: DeepLinkId, handlerInfo: Any): Boolean }`.
- `src/commonMain/kotlin/models/StoredDeepLink.kt` — NEW. `@Serializable data class StoredDeepLink(id: DeepLinkId, handlerInfo: JsonElement)`.
- `src/commonMain/kotlin/DeepLinksFeature.kt` — NEW. Server-only interface: `createDeepLink(handlerInfo: Any): DeepLinkId`, `resolveDeepLink(deeplinkId): Boolean`.
- `src/commonMain/kotlin/repo/DeepLinksRepo.kt` — NEW. `interface DeepLinksRepo : KeyValueRepo<DeepLinkId, StoredDeepLink>`.
- `src/commonMain/kotlin/services/DeepLinksService.kt` — NEW. Implements feature; uuid4 + `PolymorphicSerializer(Any::class)` encode/decode; `handlers.any { it.tryHandle(...) }`.
- `src/commonMain/kotlin/configurators/DeepLinksRoutingsConfigurator.kt` — NEW. `GET links/{deeplinkId}` → 200/404/400.
- `src/commonMain/kotlin/Plugin.kt` — registers `DeepLinksService` (handlers via `getAllDistinct()`), binds `DeepLinksFeature`, registers routing element via `singleWithRandomQualifier`.
- `src/jvmMain/kotlin/repo/ExposedDeepLinksRepo.kt` — NEW. `ExposedKeyValueRepo<String,String>` + `withMapper`, table `deeplinks` (`deeplink_id` PK, `data_json`).
- `src/jvmMain/kotlin/JVMPlugin.kt` — binds `DeepLinksRepo` to `ExposedDeepLinksRepo(get(), get())`; delegates to common module + common Plugin.
- `build.gradle` — scaffold default already had `:wishlist.features.deeplinks.common` + `:wishlist.features.common.server`. No extra deps needed (uuid, getAllDistinct, ktor server, exposed repos all transitive via `common.server`).

### client (`features/deeplinks/client`)
- Scaffold-only. No logic (requirement #6).

## Wiring

- `settings.gradle` — added `:features:deeplinks:{common,server,client}` after booking.
- `server/build.gradle` — added `api project(":wishlist.features.deeplinks.server")`.
- `server/sample.config.json` — added `"dev.inmo.wishlist.features.deeplinks.server.JVMPlugin"` (last in `plugins`).
- `client/build.gradle` — added `api project(":wishlist.features.deeplinks.client")` (commonMain). `client/android/build.gradle` unchanged (gets it transitively via `:wishlist.client`).
- `client/src/jsMain/kotlin/Main.kt`, `client/src/jvmMain/kotlin/Main.kt`, `client/android/src/main/kotlin/MainActivity.kt` — added the (delegating) deeplinks client plugin to each plugin list.

### DI summary
- Routing: `singleWithRandomQualifier<ApplicationRoutingConfigurator.Element> { DeepLinksRoutingsConfigurator(get()) }` → collected by `features/common/server/JVMPlugin` via `getAllDistinct()`.
- Handlers: `DeepLinkHandler`s collected via `getAllDistinct()`; consuming features register `singleWithRandomQualifier<DeepLinkHandler> { ... }`.
- Feature: `single { DeepLinksService(get(), get(), getAllDistinct()) }` + `single<DeepLinksFeature> { get<DeepLinksService>() }`.
- Repo: `single<DeepLinksRepo> { ExposedDeepLinksRepo(get(), get()) }` (JVM).

## README

`features/deeplinks/README.md` — full required structure (Operator Notes placeholder, Overview, Routes, Models, Architecture Notes).

## Build commands + results

- `./gradlew :wishlist.features.deeplinks.common:build :wishlist.features.deeplinks.server:build :wishlist.features.deeplinks.client:build` → **BUILD SUCCESSFUL in 34s**.
- `./gradlew :wishlist.server:compileKotlin` → **BUILD SUCCESSFUL in 23s** (verifies new server dependency wires cleanly).

ast-index rebuilt after source changes (42 modules indexed).

No fix cycle needed — clean on first build.

## Assumptions carried from STEP_0/1
- Resolution route public (uuid = capability token).
- Handler-info polymorphism reuses the shared `Json` + aggregated `SerializersModule` (`useArrayPolymorphism = true`); consumers register their handler-info subclasses themselves.
- "No client code except boilerplate" = keep scaffold client module + delegating plugins; no logic added.

## Not done (out of scope / orchestrator-owned)
- No git operations / PR (per task constraints).
