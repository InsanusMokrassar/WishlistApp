Model: gpt-5.6-terra (ML Coding implementation); gpt-5.6-luna (LL report filling)
Changed files: features/admin/common/src/commonMain/kotlin/models/AdminUser.kt; features/admin/common/src/commonTest/kotlin/models/AdminUserTest.kt; features/admin/server/src/commonTest/kotlin/UsersManagementFeatureTest.kt; features/admin/server/src/commonTest/kotlin/configurators/AdminRoutingsConfiguratorTest.kt; features/auth/common/src/commonMain/kotlin/models/AuthFeatureUser.kt; features/auth/common/src/commonTest/kotlin/models/AuthFeatureUserTest.kt; features/auth/server/src/commonTest/kotlin/services/AuthFeatureServiceTest.kt; features/auth/server/src/jvmTest/kotlin/services/AuthFeatureServiceSqliteTest.kt; features/email/server/src/commonTest/kotlin/services/EmailRegistrationInviteSenderTest.kt; features/email/server/src/commonTest/kotlin/services/EmailVerificationAccountCoordinatorTest.kt; features/email/server/src/commonTest/kotlin/services/FakeUsersRepo.kt; features/roles/server/src/commonTest/kotlin/FakeUsersRepo.kt; features/users/common/src/commonMain/kotlin/models/User.kt; features/users/common/src/commonMain/kotlin/models/UsersFeatureUser.kt; features/users/common/src/commonMain/kotlin/utils/UserEmailState.kt (deleted); features/users/common/src/commonTest/kotlin/models/UsersFeatureUserTest.kt; features/users/common/src/jvmMain/kotlin/repo/ExposedUsersRepo.kt; features/users/common/src/jvmTest/kotlin/repo/CacheUsersRepoSqliteTest.kt; features/users/common/src/jvmTest/kotlin/repo/ExposedUsersRepoSqliteTest.kt; features/users/common/src/jvmTest/kotlin/repo/PostgresUsersRepoTest.kt; features/users/server/src/commonTest/kotlin/services/UsersServiceTest.kt; agents/task/18.09.2026_07.24.45-9d85e0a2-9584-4775-bbfd-2774f2d30a33/006-coding.md.

The model contraction is complete. `RegisteredUser`, `AuthFeatureUser`, and `AdminUser` serialize only `id`, `username`, `email`, and `emailApproved`. Pending-email, requested-change time, cooldown fields, aliases, defaults, nested wrappers, and reverse-mapping arguments are absent from those public user, auth, and admin models. `UsersFeatureUser.asRegisteredUser` accepts only explicit current email and approval. The obsolete users-common `UserEmailState.kt` helper was deleted.

Email lifecycle ownership remains in `EmailProfile`: `pendingEmail` and the time when the change was requested remain persisted as raw Exposed pending/cooldown columns and are handled by email-service profile mutations, events, and coordinator behavior. `ExposedUsersRepo` projects reduced user models separately from the full `EmailProfile` row projection, including approval-candidate checks. User-profile editing MVVM therefore obtains pending-change information from the email service and returns an email verification model rather than exposing a direct user model; pending email and requested-change time are not presented in user or admin-user models.

Behavioral tests were added or strengthened for exact serializer descriptors and JSON keys for `RegisteredUser`, `AuthFeatureUser`, `AdminUser`, and public users; old lifecycle JSON keys are ignored during decode and never emitted; current email and approval round-trip; and a separate persisted `EmailProfile` with populated pending lifecycle proving public endpoint output remains only `id` and `username`. Fakes now maintain independent `EmailProfile` state, and lifecycle assertions use `getEmailProfileFresh` rather than a user DTO.

Verification passed for users common JVM; users server JVM; auth common/server JVM; admin common/server JVM; email server JVM; roles server JVM; email common JS Node; users/auth/admin common JS Node with fresh model suites users 6, auth 5, and admin 5, all zero failures; UI users/adminPanel/sidebar/auth JVM with users 100, adminPanel 10, sidebar 11, and auth 6, all zero failures; UI users JS Node fresh 84/84; UI users JS Browser fresh 84/84; UI users Android `testDebugUnitTest` fresh 84/84; and the UI users JVM+JS+Android compile trio. `git diff --check` passed. Final `ast-index rebuild` indexed 815 files; the `UserEmailState` file and references are absent. Targeted source audit found no lifecycle fields in the three user/auth/admin model definitions or the users reverse mapping.

The PostgreSQL lifecycle test source was migrated, but PostgreSQL execution was not performed and must not be claimed: persistence behavior and schema were unchanged, so PostgreSQL execution remains for the final Verification slice.

The remaining work is a docs-only slice: update deferred README/step prose if the final operator requests it. No source, schema, or persistence contraction remains.
