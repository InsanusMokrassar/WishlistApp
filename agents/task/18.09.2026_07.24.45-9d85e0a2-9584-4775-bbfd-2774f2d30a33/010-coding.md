Model: Codex GPT-5 (Coding implementation); gpt-5.6-luna (required LL step-report writer).
Changed files: features/admin/server/src/commonTest/kotlin/UsersManagementFeatureTest.kt; features/auth/server/src/commonTest/kotlin/services/AuthFeatureServiceTest.kt; features/email/server/src/commonTest/kotlin/services/EmailVerificationAccountCoordinatorTest.kt; features/email/server/src/commonTest/kotlin/services/FakeUsersRepo.kt; features/roles/server/src/commonTest/kotlin/FakeUsersRepo.kt; features/roles/server/src/commonTest/kotlin/RolesBootstrapTest.kt; features/users/common/src/jvmTest/kotlin/repo/ExposedUsersRepoSqliteTest.kt; features/users/common/src/jvmTest/kotlin/repo/PostgresUsersRepoTest.kt; agents/task/18.09.2026_07.24.45-9d85e0a2-9584-4775-bbfd-2774f2d30a33/010-coding.md.

# Coding: validation corrections for fake clear semantics and legacy lifecycle migration

Implemented only the two corrections returned by Validation 009. No production source, schema, configuration, dependency, or README file changed.

All four changed full-update user fakes now handle `NewUser(..., email = null)` before comparing nullable current and pending slots. The auth, admin, and roles fakes also apply the same null-first ordering in their dedicated `setEmail` branches; the email fake already had that dedicated-write ordering. Admin and roles fakes gained an independently seeded `EmailProfile` map, matching the existing email and auth test-double boundary.

Focused regression tests now exercise every affected fake. A non-null same-address full update preserves an unapproved profile's requested-at and an approved profile's cooldown deadline. An explicit null full update clears each profile to exactly `EmailProfile(userId)`, proving first-unapproved requested-at and approved-current cooldown metadata are removed rather than being masked by a nullable pending-slot equality check.

SQLite and PostgreSQL migration coverage now begins from the immediately preceding populated lifecycle schema: `email`, `email_approved`, `pending_email`, and `email_change_allowed_at` exist and contain an approved current address with a pending replacement/deadline, a first unapproved address, and an empty account; `email_change_requested_at` does not exist before repository initialization. The tests initialize and reopen twice, assert raw current/pending/deadline values remain unchanged, assert historical requested-at stays null through same-current, same-pending, same-first, and empty saves, then replace the first candidate under a controlled clock and assert only that accepted new candidate receives requested-at `1000`. No legacy timestamp is fabricated and no lifecycle column is destructively rewritten.

Fresh focused JVM command passed with exit code 0: `./gradlew --no-daemon --no-parallel --console=plain --rerun-tasks :wishlist.features.users.common:jvmTest :wishlist.features.email.server:jvmTest :wishlist.features.auth.server:jvmTest :wishlist.features.admin.server:jvmTest :wishlist.features.roles.server:jvmTest`. Gradle reported 58 executed actionable tasks. Fresh XML totals were users common 42, email server 104, auth server 25, admin server 10, and roles server 31: 212 tests total with zero failures, errors, or skips.

The required PostgreSQL migration/lifecycle gate ran against only the disposable `/tmp/wishlist-postgres-email.7pj7y8/data` PostgreSQL 18.6 fixture on `127.0.0.1:55433`. `env 'WISHLIST_POSTGRES_TEST_JDBC_URL=jdbc:postgresql://127.0.0.1:55433/postgres?user=postgres' ./gradlew --no-daemon --no-parallel --console=plain --rerun-tasks :wishlist.features.users.common:postgresEmailLifecycleTest` passed with exit code 0, 14 executed actionable tasks, and 8 tests with zero failures, errors, or skips. The fixture was stopped with `pg_ctl stop -m fast`; subsequent `pg_ctl status` reported no server, `postmaster.pid` was absent, and fixture-specific `pgrep` returned no process. No production database or pre-existing 55432 fixture was altered.

Final `ast-index rebuild` with `XDG_CACHE_HOME=/tmp/wishlist-010-ast` indexed 815 files. The AST audit finds all four required `newValue.email == null` branches in email, auth, admin, and roles fakes. `git diff --check` passed. The working tree contains only the eight intended test/fake files listed above. No commit and no push were performed in this Coding pass.

The next step is LL materialization of `010-coding.md`, followed by Coding inspection and one local commit containing only these eight implementation/test files and the completed report. No other source work remains from Validation 009.
