Model: Codex GPT-6 (HL independent Validation reasoning, following HL-before-ML priority); gpt-5.6-luna (required LL report materialization).
Changed files: agents/task/18.09.2026_07.24.45-9d85e0a2-9584-4775-bbfd-2774f2d30a33/012-validation.md

# Validation: email-owned verification state after corrections

## Validation Result: PASS

Both Medium findings from Validation 009 are resolved. The final email-ownership implementation satisfies the task acceptance conditions, and the corrections provide the missing regression and migration proof. No open finding or production defect was identified.

## Scope and review method

This final review covers task bootstrap `c046d7a` through current HEAD `a67128b3abe228270dddc4a7f48982cd098dd42e`, including correction commit `cfe9762526a6262cbc3533ca416aa73a6f58594d` and Verification 011. Validation resumed after its preceding report as the role protocol requires: it read Coding 010 and Verification 011 completely and inspected the actual correction diff and current task-wide change inventory. The earlier independent source review, recorded in Validation 009, supplies the full implementation context for issue 79 and pull request 82.

The correction changes eight test/fake files and its report. A direct Git comparison confirms that production source, schema declarations, feature documentation, configuration, and dependencies are unchanged since the previously reviewed verification commit `110f3299ee1c391e51ef757d303e852b7ec2334f`. The complete acceptance audit below therefore combines that unchanged source review with independent inspection of the new regression tests, raw database assertions, and current ownership searches. Validation used the rebuilt ast-index, direct file and diff reads, focused literal checks, test-result XML, and Git hygiene checks. No implementation or test file was edited, and no aggregate test run was repeated.

## Resolution of prior findings

VEO-01 is closed. Each email, auth, admin, and roles fake now handles explicit null in `updateObject` before comparing nullable current and pending slots. The auth/admin/roles dedicated `setEmail` branches have the same corrected ordering; the email fake already handled explicit null first. This restores full-update clearing when the current email exists and the pending slot is null.

The four `fakeFullUpdateClearsLifecycleAndRetainsSameAddressNoOps` regression tests are located at `features/email/server/src/commonTest/kotlin/services/EmailVerificationAccountCoordinatorTest.kt:134`, `features/auth/server/src/commonTest/kotlin/services/AuthFeatureServiceTest.kt:462`, `features/admin/server/src/commonTest/kotlin/UsersManagementFeatureTest.kt:234`, and `features/roles/server/src/commonTest/kotlin/RolesBootstrapTest.kt:47`. Each seeds independent email-owned state for an unapproved first address with requested-at and an approved current address with a cooldown deadline. The tests assert that non-null same-address updates preserve the entire profile, then assert that explicit-null full updates return reduced empty users and exactly `EmailProfile(userId)` for both lifecycle states. These assertions would fail with the earlier nullable-equality ordering. The independent profile seed support added to admin and roles fixtures remains confined to test setup.

VEO-02 is closed. `features/users/common/src/jvmTest/kotlin/repo/ExposedUsersRepoSqliteTest.kt:514` and `features/users/common/src/jvmTest/kotlin/repo/PostgresUsersRepoTest.kt:305` now exercise populated schemas that already contain current email, approval, pending email, and cooldown deadline, but omit requested-at. Each seeds an approved current address with a pending replacement and deadline, a first unapproved address, and an empty account. The assertions inspect raw stored lifecycle columns, including null-aware JDBC reads of both timestamps, rather than relying only on reduced user projections.

Both database tests prove that initialization and repeated reopening preserve the seeded addresses, approval, pending candidate, deadline, and unknown request time. Same-current, same-pending, same-first, and empty full updates do not invent a timestamp or erase pending state. After the second reopen, a different first candidate receives the controlled clock value `1000`, while the unrelated approved/pending row retains its original deadline and null request time. The singleton write-lock row is also checked. SQLite retains its older migration/reopen cases; the PostgreSQL migration case now targets the specific preceding lifecycle schema required by this task. Existing PostgreSQL lifecycle, lock-wait, cross-slot uniqueness, clear, and contention cases remain present and passing. No production behavior was changed to close either finding, and the retained suites continue to cover the established lifecycle behavior.

## Full acceptance audit

Email-model ownership passes. The serializable `EmailProfile` belongs to email/common and contains only the required owner identifier plus current email, approval, pending email, requested-at, and cooldown deadline. It neither wraps nor implements a user model. `RegisteredUser`, `AuthFeatureUser`, and `AdminUser` contain only id, username, current email, and approval; pending/request/deadline properties and reverse-mapping arguments are absent. Public `UsersFeatureUser` remains id and username only. Exact serializer/JSON-field tests, old-extra-key decoding tests, independently seeded email fixtures, and current source searches support this boundary. No hidden user-model alias, nested compatibility carrier, or obsolete user-owned lifecycle helper remains.

The profile-editing MVVM boundary passes. `UsersModel.getMyEmailProfile()` exposes `EmailProfile`, and `DefaultUsersModel` delegates directly to `EmailFeature.getMyEmail()`. The former direct `ClientAuthFeature` dependency and `getMyProfile` method are absent. Auth state remains the source of caller identity and authorization. The ViewModel publishes email-owned pending verification state and request time, matches the Long response owner to the expected user id, and uses email-owned candidate/draft helpers. No timestamp widget is required by the accepted architecture.

Production lifecycle semantics pass. Before first approval, the current slot contains the unapproved candidate. After approval, a replacement occupies pending email while the latest approved current address and approval flag remain intact. Accepted candidate writes sample the server clock after the database write lock and persist candidate/time atomically. Same-current, same-pending, username-only, read, and resend operations retain the time; approval and explicit clear remove it. Raw clear classification includes request-time-only residue. Cooldown is rooted at approval, remains independent of requested-at, and permits exact expiry. The inspected transaction, batch rollback, stale-link, overflow, normalized input, raw cross-slot uniqueness, and post-commit event paths retain their established behavior. Pending-only changes still emit committed events even when reduced user values compare equal.

Authentication, privacy, and HTTP behavior pass. The owner GET is inside bearer authentication and derives its target exclusively from the authenticated caller. Forged target selectors do not expose another account. Existing email-less accounts return an empty profile; missing profiles produce 404. Anonymous and invalid bearer requests are rejected before service access. The Ktor client enforces successful status and non-null model decoding, maps only explicit 404 to null, and propagates other status, malformed-model, transport, and cancellation failures. Existing PUT, verification POST, typed cooldown, duplicate-conflict, and administrative authorization contracts remain intact.

Freshness, caching, and delivery pass. `getEmailProfileFresh` is a mandatory read capability. Exposed reads one users row into the email projection, and the cache adapter delegates directly to backing storage without reconstructing or caching email lifecycle state. The shared coordinator protects fresh reads and account operations in both SMTP graph shapes. Owner verification selects the exact candidate from `EmailProfile`, passes an id/recipient pair to the sender, and rechecks after SMTP. Registration uses the first unapproved current address. Existing exact-link compensation, cancellation behavior, delivery-race classifications, approval-before-role transition, and SMTP-disabled reads/storage remain preserved.

Migration and rollout pass. The physical change is an additive nullable `email_change_requested_at` column with no fabricated historical default or backfill. Existing lifecycle columns remain in the locked users row, with no destructive contraction or separate-store atomicity problem. The corrected SQLite and actual PostgreSQL tests now prove populated legacy preservation and unknown-time stability. Documentation requires matching server/UI versions, stopped old writers, and a verified backup; recovery uses forward repair, a compatible backport, or an explicitly understood backup restoration. Retaining columns does not claim safe mixed-version writes or unrestricted old-binary rollback.

UI and cross-platform contracts pass. Every feedback snapshot includes requested-at. Timestamp-only refresh tests retire both saved and successful-delivery feedback while preserving a dirty draft. Owner/session/live-target checks, cancellation and stale-result suppression, checked save/resend reconciliation, approved-current retention, cooldown admission and refresh, and disabled-SMTP storage remain covered. Exact DTO field-set tests and the existing JVM desktop renderer contracts remain intact. Verification 008 supplies JVM, JS Node, JS Browser harness, Android debug unit, and platform-compile evidence; the corrections introduce no renderer or production UI change.

Documentation, role sequence, and scope pass. READMEs and KDocs describe the final ownership, fresh owner reads, requested-at/deadline distinction, and deployment limits. Operator Notes were unchanged in the reviewed task diff, and the correction adds no feature-documentation change. Coding 010 responds directly to the two findings, and Verification 011 records the relevant fresh gates. There is no new dependency, generic lifecycle framework, timestamp control, admin pending-email endpoint, or compatibility wrapper outside the task's scope.

## Verification evidence and hygiene

Verification 011 records fresh execution at corrected Coding commit `cfe9762526a6262cbc3533ca416aa73a6f58594d` of 212 focused JVM tests across users common, email server, auth server, admin server, and roles server, plus eight actual PostgreSQL tests. Both commands exited zero, with no failures, errors, or skips. Validation independently totaled the current focused JVM XML: 25 suites, 212 tests, zero failures/errors/skips. The PostgreSQL XML independently confirms eight passing tests and includes `postgresPopulatedLegacyLifecycleSchemaSurvivesRepeatedReopenAndSameAddressSaves`; this was not a missing-URL skip.

Verification 011 also records the final aggregate build: exit zero, 4,605 actionable tasks, 187 executed and 4,418 up-to-date. Verification 008 remains the broader unchanged-source evidence of 377 JVM plus 298 JS/Android plus eight PostgreSQL test executions, totaling 683 with zero failures/errors/skips, and explicit JVM/JS/Android compiles. These are separate recorded runs, not additive counts of unique tests. Validation did not rerun or claim to execute these gates.

The rebuilt AST contains 815 files and locates all four corrected fake regressions and the intended fresh-profile surfaces. Current ownership searches find no pending/request/deadline fields in the four reduced user models and no `getMyProfile` symbol. `git diff --check c046d7a..HEAD` passes and `git status --porcelain=v1` is empty. The corrected production/documentation comparison is empty. Verification reports the disposable PostgreSQL instance stopped, and Validation independently confirmed its `postmaster.pid` is absent. No production database or unrelated fixture was touched by this role.

## Findings and residual limits

There are no open findings. VEO-01 and VEO-02 are closed, and no new Low, Medium, High, or Critical issue was identified in the reviewed changes. No production defect was found.

The evidence covers the configured repository test harnesses, persistence adapters, platform compiles, and aggregate build. It does not claim live SMTP delivery, physical Android rendering or IME execution, browser DOM interaction beyond the configured harness, or production deployment. Legacy null request time remains intentionally unknown, and requested-at is metadata rather than a unique generation token. The documented matching-version rollout and existing in-process compensation limits remain applicable.

## Conclusion

PASS. The email-owned profile migration, user-model contraction, owner-only read path, MVVM consumption, timestamp lifecycle, and required preservation proof meet the task acceptance conditions. The completed cycle may proceed to the Orchestrator's final handoff. Validation made no product changes and performed no push.
