Model: gpt-6-astra (Coding implementation); gpt-5.6-luna (required LL step-report filling).
Changed files: features/email/common/src/commonMain/kotlin/models/EmailProfile.kt; features/email/common/src/commonMain/kotlin/utils/EmailProfileState.kt; features/email/common/src/commonTest/kotlin/models/EmailProfileTest.kt; features/users/common/src/commonMain/kotlin/repo/ReadUsersRepo.kt; features/users/common/src/commonMain/kotlin/repo/CacheUsersRepo.kt; features/users/common/src/jvmMain/kotlin/repo/ExposedUsersRepo.kt; features/users/common/src/jvmTest/kotlin/repo/CacheUsersRepoSqliteTest.kt; features/users/common/src/jvmTest/kotlin/repo/ExposedUsersRepoSqliteTest.kt; features/users/common/src/jvmTest/kotlin/repo/PostgresUsersRepoTest.kt; features/email/server/src/commonTest/kotlin/services/FakeUsersRepo.kt; features/auth/server/src/commonTest/kotlin/services/AuthFeatureServiceTest.kt; features/roles/server/src/commonTest/kotlin/FakeUsersRepo.kt; features/admin/server/src/commonTest/kotlin/UsersManagementFeatureTest.kt; features/users/server/src/commonTest/kotlin/services/UsersServiceTest.kt.

# Coding: email profile and persistence foundation

Implemented architecture checkpoint 2 only. `EmailProfile` is a serializable email/common owner model with the required `Long userId`, current email, approval, pending replacement, requested-at, and cooldown deadline fields. New email/common helpers select the pending candidate first for verification and drafts. The profile does not depend on or wrap a users/auth/admin model.

`ReadUsersRepo.getEmailProfileFresh(UserId): EmailProfile?` is mandatory without a default implementation. `ExposedUsersRepo` reads exactly one users row into a defensive email-owned projection, returning an empty profile for an existing email-less user and null only for a missing row. `CacheUsersRepo` delegates directly to its backing repository without consulting or modifying its user cache. All compilation-required fakes implement the method explicitly and fail loudly when an unsupported read is called; no fake returns a silent null fallback.

The users schema now declares nullable `email_change_requested_at`; the existing additive `initTable` initialization adds the column idempotently with no default and no backfill. Candidate writes sample the injected server clock only after acquiring `users_write_lock`: non-null creates, first-address replacements, and pending replacements persist that sample. Exact current/pending saves, no-op writes, username changes, reads, and resends do not overwrite it. Approval and explicit clear null the timestamp. Complete-clear detection now includes a request-time-only raw residue. Candidate timestamp writes share the existing transaction, duplicate/cooldown/SQL failure behavior, batched rollback, lock order, and post-commit event behavior. Legacy RegisteredUser lifecycle properties remain intentionally for this temporary buildable checkpoint; service/UI/model contraction is deferred to downstream slices.

Tests added or extended: EmailProfile serialization/default/default-invalid-input and helper selection; SQLite single-row fresh projection, cache bypass, candidate timestamp transitions, approval/clear, raw request-only cleanup event, batch rollback, legacy additive null migration, reopen persistence, and native SQLite lock-wait timestamp; PostgreSQL additive/reopen timestamp proof and `pg_stat_activity`-verified lock-wait timestamp. Existing timestamp/cooldown/duplicate lifecycle assertions remain.

Verification completed:

- `./gradlew --no-parallel :wishlist.features.email.common:jvmTest :wishlist.features.users.common:jvmTest` passed. XML evidence: email/common 4 tests; users/common 40 tests total (33 `ExposedUsersRepoSqliteTest`, 2 `CacheUsersRepoSqliteTest`, 5 `UsersFeatureUserTest`); zero failures/errors.
- Relevant production and dependent test compilation ran through `:wishlist.features.email.common:compileKotlinJvm`, `:wishlist.features.users.common:compileKotlinJvm`, and `compileTestKotlinJvm` for email/auth/roles/admin/users server modules; no semantic compiler errors. Existing Exposed additive-migration deprecation warning remains.
- `WISHLIST_POSTGRES_TEST_JDBC_URL='jdbc:postgresql://127.0.0.1:55433/postgres?user=postgres' ./gradlew --no-parallel :wishlist.features.users.common:postgresEmailLifecycleTest --rerun-tasks` passed 8/8 with zero failures/errors. The task used a new `/tmp/wishlist-postgres-email.7pj7y8` PostgreSQL instance initialized solely for this run. The fixture created and dropped generated schemas only. The initial supplied `/tmp/wishlist-postgres.4KE0JW` directory had a stale PID and no listener, so it was not modified. The temporary test server was stopped cleanly after verification.
- `ast-index rebuild` completed with `XDG_CACHE_HOME=/tmp/wishlist-email-foundation-ast`, indexing 816 files. `ast-index refs getEmailProfileFresh` confirms the production adapters and every direct fake implementation. `git diff --check` passed.

No source blocker remains for this checkpoint. Feature README updates and removal of transient user/auth/admin fields are intentionally deferred until the ownership consumer, MVVM, and final contraction slices make their final documentation accurate. No push was performed.

Remaining slices: migrate coordinator/services/route/client reads; migrate UsersModel and profile-edit MVVM; then remove pending/deadline lifecycle data from RegisteredUser, AuthFeatureUser, and AdminUser mappings/wire models, update fakes/docs, and run final whole-task validation.
