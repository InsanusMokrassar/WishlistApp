Model: Inherited Codex Architecture agent, assigned to the HL reasoning role for the installed Navigation APIs, cancellation ordering, authorization fixture controls, and evidence-ledger decisions. The explicit prohibition on nested agents requires this role to write its own architecture report; subsequent KDoc and documentation fillings belong to an LL Coding agent.
Changed files: agents/task/09.09.2026_09.40.05-b512784f-672e-4309-aa90-4551cb0c3361/049-architecturing.md

## Decision and evidence boundary

Correction cycle 4 is ready for sequential Coding increments. Entry HEAD is clean `87e27416e521ab773d572034bed37598114fd6e0` on `fix/issue-78-email-authorized-password-change`. Validation 047 remains FAIL. Retain V78-04 High/count 3, V78-05 High/count 3, V78-07 Medium/count 3, V78-11 Low/count 2, and V78-12 Medium/count 1. Preserve the seven resolutions and every historical count recorded below. Architecture neither closes findings nor increments, resets, or re-escalates counts.

I read AGENTS, SHORTCUTS, ALL/local.ALL, ARCHITECTURE, MODELS, PROTOCOL, GIT, TOOLS, AST_INDEX, the caveman skill, PROMPT, complete reports 032–048, and the declaration requirements in CODING. No local.ARCHITECTURE.md exists. Common, Auth, Email, DeepLinks, and UI/users READMEs were read completely, including Operator Notes. There is no client README. I inspected the owner, ClientPlugin root composition, actual PasswordChangeViewModel, common/browser lifecycle tests, service/routing fixtures, Auth/coordinator checks, production direct-role conversion, transport, and bounded declaration diff. Caveman full was used only internally. The initially sandboxed AST lookup could not see its cache and the attempted initialization failed on the read-only cache; approved read access found the existing 1,460-file index. No index rebuild completed or was required by source changes, and no product test or Gradle task was run by Architecture.

The authoritative production observations remain Validation 047's two separate diagnostics. Joining the actual submitting ViewModel Job while root work was held produced cancelled/completed lifecycle, destroyed Pending, empty fields, and zero saves; releasing root work then saved one structurally inspected Completed hierarchy with the exact request. That resolves V78-10's original ownership defect, without supplying a retained regression. Separately, replacing an ancestor left immutable ancestry equal to the root, admitted one detached transition, saved nothing, and required 5,001 virtual milliseconds for cleanup. That proves V78-12's stale admission and delayed cleanup; it does not establish an obsolete save or authorization bypass.

Preserve authenticated owner issuance, enabled SMTP and exact approved email, canonical immutable approval UUID, approval-selected completion subject independent of ambient bearer identity, 15-minute expiry, purpose/type/email/direct-User/fingerprint checks, coordinator-before-Auth locking, final reread, consume-before-write, cancellation/cleanup precedence, and unchanged sessions and roles. Preserve the concrete serializers, raw synchronous form admission, plaintext clearing, owner eligibility/DOM matrix, live feedback and protected native fields, trusted completion URL, policy headers, and sanitized typed HTTP classification. No schema, dependency version, route, global saver, authentication policy, UI design, or approval scheme changes are included.

## Installed APIs and research adaptation

For dev.inmo sources I first inspected `/home/aleksey/projects/own/navigation` and MicroUtils, then read the exact Maven-local Navigation 0.7.7 source JARs under `/home/aleksey/.m2/repository/dev/inmo/`. Relevant core sources are `NavigationChain.kt`, `NavigationNode.kt`, `StateExtensions.kt`, `NavigationNodeStateListeners.kt`, `extensions/Chain.kt`, `extensions/EitherChainOrNode.kt`, `extensions/ChangesInSubtreeFlow.kt`, `extensions/ChainSubChanges.kt`, `visiter/Walk.kt`, and the ConfigHolder/hierarchy files. Relevant compose sources are `InitNavigation.kt` and `ComposeNavigationExtensions.kt`; mvvm supplies `ViewModel.kt`. The installed MicroUtils 0.30.1 `FlowSubscription.kt` confirms that the ViewModel's destruction subscriber uses `launchIn(scope)`; registration/handling is asynchronous on its unchanged Default scope. Test scheduler draining alone therefore cannot certify destruction-collector completion.

Select the public `NavigationChain<Base>.findNodeInSubTree(filter)` and `findChainInSubTree(filter)` overloads from `dev.inmo.navigation.core.extensions`, always with identity predicates such as `{ candidate -> candidate === node }`. Their `ChainOrNodeEither` traversal uses the nonrecursive `walk`, visiting current `stackFlow.value` and `subchainsFlow.value` with a seen set. NavigationChain and NavigationNode retain object identity equality. The predicate overloads inspect the currently reachable hierarchy; the id overloads and `rootChain()` ancestry are not substitutes.

Select the public `NavigationChain<Base>.changesInSubTreeFlow()` wrapper, which delegates through `Either<NavigationChain<Base>, NavigationNode<out Base, Base>>.changesInSubtreeFlow()`. Its result is `Flow<Either<Pair<NavigationChain<Base>, List<NavigationNode<out Base, Base>>>, Pair<NavigationNode<out Base, Base>, List<NavigationChain<Base>>?>>>`. Chains emit their own stack and recursively switch node subscriptions with `flatMapLatest`; nodes emit subchains, state/config notifications with null subchain payloads, and recursively switch chain subscriptions. StateFlows replay current values. Ancestor stack replacement emits at that reachable ancestor even if the detached leaf never emits again, and `flatMapLatest` cancels obsolete subtree subscriptions. Use each emission only as a wakeup; reread live membership, never interpret a buffered event as the current hierarchy. The callback-style `onChangesInSubTree` in `ChainSubChanges.kt` handles added/removed entries but does not maintain replacement subscriptions correctly for this requirement; the generic hierarchy saver also omits replacement-only saves. Neither is selected for the owner observer.

Navigation `replace(node, config)` synchronously creates/returns an exact replacement pair but queues publication. The queue rechecks the old identity, calls `changeState(NEW)`, and then publishes the new stack. The ViewModel owns a Default `SupervisorJob` cancelled by the real destruction collector. `NavigationConfigsRepo.save` and `storeHierarchy` are synchronous. Compose `initNavigation` accepts a scope, derives linked scopes for saver/navigation, and draws an Empty node's subchains without requiring a concrete ComposeView; a small real Compose HTML host can therefore run the production binding and restoration without instantiating the entire application UI.

Current [Compose side-effect guidance](https://developer.android.com/develop/ui/compose/side-effects) supports keyed DisposableEffect cleanup and a composition-owned coroutine scope. Adapt it by sharing the same binding composable and passing the resulting scope to existing initNavigation. Current [Kotlin runTest guidance](https://kotlinlang.org/api/kotlinx.coroutines/kotlinx-coroutines-test/kotlinx.coroutines.test/run-test.html) distinguishes controlled test dispatchers from work on real dispatchers; adapt it with actual lifecycle Job joins and deferred request/save gates. The [flatMapLatest contract](https://kotlinlang.org/api/kotlinx.coroutines/kotlinx-coroutines-core/kotlinx.coroutines.flow/flat-map-latest.html) supports cancelling obsolete subtree observation; retain an explicit current-state sample because flow collection and merge children can be scheduled later. No dependency upgrade or generic coroutine abstraction follows from this research.

## V78-04: deterministic authorization and observable direct roles

In `features/email/server/src/commonTest/kotlin/services/EmailPasswordChangeServiceTest.kt`, extract the missing-bridge branch into `missingRoleBridgeRejectsOtherwiseIdenticalUnexpiredApproval`. Keep the existing matrix's revoked-role and policy cases. Construct one `FakeEmailsService` and one `val nowEpochMillis: () -> Long = { 1_000L }`. Call `PasswordChangeTestFixtures.fixture(emails = emails, nowEpochMillis = nowEpochMillis)`. Issue once through the real accepting service; obtain the sole actual id and cast the persisted payload to `EmailPasswordChangePayload`. Assert handler `EmailPasswordChange.handlerId`, subject 7, exact `owner@example.com`, expiry 901,000, `nowEpochMillis() < expiry`, and credentialState equal to `fixture.auth.passwordChangeState(fixture.user.id)`. Do not rehash or re-create the user's old password between controls.

Create `AuthFeatureService(usersRepo = fixture.trackedUsers, writeUsersRepo = fixture.trackedUsers, passwordsRepo = fixture.passwords, userRoleAuthorization = null)`. Create the rejecting `EmailPasswordChangeService(emailsService = emails, deepLinksService = fixture.links, accountCoordinator = fixture.coordinator, authFeatureService = bridgeAbsentAuth, publicHttpOrigin = "https://wishlist.example", nowEpochMillis = nowEpochMillis)`. SMTP, clock, origin, user/email/password storage, coordinator, links, payload, fingerprint, and completion request are thus identical; only the Auth role bridge differs. Use one `CompletePasswordChangeRequest(fixture.user.id, id, Password("new-password"))` for both attempts. Before measurement, require the accepting service's `pendingPasswordChangePath(id)` to equal the exact canonical pending path and retain the id. This is a read-only positive control.

Use the existing repository hooks, without adding production test APIs. Immediately before bridge-null completion, reset password write count and deeplink operation records, clear a local ordered event list, and install `trackedUsers.beforeGetById`, `passwords.beforeGet`, `linksRepo.beforeGet`, `linksRepo.beforeUnset`, and `passwords.beforeSet` hooks that append fixed labels. The expected negative sequence is exactly link-read, user-read, user-read. The first user read is coordinator `withApprovedEmail`; the second is Auth `passwordChangeStateWhileLocked`. The absent bridge then denies before the password read. Assert user reads 2, password reads 0, link reads exactly `[id]`, removals 0, password writes 0, and InvalidApproval. The second user read plus identical unexpired state proves the rejection reached Auth; an assertion of InvalidApproval alone does not.

Before inspecting retained state, detach/clear the event hooks so inspection cannot inflate the measured sequence. Assert the persisted info still equals the original info and the stored salted password equals its initial value. Reinstall the same hooks after another reset and complete through the original accepting service. The expected positive sequence is link-read, user-read, user-read, password-read, link-read, link-unset, password-set. Require exactly two reads of the same approval id, one removal of that id, exactly one password write, Changed, consumed approval, rejected old password, and accepted new password. Take post-operation authentication assertions outside the measured hooks. Restore every hook in finally. These sequence assertions fail on entry HEAD's wall-clock-confounded negative path, which has only the coordinator user read.

In `features/email/server/src/commonTest/kotlin/services/PasswordChangeTestFixtures.kt`, extend `PasswordChangeRoleAuthorization` with optional constructor `rolesRepo: RolesRepo? = null`, preserving `directRolePresent` for existing single-account matrix tests. `hasUserRole(userId)` must increment `hasAttempts` and, when a repo is supplied, return `UserRole in rolesRepo.getDirectRoles(BaseRoleSubject.Direct(userId.long.toString()))`; the Boolean fallback applies only when no repo was supplied. That conversion matches production `RolesBootstrap.roleSubject` and `hasDirectUserRole`. `ensureUserRole` increments `ensureAttempts` before any branch; in repository mode, deny a direct NewUser marker, otherwise delegate `includeDirect(subject, UserRole)` and confirm the direct membership. Keep the independent Boolean fixture behavior when no repo exists. No completion path should call ensure; counting before delegation catches even idempotent attempted mutation.

In `PasswordChangeFlowRoutingTest.graph`, create the existing `FakeRolesRepo`, seed `includeDirect(BaseRoleSubject.Direct("7"), UserRole)` and `includeDirect(BaseRoleSubject.Direct("8"), UserRole)`, then construct `PasswordChangeRoleAuthorization(rolesRepo = rolesRepo)`. Bind that exact repo to RolesRepo and that exact bridge to UserRoleAuthorization, shared by the real coordinator/Auth graph. Set both old passwords and mint both actual AuthCredentials only after membership exists. Reset password and role include/exclude/create/remove/ensure/has counters after credentials and fixture controls are ready. Keep FakeRolesRepo's current counters at the start of each mutation method; no new constructor or role implementation is needed there.

Add `repositoryBackedRoleBridgeChecksTheRequestedSubject` in `EmailPasswordChangeServiceTest`: seed User for 7 and NewUser for 8, then assert hasUserRole(7) true, hasUserRole(8) false, and hasUserRole(9) false; optionally set the fallback Boolean true to prove it cannot override a supplied repo. Exercise one idempotent ensure on already-approved 7 and assert one ensure attempt and one include attempt, then confirm direct 8 remains NewUser. This focused fixture test proves the bridge cannot accidentally ignore subject or hide mutation attempts.

Extend the existing sibling/session route test with immutable `getDirectRoles(subject).toSet()` and `getAll().mapValues { roles.toSet() }` snapshots. Both initial subject snapshots must equal `setOf(UserRole)`, and the full initial map must contain exactly the two seeded subjects. Assert snapshot equality and all five mutation-attempt counters zero after A succeeds, B rejects, and account 8 succeeds. Retain three real approvals and read-only initial GETs, owner write counts 0→1→1, account-8 independent completion 1→2, sibling failure and unrelated salted credential stability before account-8 completion. Keep real pre-existing access/getMe checks for both subjects before refresh rotation, real refresh/getMe afterward, and old/new/wrong login assertions. Do not reset counters during the measured flow. Security production changes are conditional on a newly failing production assertion; these repairs require only fixture/tests.

## V78-12: current membership and finite replacement observation

Confine behavior changes to `client/src/commonMain/kotlin/PasswordChangeNavigationOwner.kt`, plus the composition seam below. At `activeBindingFor(node)` require a current binding whose scope Job is active, `binding.root.findNodeInSubTree { candidate -> candidate === node } === node`, and `node.chain.stackFlow.value.lastOrNull() === node`. Keep caller `ensureActive()` and Pending-only onChanged admission. Continue accepts the current password page, including Completed and Pending with terminal invalid-approval presentation, under the same membership/last-node check. A detached-at-entry or obsolete-binding callback starts zero jobs, invokes no replacement factory, and saves nothing.

At root-job entry, recheck binding identity, scope activity, live reachability, and current-last-node admission. Keep the accepted chain and old node identities separate from the returned replacement. Jobs stay in the root scope, with no caller await. Deduplicate by node identity. Register an UNDISTPATCHED child observer before calling the exact `acceptedChain.replace(node, destination)`; observe a merge of `acceptedBinding.root.changesInSubTreeFlow().map { Unit }` and `acceptedChain.stackFlow.map { Unit }`. The leaf subscription supplements current subtree discovery; the root subscription is essential for ancestor detachment. Apply `onStart { emit(Unit) }` to sample live state before waiting, and resample immediately after assigning the returned replacement. UNDISTPATCHED registration alone does not guarantee that merge's children already subscribed, whereas current StateFlow replay and synchronous resampling close that scheduling window.

Use one local `sampleTransition()` function within the root operation and one `CompletableDeferred<Boolean>` result. On every sample, first reject changed binding/inactive scope or a chain no longer reachable via `root.findChainInSubTree { candidate -> candidate === acceptedChain }`. Then accept only if the exact non-null replacement is currently reachable in root and present by identity in the accepted chain. Otherwise reject if the old node is absent by identity from that chain/current root; otherwise continue waiting. An ancestor's NEW state notification before stack publication is only a wakeup: do not reject solely because state is NEW while the old node still exists. A new node pushed above the accepted old node after root-job admission is not grounds to abandon an exact replacement still queued for that old node.

Bound observation by the existing 5,000 milliseconds. Null replacement, observed detachment, supersession, timeout, rebinding, or root cancellation terminates the operation. After a true result, immediately recheck binding identity and active root Job, current membership of the accepted chain, current root membership of the exact replacement, and replacement presence in the accepted stack. The old Pending is expected to be absent after success and is not a postcondition. The replacement need not be last: a newer current destination above Completed must remain current and be included in the saved hierarchy. Build `root.storeHierarchy()` and call synchronous `navigationConfigsRepo.save` in the same root-dispatcher turn without suspension between those final checks, snapshot, and save. A superseded replacement or detached containing subtree saves nothing and triggers no back navigation. This is the existing UI/root-dispatcher confinement contract, not arbitrary cross-thread atomicity.

Use structured child cleanup: put the observer in an inner `coroutineScope`, cancel it in that scope's finally, and let the scope join its children on exit; remove the transition entry in an outer finally after the child scope has terminated. Also attach `invokeOnCompletion` to each LAZY transition to remove the entry only when the mapped value is that exact Job, covering cancellation before the body ever starts. On rebinding/unbinding, cancel a `values.toList()` snapshot to avoid modifying a map during iteration. Cleanup always cancels the captured binding's jobs; clearing the owner's current binding remains guarded by `binding === capturedBinding`. An old cleanup must neither clear nor cancel a newer binding. The supplied composition scope is cancelled by Compose, not by owner.bind. Keep cancellation rethrow, fixed credential-free failure logging, no HTTP dependencies, and no retries. Job joins in tests establish cleanup; activeTransitionCount alone is insufficient.

Add these named cases to the existing `PasswordChangeInteractorTest`; keep all cases in that class for existing platform filters. Use real NavigationChains, identity-capturing node factories, and the FIFO dispatcher described below. For a held leaf, first publish the complete hierarchy on the standard scheduler, call `leaf.start(heldLeafScope)` with a separate FIFO dispatcher, and drain that dispatcher until the replacement channel consumer is installed and idle. The installed start implementation cancels the previous channel consumer under its mutex; the outer chain remains on the standard dispatcher. During the measured interval, enqueue the leaf replacement without draining the leaf FIFO, then process the outer ancestor replacement and root observation independently. Capture and cancel/join the explicit leaf start Job and scope in cleanup alongside the original navigation jobs. This uses the public start API and does not alter leaf stack state or mock membership predicates.

1. `detachedPendingAtEntryStartsNoTransition` creates root→ancestor→leaf Pending, lets the real queues publish, replaces the ancestor, and calls onChanged on the detached leaf. Require immutable ancestry still matching as a fixture sanity check, zero newly created Completed nodes, zero accepted jobs immediately, zero saves, and the replacement ancestor unchanged. Repeat admission through stale Continue. The onChanged assertion is red at entry HEAD.
2. `detachedWhileQueuedStopsWithoutLeafEmission` admits/starts one root transition while the leaf processing continuation is held, then replaces the ancestor using the live outer chain. Record the leaf stack before/after and assert equality with no leaf stack emission; drain root event work without advancing virtual time. Require the accepted Job completed, zero owner jobs, zero saves, and preserved outer replacement. The entry owner remains waiting until timeout, so prompt completion is red.
3. `queuedNodeSupersessionNeverSavesCompleted` queues a competing exact replacement of Pending before the owner's already-admitted queued replacement, then processes the leaf. The first replacement removes Pending, the owner queue recheck cannot replace it, and observation must finish with zero owner saves/jobs and the competing destination intact.
4. `replacementSupersededBeforeSaveIsIgnored` processes the accepted Pending→Completed replacement while root persistence is held, replaces that exact Completed with a newer destination, then releases root. Require zero owner saves, no back navigation, and zero remaining jobs. The case tests pre-save live checks even if a true completion wakeup had already been buffered.
5. `newerDestinationAboveCompletedIsPreservedInSavedHierarchy` processes the accepted replacement, pushes a distinguishable `UserViewConfig(UserId(8))` above Completed while root persistence is held, and releases root. Require one save of the actual current stack, Completed still beneath that exact newer node, no Pending, and the newer node still last. Do not assert Completed last or save a manufactured earlier snapshot.
6. `oldBindingCleanupCannotClearNewBinding` admits a queued operation in root A, binds root B, runs A's cleanup, and proves A's retained Job has completed while a fresh exact-node transition in B succeeds. A stale A Continue saves nothing. Add an inactive-scope/LAZY-cancellation assertion so no map entry survives a cancelled-before-start root job. Preserve the existing timeout, current-nonlast stale, and disposal cases with retained Jobs joined.

## V78-05: actual lifecycle, transport, and production composition

Add the bounded common-test fixture file `client/src/commonTest/kotlin/PasswordChangeNavigationTestFixtures.kt`. Define `HeldNavigationDispatcher(private val schedulerDispatcher: TestDispatcher) : CoroutineDispatcher(), Delay by schedulerDispatcher` with the required `InternalCoroutinesApi` opt-in. Create a separate `val rootTimerScheduler = TestCoroutineScheduler()` and pass `StandardTestDispatcher(rootTimerScheduler)`; the installed coroutines-test 1.11.0 TestDispatcher implements Delay and registers both delayed resumption and invokeOnTimeout with its TestCoroutineScheduler. Construct held scopes from the parent Job and FIFO dispatcher only, not the runTest coroutineContext/TestCoroutineScheduler element. The separately driven root timer cannot be auto-advanced by runTest while an actual Default lifecycle Job is being joined. The FIFO dispatcher owns `ArrayDeque<Runnable>`; dispatch enqueues, `runNext()` executes one task, `drain()` executes available tasks with a finite maximum and fails on runaway rescheduling, and `queuedTaskCount` exposes scheduling evidence. Delay delegation is essential: CoroutineDispatcher alone would fall back to a real-time timeout. Only the explicit timeout test calls rootTimerScheduler.advanceTimeBy(5_001) and runCurrent, then drains the held FIFO to finish cancellation; detachment and lifecycle tests do not advance root time. Chain/ViewModel work still uses StandardTestDispatcher(testScheduler). Add `RecordingPasswordNavigationRepo(initialHolder = null, failFirstSave = false)` with immutable saved-holder snapshots, saveAttempts, get(), save(), and resetObservations(). Add `ConfigHolder<ViewConfig>.passwordNavigationConfigs()` to traverse Chain.firstNodeConfig and every Node.config, Node.subnode and Node.subchains, and `serializedPasswordNavigationHolder(json, holder)` using `ConfigHolder.serializer(PolymorphicSerializer(ViewConfig::class))`. Purpose KDocs and constructor @param fillings for these helpers are assigned to the later LL increment.

Retain `HeldPasswordChangeUsersModel` in `PasswordChangeInteractorTest.kt`; add optional `completionFeature: PasswordChangeFeature? = null` and `returnedAfterCancellation: CompletableDeferred<Unit>`. Its existing requestReceived gate fires only after copying the exact request into requests. If a feature was supplied, call that feature and preserve a null result as a result; otherwise await the existing completion gate. Do not use an Elvis fallback that turns a real null transport result into waiting on the fake gate. In the cancellation-resistant branch record returnedAfterCancellation immediately before returning Changed. Keep unrelated UsersModel methods inert. This is explicitly a UsersModel fixture plus, where specified, a production KtorPasswordChangeFeature transport, rather than a claim that the entire UsersModel DI facade was instantiated.

Upgrade `actualSubmittingViewModelDestructionPrecedesCompletedPersistence` as follows. Resolve the actual owner and PasswordChangeViewInteractor from ClientPlugin DI, retaining Common's Json and UI/users concrete serializers. Start a real chain with UsersList and Pending on `StandardTestDispatcher(testScheduler)`. Bind the owner to a separate `CoroutineScope(SupervisorJob(parentTestJob) + heldRootDispatcher)`; retain that scope Job and each accepted transition Job from its children. Construct the actual PasswordChangeViewModel with the standard dispatcher and retain `checkNotNull(viewModel.scope.coroutineContext[Job])` before submission. Start an independent UNDISTPATCHED onDestroyFlow waiter before releasing the model result; leave Navigation's real Default destruction collector and the ViewModel Job unmodified. Await requestReceived before completing Changed and directly assert request.userId, request.approvalId, and request.password.string.

Run the standard scheduler so Changed is accepted; execute root FIFO tasks only until the exact replacement has been enqueued and the observer is waiting. Hold every further root task. Process the real chain queue so Pending is destroyed and Completed published. Await the independent real destruction event and, from the independent test scope, join the actual ViewModel lifecycle Job. Because the Default subscriber is asynchronous, job completion—not merely node NEW or advanceUntilIdle—is the barrier. Use `withContext(Dispatchers.Default) { withTimeout(5_000L) { destroyObserved.await(); viewModelJob.join() } }` so the safety bound is not auto-advanced virtual time while the actual Default/event-loop collector is running. This bound is separate from the owner's virtual-time timeout. Do not manually cancel that Job to make the ordering pass. If the collector missed an event, the join fails the test; do not replace the assertion with a sleep or accept a timeout. Require cancelled and completed Job, node NEW, both fields empty, and saveAttempts zero before any root persistence task is released. Then drain root work and require exactly one successful save and a completed transition Job. Apply the same real-dispatcher bounded join to the remaining actual ViewModel destruction assertions.

Inspect the simple saved holder structurally: one root Chain, first Node UsersList, its single successor Completed, no further successor, and no subchains in either node. Traverse the entire holder, requiring no Pending. Serialize using the production Json and ConfigHolder serializer; require absence of the exact approval UUID and plaintext sentinel in the serialized bytes. Redacted toString() is never used as absence evidence. Then construct the actual Completed ViewModel, retain its actual Job, attempt submission and require requests still exactly one, snapshot saveAttempts, call `completedViewModel.onContinue()`, process replacement/destruction, join the actual Completed Job, and release root work. Require a second successful save with a nonempty usable UsersList stack and no password-page config. Compare the new saved holder/save count, not an already populated saved variable. This retained regression may pass before cycle-4 production edits because V78-10 is resolved; do not claim a new red ownership defect.

Upgrade `cancelledSubmittingViewModelCannotStartChangedHandoff` with the real Pending submit, requestReceived, cancellation-resistant model branch, and a competing real navigation replacement. Await the actual destruction event, returnedAfterCancellation, and actual ViewModel Job completion before asserting no Completed factory invocation, no owner job/save, exactly one request, cleared fields, and the competing destination still current. The deferred signal proves Changed returned despite cancellation; ensureActive must prevent acceptance afterward.

Upgrade `saveFailureDoesNotReplayAndLaterContinuePersistsUsersList` to use an actual Pending ViewModel and actual KtorPasswordChangeFeature backed by MockEngine. Add only `implementation libs.ktor.client.mock` to `client/build.gradle` commonTest; reuse the existing version catalog and ContentNegotiation/production Json pattern from `KtorPasswordChangeFeatureTest`. The engine captures POST method, trusted endpoint, and decoded CompletePasswordChangeRequest from OutgoingContent, signals request-received, and holds its one successful JSON Changed response. Supply that feature through the model's optional port. Release once, let real replacement destroy Pending, join its Job, and make RecordingPasswordNavigationRepo throw on the first synchronous save. Require exactly one model request and one engine request, one save attempt, current Completed, cleared fields, no surviving owner job, and no persisted holder. Drain, try stale Pending submission, try actual Completed submission, and call actual Completed Continue; join its Job and require one separate successful UsersList save while both request counts remain one. Do not call owner.onChanged directly for this proof. Use the same real transport fixture for the successful browser case, so its zero-additional-HTTP claim is literal.

Create the production seam `client/src/commonMain/kotlin/utils/PasswordChangeNavigationBinding.kt` with `@Composable internal fun WithPasswordChangeNavigationBinding(owner: PasswordChangeNavigationOwner, root: NavigationChain<ViewConfig>, content: @Composable (CoroutineScope) -> Unit)`. Inside, create `val rootScope = rememberCoroutineScope()`, run `DisposableEffect(owner, root, rootScope)` with `val unbind = owner.bind(root, rootScope)` and `onDispose(unbind)`, then call content(rootScope). Move only that existing scope/effect wiring from `ClientPlugin.startPlugin` into the seam. ClientPlugin's drawing callback calls the seam and passes its content scope unchanged to the existing initNavigation invocation. Keep the same default scaffold injection and factories. No eager bind, global scope, owner.bind call copied into a browser fixture, or test-only production callback is needed.

In `client/src/jsTest/kotlin/PasswordChangeNavigationBrowserTest.kt`, retain the one canonical test and mount two small `renderComposable(host)` compositions under one JSDOM window as needed for rebinding. In the canonical composition call WithPasswordChangeNavigationBinding and call actual `initNavigation<ViewConfig>(EmptyConfig(), configsRepo = persistenceRepo, nodesFactory = liveFactory, scope = suppliedScope, dropRedundantChainsOnRestore = true, rootChain = rootChain) { }`; the canonical Pending URL supplies a non-null restored scaffold so the empty default block is unused. Use NavigationNode.Empty factories, which the installed DrawNode supports. Capture the supplied scope/Job in a test-side SideEffect deferred after successful composition and wait for actual root/main stack restoration. This mounts the same production DisposableEffect and composition-owned scope used by ClientPlugin. Remove manually started restoreHierarchy/root/saver jobs and direct owner.bind/unbind from this browser flow; initNavigation now owns them.

Begin at `https://wishlist.test/ui/password-change/7/123e4567-e89b-42d3-a456-426614174000` with `<base href="/ui/">`. Keep actual WishlistsAppUrlNavigationConfigsRepo, Empty/scaffold/top/sidebar/main restoration, and the production ClientPlugin-resolved owner/interactor. Register the existing Common, Common-client, UI/users, scaffold, topBar, sidebar, and wishlist serializer contributions needed to serialize the full holder; avoid resolving unrelated models. Wrap save only to record attempts and signal after delegation to the real adapter; wait for the exact Completed-containing holder, excluding earlier generic restoration saves from the transition assertion. Submit through the actual Pending ViewModel/MockEngine model, await the transport request and release Changed, await real destruction, join its lifecycle Job, and await the delegated save. Assert exact subject/UUID/plaintext request fields, pathname `/ui/password-changed`, empty search, null history.state, no UUID/plaintext sentinel in active pathname/search/JSON-stringified state, typed Completed and no Pending anywhere, and empty ViewModel fields.

Immediately create a new WishlistsAppUrlNavigationConfigsRepo from that successful current URL and assert restored Completed/no Pending before Continue or any history mutation. Delete the existing synthetic Completed holder/save that currently precedes reload. Construct the actual live Completed ViewModel, reject submit without another engine/model request, call its onContinue, join its real lifecycle Job, and await a newly delegated UsersList save with pathname `/ui`, empty search and null state. Keep representative user/wishlist/item route parsing, malformed approval paths, and the approval-notification marker after the lifecycle assertions. Existing UI/users completed-form DOM tests remain renderer evidence.

For production disposal/rebinding, reuse the same JSDOM host lifetime and seam: admit a root-A operation whose leaf queue is deliberately held, retain the scope-A Job and accepted transition Job, mount a separate root-B composition with the same owner, then dispose A. Assert A's Job/transition complete after queue drainage and A saves nothing; an A callback/Continue remains stale. Submit/continue a valid B node through the actual interactor, require its save and current hierarchy, and prove A disposal did not clear B. Finally dispose B and join its captured composition scope Job and all retained transition/VM Jobs. This observes cleanup completion, including linked navigation/saver children, rather than relying on cleared counters. The deterministic common tests supply the exact queue schedules; the browser assertion establishes the actual production binding/disposal path.

The browser file may add private `capturePasswordNavigationGlobals`, `installPasswordNavigationGlobals`, and `restorePasswordNavigationGlobals` helpers. Snapshot property descriptors for every global key overwritten, including window, document, Node, Element, HTMLElement, HTMLDivElement, HTMLBaseElement, Text, Comment, DocumentFragment, Event and request/cancelAnimationFrame; restore absent keys by deletion and present descriptors exactly. If installation copies further constructors, snapshot that exact key set before assignment. Keep all compositions in one test-owned JSDOM document to respect Compose element caching, dispose compositions before closing the window, restore ClientPlugin.mainScaffoldConfigProvider and every changed global, remove hosts, close HttpClient/Koin, and cancel/join all retained jobs in finally. Use deferred events, bounded timeouts and actual rendering turns; do not lengthen Mocha timeout or insert sleeps. JSDOM and Android unit hosts remain the stated evidence limits. Every acceptance case above is automatable in the existing hosts; no operator waiver is required.

## V78-07: exact remaining declaration inventory at entry HEAD

The read-only AST/diff audit independently reproduced 73 issue-changed Kotlin files and 390 changed function/class/interface/object declarations by joining AST declaration start lines against `git diff --unified=0 master..HEAD`. I checked missing-comment candidates against source and inspected constructor signatures and anonymous-object member scopes separately. The finite remaining baseline is 24 functions, six non-constructor member properties, and 22 constructors missing 68 @param tags. Constructor members already described by inline KDocs retain those comments; constructor tags are still required. Local variables do not require KDocs. The newly added CommitThenThrowDeepLinksRepo.delegate member is also covered by its explicit constructor @param/purpose entry below. Unchanged legacy constructors such as DeepLinksServiceTest.FakeHandler and UserEditTestUsersModel, and untouched legacy Plugin overrides, are excluded. Existing sufficient purpose KDocs, stable serial names, five backing-state fixes, and four-factory descriptions remain intact.

The 24 function entries are the following exact declarations. Each sentence specifies the purpose to document; line numbers are entry-HEAD coordinates and enclosing names remain the identities after edits.

1. `features/ui/users/src/commonMain/kotlin/Plugin.kt:125`, the anonymous UsersModel `requestPasswordChangeEmail`, needs delegation purpose and exact expected approved email preservation; `:130`, `completePasswordChange`, needs exact approval-bound request delegation without account selection or retry.
2. `client/src/commonTest/kotlin/PasswordChangeInteractorTest.kt:132/:133`, `actualSubmittingViewModelDestructionPrecedesCompletedPersistence`'s anonymous repo save/get, need recorded hierarchy purpose and absent-restoration return semantics.
3. The same file `:207/:211`, `changedPendingReplacesCredentialRouteAndContinueAlwaysReachesUsersList`'s save/get, need transition recording and no initial holder semantics.
4. The same file `:275/:276`, `stalePendingCannotReplaceNewerDestination`'s save/get, need forbidden-save counting and no initial holder semantics.
5. The same file `:320/:321`, `cancelledSubmittingViewModelCannotStartChangedHandoff`'s save/get, need post-cancellation save counting and no initial holder semantics.
6. The same file `:358/:359`, `rootBindingDisposalCancelsPendingTransitionAndCleansOwnerJobs`'s save/get, need disposal save-attempt observation and no initial holder semantics.
7. The same file `:390/:391`, `unprocessedReplacementTimesOutAndCleansOwnerJobs`'s save/get, need timeout save-attempt observation and no initial holder semantics.
8. The same file `:422/:427`, `saveFailureDoesNotReplayAndLaterContinuePersistsUsersList`'s save/get, need first-attempt failure/subsequent persistence and no initial holder semantics.
9. `client/src/jsTest/kotlin/PasswordChangeNavigationBrowserTest.kt:127/:134`, canonical test persistenceRepo save/get, need real-adapter delegation followed by a matching-save signal, and current-URL restoration semantics.
10. `features/deeplinks/server/src/commonTest/kotlin/configurators/DeepLinksRoutingConfiguratorTest.kt:37`, `everyResolutionOutcomeCarriesNoStoreAndNoReferrer`'s handler tryHandle, needs configured success/unhandled behavior; `:71`, `handlerFailureIsSanitizedToInternalServerError`'s handler tryHandle, needs deliberate sensitive failure for sanitizer evidence.
11. `features/email/server/src/commonTest/kotlin/services/EmailPasswordChangeServiceTest.kt:204/:210`, `invalidApprovalMatrixFailsClosedWithoutPasswordWrites`'s local issued/invalid helpers, need real approval construction and invalid-completion/zero-write assertion purposes.
12. The same file `:230/:236`, `authorizationAndPolicyMatrixFailsClosedWithoutPasswordWrites`'s local issued/invalid helpers, need explicit fake clock/bridge fixture controls and InvalidApproval/zero-write assertion purposes.

The six missing non-constructor member comments are `PasswordChangeInteractorTest.kt:131` saved in the actual-submitting test, `:205` saved in changedPending/Continue, and `:421` persisted in saveFailure, each documenting what successful save snapshot is retained; `DeepLinksRoutingConfiguratorTest.kt:35` and `:70` anonymous handler id, each documenting the dispatch id selected by its enclosing test; and `features/ui/users/src/jsTest/kotlin/ui/BrowserViewTestFixture.kt:78` BrowserDomEnvironment.Companion.installed, documenting the shared installed DOM needed for Compose's cached element builders. The apparent `PasswordChangeFlowRoutingTest.kt:308` cause candidate is a local variable, not a missing member. If the RecordingPasswordNavigationRepo removes old anonymous repositories, record each removed save/get/property identity as replaced by that exact documented helper; do not claim a comment was added to a deleted declaration.

The 22 constructor entries below are exhaustive for the confirmed remaining baseline. Add the listed @param tags to the class KDoc, describing each existing parameter's actual role; keep existing member comments. Do not fill unrelated legacy parameters.

1. `client/src/commonMain/kotlin/PasswordChangeNavigationOwner.kt:151`, Binding: `root` is the currently bound hierarchy and `scope` owns accepted transition jobs.
2. `features/auth/client/src/commonMain/kotlin/utils/PasswordChangeTransport.kt:9`, PasswordChangeCompletionUrl: `string` is the JS-supplied trusted absolute completion endpoint.
3. `features/auth/client/src/commonTest/kotlin/KtorPasswordChangeFeatureTest.kt:48`, RecordingCredentialsStorage: `initialCredentials` seeds absent/unrelated browser credentials.
4. The same file `:84`, RecordingServerUrlStorage: `initialUrl` seeds the configured or hostile saved server URL.
5. `features/auth/common/src/commonMain/kotlin/models/PasswordChange.kt:10`, PasswordChangeEmailRequest: `expectedEmail` is the exact currently displayed approved address.
6. The same file `:33`, CompletePasswordChangeRequest: `userId` is the persisted-subject equality assertion, `approvalId` is the exact delivered approval, and `password` is request-only plaintext.
7. `features/deeplinks/server/src/commonTest/kotlin/services/DeepLinksServiceTest.kt:24`, CommitThenThrowDeepLinksRepo: `delegate` is actual map persistence that commits before the simulated lost response. Document the delegate member's purpose with that tag or an adjacent member KDoc as well.
8. `features/email/server/src/commonMain/kotlin/models/EmailPasswordChangePayload.kt:11`, EmailPasswordChangePayload: `userId`, `approvedEmail`, `expiresAtEpochMillis`, and `credentialState` describe bound subject, delivered current approved address, absolute expiry, and salted-credential fingerprint respectively.
9. `features/email/server/src/commonTest/kotlin/PasswordChangeFlowRoutingTest.kt:102`, PasswordChangeFlowGraph: `application`, `json`, `auth`, `passwordChange`, `links`, `emails`, `linksRepo`, `passwords`, `rolesRepo`, `roleAuthorization`, `ownerCredentials`, and `unrelatedCredentials` describe the isolated DI owner, production serializer, real services, external doubles, shared seeded membership/bridge, and pre-change credential pairs. Do not call the fake role repository production storage.
10. The same file `:135`, KtorLogCapture: `loggers` identifies the actual application/Ktor logger instances whose levels and appenders are restored.
11. `features/email/server/src/commonTest/kotlin/PasswordChangePluginTest.kt:75`, PasswordChangePluginGraph: `application`, `users`, `passwords`, and `linksRepo` describe isolated production DI and substituted persistence ownership.
12. `features/email/server/src/commonTest/kotlin/services/EmailPasswordChangeServiceTest.kt:32`, CredentialReplacingEmailsService: `beforeSuccess` performs the controlled credential mutation immediately before successful delivery returns.
13. `features/email/server/src/commonTest/kotlin/services/PasswordChangeTestFixtures.kt:26`, PasswordChangeRoleAuthorization: `directRolePresent` is the legacy fixture fallback; cycle 4 additionally needs `rolesRepo` explaining that supplied subject-specific direct membership takes precedence.
14. The same file `:58`, PasswordChangePasswordsRepo: `delegate` holds actual salted credentials under read/write hooks.
15. The same file `:112`, PasswordChangeDeepLinksRepo: `delegate` holds actual approval records under exact-id hooks.
16. The same file `:180`, PasswordChangeUsersRepo: `delegate` supplies real fixture users beneath coordinator/Auth read interception.
17. The same file `:198`, PasswordChangeFixture: `user`, `users`, `trackedUsers`, `roles`, `passwords`, `auth`, `coordinator`, `linksRepo`, `links`, and `service` describe the eligible account, underlying/hooked state, bridge, and real Auth/coordinator/deeplink/Email service graph.
18. `features/ui/users/src/androidUnitTest/kotlin/ui/PasswordChangeViewTest.kt:174`, PasswordChangeViewFixture: `application`, `chainScope`, `chain`, `chainJob`, `activity`, `config`, and `view` document owned DI/navigation/Robolectric resources, exact factory input, and concrete view.
19. `features/ui/users/src/commonMain/kotlin/ui/PasswordChangeViewConfig.kt:13`, Pending: `userId` is immutable approval subject and `approvalId` is the exact canonical opened UUID; neither parameter contains entered plaintext.
20. `features/ui/users/src/jsTest/kotlin/ui/BrowserViewTestFixture.kt:64`, BrowserDomEnvironment: `global`, `previousWindow`, `previousDocument`, `previousConstructors`, and `dom` document changed global owner, previous values, constructor snapshots, and shared JSDOM lifetime.
21. The same file `:158`, BrowserViewTestFixture: `host` is the per-test mount and `environment` is the shared DOM retained for Compose element cache identity; include the annotated environment parameter.
22. `features/ui/users/src/jvmTest/kotlin/ui/PasswordChangeViewTest.kt:163`, PasswordChangeViewFixture: `application`, `chainScope`, `chain`, `chainJob`, `config`, and `view` document DI/navigation cleanup ownership, exact factory input, and concrete JVM view.

The finite cycle-4 extension is also explicit. Update the purpose contracts for PasswordChangeNavigationOwner.bind, startTransition, activeBindingFor, onChanged, onContinue, Binding and transitions; document the local sampleTransition function and completion cleanup behavior. Document WithPasswordChangeNavigationBinding and its owner/root/content parameters. Update ClientPlugin.startPlugin's existing binding description. In PasswordChangeNavigationTestFixtures document HeldNavigationDispatcher, its schedulerDispatcher constructor @param/member and delegated virtual-time purpose, queue, queuedTaskCount, dispatch, runNext and drain; RecordingPasswordNavigationRepo, initialHolder/failFirstSave constructor parameters, holders/saveAttempts state, get/save/resetObservations; passwordNavigationConfigs and serializedPasswordNavigationHolder. Update HeldPasswordChangeUsersModel's class @param completionFeature, member completionFeature, returnedAfterCancellation and completePasswordChange contract. Document the six named new navigation tests and update the existing three actual-VM/cancellation/save-failure test purposes to match their actual assertions. Document the browser test's adapter save/get overrides and the three globals helpers if introduced. Update PasswordChangeRoleAuthorization's rolesRepo member/constructor and has/ensure purpose; document missingRoleBridgeRejectsOtherwiseIdenticalUnexpiredApproval and repositoryBackedRoleBridgeChecksTheRequestedSubject; update graph and sibling test descriptions for actual seeded membership assertions. These are the planned declaration additions/changes, not permission to create another fixture framework. Any routine local named function subsequently needed must be added by exact file/name to that increment's ledger before the LL pass.

## README updates

V78-08 remains resolved. No README text change is required by this architecture: the existing exact-node stale-call and root-owned persistence descriptions already state the intended corrected contract. If the final implementation makes a concrete sentence inaccurate, the LL Coding increment may update only that sentence in Common/UI-users Architecture Notes and identify it in its report. Never change Operator Notes. Compare each exact `## Operator Notes` heading through the byte immediately preceding the next `## ` heading with master and cycle entry; the five sections have the same hash boundary used by Validation 047. No renderer description, policy, older-history promise, or durability limit is weakened.

## V78-11: canonical forward regeneration and source claim corrections

The single AML-HIP block below is the canonical regenerated handoff `issue_78_cycle4_regenerated_handoff`. It replaces operational reliance on invalid or unsupported source claims while leaving every old file immutable. Every source report 032–047 has an exact filename, original COMMUNICATION UUID, classification, corrected claim, and evidence limit in ACTION. The current finding records fully repeat all twelve identities and counts. Planning 048 and this Architecture do not supersede Validation 047's FAIL or its evidence authority.

Reports 032–039 and 041 contain a literal false self-check flag and therefore require regeneration under the user's all-parameters-true rule. Their plans/execution history can remain evidence with the limits encoded below. In particular, 032/033 are historical design intent, not implementation proof. 034's retained test did not join the real Job before a held save or inspect actual serialized secret absence. 035 did not prove a new Continue save by checking an already non-null saved field. 037's claims of forced destruction/save, cancellation completion, current-root detachment, HTTP non-replay, production composition, and direct transition reload exceed the retained tests; replace them with Validation 047's bounded ownership diagnostic and the still-required V78-05/V78-12 cases. V78-10 stays resolved on that independent diagnostic.

038's missing-bridge claim is limited by the 1,000-versus-wall-clock expiry confounder, and its grant comparison used empty lists authorized by a subject-independent Boolean. Retain the actual three approvals, sibling rejection, account separation, real access/refresh/getMe, and exact write counts accepted by 047. 039 establishes the owner eligibility and DOM portion only; V78-05 as a whole remains open. 040's all-declarations and V78-07 closure claims are superseded by the 24 functions, six member properties and 22 constructors listed here; its evaluative `results_recorded_and_honest` value is replaced by factual states. V78-08's resolution rests on 047's independent README comparison. 036's valid typed-classifier/cancellation/log evidence remains under resolved V78-09 despite its invalid self-check flag.

For 040–046, mechanical heading/UUID/pronoun checks establish only those checked properties. They do not validate literal false flags, evidence accuracy, or finding closure. Retain 041's eventual full browser-suite pass as execution evidence, without inferring the missing lifecycle assertions. Retain 042's failed full gate and overlapping-output limitations. Retain 043's recorded 151-second overlap, isolated scaffold pass, nonzero generated class, and lint result; the overlap is causal evidence, not a newly controlled reproduction of a race or proof excluding every source cause. Retain 044's wrapper-lock failure with zero initialized Gradle tasks/XML and 045's permission handback without new execution. Retain 046's source-equivalent full build, 4,548 tasks and 208 suites/995 tests as historical execution, not a cycle-4 aggregate. Retain 047's fresh 19 suites/112 selected tests, separately run positive DOM case, both diagnostics, and distinction between that run and the mixed 184-suite/863-test XML set.

All new 048+ blocks must have exactly the eleven headings used below, factual explicit values, a fresh communication UUID, no pronouns, and all six self-check fields literally true, including ambiguity_detected. Actual unexecuted cycle-4 implementation and external-runtime evidence supplies the uncertainty. Do not invent certainty to close that field or change it to false. Later Coding/Verification reports reference this canonical source ledger and add only their actual deltas/results. No old report is edited, renamed, or rewritten, and no personal auto-memory is used.

## Sequential implementation, exact gates, and stop boundary

The Orchestrator assigns ML Coding to implementation/test increments and LL Coding to KDoc/documentation fillings. The sequence is finite: first add the detached-at-entry and detached-while-queued regressions and run the red gate; then fix live membership/observation/cleanup and add the shared production binding seam, running the green owner gate; then retain actual-VM/MockEngine/production-composition regressions and their focused gates; then repair security fixture controls and execute security gates; then the LL agent fills the exact declaration inventory and any required wording delta, recording removed/moved declarations accurately. A later ML gate increment can execute fresh aggregates after LL changes, without asking the LL agent to redesign behavior. Each increment writes only its own next monotonic report plus authorized files and commits without push. Source changes require ast-index rebuild; report/README-only changes do not. Preserve Operator Notes and run git diff --check per increment.

The initial red and owner green gate is `./gradlew --no-daemon :wishlist.client:jvmTest --tests '*PasswordChangeInteractorTest' --console=plain`. Record exact failures for detachedPendingAtEntryStartsNoTransition and detachedWhileQueuedStopsWithoutLeafEmission before the narrow owner correction; no earlier finding identity changes. The same class gate after implementation must exercise every named owner case and actual-VM lifecycle test. Run `./gradlew --no-daemon :wishlist.client:jsBrowserTest --tests '*PasswordChangeNavigationBrowserTest' --console=plain` for actual production composition, transport, direct reload, and disposal/rebinding. Run `./gradlew --no-daemon :wishlist.features.email.server:jvmTest --tests '*EmailPasswordChangeServiceTest' --tests '*PasswordChangeFlowRoutingTest' --console=plain` for corrected bridge and seeded-role evidence. Preserve the focused UI regression gate `./gradlew --no-daemon :wishlist.features.ui.users:jvmTest --tests '*PasswordChange*' --tests '*UserEditViewModelPasswordChangeTest' --console=plain`. Test names/discovery and actual assertions must be inspected, not only task exits.

After bounded implementation and LL documentation, run one fresh server aggregate: `./gradlew --no-daemon :wishlist.features.common.server:jvmTest :wishlist.features.email.server:jvmTest :wishlist.features.auth.server:jvmTest :wishlist.features.deeplinks.server:jvmTest :wishlist.features.auth.common:jvmTest :wishlist.features.auth.client:jvmTest --rerun-tasks --console=plain`. Then run one fresh platform aggregate: `./gradlew --no-daemon :wishlist.features.ui.users:jvmTest :wishlist.features.ui.users:jsNodeTest :wishlist.features.ui.users:jsBrowserTest :wishlist.features.ui.users:testDebugUnitTest :wishlist.features.ui.users:testReleaseUnitTest :wishlist.client:jvmTest :wishlist.client:jsNodeTest :wishlist.client:jsBrowserTest --rerun-tasks --console=plain`. Inspect fresh per-task XML for shared owner/lifecycle cases on JVM/Node/browser, actual composition browser case, positive and negative owner DOM, concrete JVM/Android debug/release forms, zero relevant failures/errors/skips, and browser-only Node exclusions. An unfiltered platform aggregate avoids omission of the positively named owner DOM case. Record actual totals; do not reuse old counts or relabel a subsequent filtered XML mixture as another aggregate.

Every Gradle invocation requires a preflight check for active Gradle/Java build processes and Orchestrator coordination. Use approved/escalated wrapper-cache access from the outset, one foreground execution session, and poll that same session through its actual exit. No overlapping builds, detached retry, second tee writer, or redundant concurrent compile may touch shared outputs. No build is run by this Architecture. Independent Verification subsequently runs exactly one escalated foreground gate with pipefail: `./gradlew build --rerun-tasks 2>&1 | tee /tmp/build-output.txt`. Capture the actual pipeline exit and terminal result; after completion archive under a distinct issue-78/step path and compare bytes. Inspect fresh XML/lint/source-HEAD equivalence and all integrity checks. The established allTests fallback applies only after successful build without test execution, never as an automatic retry of failure.

Before committing this report, programmatic and manual checks cover one AML block, eleven ordered headings, a fresh UUIDv4, exact mappings for all 16 source communications, twelve distinct current finding records, all six literal true flags, pronoun absence, dense factual lines, and observed/planned claim separation. Old reports are compared against `d839d39e72b5aaf4c5f187c5cbc4b15dcc9786f1`; 048 is additionally compared against entry HEAD. The only authored file is 049. Stop Coding after these bounded corrections, matching focused/aggregate evidence, and complete reports; the Orchestrator then obtains independent Verification and Validation. Only Validation can close/recur findings or apply subsequent escalation. No live SMTP, device/browser farm, external message, PR action, or push is included.

## QUESTIONS FOR OPERATOR

There are no operator questions. The issue, Operator Notes, Validation 047 and Planning 048 establish the acceptance boundary. Remaining uncertainty is implementation and execution evidence to be supplied by subsequent roles, not a missing product decision.

```text
ENTITY:
entity_id=issue_78_cycle4_regenerated_handoff; type=canonical_correction_architecture; state=ready_for_sequential_coding

CONTEXT:
* task_id=09.09.2026_09.40.05-b512784f-672e-4309-aa90-4551cb0c3361; agent_id=issue78_architecture_cycle4; memory_ref=[PROMPT.md,032-planning.md,033-architecturing.md,034-coding.md,035-coding.md,036-coding.md,037-coding.md,038-coding.md,039-coding.md,040-coding.md,041-coding.md,042-verification.md,043-coding.md,044-verification.md,045-coding.md,046-verification.md,047-validating.md,048-planning.md]
* entry_head=87e27416e521ab773d572034bed37598114fd6e0; cycle_entry=d839d39e72b5aaf4c5f187c5cbc4b15dcc9786f1; branch=fix/issue-78-email-authorized-password-change; constraints=[049_only,no_nested_agents,no_push,no_checkout,prior_reports_immutable,Operator_Notes_unchanged,serial_approved_Gradle,ML_implementation,LL_KDocs]

ACTION:
1. action=regenerate_source_record; target=032-planning.md; source_uuid=503d9759-ac50-4449-b95f-1913c51fa019; classification=invalid_literal_selfcheck_historical_plan; corrected_claim=cycle3_design_intent; evidence_limit=implementation_unproven; current_state_authority=047-validating.md
2. action=regenerate_source_record; target=033-architecturing.md; source_uuid=57f2aaf4-22e4-4914-8eb9-418095b42e50; classification=invalid_literal_selfcheck_historical_architecture; corrected_claim=installed_API_and_planned_root_membership_contract; evidence_limit=architecture_intent_without_implementation_or_retained_test_proof; replacement_design=049
3. action=regenerate_source_record; target=034-coding.md; source_uuid=20f5ba69-8e68-45a8-9161-3614960ba2d8; classification=invalid_literal_selfcheck_lifecycle_claim_limited; corrected_claim=root_owner_and_cancellation_guard_implemented; evidence_limit=[no_held_root_actual_Job_join,no_serialized_secret_assertion]; V78-10_authority=047_diagnostic; V78-05=open
4. action=regenerate_source_record; target=035-coding.md; source_uuid=8c09a330-f94f-4b29-b759-1a29c6d41a22; classification=invalid_literal_selfcheck_Continue_claim_limited; corrected_claim=nonlast_stale_rejection_and_UsersList_transition_tested; evidence_limit=already_nonnull_holder_does_not_prove_new_Continue_save; required=actual_Completed_VM_and_new_save_assertion
5. action=regenerate_source_record; target=036-coding.md; source_uuid=6fa55006-2e23-4b1f-b631-8f11e6244c54; classification=invalid_literal_selfcheck_retained_classifier_evidence; corrected_claim=production_StatusPages_Auth_status_log_cancellation_matrix; evidence_limit=callback_cancellation_contract_excludes_cancelled_network_response_guarantee; V78-09=resolved_by_047
6. action=regenerate_source_record; target=037-coding.md; source_uuid=3d9a1393-e7b3-4eb7-b44c-0d1df54b8592; classification=invalid_literal_selfcheck_unsupported_lifecycle_claims; corrected_claim=seven_existing_lifecycle_tests_and_adapter_URL_assertions_executed; evidence_limit=[forced_VM_join_absent,cancellation_collector_join_absent,current_root_membership_absent,HTTP_model_absent_in_save_failure,direct_bind_only,synthetic_Completed_reload]; replacement_evidence_required=[V78-05,V78-12]; V78-10=resolved_by_047_diagnostic
7. action=regenerate_source_record; target=038-coding.md; source_uuid=203e1da4-78e4-4187-a4aa-4cdc2c909148; classification=invalid_literal_selfcheck_security_claims_limited; corrected_claim=[three_real_approvals,sibling_rejection,two_accounts,real_access_refresh_getMe,exact_writes_1_1_2]; evidence_limit=[bridge_null_wall_clock_expiry,empty_role_snapshots,subject_independent_Boolean_bridge]; required=[same_clock_Auth_read_sequence,seeded_repo_backed_memberships]; V78-04=open
8. action=regenerate_source_record; target=039-coding.md; source_uuid=8588de83-fa9b-44c8-8a84-b75de2b40a81; classification=invalid_literal_selfcheck_scope_limited; corrected_claim=owner_eligibility_stale_state_later_mutation_and_DOM_portion_verified; evidence_limit=whole_V78-05_completion_unsupported; V78-05=open
9. action=regenerate_source_record; target=040-coding.md; source_uuid=a63aff60-398b-4452-b4a5-6d75b3cd2ffd; classification=semantic_claims_superseded; corrected_claim=partial_KDoc_and_README_edits_with_browser_timeout_recorded; evidence_limit=[all_declarations_audit_unsupported,V78-07_closure_unsupported,evaluative_status_removed,mechanical_protocol_checks_only]; remaining_inventory={functions:24,member_properties:6,constructors:22,param_tags:68}; V78-08=resolved_by_047
10. action=regenerate_source_record; target=041-coding.md; source_uuid=91f70bd8-e44b-4d71-a3f9-07e90c3ef1e2; classification=invalid_literal_selfcheck_historical_execution; corrected_claim=eventual_unfiltered_browser_and_aggregate_pass; evidence_limit=[missing_lifecycle_assertions_not_proven,protocol_semantics_not_established,Android_unit_hosts_only]; V78-05=open
11. action=retain_source_record; target=042-verification.md; source_uuid=3c2ad9ff-d4fc-4d8d-b02c-2a25b51935e3; classification=historical_failed_build_limited; corrected_claim=BUILD_FAILED_lintAnalyzeDebug_after_2351_tasks_with_208_suites_995_tests_recorded; evidence_limit=[overlapping_output_writers,missing_runner_status_sidecar,mechanical_protocol_checks_only]; full_gate=failed
12. action=retain_source_record; target=043-coding.md; source_uuid=6092c783-907b-4649-a245-9f1d471954e8; classification=historical_diagnostic_limited; corrected_claim=[recorded_overlap_151_seconds,isolated_scaffold_BUILD_SUCCESSFUL_44s,lint_errors_0,lint_warnings_1,class_bytes_7014]; evidence_limit=[overlap_causal_inference_not_controlled_race_reproduction,no_full_build,no_source_edit,mechanical_protocol_checks_only]
13. action=retain_source_record; target=044-verification.md; source_uuid=039160f3-4b08-4cb4-ac97-074ff8ffd51b; classification=historical_wrapper_access_failure; corrected_claim=wrapper_lock_read_only_before_Gradle_initialization; evidence_limit={Gradle_tasks:0,lint_tasks:0,test_tasks:0,fresh_XML:0,protocol_checks:mechanical_only}; source_failure_reproduction=absent
14. action=retain_source_record; target=045-coding.md; source_uuid=0c2f4691-bd0e-4b1c-bcf2-9685af2ad31b; classification=historical_permission_handback; corrected_claim=approved_wrapper_and_single_foreground_build_required; evidence_limit=[no_new_build,no_product_change,043_isolated_result_retained,protocol_checks_mechanical_only]
15. action=retain_source_record; target=046-verification.md; source_uuid=9ecdd25b-08b2-4818-acd2-5026495b4e71; classification=historical_source_equivalent_full_build; corrected_claim={pipeline_exit:0,tasks:4548,suites:208,tests:995,failures:0,errors:0,skips:0,log_bytes:600662}; evidence_limit=[cycle4_gate_not_run,absent_assertions_not_proven,protocol_checks_mechanical_only,JSDOM_and_Android_unit_hosts]
16. action=retain_source_record; target=047-validating.md; source_uuid=3931f0bd-2fb8-43f9-a414-f5914ad526da; classification=current_independent_validation_authority; corrected_claim={result:FAIL,open:5,resolved:7,High:2,Medium:2,Low:1,focused_suites:19,focused_tests:112,positive_DOM_extra:1}; evidence_limit=[mixed_XML_184_863_not_fresh_full_build,ownership_diagnostic_not_retained_test,detachment_diagnostic_no_obsolete_save_proof,external_runtime_unexecuted]
17. action=record_current_finding; target=V78-01; severity=High; historical_count=1; consecutive_unresolved_count=0; state=resolved; correction=concrete_Pending_Completed_polymorphic_serializers; evidence_authority=047-validating.md; evidence_limit=retain_serializer_regressions
18. action=record_current_finding; target=V78-02; severity=High; historical_count=1; consecutive_unresolved_count=0; state=resolved; correction=raw_synchronous_submission_and_duplicate_admission_guard; evidence_authority=047-validating.md; evidence_limit=retain_raw_guard_regressions
19. action=record_current_finding; target=V78-03; severity=Medium; historical_count=1; consecutive_unresolved_count=0; state=resolved; correction=SMTP_only_DeliveryFailed_exact_cleanup_and_exception_precedence; evidence_authority=047-validating.md; evidence_limit=in_process_compensation_excludes_crash_recovery
20. action=record_current_finding; target=V78-04; severity=High; prior_severity=Medium; historical_count=3; consecutive_unresolved_count=3; state=open; correction=[identical_1000_clock_and_real_fingerprint,negative_Auth_two_user_reads,seeded_subject_repo_bridge,nonempty_grant_snapshots]; retained=[three_approvals,two_accounts,access_refresh_getMe,writes_1_1_2,zero_mutation_attempts]; evidence_authority=047-validating.md; evidence_limit=cycle4_security_assertions_unexecuted; escalation_authority=047
21. action=record_current_finding; target=V78-05; severity=High; prior_severity=Medium; historical_count=3; consecutive_unresolved_count=3; state=open; correction=[held_root_actual_VM_Job_join,serialized_holder,actual_Completed_Continue,cancelled_return_join,MockEngine_single_request,shared_production_composition,direct_success_URL_reload,disposal_rebinding]; corrected_portion=owner_eligibility_DOM; evidence_authority=047-validating.md; evidence_limit=retained_lifecycle_assertions_incomplete; escalation_authority=047
22. action=record_current_finding; target=V78-06; severity=Medium; historical_count=1; consecutive_unresolved_count=0; state=resolved; correction=live_feedback_masked_singleline_native_inputs_guarded_Done; evidence_authority=047-validating.md; evidence_limit=JVM_Android_unit_host_forms
23. action=record_current_finding; target=V78-07; severity=Medium; prior_severity=Low; historical_count=3; consecutive_unresolved_count=3; state=open; correction=LL_purpose_KDocs_and_constructor_tags; inventory={Kotlin_files:73,changed_nonproperty_declarations:390,missing_functions:24,missing_member_properties:6,missing_constructors:22,missing_param_tags:68}; evidence_authority=[047-validating.md,049_AST_diff_audit]; evidence_limit=fillings_and_cycle4_extensions_pending; escalation_authority=047
24. action=record_current_finding; target=V78-08; severity=Low; historical_count=2; consecutive_unresolved_count=0; state=resolved; correction=accurate_renderer_root_lifetime_URL_and_HTTP_documentation; evidence_authority=047-validating.md; evidence_limit=Operator_Notes_immutable_and_durability_limits_retained
25. action=record_current_finding; target=V78-09; severity=High; historical_count=1; consecutive_unresolved_count=0; state=resolved; correction=typed_Ktor_status_preservation_sanitized_logs_cancellation_identity; evidence_authority=047-validating.md; evidence_limit=callback_contract_excludes_network_cancellation_response_guarantee
26. action=record_current_finding; target=V78-10; severity=High; historical_count=1; consecutive_unresolved_count=0; state=resolved; correction=root_owned_success_handoff_outlives_Pending; evidence_authority=047_actual_VM_Job_join_diagnostic; evidence_limit=diagnostic_not_retained_regression_and_no_crash_durability; remaining_regression_identity=V78-05
27. action=record_current_finding; target=V78-11; severity=Low; historical_count=2; consecutive_unresolved_count=2; state=open; correction=canonical_16_source_12_finding_regeneration_and_literal_048_plus_blocks; evidence_authority=047-validating.md; evidence_limit=independent_forward_semantic_validation_pending
28. action=record_current_finding; target=V78-12; severity=Medium; historical_count=1; consecutive_unresolved_count=1; state=open; correction=[identity_findNodeInSubTree_findChainInSubTree,current_root_changesInSubTreeFlow,admission_and_presave_checks,prompt_detachment_cleanup]; evidence_authority=047_detached_subtree_diagnostic; evidence_limit={accepted_detached_jobs:1,saves:0,timeout_cleanup_ms:5001,obsolete_save_proof:absent}
29. action=specify_implementation; target=sequential_Coding; params={order=[red_owner_cases,ML_owner_binding_fix,ML_actual_VM_transport_browser_tests,ML_security_fixtures,LL_KDocs,ML_fresh_aggregates],production_files=[PasswordChangeNavigationOwner.kt,ClientPlugin.kt,utils/PasswordChangeNavigationBinding.kt],dependency_delta=existing_ktor_client_mock_commonTest_only,Operator_Notes=immutable}
30. action=handoff; target=orchestrator_and_Coding; params={canonical_entity=issue_78_cycle4_regenerated_handoff,source_records:16,finding_records:12,open_total:5,resolved_total:7,count_changes:0,operator_questions:0,next_report:050-coding.md,push:absent}

REASON:
* condition=unexpired_identical_approval_with_absent_Auth_bridge; requirement=independent_bridge_guard_evidence; causal_chain=shared_clock_and_repository_hooks→two_user_reads_without_password_read→Auth_boundary_rejection_proof
* condition=ancestor_removal_preserves_parent_links; requirement=current_root_identity_membership; causal_chain=live_traversal_and_root_subtree_wakeup→prompt_stale_operation_termination→zero_stale_save
* condition=Pending_lifecycle_Job_cancels_before_persistence; requirement=retained_root_ownership_regression; causal_chain=held_root_and_actual_Job_join→serialized_Completed_save→direct_URL_reload
* condition=literal_false_flags_or_unsupported_historical_claims; requirement=immutable_history_and_complete_regeneration; causal_chain=source_UUID_mapping_and_explicit_evidence_limits→canonical_twelve_finding_handoff→independent_forward_validation

EXPECTED RESULT:
* entity_id=issue_78_cycle4_regenerated_handoff; new_state=committed_architecture_authority_for_bounded_Coding; location=agents/task/09.09.2026_09.40.05-b512784f-672e-4309-aa90-4551cb0c3361/049-architecturing.md
* entity_id=cycle4_implementation; new_state=focused_and_aggregate_evidence_required; location=subsequent_monotonic_Coding_reports; closure_authority=independent_Validation

VERIFICATION:
* check=entry_integrity; expected=clean_assigned_branch_at_87e27416e521ab773d572034bed37598114fd6e0; result=matched; authored_files=049_only
* check=installed_Navigation_0_7_7_sources; expected=[identity_live_walk,subtree_StateFlow_replay,flatMapLatest_detachment,queued_replace,synchronous_save,composition_scope]; result=inspected; dependency_changes=0
* check=bounded_declaration_audit; expected=Validation_047_baseline_reproduction; result={files:73,changed_nonproperty_declarations:390,missing_functions:24,missing_member_properties:6,missing_constructors:22,missing_param_tags:68}; method=AST_diff_and_manual_source_review
* check=canonical_ledger; expected={source_reports:16,exact_source_UUIDs:16,current_finding_records:12,ordered_sections:11,fresh_UUID_v4:1,literal_true_selfchecks:6,prohibited_pronouns:0}; result=validated_before_commit
* check=repository_report_scope; expected=[prior_reports_byte_unchanged,048_entry_blob_unchanged,five_Operator_Notes_master_equal,diff_check_pass]; result=validated_before_commit; product_test_runs=0; completed_index_rebuilds=0
* check=cycle4_runtime_gates; expected=[red_green_owner,production_composition_browser,security,UI_preservation,server_aggregate,platform_aggregate,independent_full_build,independent_Validation]; result=specified_pending_Coding_and_Verification

UNCERTAINTY:
* missing=cycle4_implementation_and_execution_results; ambiguity=specified_regressions_and_cleanup_schedules_require_subsequent_execution
* missing=independent_acceptance_of_canonical_forward_regeneration; ambiguity=V78-11_remains_open_until_Validation
* missing=[live_SMTP,graphical_browser_runtime,native_device_runtime,process_crash_durability]; ambiguity=repository_hosts_and_in_process_boundaries_exclude_external_runtime_guarantees

REPETITION OF RESULT:
* entity_id=issue_78_cycle4_regenerated_handoff; stored_in=shared_memory; status=available; source_records=16; finding_records=12; open_findings=[V78-04,V78-05,V78-07,V78-11,V78-12]; resolved_findings=[V78-01,V78-02,V78-03,V78-06,V78-08,V78-09,V78-10]; count_changes=0

COMMUNICATION:
* sender=issue78_architecture_cycle4; receiver=orchestrator_and_Coding; task_id=09.09.2026_09.40.05-b512784f-672e-4309-aa90-4551cb0c3361; message_id=5f22ce1f-8f3b-46e7-a1a5-f22b335bbf3d; protocol=AML-HIP; canonical_entity=issue_78_cycle4_regenerated_handoff; next_step=050; operator_questions=0

PERSISTENCE:
* local_memory=true; shared_memory=true; index_keys=[task_id,issue_78_cycle4_regenerated_handoff,cycle4_architecture,source_ledger_032_047,finding_ledger_12,V78-04,V78-05,V78-07,V78-11,V78-12]; persistence_medium=tracked_step_report_only; personal_auto_memory=disabled

VALIDATION:
* format_valid=true; no_pronouns=true; entities_explicit=true; high_density=true; causal_chain_present=true; ambiguity_detected=true
```
