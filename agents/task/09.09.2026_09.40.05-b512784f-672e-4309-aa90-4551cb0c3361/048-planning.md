Model: Inherited Codex GPT-6 Planning agent. The assigned HL reasoning role is appropriate for the authorization evidence, lifecycle ordering, reachability, and report-correction decisions. The explicit prohibition on nested agents takes precedence over the documentation-model preference, so this role writes its own report.
Changed files: agents/task/09.09.2026_09.40.05-b512784f-672e-4309-aa90-4551cb0c3361/048-planning.md

## Decision and inspected evidence

Correction cycle 4 is ready for Architecture at step 049. Entry HEAD was clean `d839d39e72b5aaf4c5f187c5cbc4b15dcc9786f1` on `fix/issue-78-email-authorized-password-change`. Validation 047 remains FAIL. The correction contains exactly its five open findings: V78-04, V78-05, V78-07, V78-11, and V78-12. Planning neither closes findings nor changes counts. The original V78-10 ownership defect remains resolved; V78-05 retains the missing durable regression, and V78-12 identifies a separate current-root membership defect.

I read AGENTS, SHORTCUTS, ALL/local.ALL, PLAN, MODELS, PROTOCOL, GIT, TOOLS, AST_INDEX, the caveman skill, PROMPT, complete Validation 047, and the relevant complete reports 031–046, including Verification 042/044 and Coding 043/045. No local.PLAN.md exists. I read the Common, Auth, Email, DeepLinks, and UI/users READMEs in full, including Operator Notes. There is no client README. I inspected the affected navigation owner, production root binding, actual PasswordChangeViewModel, common/browser lifecycle tests, real service/routing fixtures, role repository/bridge, and Auth/Email approval checks. Existing ast-index supplied file/symbol/outline/reference navigation and the bounded declaration locations. The sandbox initially reported no index; approved cache access found the existing index. No source index rebuild, product test, Gradle session, source edit, previous-report edit, checkout, push, or external message was needed for Planning. Caveman full applies only to internal working notes; report and commit use normal prose.

Validation 047's independent source review and compiled-runtime diagnostics determine the correction targets. Its forced ordering joined the actual submitting ViewModel Job while root persistence was held: Pending was destroyed, plaintext was empty, and saves were zero; releasing root work then saved exactly one structurally checked Completed hierarchy with the exact request. That resolves the original ownership defect, but the diagnostic is not a retained repository test. Its detached-subtree diagnostic separately showed immutable ancestry still matching after ancestor replacement, one accepted owner job, zero saves, and cleanup only after 5,001 virtual milliseconds. That proves needless stale admission and delayed cleanup; it does not prove an obsolete save or a password authorization bypass.

The source independently confirms the remaining evidence gaps. `EmailPasswordChangeServiceTest.kt:259` omits the deterministic clock on the bridge-null service, allowing the expiry guard to reject before Auth. `PasswordChangeFlowRoutingTest.kt:206` constructs an unseeded FakeRolesRepo and a separate Boolean bridge. `PasswordChangeInteractorTest.kt:163` drains the scheduler without holding root persistence or joining the actual ViewModel Job, and its secret assertions use redacted `toString()`. The browser test directly binds the owner at line 159 and later saves a synthetic Completed holder at line 209 before reload. `PasswordChangeNavigationOwner.kt:145` checks ancestry, and its observer at line 102 listens only to the leaf stack. These concrete omissions, rather than suite names or counts, define the work below.

Verification 046's source-equivalent full build remains historical evidence: 4,548 executed tasks and 208 suites/995 tests. Validation 047 then reran selected suites and reported 19 suites/112 tests plus the separately executed positive owner DOM case. The resulting mixed repository XML set was 184 suites/863 tests. None of those counts is a fresh cycle-4 aggregate or proof of an assertion absent from the tests.

## Retained findings and acceptance boundaries

Retain V78-04 as High, consecutive unresolved count 3, automatically escalated from Medium; V78-05 as High, count 3, automatically escalated from Medium; V78-07 as Medium, count 3, automatically escalated from Low; V78-11 as Low, count 2; and V78-12 as Medium, count 1. There are zero Critical, two High, two Medium, and one Low findings, totaling five open findings. The three escalation events recorded in Validation 047 remain historical events; Planning does not escalate again or restart any counter.

Keep seven resolved identities. V78-01 and V78-02 retain historical High severity and historical count 1 each. V78-03 and V78-06 retain historical Medium severity and historical count 1 each. V78-08 retains historical Low severity and historical count 2. V78-09 and V78-10 retain historical High severity and historical count 1 each. Every resolved finding has consecutive unresolved count zero. Preserve the owner-eligibility/DOM corrections within V78-05 without treating that completed portion as closure of the remaining lifecycle evidence. Independent Validation alone determines subsequent resolution, recurrence, and application of the existing escalation rules.

Preserve the issue's six acceptance conditions: authenticated owner issuance requires enabled SMTP and the matching approved non-null current email; delivery targets that address; the purpose-specific GET is read-only and carries the same canonical approval UUID to the client; matching password submission redeems that immutable UUID for its persisted account; invalid approvals fail closed; and the implementation reuses existing deeplink/password boundaries with server, client, navigation, and UI tests. Ambient browser credentials never select the completion account.

The correction preserves the 15-minute expiry, purpose/type/subject checks, approved-email equality, direct User requirement, salted credential fingerprint, coordinator-before-Auth lock ordering, final authoritative reread, consume-before-write, cancellation/cleanup precedence, and unchanged roles/access/refresh sessions. Retain concrete Pending/Completed serializers, raw synchronous admission, field clearing, live validation, masked native input and guarded Done, trusted completion transport, policy headers, and typed sanitized HTTP classification. Do not introduce another approval scheme, dependency migration, global saver, role/session policy, route family, logging framework, live SMTP requirement, device/browser farm, or application E2E framework.

## V78-04: isolate the bridge and observe real memberships

Correct the missing-bridge case in `features/email/server/src/commonTest/kotlin/services/EmailPasswordChangeServiceTest.kt` with one explicit deterministic `nowEpochMillis` function passed to both the issuing/accepting fixture and the bridge-null Email service. The same function must return the same instant during both completion attempts. Use one real persisted approval, its real current salted credential fingerprint, identical subject/current approved email/purpose/expiry/password policy, the same users/password/deeplink/coordinator state, and the same completion request. Confirm the payload is unexpired and the bridge-present read-only validation succeeds before the negative attempt. Change only Auth's role bridge for that attempt; unrelated fixture options must not introduce another rejection condition. The same approval must remain present after bridge-null rejection and complete successfully through the bridge-present service with exactly one password write.

Make reaching Auth observable through existing repository hooks, without a production testing API. Auth's `passwordChangeStateWhileLocked` reads the current user before checking its bridge and reads the password only after that bridge succeeds. Architecture should specify the exact expected coordinator/Auth read sequence from the inspected code: after resetting counters immediately before completion, record the coordinator user read and the additional Auth current-user read, while the bridge-null path performs no subsequent password read, no consume, and no password write. The deterministic unexpired clock and successful identical-state control rule out earlier Email rejection; the additional Auth read proves execution reached the intended boundary. A separate absent-bridge test at the real Auth method can supplement that proof if needed. Keep the separately valid revoked-role test and all existing policy/fingerprint/final-reread/overlap/cancellation/write-failure cases.

In `PasswordChangeFlowRoutingTest.graph`, seed real direct User memberships for subjects 7 and 8 in the existing FakeRolesRepo before credentials are issued. Bind PasswordChangeRoleAuthorization to that exact repository and the production subject conversion, so `hasUserRole(userId)` derives its answer from the requested subject's direct User membership. The route graph must not authorize with the independent default Boolean. A fixture-only repository/lookup parameter can preserve the separately controlled single-account fixtures, provided the route graph explicitly selects repository-backed authorization. Keep `ensureUserRole` and all include/exclude/create/remove attempt counters observable; an attempted mutation must count even if idempotent. Reset bootstrap counters only after the memberships and pre-existing credentials exist.

Assert that both before snapshots actually contain the expected direct User membership and are not empty. Compare immutable membership snapshots after owner A completes, after sibling B rejects, and after account 8 independently completes; compare the complete grant map as well when useful to exclude additional grants. Require zero include/exclude/create/remove/ensure attempts throughout the measured flow. Preserve the three actual approvals, initial read-only GETs, exact owner write count 1, unchanged count after sibling rejection, final count 2 after account 8 completion, and both accounts' old/new password assertions. Keep pre-existing access-token getMe checks before refresh rotation, then real refresh/getMe checks for both original subjects. Changing production authorization is conditional on a newly failing assertion; the present finding is incomplete security evidence.

## V78-12: current-root membership and prompt observer cleanup

Confine the production correction to `client/src/commonMain/kotlin/PasswordChangeNavigationOwner.kt` and the minimal production root-binding seam needed by V78-05. Architecture must select an exact identity-based traversal of the current bound root's live stacks and current node subchains, or prove an installed equivalent. Following `parentNode.chain` upward establishes ancestry but cannot establish current membership after detachment. At admission require the current live binding/scope, the exact node reachable from that binding's root, the exact node still last in its own chain, and Pending for onChanged. A detached-at-entry node must allocate zero transition jobs and queue zero replacement/save work. Apply equivalent stale-call protection to Continue.

Recheck membership when accepted root work starts. Register observation before queueing replacement, covering both the leaf stack and bound root-subtree removals through the existing Navigation APIs. Architecture must inspect the exact installed 0.7.7 subtree-flow behavior, including initial-state sampling and removal propagation, before choosing the implementation. Root removal of an ancestor must terminate a queued observer promptly even if the removed leaf never emits again. Do not wait for the five-second fallback when detachment or supersession is already observable. Keep deduplication, binding identity protection, root cancellation, finite five-second timeout, and cancellation/join cleanup.

After replacement, validate the exact returned replacement identity in the current root hierarchy immediately before synchronous save. The destroyed old Pending node need not remain reachable after successful replacement; apply post-replacement membership checks to its accepted chain and exact replacement. Save `root.storeHierarchy()` only after those checks, in the same root-dispatcher turn without a suspension between snapshot and save. A newer pushed destination must be preserved when the exact replacement still exists underneath it. If the exact replacement disappears or the containing subtree detaches before save, save nothing and do not navigate back. Parent ancestry alone must not authorize the save. No navigation-library or global hierarchy-saver redesign is needed.

Retain tests for detached-at-entry, detached-while-queued with no leaf emission, replacement superseded before save, and accepted replacement followed by a newer current destination. Observe exact current configs and node identities, replacement/save counts, and zero remaining owner jobs. The latter case must save the actual later hierarchy with Completed present where appropriate and the newer destination still current. Hold the root continuation to arrange these schedules rather than relying on timing. Keep timeout and root-disposal tests, add stale Continue/binding replacement coverage within the same owner fixture, and release/drain/cancel/join every owned job in cleanup. The detached-at-entry and detached-while-queued assertions must fail on the present ancestry-only implementation and pass after the narrow correction.

## V78-05: retain the actual lifecycle and browser regression

Upgrade `client/src/commonTest/kotlin/PasswordChangeInteractorTest.kt` using the actual PasswordChangeViewModel, its unmodified lifecycle Job, the production ClientPlugin-resolved PasswordChangeNavigationOwner/interactor, and real NavigationChain nodes. A small test-only FIFO dispatcher and typed holder traversal may live beside the existing HeldPasswordChangeUsersModel or in one bounded commonTest fixture file. Keep the real destruction collector; manually cancelling a test Job is not the required ordering.

Capture the submitting ViewModel's actual lifecycle Job and install observable gates before submission. Hold the model response until one exact immutable request has been captured. Release Changed and allow the real queue to replace Pending and destroy that node while a separate root dispatcher holds the observer/save continuation. From the independent test scope, join the actual ViewModel Job before releasing root persistence. Assert cancellation and completion of that Job, real Pending destruction, empty password/confirmation fields, and zero saves at that point. Then release root work and require exactly one successful save. This reproduces the ordering already shown feasible by Validation 047; an `advanceUntilIdle` happy path cannot substitute for it.

Inspect the entire saved holder structurally, including node successors and subchains, and serialize with the production Json/ConfigHolder serializer where available. Require the expected UsersList/Completed structure, no Pending config anywhere, no extra unexpected node in the controlled simple hierarchy, and no approval UUID or plaintext sentinel in the actual serialized data. Assert the captured request's user id, UUID, and password fields directly; its redacted `toString()` is not evidence. Construct the actual Completed ViewModel, prove submit adds no request, call its `onContinue`, join its lifecycle Job after replacement, and assert a new save containing a usable nonempty UsersList destination. An already non-null saved field cannot prove Continue persistence.

For cancellation-resistant completion, wait for the actual Default-dispatcher destruction collector to cancel the submitting ViewModel, observe the fake returning Changed despite cancellation, and join the actual lifecycle Job before asserting no handoff/save. Keep the newer destination current and assert plaintext cleanup and zero active owner jobs. Upgrade save-failure evidence to begin with an actual ViewModel submission and a captured model/transport completion request. Throw on the first synchronous navigation save, then prove exactly one completion request remains after scheduler draining, stale submit attempts, and actual Completed Continue; Continue may produce its separate successful UsersList save. If the fixture intercepts UsersModel rather than HTTP transport, explicitly identify that boundary and add an existing MockEngine-backed production PasswordChangeFeature path for the zero-HTTP-replay assertion. A direct interactor call with no model or transport cannot prove HTTP behavior. Combine queued supersession/current-hierarchy evidence with V78-12 rather than create another owner implementation.

The browser regression must execute the production root DisposableEffect and composition disposal. Prefer the existing `ClientPlugin.startPlugin` root drawing callback with a small real Compose HTML host. If that host requires extracting a tiny internal binding composable, make both production and the test call the same composable and pass the same composition-owned scope to binding and `initNavigation`; copying the effect or calling `owner.bind` directly in the browser test is insufficient. Architecture must name the concrete host and lifetime wiring before Coding. Keep the common test's manually controlled binding for deterministic scheduling, complemented by this production composition-path test.

Begin the JSDOM browser case at the real canonical Pending URL under `/ui/`, restore the actual scaffold/main hierarchy, submit the actual Pending ViewModel, and await real destruction, its lifecycle Job, and the delegated URL-adapter save. Assert the exact request, `/ui/password-changed`, empty search, null history.state, no UUID/password in active path/search/state, typed Completed with no Pending, and cleared fields. Immediately create the reload adapter from that successful URL and inspect restored Completed, before Continue and without any synthetic Completed save or URL replacement. Then exercise actual Completed ViewModel submission rejection and Continue, join its lifecycle, and verify the new persisted `/ui` UsersList route with no additional completion HTTP. Mount/dispose the same production binding path to prove disposal cancels pending root work and an old binding's cleanup cannot clear a newer binding. Observe cleanup completion, not merely an active-count field cleared by unbind.

Retain the working owner eligibility and Compose HTML negative/positive DOM cases, serializer/form suites, representative user/wishlist/item URL parsing, malformed approval routes, and the approval-notification marker. Restore every replaced JSDOM global and provider, dispose the composition before closing its window, and cancel/join all model/root/saver/owner work. Use deferred events and bounded coroutine timeouts; do not fix these assertions with sleeps or a longer Mocha timeout. JSDOM remains a DOM/URL host, and Android remains unit-host evidence rather than a device run.

## V78-07: enumerate and fill the remaining declaration gaps

Architecture must produce the exact finite AST/diff declaration inventory at its entry HEAD, then extend it only for declarations introduced or changed in cycle 4. Validation 047 reported 390 changed function/class/interface/object declarations across the 73-file Kotlin diff and 24 undocumented functions. Planning confirmed the reported function locations through ast-index outlines and source reads; it does not claim a new exhaustive 390-declaration audit.

The 24-function baseline consists of UI/users common `Plugin.kt` password-email and completion overrides at lines 125 and 130; fourteen anonymous repository overrides in client `PasswordChangeInteractorTest.kt` at lines 132/133, 207/211, 275/276, 320/321, 358/359, 390/391, and 422/427; browser adapter save/get overrides at `PasswordChangeNavigationBrowserTest.kt:127` and `:134`; the two handler `tryHandle` overrides at `DeepLinksRoutingConfiguratorTest.kt:37` and `:71`; and four local helper functions at `EmailPasswordChangeServiceTest.kt:204`, `:210`, `:230`, and `:236`. Line numbers refer to Planning entry HEAD and will move during correction; Architecture must record enclosing declaration identities as well as locations.

Include anonymous-object members omitted by a functions-only audit: client repository `saved` properties at lines 131 and 205, `persisted` at line 421, and the deeplink handler `id` properties at lines 35 and 70. Add constructor `@param root` and `@param scope` documentation to `PasswordChangeNavigationOwner.Binding` at line 151; inline member comments alone do not meet that requirement. Inspect other issue-changed constructor/member declarations in the bounded diff and document any additional confirmed omissions with exact locations. Include the cycle-4 role lookup, dispatcher, hierarchy observer, and composition/test helpers. Preserve sufficient existing purpose KDocs and the resolved backing states, stable SerialName, and four-factory descriptions. Coding fills missing purpose-bearing KDocs only; no global legacy rewrite, duplicated blanket KDocs, or new lint framework is authorized.

## V78-11: explicit forward regeneration and correction ledger

Old committed reports are immutable. Mandatory regeneration therefore takes the form of a new complete replacement handoff in the next step, explicitly superseding the defective operational records while retaining their historical bytes. One canonical replacement block can satisfy that requirement if it fully regenerates the current handoff and maps every defective source record to its replacement claims; a heading/UUID audit or a sentence saying that history is superseded cannot. This is the selected interpretation of the regeneration instruction together with PROTOCOL's explicit prohibition on overwriting prior steps. It does not waive either instruction and requires no operator decision.

Architecture 049 must make its single eleven-section AML-HIP block the canonical regenerated handoff, with a stable entity identifier such as `issue_78_cycle4_regenerated_handoff`, its own new UUID, the exact entry SHA, and explicit references to Validation 047 and Planning 048. In ACTION, include one source-ledger entry for each report 032–047, identified by exact filename, original communication UUID, and corrected claim keys. A ledger entry must distinguish a regenerated invalid source block, a limited historical evidence claim, and the current independent validation authority. Include fully explicit replacement finding records for all five open and seven resolved identities, severities, historical counts, consecutive counts, required corrections, and evidence limits. Cross-referencing the old prose without supplying the corrected current data is insufficient.

The source ledger must specifically correct the following claims in normal prose and encode the same corrections in the canonical ACTION records. Blocks 032–039 and 041 contain `ambiguity_detected=false`, which triggers the user's literal “If any parameter=false” rule; regenerate those handoffs with all six self-check values literally true. The historical false flags remain visible only in immutable source reports. Report 034's lifecycle description, 035's Continue assertion, and 037's claimed forced destruction/save, cancellation, root-detachment, save-failure HTTP, production-composition, and transition-reload evidence exceed the retained assertions identified in 047. Replace those claims with the exact observed owner behavior and the V78-05/V78-12 evidence still required. Keep V78-10 resolved on 047's independent diagnostic, not on unsupported retained-test claims.

Report 038's missing-bridge and role-snapshot claims must explicitly state the clock confounder and empty memberships/Boolean bridge; retain its actual sibling, two-account, access/refresh, and write-count evidence as accepted by 047. Report 039's whole-finding completion language must be limited to the corrected owner eligibility/DOM portion of V78-05. Report 040's V78-07 closure and all-declarations audit claims are superseded by the 24 functions and remaining members/constructor identified in 047; replace the evaluative structured value `results_recorded_and_honest` with factual evidence/state values. V78-08 remains resolved by 047's independent README comparison. The 032/033 plans remain historical intent, not implementation proof; 036's classifier evidence is retained under resolved V78-09 despite its invalid literal self-check.

For 040–046, distinguish mechanical section/UUID/pronoun checks from semantic compliance or closure. Such checks do not validate unsupported claims or the literal false-flag rule. Preserve 041's eventual browser execution evidence without describing that success as proof of the missing lifecycle assertions. Preserve 042's failed overlapped build, 043's isolated scaffold pass and recorded overlap evidence, 044's wrapper failure with zero executed tasks, 045's permission handback, and 046's source-equivalent full build as historical execution records with their original limits. Do not convert an inference about overlap into a newly reproduced causal experiment. Report 047 remains the current independent source of finding states/counts and diagnostic observations; no inaccurate 047 finding claim was identified in this Planning review. Its missing acceptance evidence and explicit runtime limitations remain uncertainty, and its Validation FAIL is not superseded by this plan.

Each new block from 048 onward must contain exactly ENTITY, CONTEXT, ACTION, REASON, EXPECTED RESULT, VERIFICATION, UNCERTAINTY, REPETITION OF RESULT, COMMUNICATION, PERSISTENCE, and VALIDATION. Inspect explicit entity identities, factual dense values, no pronouns, real UUIDs, causal chains, and support for every result. All six SELF-CHECK values in VALIDATION must be literally `true`, including `ambiguity_detected=true`. State the actual missing implementation/execution or external-runtime evidence in UNCERTAINTY so that flag is factual; do not silently reinterpret it as false or invent uncertainty. Expected future results belong in EXPECTED RESULT, while VERIFICATION must distinguish observed checks from planned gates. Persistence refers only to tracked step reports, never prohibited personal auto-memory.

Before committing 049, run a read-only parser over the regenerated block to check section order/count, UUIDs, six literal true flags, and complete source/finding-ledger coverage, then inspect content manually for evidence accuracy and non-evaluative values. Verify old report blobs against cycle-entry `d839d39e72b5aaf4c5f187c5cbc4b15dcc9786f1`. Later Coding and Verification blocks reference the canonical ledger and record only their concrete deltas/evidence. Independent Validation audits 048 onward plus canonical replacement coverage; V78-11 remains open at count 2 until that assessment.

## Implementation order, gates, and stop condition

Architecture 049 must preserve counts, define exact membership observation and the production composition host, enumerate the remaining declarations, and emit the canonical regenerated handoff before Coding. Coding first adds the deterministic detached-node regressions and records the current failures, then corrects the owner at the existing boundary. Retain the forced actual-ViewModel lifecycle/browser assertions and repair both security fixture confounders. Finish the bounded KDocs and forward reports after the code/test shape is fixed. Root ownership already works, so the new lifecycle test may pass immediately; report that observation rather than claiming a new red reproduction of resolved V78-10.

Read Operator Notes before each affected feature change and preserve every section byte-for-byte. The five inspected sections contain the human-only placeholder; no note requires a product change or question. Compare the exact heading-through-next-heading section bytes against master and the cycle-entry tree, using one documented extraction boundary. READMEs already satisfy V78-08; update only implementation wording made inaccurate by an authorized cycle-4 change, never weaken the stated stale-call contract to match a defect. Run `ast-index rebuild` after Kotlin changes and `git diff --check` for each increment.

Use focused gates when the corresponding increment is ready: `./gradlew --no-daemon :wishlist.features.email.server:jvmTest --tests '*EmailPasswordChangeServiceTest' --tests '*PasswordChangeFlowRoutingTest' --console=plain`; `./gradlew --no-daemon :wishlist.client:jvmTest --tests '*PasswordChangeInteractorTest' --console=plain`; `./gradlew --no-daemon :wishlist.client:jsBrowserTest --tests '*PasswordChangeNavigationBrowserTest' --console=plain`; and `./gradlew --no-daemon :wishlist.features.ui.users:jvmTest --tests '*PasswordChange*' --tests '*UserEditViewModelPasswordChangeTest' --console=plain`. If Architecture selects an adjacent lifecycle fixture/test class, add its exact filter. Check discovered test names and assertion boundaries, not only exit status. Avoid a JS filter that silently omits the positive owner DOM test; the unfiltered platform aggregate below supplies complete existing host discovery.

After the bounded corrections, run one fresh server aggregate: `./gradlew --no-daemon :wishlist.features.common.server:jvmTest :wishlist.features.email.server:jvmTest :wishlist.features.auth.server:jvmTest :wishlist.features.deeplinks.server:jvmTest :wishlist.features.auth.common:jvmTest :wishlist.features.auth.client:jvmTest --rerun-tasks --console=plain`. Then run one fresh platform aggregate: `./gradlew --no-daemon :wishlist.features.ui.users:jvmTest :wishlist.features.ui.users:jsNodeTest :wishlist.features.ui.users:jsBrowserTest :wishlist.features.ui.users:testDebugUnitTest :wishlist.features.ui.users:testReleaseUnitTest :wishlist.client:jvmTest :wishlist.client:jsNodeTest :wishlist.client:jsBrowserTest --rerun-tasks --console=plain`. Inspect the fresh per-task XML and actual discovery: shared lifecycle tests on JVM/Node/browser, the production-composition browser case, owner positive and negative DOM, browser-only suites excluded only from Node, and existing concrete JVM/Android debug/release forms. Require zero relevant failures, errors, and skips. Record actual totals from that run; do not reuse 046/047 counts or label a later filtered XML mix as a new full aggregate.

All build work is serial per worktree. Before each invocation inspect active Gradle/Java build processes and coordinate with the Orchestrator; no parallel agent or shell may build against the same generated outputs. Use approved/escalated wrapper access from the outset, because the known sandbox cannot create its distribution lock. Keep each command in one foreground execution session, poll that same session until termination, and inspect its actual exit before any next command. Do not detach, launch a second session as a retry, or overwrite another active tee writer's log. The overlap in 042 and pre-initialization permission failure in 044 must not recur through the same known setup choices.

Independent Verification then runs the mandatory full gate in exactly one escalated foreground Gradle session with shell pipefail enabled: `./gradlew build --rerun-tasks 2>&1 | tee /tmp/build-output.txt`. Capture the actual pipeline exit and terminal build result; after completion archive the log under a distinct issue-78/step path and verify byte equality. Inspect fresh XML, source-HEAD equivalence, lint/test execution, diff checks, immutable old reports, and unchanged Operator Notes. The established allTests fallback applies only if a successful full build did not execute tests; it is not an automatic retry after failure. A failing gate receives its isolated evidence and role handback without concurrent reruns.

Independent Validation follows the fresh successful gate and evaluates the five retained findings against the actual source/assertions, the twelve-identity ledger, and all new structured handoffs. Passing compilation or a suite total cannot close an absent assertion. Stop Coding once the bounded corrections have matching focused/aggregate evidence and complete reports; the Orchestrator manages the Verification/Validation sequence and any recurrence. This plan requests no broader work and supplies no finding waiver.

Planning's report check found one structured block, eleven headings in the required order, one UUIDv4, all six self-check fields literally true, no prohibited pronoun matches, and at least two facts per data line. Manual review checked the distinction between observed evidence and planned corrections. The five Operator Notes sections match master under the same extraction boundary, and the tracked diff against cycle entry contains no previous-report or product change. A sandboxed Node child-process comparison encountered EPERM and was replaced by a successful shell-piped read-only comparison. No product test result is claimed by this report check.

## QUESTIONS FOR OPERATOR

There are no operator questions and no answers are required. The issue, existing Operator Notes, Validation 047, and authorized correction scope resolve the product requirements. Architecture's exact observer/host selection is a technical implementation decision within those boundaries. Missing cycle-4 implementation and execution evidence is recorded below and will be supplied by the subsequent roles.

```text
ENTITY:
entity_id=issue_78_planning_048; type=correction_plan; state=architecture_handoff

CONTEXT:
* task_id=09.09.2026_09.40.05-b512784f-672e-4309-aa90-4551cb0c3361; agent_id=issue78_planning_cycle4; memory_ref=[PROMPT.md,031-validating.md,032-planning.md,033-architecturing.md,034-coding.md,035-coding.md,036-coding.md,037-coding.md,038-coding.md,039-coding.md,040-coding.md,041-coding.md,042-verification.md,043-coding.md,044-verification.md,045-coding.md,046-verification.md,047-validating.md]
* cycle=4; base_head=d839d39e72b5aaf4c5f187c5cbc4b15dcc9786f1; branch=fix/issue-78-email-authorized-password-change; constraints=[048_only,no_nested_agents,no_push,no_checkout,prior_reports_immutable,Operator_Notes_unchanged,serial_Gradle_sessions]

ACTION:
1. action=retain_and_plan; target=V78-04; params={state=open,severity=High,prior_severity=Medium,consecutive_unresolved_count=3,recorded_escalation=Validation_047,corrections=[identical_deterministic_clock,Auth_read_boundary_observation,seeded_direct_User_memberships,repository_subject_backed_bridge],retained=[three_real_approvals,two_accounts,sibling_rejection,access_refresh_preservation,zero_role_mutation_attempts,exact_password_writes]}
2. action=retain_and_plan; target=V78-05; params={state=open,severity=High,prior_severity=Medium,consecutive_unresolved_count=3,recorded_escalation=Validation_047,corrected_portion=owner_eligibility_DOM,remaining=[held_root_actual_VM_Job_join,structural_saved_hierarchy,actual_Completed_Continue,cancellation_collector_join,production_DisposableEffect_host,successful_URL_direct_reload,queued_current_hierarchy,zero_HTTP_replay]}
3. action=retain_and_plan; target=V78-07; params={state=open,severity=Medium,prior_severity=Low,consecutive_unresolved_count=3,recorded_escalation=Validation_047,baseline_missing_functions=24,additional_inventory=[anonymous_repository_members,handler_id_members,Binding_constructor_params,cycle4_changed_declarations],scope=issue_78_AST_diff_declarations}
4. action=retain_and_plan; target=V78-11; params={state=open,severity=Low,consecutive_unresolved_count=2,canonical_regeneration_step=049,source_ledger=032_through_047,source_records=16,mandatory_sections=11,SELF_CHECK_literal_true_fields=6,old_report_mutations=0,required=[claim_corrections,evidence_limits,source_UUIDs,twelve_finding_records]}
5. action=retain_and_plan; target=V78-12; params={state=open,severity=Medium,consecutive_unresolved_count=1,owner=PasswordChangeNavigationOwner,corrections=[current_root_identity_membership,root_subtree_removal_observation,admission_recheck,exact_replacement_presave_recheck,prompt_detachment_cleanup],timeout_ms=5000,scope=existing_password_navigation_boundary}
6. action=preserve_resolution; target=[V78-01,V78-02]; params={state=resolved,historical_severity=High,historical_count=1,consecutive_unresolved_count=0,evidence_authority=Validation_047}
7. action=preserve_resolution; target=[V78-03,V78-06]; params={state=resolved,historical_severity=Medium,historical_count=1,consecutive_unresolved_count=0,evidence_authority=Validation_047}
8. action=preserve_resolution; target=V78-08; params={state=resolved,historical_severity=Low,historical_count=2,consecutive_unresolved_count=0,evidence_authority=Validation_047}
9. action=preserve_resolution; target=[V78-09,V78-10]; params={state=resolved,historical_severity=High,historical_count=1,consecutive_unresolved_count=0,evidence_authority=Validation_047}
10. action=handoff; target=architecture; params={next_step=049,open_total=5,resolved_total=7,critical=0,high=2,medium=2,low=1,count_changes=0,operator_question_count=0,implementation_tests_executed_by_Planning=0}

REASON:
* condition=V78-04_expired_bridge_null_fixture_and_empty_role_snapshots; requirement=independent_authorization_evidence; causal_chain=shared_clock_and_seeded_subject_memberships→Auth_boundary_and_role_preservation_assertions→independent_validation
* condition=V78-05_retained_tests_omit_forced_lifecycle_and_production_composition_reload; requirement=actual_VM_and_root_binding_regression; causal_chain=Pending_destruction_before_held_root_save→actual_Job_join→structural_Completed_save_and_direct_URL_reload
* condition=V78-12_ancestry_survives_parent_replacement; requirement=current_root_membership; causal_chain=live_root_traversal_and_removal_observation→detached_call_rejection→zero_stale_save_and_prompt_job_cleanup
* condition=V78-07_remaining_changed_declaration_KDocs; requirement=bounded_documentation_rule; causal_chain=AST_diff_inventory→missing_purpose_KDocs→declaration_location_audit
* condition=V78-11_literal_false_flags_and_unsupported_claims; requirement=mandatory_regeneration_with_immutable_history; causal_chain=explicit_source_claim_ledger→complete_049_replacement_handoff→048_onward_semantic_and_literal_selfcheck_audit

EXPECTED RESULT:
* entity_id=issue_78_planning_048; new_state=committed_architecture_handoff; location=agents/task/09.09.2026_09.40.05-b512784f-672e-4309-aa90-4551cb0c3361/048-planning.md
* entity_id=issue_78_architecture_049; new_state=concrete_reachability_host_declaration_and_regeneration_specification_required; location=agents/task/09.09.2026_09.40.05-b512784f-672e-4309-aa90-4551cb0c3361/049-architecturing.md

VERIFICATION:
* check=entry_branch_HEAD_worktree; expected=clean_assigned_branch_at_d839d39e72b5aaf4c5f187c5cbc4b15dcc9786f1; result=matched
* check=Validation_047_finding_ledger; expected={open:5,resolved:7,High:2,Medium:2,Low:1}; result=retained; count_changes=0
* check=bounded_source_locations; expected=[clock_omission,Boolean_bridge,empty_role_seed,unjoined_VM_tests,synthetic_reload_save,ancestry_guard,24_function_locations]; result=source_and_AST_inspected
* check=historical_SELF_CHECK_flags; expected=literal_rule_audit; result={ambiguity_detected_false_reports:[032,033,034,035,036,037,038,039,041],source_UUIDs:16}
* check=Planning_execution_scope; expected=report_only; product_test_runs=0; source_edits=0; index_rebuilds=0; previous_report_edits=0
* check=048_block_syntax; expected={blocks:1,sections:11,UUID_v4:1,literal_true_SELF_CHECK_fields:6,prohibited_pronouns:0}; result=matched; semantic_review=observed_and_planned_claims_distinguished
* check=Operator_Notes_comparison; expected=master_byte_equality; result=matched; compared_README_sections=5
* check=planned_gates; expected=[focused_security,focused_lifecycle,production_composition_browser,fresh_server_aggregate,fresh_platform_aggregate,single_escalated_foreground_pipefail_full_build,independent_Validation]; execution_state=pending_subsequent_roles

UNCERTAINTY:
* missing=cycle4_implementation_and_retained_regression_results; ambiguity=required_assertions_remain_unexecuted_in_Planning
* missing=Architecture_exact_installed_subtree_observer_and_production_composition_host_selection; ambiguity=implementation_selection_within_existing_navigation_interfaces
* missing=Architecture_full_remaining_member_constructor_inventory_and_canonical_source_claim_ledger; ambiguity=049_explicit_enumeration_pending
* missing=[graphical_browser_runtime,native_device_runtime,live_SMTP,process_crash_durability]; ambiguity=repository_hosts_and_in_process_boundaries_do_not_establish_external_runtime_guarantees

REPETITION OF RESULT:
* entity_id=issue_78_planning_048; stored_in=shared_memory; status=available; next_stage=architecture; next_step=049; open_findings=5; resolved_findings=7; count_changes=0

COMMUNICATION:
* sender=issue78_planning_cycle4; receiver=architecture_and_orchestrator; task_id=09.09.2026_09.40.05-b512784f-672e-4309-aa90-4551cb0c3361; message_id=2d42da5d-8b78-4b68-a029-683238bbde58; protocol=AML-HIP; next_step=049; open_findings=[V78-04,V78-05,V78-07,V78-11,V78-12]; count_changes=0

PERSISTENCE:
* local_memory=true; shared_memory=true; index_keys=[task_id,issue_78_planning_048,cycle4_correction,V78-04,V78-05,V78-07,V78-11,V78-12]; persistence_medium=tracked_step_report_only; personal_auto_memory=disabled

VALIDATION:
* format_valid=true; no_pronouns=true; entities_explicit=true; high_density=true; causal_chain_present=true; ambiguity_detected=true
```
