Model: Inherited Codex GPT-6 Planning agent, retained for the assigned HL reasoning role because the correction crosses server error classification and navigation lifecycle ownership. The explicit no-nested-agent assignment governs this report; no documentation agent was spawned.
Changed files: agents/task/09.09.2026_09.40.05-b512784f-672e-4309-aa90-4551cb0c3361/032-planning.md

## Decision and evidence

Correction cycle 3 is ready for Architecture. Start from clean HEAD `4495df4098314031a346d7541119cf86fc81afa0` on `fix/issue-78-email-authorized-password-change`. Validation 031 remains FAIL. Prioritize direct regressions and narrow fixes for V78-09 and V78-10, then supply the distinct missing evidence for V78-04/V78-05 and finish the bounded documentation/process corrections. Planning does not close findings or change their repeat counts.

I read AGENTS.md, SHORTCUTS, ALL/local.ALL, PLAN, MODELS, PROTOCOL, GIT, TOOLS, the caveman skill, PROMPT, complete Validation 031, and the relevant Planning 010, Architecture 011, Coding 012–029, and Verification 030 history. No local.PLAN.md exists. I checked the declaration and escalation rules, the live issue #78 and its empty comments, and the complete Common, Auth, Email, DeepLinks, and UI/users READMEs with their unchanged placeholder Operator Notes. There is no client/README.md; existing Common/UI-users navigation documentation supplies the relevant context. Code inspection used the existing ast-index cache through approved access after the sandbox could not see the index. No index rebuild or product test run was needed for this report-only stage. Caveman full applied only to internal working notes; narrative and commit use normal prose.

Evidence ranks as follows. Validation 031's compiled-runtime diagnostics directly establish V78-09's baseline 400 becoming 500 under the production helper and V78-10's possible destruction-before-save ordering (14 saves and 16 cancelled callers in 30 runs). Source inspection confirms the application-wide Throwable handler and the pending ViewModel's lifecycle Job owning the interactor's observer/await/save path. The exact installed Navigation 0.7.7 ordering and replacement-only saver limitation recorded in Validation explain the latter failure. Those observations outrank Coding 021/027's completion claims. The race diagnostic demonstrates an allowed ordering, not a platform failure rate.

The retained Medium findings are missing assertions, not established authorization exploits. Existing raw owner admission, credential checks, lock ordering, and mutation-generation code support the intended behavior; test names and aggregate counts do not establish the missing invariants. Verification 030's fresh successful 4,548-task build and 944 tests remain evidence for the prior product tree, but cannot close either reproduced High or substitute for the missing assertions. The next build must cover the corrected product tree.

## Finding retention and escalation

Retain V78-04 as Medium, repeat count 2; V78-05 as Medium, repeat count 2; V78-07 as Low, repeat count 2; V78-08 as Low, repeat count 2; V78-09 as High, repeat count 1; V78-10 as High, repeat count 1; and V78-11 as Low, repeat count 1. There are seven open findings: two High, two Medium, and three Low. V78-01, V78-02, V78-03, and V78-06 remain resolved, each with historical repeat count 1 and consecutive unresolved count zero. Preserve their existing regressions.

Planning and Coding must neither reset nor increment these counts. Independent Validation determines recurrence. No finding currently reaches the three-consecutive-validation-cycle escalation threshold. If V78-04 or V78-05 remains unresolved at the next Validation, repeat count 3 automatically raises that finding from Medium to High. If V78-07 or V78-08 remains unresolved, repeat count 3 raises Low to Medium. A further recurrence of V78-09, V78-10, or V78-11 first reaches count 2. Apply the existing Critical stop rule if a later automatic escalation reaches Critical. Renaming a test or splitting a report must not create a new identity for the same unresolved problem.

## Preserved requirements

The issue still requires authenticated owner-only issuance when SMTP is enabled and the current private email is non-null and approved; delivery only to that address; purpose-specific read-only GET redirection; immutable canonical approval UUID and bound subject in completion; fail-closed invalid approvals; and reuse of existing deeplink/password boundaries. Completion remains independent of ambient browser login. Preserve the 15-minute expiry, purpose/type checks, current approved address, direct role requirement, salted credential fingerprint, coordinator-before-Auth lock order, authoritative final reread, consume-before-write, and bounded server commit/cancellation behavior. Sessions and roles remain unchanged. The server response cannot promise rollback after an uncertain committed write.

Preserve concrete Pending/Completed serializers, synchronous raw form admission, exact cleanup ownership and exception propagation, live validation, masked native fields and guarded Done, trusted completion origin, policy headers, and secret-safe diagnostics. Keep the current browser-history policy: a consumed Pending URL may remain in earlier history, but the active successful route and its reload state must contain no approval. No new schema, token system, role/session policy, route family, navigation dependency upgrade, or full application test framework is needed.

## First correction: V78-09, preserve standard client-error statuses

Add a failing regression before changing `features/common/server/src/jvmMain/kotlin/utils/CallLoggingFormat.kt`. The test must apply the actual Common JVM plugin's contributed StatusPages configuration, production JSON configuration, and existing Auth registration/login/refresh routing. Send malformed JSON or missing required body fields through the existing `/api/auth/register`, `/api/auth/login`, and `/api/auth/refresh` routes. Assert their established client-error status, with 400 as the direct malformed-body contract. A synthetic BadRequestException route may demonstrate baseline parity but cannot replace the Auth-route assertions. The password-change routes already catch their own body failures and are insufficient for this regression.

Architecture must inspect the installed Ktor 3.5.2 standard exception-to-status behavior and preserve the applicable standard 4xx classifications at the existing sanitized boundary. Use exception types or Ktor's supported classification API, never exception-message parsing. An unrelated explicit 401/404 response must remain unchanged, and a representative other standard client-error type from the installed runtime should prove that the fix does not special-case only malformed JSON. Unexpected application/repository failures still produce a sanitized 500. Cancellation still propagates. Keep error text, causes, URL, query, request body, private email, approval UUID, credentials, and redirect locations out of public error bodies and captured request/application logs.

Reuse `PasswordChangeFlowRoutingTest` and its Logback capture or an adjacent small Common/Auth test fixture. Place sensitive sentinels in malformed input and exception data; assert absent body/log sentinels and absence of throwable attachments, while retaining method/final-status logging. Preserve the existing password-change/deeplink no-store/no-referrer assertions and 500 handling. Add no generic logging framework and do not broadly catch unrelated routes merely to compensate for the global helper. Architecture must specify the exact finite status cases and real configurator wiring in its file/test inventory. Closure requires both status preservation and sanitization assertions against the installed configuration.

## Second correction: V78-10, make completion persistence survive node destruction

Replace the completion transition ownership in `client/src/commonMain/kotlin/ClientPlugin.kt`. The pending screen's coroutine must not own the work required after replacing that screen. The preferred ownership is the existing root/navigation lifetime: transfer an accepted success transition to that surviving owner before replacement, observe the actual resulting hierarchy, and save the credential-free Completed state there. Architecture must identify the concrete existing lifecycle owner and cancellation sequence from Navigation 0.7.7 before Coding; an observer registered early but still parented to the pending ViewModel repeats the defect. An alternative bounded sequence is acceptable only if Architecture proves that pending-node destruction cannot interrupt persistence and that abandoned work terminates when its actual navigation owner ends.

Keep network submission and plaintext in the pending ViewModel lifetime. Transfer only the minimal navigation work after confirmed Changed; do not detach the entire submission into GlobalScope, retain plaintext in a navigation job, introduce an unowned supervisor, or wrap the whole request/indefinite stack wait in NonCancellable. Preserve cancellation before accepted success and the existing failure outcomes. After handoff, destruction caused by successful replacement must not cancel the save. Root/application shutdown still has a defined cleanup owner. A failed persistence operation must not replay the consumed approval or resubmit the password. Do not claim survival across process termination or repository failure. Avoid an observer that waits forever if an intervening navigation action makes its expected replacement impossible, and avoid saving an obsolete snapshot over a later active destination.

Add a direct lifecycle regression to `client/src/commonTest/kotlin/PasswordChangeInteractorTest.kt`, or an adjacent client test sharing its production DI fixture. Construct the actual PasswordChangeViewModel attached to the Pending node of a real NavigationChain and resolve the actual ClientPlugin interactor. Control only model completion and the repository boundary. Submit through `onSubmitPasswordChange`, release one Changed result, and force/observe the real pending-node destruction and cancellation of the ViewModel Job. Assert one exact submitted UUID/subject, one successful transition, cleared plaintext, a persisted hierarchy with Completed and no Pending approval, and a usable users-list destination on Continue. Inspect typed configs or serialized content; the deliberately redacted `toString()` cannot prove UUID absence.

Exercise the destruction-before-persistence ordering deterministically with dispatcher control and lifecycle signals. The test must fail on the current caller-owned implementation. Repeating a happy schedule or calling `interactor.onChanged` from the test scope is not enough. Retain a focused cancellation-before-success case proving no completion save after an abandoned request; verify owned navigation work can be cleaned up without hanging. Prefer existing lifecycle seams and production factories, without modifying the Navigation dependency solely for testing.

Extend `client/src/jsTest/kotlin/PasswordChangeNavigationBrowserTest.kt` to combine that successful real ViewModel submission with the production URL repository. Begin at the canonical emailed Pending route under the controlled base path, restore the actual scaffold/main chain, attach the real submitting ViewModel, and use the actual interactor. Await both real node destruction and actual adapter save. Assert active pathname `/password-changed` under the base, no approval in path/query/history state, then recreate the adapter and restore Completed from the resulting URL. A restored Completed ViewModel must expose no password form or submission; retain the existing concrete Completed DOM form assertion rather than duplicating its full matrix. Keep `pushState(null, ...)` compatible with path/search restoration, representative existing user/wishlist routes, and the existing policy marker behavior. Preserve fixture disposal and cancellation/join discipline.

The shared lifecycle test establishes ownership; the JS extension establishes the real persisted URL and reload after the same lifecycle transition. Together they satisfy the lifecycle portion retained in V78-05 as well as V78-10. Keep the existing JVM Compose and Android Robolectric form evidence; do not repeat the lifecycle/security matrix on every renderer. Describe JSDOM as a DOM host, without claiming graphical-browser or native keyboard-default coverage.

## V78-04, complete four distinct security proofs

Extend the existing service/routing fixtures; keep real Email, Auth, coordinator, deeplink services, and production password storage behavior. First, issue two actual password-change approvals for the same eligible account and one for a second eligible account in the same underlying state. Establish that all three are initially usable, for example through read-only validation. Complete the first owner's approval, reject the real sibling with zero additional password writes, and successfully complete the other account's still-valid approval. Verify the unrelated credential is unchanged before its independently authorized completion. Generic handler/string records used by issuance-cleanup tests remain cleanup evidence only.

Second, retain both pre-existing access and refresh credentials in the production route-flow fixture, for the changed owner and the unrelated account. After completion, use each pre-existing access token through real authentication/getMe and assert its original subject. Redeem each pre-existing refresh token through the real refresh path and verify the resulting credentials still represent that subject. Perform the access check before any refresh rotation that ordinarily replaces a token. Assert old/new password behavior separately. A post-change login is not proof that old sessions survived.

Third, record direct roles/authorization snapshots before and after completion and intercept role grants/removals or equivalent bridge mutation methods during the operation, excluding fixture bootstrap. Assert zero completion role mutations and unchanged memberships for both accounts. A fixture whose bridge returns true without observable mutation state is insufficient.

Fourth, isolate the absent-role-bridge guard. Seed an otherwise valid approval using the real credential fingerprint for the fixture's exact current salted password record, matching subject, approved email, handler/payload, and unexpired time; then remove/omit only the role bridge at completion. A corresponding bridge-present control with the same state must be accepted. Retain a separately valid revoked-role case. Do not combine a missing bridge with the current `untrusted-state` fingerprint. Every independent rejection asserts zero completion password writes.

These are complementary assertions, not a request to replay the authorization matrix through every HTTP route or platform. Preserve the already established same-plaintext salted administrator replacement, final reread, overlap, four write/removal failure positions, and cancellation cases. Change production services only if a new targeted assertion exposes a defect at an existing boundary.

## V78-05, prove owner presentation and asynchronous admission

Expand `UserEditViewModelPasswordChangeTest` to assert `canRequestPasswordChangeEmailState` explicitly. Check true after eligible ordinary-owner/root-on-self reconciliation and false for anonymous, another user, root-on-other, null/unapproved email, mismatching private profile, disabled/failed SMTP, and busy mutation. Add controlled cold Unknown, held capability Loading, and held profile-refresh cases using existing deferred model seams. Assert false while each is held, no password-email call on attempted admission, and correct eligibility after successful release or failure. During refresh the prior approved profile must not remain actionable. Record rendered visibility separately from derived button enabledness.

Replace the misleading stale-derived test with two proven scheduling cases using a serial controllable dispatcher. Establish derived true, change a raw eligibility predicate, assert the derived value is still true at the attempted call, then prove zero requests. Separately reconcile valid raw capability/profile/owner state while derived eligibility is deliberately still false, assert that false immediately before the call, then prove one admitted request with the captured approved address. Do not call `advanceUntilIdle` before either assertion and then label reconciled state stale. Architecture should select the smallest existing observable dispatcher/flow seam for the second ordering; no public production mutator for private state is justified merely to force the test.

Complete the actual later-mutation scenario. Hold the first password-email request in a cancellation-resistant test double, invalidate its owner generation, restore a valid current owner session, and start a distinct later mutation whose completion is also held. Release the obsolete operation while the later mutation remains busy. Assert that obsolete completion publishes no result, triggers no extra private-profile reconciliation, and cannot clear the later mutation's busy slot or result. Then release the current mutation and assert normal completion. Compare read counters after the legitimate new-session refresh so those expected reads are not mistaken for obsolete work. Always release deferred gates and cancel/join lifecycle work during cleanup.

Extend the existing `UserEditViewBrowserTest` mounted production editor with a compact set of independent negative cases: disabled SMTP, missing email, unapproved email, another user, and root-on-other. Assert the password-email request control is absent and private owner feedback is hidden where applicable. Keep the positive approved-owner control and assert disabled/absent behavior under a held refresh according to the existing approved-profile rendering branch. Avoid a cross-product with root flags, all transport outcomes, and all native hosts. Source inspection currently supports the intended gating; adjust production owner/form code only for a directly exposed discrepancy.

## V78-07 and V78-08, bounded declaration and README corrections

Perform an explicit declaration audit over issue-78-created Kotlin files and declarations changed by the issue/correction, including test fixtures. Supply missing purpose-bearing KDocs for PasswordChangeViewModel's five reported backing states, new route/client/service overrides, view constructor members/parameters, and missing new test declarations. Update the JS/JVM/Android plugin descriptions to include the password-change factory. Preserve the already corrected stable SerialName and control-flow style. Architecture should enumerate the bounded declarations; Coding must report checked locations and exceptions honestly rather than repeat Coding 028's unsupported T16 closure. Do not document untouched legacy declarations across the repository or introduce a lint framework.

After the two High fixes, correct the UI/users Overview to describe the actual Calm Studio JS edit/owner controls and accurate native UI technology. Amend existing UI/users and Common Architecture Notes to explain the final completion persistence owner, why Pending-node destruction cannot cancel the accepted save, and credential-free reload. Document null browser-history state and restoration from pathname/search without promising changes to older history entries. Common's server notes must explain the installed sanitized global boundary, preserved standard client-error status classes, unexpected 500 behavior, and cancellation propagation. Update Auth error-contract wording only where the actual correction requires it. Existing Email/DeepLinks policy and cleanup descriptions remain accurate unless Coding changes their relevant behavior. Preserve every Operator Notes section verbatim.

## V78-11, repair future handoffs without rewriting history

Coding 012–029 and Verification 030 contain incomplete structured blocks; their `format_valid=true` assertions are superseded by Validation 031. Those prior step files are immutable and must not be edited. V78-11 remains Low with repeat count 1 until independent Validation accepts the corrective forward process. Every new AML-HIP block in step 032 onward must include ENTITY, CONTEXT, ACTION, REASON, EXPECTED RESULT, VERIFICATION, UNCERTAINTY, REPETITION OF RESULT, COMMUNICATION, PERSISTENCE, and VALIDATION. Each communication record must contain a real UUID, not a report name or placeholder.

Validate section presence, explicit entities, causal links, no pronouns, meaningful dense data lines, and a parseable UUID before claiming format validity. Explicitly record missing execution evidence in UNCERTAINTY when a role has not run the implementation. Keep natural-language narrative outside the block. Persistence refers only to tracked step reports; prohibited personal auto-memory remains disabled. No new protocol tool or shared memory service is required. The next Validator should examine all newly produced 032+ blocks while retaining the historical defect acknowledgment.

## Operator questions, implementation gates, and handoff

There are no operator questions and no answers are required. The current issue, Operator Notes, and preserved behavior resolve the product requirements. The precise surviving navigation owner and supported Ktor classifier are technical decisions for Architecture to establish from the installed implementation, within the authorized correction. Missing new execution evidence is expected at Planning, not a reason to request a product waiver.

Architecture should write the next monotonic `033-architecturing.md`, preserving every finding/count and providing the finite file/test inventory plus concrete navigation ownership/cancellation sequence and exception classification cases. Coding should first make both direct High regressions fail, implement the two narrow production fixes, and rerun those regressions. Add the four security proofs and owner evidence using existing fixtures, then audit declarations, READMEs, and each new handoff. Preserve resolved-finding tests. Rebuild ast-index after source changes.

Focused execution must include Common/Auth/Email/DeepLinks server suites, the real client lifecycle suite, UI/users owner tests, and the client/UI-users JS DOM suites affected by this cycle. Use the established server/platform task paths from Architecture 011 and Coding 029, adding `:wishlist.features.common.server:jvmTest` for the changed global boundary. Existing native form suites remain part of the established platform/full-build gates; do not recreate already passing host infrastructure. Record actual assertions, discovered suites, failures, and skips separately from compilation. A renamed or skipped test is not closure evidence.

Verification must run the required fresh gate with pipefail, `./gradlew build --rerun-tasks 2>&1 | tee /tmp/build-output.txt`, inspect its real exit status and current XML, and follow the existing allTests fallback only if tests were not executed. Independent Validation then reviews the corrected production tree, the two direct regressions, every retained assertion gap, and all new handoff blocks. Both High defects must close before PASS. The Orchestrator owns any Medium/Low disposition and automatic escalation handling; this plan does not waive findings. Stop the correction when the bounded work has matching execution evidence and independent Validation accepts it. This Planning stage edits/commits only 032, with no source/tests/docs edit, checkout, push, or external message.

```text
ENTITY:
entity_id=issue_78_planning_032; type=correction_plan; state=ready_for_architecture

CONTEXT:
* task_id=09.09.2026_09.40.05-b512784f-672e-4309-aa90-4551cb0c3361; agent_id=issue78_planning_cycle3; memory_ref=[010-planning.md,011-architecturing.md,012-coding.md..029-coding.md,030-verification.md,031-validating.md]
* cycle=3; base_head=4495df4098314031a346d7541119cf86fc81afa0; branch=fix/issue-78-email-authorized-password-change; constraints=[032_only,no_nested_agents,no_push,no_checkout,prior_steps_immutable,Operator_Notes_unchanged]

ACTION:
1. action=retain_and_plan_regression; target=V78-09; params={severity=High,state=open,repeat_count=1,owner=Common_server_error_boundary,proof=[production_configurator,malformed_Auth_bodies,standard_4xx_parity,sanitized_logs_and_responses,cancellation]}
2. action=retain_and_plan_regression; target=V78-10; params={severity=High,state=open,repeat_count=1,owner=surviving_navigation_lifetime,proof=[actual_PasswordChangeViewModel_submission,pending_destruction,lifecycle_Job_cancellation,persisted_Completed,real_URL_reload]}
3. action=retain_and_plan_evidence; target=V78-04; params={severity=Medium,state=open,repeat_count=2,proof=[real_same_owner_sibling_rejection,other_owner_approval_completion,existing_access_refresh_preservation,zero_role_mutations,isolated_valid_fingerprint_missing_bridge]}
4. action=retain_and_plan_evidence; target=V78-05; params={severity=Medium,state=open,repeat_count=2,proof=[eligibility_StateFlow,Unknown_Loading_held_refresh,negative_DOM_visibility,stale_true_rejection,stale_false_admission,obsolete_completion_during_later_mutation,V78-10_lifecycle_proof]}
5. action=retain_and_plan_audit; target=V78-07; params={severity=Low,state=open,repeat_count=2,scope=[issue_78_created_declarations,issue_78_changed_declarations,backing_states,overrides,constructor_members,platform_plugin_descriptions]}
6. action=retain_and_plan_documentation; target=V78-08; params={severity=Low,state=open,repeat_count=2,scope=[JS_edit_technology,completion_persistence_owner,null_history_state,path_search_reload,global_status_classification]}
7. action=retain_and_correct_forward_protocol; target=V78-11; params={severity=Low,state=open,repeat_count=1,prior_steps=immutable,new_scope=032_onward,required_sections=11,message_id_format=UUID,prior_incomplete_handoffs=superseded_by_031}
8. action=preserve_resolution; target=[V78-01,V78-02,V78-03,V78-06]; params={state=resolved,historical_repeat_count=1,consecutive_unresolved_count=0,existing_regressions=retained}
9. action=handoff; target=architecture; params={next_step=033,open_total=7,high=2,medium=2,low=3,questions=none,repeat_count_changes=0,current_escalations=0}

REASON:
* condition=global_Throwable_handler_changes_standard_client_errors; requirement=V78-09_400_parity; causal_chain=production_Auth_regression→typed_status_preservation→secret_safe_4xx_and_500
* condition=Pending_destruction_cancels_caller_owned_save; requirement=V78-10_credential_free_reload; causal_chain=confirmed_success_handoff→surviving_owner_persistence→Completed_reload
* condition=missing_distinct_security_and_owner_assertions; requirement=V78-04_and_V78-05_evidence; causal_chain=bounded_fixture_extensions→observable_invariant_assertions→independent_validation
* condition=prior_incomplete_AML_HIP_blocks; requirement=immutable_history_and_valid_future_handoffs; causal_chain=031_acknowledgment→032_plus_complete_sections_and_UUIDs→forward_protocol_verification

EXPECTED RESULT:
* entity_id=issue_78_planning_032; new_state=architecture_handoff; location=agents/task/09.09.2026_09.40.05-b512784f-672e-4309-aa90-4551cb0c3361/032-planning.md
* entity_id=issue_78_architecture_033; new_state=concrete_owner_status_and_test_inventory_required; location=agents/task/09.09.2026_09.40.05-b512784f-672e-4309-aa90-4551cb0c3361/033-architecturing.md

VERIFICATION:
* check=entry_worktree_and_HEAD; expected=clean_4495df4098314031a346d7541119cf86fc81afa0; result=matched
* check=live_issue_78; expected=task_PROMPT_requirements; result=matched; comments=0
* check=retained_finding_counts; expected={V78-04:2,V78-05:2,V78-07:2,V78-08:2,V78-09:1,V78-10:1,V78-11:1}; result=preserved
* check=next_validation_repeat_threshold; expected=automatic_plus_one_severity_at_3_consecutive_cycles; candidates=[V78-04,V78-05,V78-07,V78-08]
* check=Planning_edit_scope; expected=032_only; product_tests_executed=false; broad_build_claim=false
* check=operator_questions; expected=none; result=none

UNCERTAINTY:
* missing=cycle_3_implementation_and_test_results; ambiguity=none_for_required_acceptance
* missing=Architecture_concrete_surviving_owner_and_finite_Ktor_classifier_inventory; ambiguity=implementation_selection_within_existing_boundaries
* missing=graphical_browser_execution_and_platform_race_frequency; ambiguity=JSDOM_and_diagnostic_schedule_do_not_establish_platform_frequency

REPETITION OF RESULT:
* entity_id=issue_78_planning_032; stored_in=shared_memory; status=available; next_stage=architecture; next_step=033; open_findings=7; repeat_count_changes=0

COMMUNICATION:
* sender=issue78_planning_cycle3; receiver=architecture; task_id=09.09.2026_09.40.05-b512784f-672e-4309-aa90-4551cb0c3361; message_id=503d9759-ac50-4449-b95f-1913c51fa019; protocol=AML-HIP; next_step=033; high_findings=[V78-09,V78-10]

PERSISTENCE:
* local_memory=true; shared_memory=true; index_keys=[task_id,issue_78_planning_032,cycle_3_correction_intent,V78-04,V78-05,V78-07,V78-08,V78-09,V78-10,V78-11]; persistence_medium=tracked_step_report_only; personal_auto_memory=false

VALIDATION:
* format_valid=true; no_pronouns=true; entities_explicit=true; high_density=true; causal_chain_present=true; ambiguity_detected=false
```
