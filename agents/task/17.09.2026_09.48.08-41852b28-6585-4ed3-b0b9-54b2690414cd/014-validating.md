Model: gpt-6-astra (HL validation; gpt-5.6-luna LL report filling)
Changed files: agents/task/17.09.2026_09.48.08-41852b28-6585-4ed3-b0b9-54b2690414cd/014-validating.md

## Validation result

Verdict: FAIL. Restart Planning because two High defects remain. The reviewed change has 0 Critical, 2 High, 3 Medium, and 1 Low findings. Findings VEC-01 through VEC-06 are Open, each first occurrence with consecutive count 1; no repeat escalation applies.

Model selection follows the HL/ML Validator priority. Astra is the available highest-reasoning validation model. Luna is used only for filling the already determined report findings, as required for documentation work.

## Findings

VEC-01. features/users/common/src/jvmMain/kotlin/repo/ExposedUsersRepo.kt:L360: High: null equals pendingEmail when no pending replacement exists, so changingEmail becomes false. Handle explicit clear before slot equality, distinguish an already empty no-op, enforce the deadline, and clear all lifecycle fields. A clear request currently returns the unchanged row and skips cooldown handling. The defect affects dedicated setEmail and generic single, bulk, and administrative updates. A compiled-class JShell reflection probe against a new shared-memory SQLite database created validator@example.com at clock 1000; setEmail(null) returned the original email with approved false, pending null, and deadline null. Approval at the same clock with a 500-millisecond cooldown produced approved true with deadline 1500; another setEmail(null) returned the approved row instead of throwing the active-cooldown error. Ordinary approved/no-pending clearing after expiry follows the same branch. Existing repository clear tests first populate pendingEmail. The email-server FakeUsersRepo handles clear first and masks the production mismatch. Add real SQLite and PostgreSQL tests covering unapproved, approved, no-pending, active, expired, and empty states, including single and batch rollback/event behavior.

VEC-02. features/ui/users/src/commonMain/kotlin/ui/UserEditViewModel.kt:L968: High: save confirmation accepts either email slot and can report a newer pending address as saved by an earlier request. Require editableEmailBaseline() == submittedEmail, meaning pendingEmail ?: email, on both reconciliation reads. When submitted B is concurrently approved and newer C becomes pending with zero or expired cooldown, both checks pass because current equals B; lines 1015-1017 then replace the draft with C and publish saved C. The SMTP-enabled path POSTs B, receives EmailChanged, and publishVerificationResult drops that result because B is neither the candidate nor approved current without pending. Completion still reports saved C. Preserve raw B, report EmailChanged, and stop POST continuation after the mismatch, as Architecture 003 requires. Add forced scheduler-race tests for enabled and disabled SMTP, before and after POST. This is source-path proof, not a newly executed test.

VEC-03. features/email/server/src/commonMain/kotlin/services/EmailFeatureService.kt:L103: Medium: AlreadyApproved ignores expectedEmail when current is approved and no pending exists. Require current.email == expectedEmail in both the initial branch and the post-SMTP branch at line 120. Approved A plus expected B incorrectly returns AlreadyApproved. During SMTP, pending B replaced by C and C approved can likewise return AlreadyApproved for B. Return EmailChanged for those mismatches while preserving exact-link cleanup. No unauthorized approval was observed, and UI reconciliation mitigates incorrect API feedback. Add direct and suspended-SMTP wrong-approved-address tests.

VEC-04. features/users/common/src/jvmTest/kotlin/repo/PostgresUsersRepoTest.kt:L87: Medium: checking latch count immediately after Thread.start does not prove that the contender reached the database lock. Establish an actual blocked database operation before releasing the first writer. A contender can run entirely after the first release while the test passes; analogous PostgreSQL cases and the SQLite tests in features/users/common/src/jvmTest/kotlin/repo/ExposedUsersRepoSqliteTest.kt have the same weakness. Winner checks prove eventual ownership, and the timeout case proves contention, but neither proves the promised acquisition/read ordering. Use observable PostgreSQL activity/locks or an equivalent robust synchronization mechanism. Assert that the callback is unreached while SQL is blocked and reached after release; include locked-clock boundary and approve-versus-replace ordering required by Architecture 003. This data-integrity Medium requires Coding even without the High findings.

VEC-05. features/ui/users/src/jvmTest/kotlin/ui/UserEditEmailRenderTest.kt:L141: Medium: Architecture 003 acceptance proof is incomplete, while step 013 overstates completion. Add the missing focused cases through production boundaries and seed the new metadata with non-null values. Desktop live-retarget/root-other cases still use null pending/deadline; no cooldown renderer or active-cooldown captured IME case exists. The shared UI additions cover basic pending save/resend, exact expiry, and successful 429 reconciliation, but omit failed reconciliation, independent pending/deadline snapshot retirement, and new-field privacy assertions. The email-server FakeUsersRepo at line 100 does not enforce cooldown. Route-mapping tests exist, but end-to-end positive-policy enforcement through the real repository, SMTP graph, and administrative path is not covered. The three policy tests instantiate config directly rather than testing malformed/null decoding and enabled/disabled Plugin setup. Auth/admin model and public service tests leave the new fields default-null. Cache coverage adds pending expectations without an independent-repository fresh-read assertion. Cover those configuration, projection/privacy, cache, and UI boundaries without using a duplicated state machine as the sole oracle. This privacy/authorization/data-integrity Medium requires Coding. No actual metadata leak is claimed; source guards are structurally correct.

VEC-06. features/email/README.md:L66: Low: existing prose says replacement resets approval and exact current is the only target, contradicting the new retained-current/pending semantics. Update the lifecycle descriptions and supported rollout/recovery instructions. The administrative route table omits 429; WriteUsersRepo approval KDoc describes only current email; client/server/model setMyEmail KDoc omits typed cooldown. Feature documentation also lacks Architecture's backup/restore guidance. Preserve Operator Notes while correcting these operator-facing contracts.

## Positive evidence and scope

The reviewed base is 06cf01e with follow-up current 9c36362. The inspected diff contains 66 files, 2,579 insertions, and 158 deletions. AST inspection used /tmp/wishlist-postgres-verification-ast. The coordinator previously contained lookup, update, and mutex only; no pre-existing application frequency/count rate limit was found. Root independent Duration.ZERO default, positive ceil-millisecond finite/nonnegative validation, checked addition, and P1D sample are sound. Deadlines apply to real approval only; replay preserves deadlines and legacy null; configuration changes affect future issuance only.

All Exposed public CRUD writers acquire the singleton first-write lock. PostgreSQL defaults to READ_COMMITTED and SQLite uses writer locking. Raw cross-slot checks, batch commit/event ordering, SMTP and role transaction boundaries, dedicated rename field preservation, fresh cache reads, Auth backing-store bypass, pending promotion, stale-link handling, typed owner/root 429, generic 409, bearer/root guards, malformed 429 decoding failure, client cancellation, private projections, and owner/session/generation guards were reviewed positively. B-C-B address generation remains explicitly non-goal. No unrelated scope, endpoint/history/resend-rate feature, or README Operator Notes mutation was found.

Prior issue 79 findings V79-01 through V79-07 were resolved in 017 and remain preserved. Passing suites do not complete the missing VEC-04 and VEC-05 behavioral proofs. A JShell probe initially encountered classpath/module setup errors; the final run produced the production results recorded under VEC-01, then exited 1 on read-only Java preference flushing. It is diagnostic evidence, not a passing automated test. The probe modified only a new in-memory database and no product, source, test file, or external database.

## Required handoff

The independent command `./gradlew --no-parallel :wishlist.features.users.common:jvmTest :wishlist.features.email.server:jvmTest :wishlist.features.ui.users:jvmTest --rerun-tasks` exited 0 in 1m 3s with all 111 tasks executed. Fresh XML totals are users 26, email server 85, and UI 88: 199 tests with zero failures, errors, or skips.

The independent command `WISHLIST_POSTGRES_TEST_JDBC_URL='jdbc:postgresql://127.0.0.1:55432/postgres?user=aleksey' ./gradlew --no-parallel :wishlist.features.users.common:postgresEmailLifecycleTest --rerun` exited 0 in 15s with 14 tasks, five executed and nine up-to-date. All six PostgreSQL tests passed with zero failures, errors, or skips. The first sandbox invocation failed on a read-only Gradle lock; approved escalation succeeded. No other external database was accessed. Missing-URL guard, schema-cleanup catalog confirmation, and full-build/cross-platform evidence belong to step 013 and were not independently rerun here.

SMTP selects the persisted verification candidate. Credential-free approval, registration compensation, and retryable role promotion retain their existing delegation. Actual stale approval predicates are sound; VEC-03 concerns result classification. Raw owner-target guards enclose pending/deadline state across all three renderers, and equal-profile Refresh reevaluates the exact deadline.

Role audit: Planning 001 required no operator questions under zero-compatible defaults; the invalid AML handoff was corrected additively in 002. Architecture 003 used independent primary-document research and rollback design. Coding 004/005 reported incomplete work honestly; 006/007 foundation, 008 integration, 009 UI, 010 LL documentation, and 012 PostgreSQL tests followed. Role commits stayed within permitted report/product scope; no prior report was overwritten. Step 004 used generic changed-file reporting and marked LL unavailable, a recoverable traceability limitation. Independent diff check passed; no .gitignore change or unrelated scope was found, and five Operator Notes blocks remained byte-identical to 06cf01e.

Planning must revalidate bounded corrections. Architecture must tighten the missing proofs. Coding must fix VEC-01 and VEC-02 and address the proof requirements for VEC-03 through VEC-05. Verification must rerun focused and full gates plus the new behavioral proofs. Validating must perform a fresh bounded review afterward. No push or merge is authorized by this report.
