Model: gpt-6-astra
Changed files: agents/task/17.09.2026_09.48.08-41852b28-6585-4ed3-b0b9-54b2690414cd/015-planning.md

# Bounded correction plan after Validation 014

Planning uses gpt-6-astra as the available HL model, following the Planning HL-before-ML priority. Validation 014, Architecture 003, prior Planning 001/002, the affected feature READMEs including Operator Notes, and the relevant current implementation and tests were read. The surgical-patch skill governs this correction: capture the counterexample, fix the responsible branch, preserve surrounding behavior, and prove the bounded result. AST navigation used `/tmp/wishlist-postgres-verification-ast`. No product files or earlier reports were changed, and this Planning stage ran no tests.

Validation remains failed with two High findings, three Medium findings, and one Low finding. Every VEC finding is Open at first occurrence with consecutive count 1. The earlier passing suites are useful regression evidence, but do not close untested behavior. Architecture 003 remains the accepted design; the correction needs neither a new operator question nor a new configuration decision.

The zero omission default, explicit illustrative P1D sample, persisted deadlines for future approvals, existing endpoint, database lock protocol, private projections, and current/pending separation remain fixed. There is no rolling quota, resend restriction, address-generation token, history service, new admin UI, new credential policy, countdown, cache redesign, or unrelated cleanup in this plan.

## VEC-01: High — explicit clear is incorrectly classified as a no-op

The responsible owner is `features/users/common/src/jvmMain/kotlin/repo/ExposedUsersRepo.kt:354`. At line 360, null requested email equals null pending email on the common no-pending record, so `changingEmail` becomes false and bypasses both the deadline check and the explicit-clear branch at line 374. The same helper serves dedicated mutation at line 177 and generic/batch update at line 247. Validator 014 reproduced the defect against compiled production SQLite classes. The fake at `features/email/server/src/commonTest/kotlin/services/FakeUsersRepo.kt:100` handles null first and therefore cannot be the regression oracle.

Coding should first turn that production counterexample into real SQLite and PostgreSQL tests, then classify explicit null before non-null slot equality. An entirely empty lifecycle means current null, pending null, approval false, and deadline null. Only that email state is a clear no-op; a username change supplied by generic update must still behave normally. For any lifecycle state that clearing would change, enforce the locked deadline before altering username or any lifecycle field, then clear current, pending, approval, and deadline together. Non-null same-current and same-pending no-ops retain their accepted behavior. Do not change dedicated username-only handling, missing-user results, duplicate translation, or event timing.

Extend `features/users/common/src/jvmTest/kotlin/repo/ExposedUsersRepoSqliteTest.kt` and `PostgresUsersRepoTest.kt`, using existing fixtures and controlled clocks. Required cases are unapproved current/no pending, approved current/no pending with null deadline, active deadline, exact expiry, expired deadline, pending replacement, and already empty. Include an empty-address record with residual lifecycle metadata seeded only in the fixture to prove the complete definition of empty. Exercise dedicated setEmail, generic single update, bulk update, and the existing administrative full-update path. Assert complete returned and independently read rows, address release, no-op event silence, rejected-clear event silence, and rollback of an earlier batch mutation when a later clear is restricted. A blocked full update must not partially rename the user. The real admin boundary proof belongs with VEC-05, not a second copy of the lifecycle algorithm.

## VEC-02: High — a successful save can claim a different pending email

The responsible owner is `features/ui/users/src/commonMain/kotlin/ui/UserEditViewModel.kt:968`, repeated at line 996. Both save-reconciliation reads accept `containsPersistedEmail(submitted)`. If submitted B became approved and C then became pending, current B satisfies that broad test while the actual editable baseline is C. Completion at line 1015 replaces the user's draft with C and reports C saved. This conflicts with the explicit baseline requirement in Architecture 003.

Restrict both reconciliation checks to `editableEmailBaseline() == submittedEmail`. Keep the early same-slot no-op admission at line 930 unchanged because Architecture deliberately permits same-current requests without implicitly cancelling a pending replacement. On a reconciliation mismatch, retain the exact raw B draft, publish EmailChanged, and exit before any remaining POST or success publication. Approved B with no pending C is still a legitimate success that skips verification. Do not refactor the ownership/generation state machine or transport-error behavior.

Extend `features/ui/users/src/commonTest/kotlin/ui/UserEditViewModelEmailTest.kt` with controlled suspensions around private reads and verification. For enabled and disabled SMTP, force the first refreshed state to current approved B/pending C after a submitted B and assert raw draft preservation, EmailChanged, no saved marker, and no POST. For enabled SMTP, let the first read confirm pending B, suspend the single POST, then make the final read current approved B/pending C; assert one already-issued POST only, no subsequent delivery or success, and unchanged raw draft. Repeat the final-read race when no POST was needed because B was approved on the first read. Include controls where B is still pending and where B is approved with no replacement, so the narrower condition does not reject valid outcomes. These shared tests must execute on JVM, JS Node, and Android debug.

## VEC-03: Medium — AlreadyApproved must refer to the requested address

`features/email/server/src/commonMain/kotlin/services/EmailFeatureService.kt:103` and line 120 return AlreadyApproved for any approved current address with no pending replacement. Make both classifications require current email equal to expectedEmail. An approved non-null A with expected B is EmailChanged; a missing user or genuinely absent usable address retains NoEmail. Simply adding equality and falling through to the existing NoEmail branch would leave the wrong classification, so test the distinct result explicitly.

Extend `features/email/server/src/commonTest/kotlin/services/EmailFeatureServiceTest.kt`, reusing its controlled SMTP and real invite sender at lines 79 and 101. Direct approved A/expected B must return EmailChanged without sending or creating a link. During suspended SMTP for pending B, replace B with C and approve C; completion must return EmailChanged and remove only that request's link. Preserve an unrelated sibling link and assert exact cleanup counts. Controls include B approved during SMTP returning AlreadyApproved, B approved followed by pending C returning EmailChanged, pending B remaining Sent, and clear/delete remaining NoEmail. Preserve existing failure/cancellation compensation tests. No approval authorization or payload format changes are required.

## VEC-04: Medium — database contention tests need an observed blocking interval

`features/users/common/src/jvmTest/kotlin/repo/PostgresUsersRepoTest.kt:87`, line 138, and line 180 inspect the second callback immediately after starting a thread. The SQLite counterparts at `features/users/common/src/jvmTest/kotlin/repo/ExposedUsersRepoSqliteTest.kt:452` and line 498 have the same gap. A scheduled-late contender satisfies the assertions without attempting the database lock. The accepted database protocol is not being redesigned; the missing evidence concerns when the contender actually reaches the locked operation.

Change only the existing test synchronization and any strictly necessary internal test seam. PostgreSQL should identify the contender's connection and observe its blocked lock-row UPDATE through a separate database observer before releasing the holder. SQLite needs an equivalent engine-level contention observation, such as a test-local driver busy callback if the installed driver exposes one; Architecture must verify that API or specify an equally strong test-only connection instrument. A latch before invoking the repository, thread-start acknowledgement, an arbitrary delay, or a callback before calling JDBC is insufficient alone. The proof must establish an attempted database operation blocked by the holder, an unreached post-lock callback while blocked, and acquisition/completion after release. Use bounded waits and always release/join workers on failure; do not add public production behavior solely for test scheduling. The existing public constructor test callback at `ExposedUsersRepo.kt:72` must not grow into a runtime lifecycle API; keep any necessary adjustment internal and behavior-neutral.

Strengthen both same-address and pending-versus-current contention cases, plus unrelated writers. Add the exact accepted locked-clock proof: begin a mutation before expiry, hold its database acquisition, advance the injected clock to the exact deadline, release the holder, and require the mutation to use the post-acquisition time. For new approval, likewise prove the stored deadline derives from the clock after acquiring the lock. Add approval B versus replacement C in both orderings: C commits first and B approval is stale; B approval commits first with positive cooldown and C is rejected. Include the zero-policy B-first case where current B and pending C are valid. Preserve raw cross-slot uniqueness scans and timeout/BUSY classification tests. Use the existing isolated PostgreSQL task and file-backed SQLite fixtures; no new database service or runtime dependency is needed.

## VEC-05: Medium — complete the missing acceptance proofs

The following paragraphs define the missing production-boundary matrix. Tests should assert observable state, calls, persisted rows, wire values, or production semantics; no copied fake lifecycle is sufficient to prove database enforcement. Existing proofs that already meet the requirement should remain intact rather than being rewritten.

Private UI metadata: extend `UserEditViewModelEmailTest.kt` and `features/ui/users/src/jvmTest/kotlin/ui/UserEditEmailRenderTest.kt`. Seed non-null pending email and deadline for anonymous/non-owner/root-other privacy and identity/session/target invalidation. The desktop retarget test at line 141 and root-other test at line 358 currently leave both fields null. Hold the ViewModel scheduler during retarget, first prove the stale ViewModel retains the seeded private metadata, and require current, pending, deadline/cooldown text, draft, feedback, and actions to disappear when composition alone observes the new target. Extend suspended PUT/POST/private-read cancellation cases so new metadata cannot survive invalidation or trigger a later POST. Unauthorized initial state must make no private calls.

Production cooldown UI and IME: add desktop cases using the actual OwnerEmailEditor. Show retained current A, pending B, and independent draft when editable; separately show a non-null active deadline with disabled input and Save, usable Refresh, exact UTC restriction text, and no resend for an approved address without pending. Capture the production IME action before the restriction becomes active, then invoke that action under the active restriction and assert zero additional PUT/POST. Advance the controlled clock to expiry, Refresh, and assert editing and save eligibility recover even for an equal profile. Retain the existing suspended-PUT IME guard at renderer line 655. Pending and active deadline may be seeded together for privacy stress, without claiming that every such synthetic fixture is a normal public transition.

Typed rejection and snapshot retirement: extend the successful-reconciliation test at `UserEditViewModelEmailTest.kt:2726` with a failed private reconciliation after a well-formed 429. Require the authoritative rejection deadline, raw draft, separate load-failure state, no saved success, no automatic POST, and exactly one reconciliation attempt. A later checked expiry refresh retires obsolete restriction feedback. Independently vary pendingEmail and emailChangeAllowedAt while holding current email/approval constant and require completed saved/Sent/AlreadyApproved feedback to retire. Preserve a current failure through matching and failed reads according to issue #79. Keep generic false/409/network/malformed-429 failures distinct from typed cooldown.

Real positive-policy service and admin enforcement: extend the existing email service/coordinator integration fixtures and `features/admin/server/src/commonTest/kotlin/UsersManagementFeatureTest.kt` / `configurators/AdminRoutingsConfiguratorTest.kt` with a real ExposedUsersRepo and controlled clock. Approve through the shared coordinator using positive policy, then call the enabled and disabled EmailFeature paths and the guarded administrative full-update path. Assert typed deadline rejection, identical durable rows, no partial username update, and no delivery/deeplink creation. Dedicated username change remains allowed. At exact expiry the same allowed change succeeds with current approval retained and pending updated. Retain owner bearer and root/non-root route checks, 429 JSON mapping, generic duplicate 409, and no deadline disclosure to a denied admin caller. Use controlled EmailsService transport rather than live SMTP. The current fake at `FakeUsersRepo.kt:100` does not enforce cooldown, so merely adding expectations against that fake cannot close the finding.

Configuration graph decoding: extend `features/email/server/src/commonTest/kotlin/PluginTest.kt`, `EmailChangePolicyConfigTest.kt`, and the existing Koin setup in `services/EmailVerificationAccountCoordinatorTest.kt`. Execute actual Plugin.setupDI, whose root policy decode occurs at `features/email/server/src/commonMain/kotlin/Plugin.kt:65`, with SMTP absent, explicitly null, and configured. Cover omitted policy, explicit zero, P1D, positive fractional milliseconds, negative duration, malformed text, JSON null, positive/negative infinity, conversion overflow, and checked addition overflow at the appropriate actual boundary. Invalid root policy must fail setup independently of SMTP. Confirm the valid graph wires the validated policy into the shared coordinator, preserves absent EmailsService for disabled graphs, and enforces it through real repository operations. Decode the sample's explicit positive value. Retain existing direct conversion tests as supplementary evidence rather than calling them Plugin tests.

Projection and public omission: extend `features/auth/common/src/commonTest/kotlin/models/AuthFeatureUserTest.kt`, `features/admin/common/src/commonTest/kotlin/models/AdminUserTest.kt`, `features/users/common/src/commonTest/kotlin/models/UsersFeatureUserTest.kt`, and `features/users/server/src/commonTest/kotlin/services/UsersServiceTest.kt`. Existing auth cases at lines 33 and 53 and the public service seed at line 46 leave new fields null. Use distinct non-null pending email and a concrete Long deadline to verify full Auth/Admin mapper round trips and serialization, old-payload null defaults, explicit public reverse-mapping arguments, and exact public JSON keys id/username. Exercise common serialization on the existing JVM/JS targets. Assert absence of private keys and values in actual UsersService output, not only equality between default-null objects.

Independent cache and private reads: extend `features/users/common/src/jvmTest/kotlin/repo/CacheUsersRepoSqliteTest.kt`, which currently contains only same-wrapper mutation coverage at line 20. Populate one CacheUsersRepo, mutate pending/approval/deadline through a separately connected repository sharing the fixture-owned database, and establish that the first cache still holds the old complete row. Then assert getByIdFresh returns the newer complete record without manually refreshing that cache. Add the equivalent authenticated `AuthFeatureService.getUser`/getMe boundary test in `features/auth/server/src/commonTest/kotlin/services/AuthFeatureServiceTest.kt`, maintaining valid credentials and role authorization. Prove fresh-backing selection, not an event from the same cache's backing instance. Preserve failed-write cache behavior and subscription liveness.

## VEC-06: Low — correct the actual documented contracts

Update the contradictory passages in `features/email/README.md`, including Overview, verification route description, Models, storage/approval/sending, duplicate propagation, client outcomes, and registration ordering. Replace exact-current-only wording with exact candidate semantics and explain AlreadyApproved only for the exact approved expected address with no pending replacement. State retained approval for replacement, guarded explicit clear, the typed cooldown exception, and no-op/replay behavior. Correct the full administrative update route table in `features/admin/README.md` to include typed 429.

Correct the approval and cooldown KDoc in `features/users/common/src/commonMain/kotlin/repo/WriteUsersRepo.kt:34`, email server/client interfaces and implementations, and `features/ui/users/src/commonMain/kotlin/ui/UsersModel.kt` / `DefaultUsersModel.kt` setMyEmail contracts. Audit only these affected lifecycle descriptions for stale claims. The users UI README also still mentions an email-and-approval-only snapshot and replacement approval reset; make those passages match all four lifecycle fields and retained current approval. Coverage prose must describe fresh executed proofs accurately and retain browser/device/live-SMTP limits.

Add Architecture 003's existing rollout and recovery guidance to feature documentation: take a database backup, stop old writers before the additive forward deployment, deploy matching UI/server, prefer rolling forward, and avoid mixed old/new writers. Emergency old-binary recovery requires stopping writes and restoring the pre-upgrade backup with explicit post-backup data loss, or a separately designed compatible backport. Do not prescribe automatic column removal, deadline clearing, pending promotion, or data contraction. Preserve every Operator Notes block byte-for-byte. Documentation filling follows the repository's LL requirement during Coding; Planning supplies the decisions, not edits to those files.

## Sequencing, migration impact, and verification gate

Architecture should turn the bounded cases above into exact test specifications and finalize the test-only contention observation, without reopening the accepted storage/API design. Coding should add economical failing regressions for VEC-01, VEC-02, and VEC-03 first, apply the three narrow production fixes, then strengthen VEC-04 and complete the VEC-05 matrix by boundary. VEC-06 follows the final behavior and actual evidence. Keep test clocks, fixture databases, and worker cleanup isolated. Run ast-index rebuild after source edits.

The correction introduces no new schema migration, backfill, policy default, wire shape, endpoint, or dependency requirement. Existing additive columns and lock table remain unchanged. The corrected clear behavior intentionally performs the already-supported explicit removal after cooldown; callers retain responsibility for requesting that removal. Existing deployed data does not need repair by this plan. Recovery constraints remain those of Architecture 003 because the pending lifecycle already makes arbitrary old-binary rollback unsafe.

Run focused real SQLite repository/cache tests, focused shared ViewModel tests, real desktop renderer tests, and email service/Plugin/admin/auth/model/public-service suites as their corresponding corrections land. Run the explicit `:wishlist.features.users.common:postgresEmailLifecycleTest` against the already approved disposable database using the existing dedicated URL setting; preserve the missing-URL failure guard and schema cleanup proof. SQLite or timeout-only success is not a substitute for PostgreSQL's observed blocking interval.

After focused proofs pass, run one serial aggregate covering users common/server, email common/client/server, auth common/server, admin common/server, and users UI; execute common UI tests on JVM, JS Node, and Android debug and compile JS/JVM/Android renderers. Complete repository-required full build gates in Verification. Reports must map every added named test to fresh XML execution and distinguish focused, platform, PostgreSQL, and full-build evidence. Do not infer missing tests from a green aggregate or repeat unrelated gates without a change or unresolved concern.

The stop condition is all six VEC findings closed by their specified evidence, no reopened V79-01 through V79-07 regression, preserved Operator Notes and scope, and fresh focused plus required aggregate/full gates. Validation must independently review that final candidate. Planning reports a plan only, not a correction or a passing execution result. No operator question is required: all corrections restore requirements already authorized in Architecture 003 and Validation 014.

```aml-hip
ENTITY:
entity_id=email_cooldown_correction_cycle_2; type=planning_handoff; state=ready_for_architecture

CONTEXT:
* task_id=17.09.2026_09.48.08-41852b28-6585-4ed3-b0b9-54b2690414cd; agent_id=planning; memory_ref=[PROMPT,001,002,003,014,015]; branch=feat/issue-79-user-email-change
* constraints=[surgical_correction,accepted_architecture,zero_default,no_schema_change,no_push,Operator_Notes_preserved]; operator_questions=zero; findings_open=6

ACTION:
1. action=correct_explicit_clear_classification; target=VEC-01; params={severity:High,owner:ExposedUsersRepo,proof:[SQLite,PostgreSQL,dedicated,generic,batch,admin,rollback,events]}
2. action=require_exact_save_baseline; target=VEC-02; params={severity:High,owner:UserEditViewModel,proof:[first_read_race,final_read_race,raw_draft,no_later_POST,no_false_success,enabled,disabled]}
3. action=bind_approval_feedback_to_expected_address; target=VEC-03; params={severity:Medium,owner:EmailFeatureService,proof:[initial_mismatch,SMTP_race,exact_link_cleanup]}
4. action=prove_actual_database_contention; target=VEC-04; params={severity:Medium,owner:database_tests,proof:[blocked_operation,post_lock_callback,locked_clock,approval_replacement_order],public_behavior_changes:zero}
5. action=complete_required_boundary_matrix; target=VEC-05; params={severity:Medium,proof:[metadata_privacy,cooldown_IME,failed_429_reconciliation,real_policy_enforcement,Plugin_decoding,private_public_projections,independent_cache,four_field_snapshot_retirement]}
6. action=correct_lifecycle_documentation; target=VEC-06; params={severity:Low,scope:[README,KDoc,admin_429_route,backup_recovery],Operator_Notes_mutation:false}

REASON:
* condition=Validation_014_two_High_three_Medium_one_Low; requirement=restore_existing_acceptance; causal_chain=targeted_counterexamples→responsible_branch_fixes_and_missing_proofs→independent_validation
* condition=green_suites_omit_required_boundaries; requirement=observable_regression_proof; causal_chain=production_state_and_database_and_UI_assertions→fresh_named_test_execution→evidence_backed_closure

EXPECTED RESULT:
* entity_id=email_cooldown_correction_cycle_2; new_state=bounded_correction_specification_available; location=agents/task/17.09.2026_09.48.08-41852b28-6585-4ed3-b0b9-54b2690414cd/015-planning.md
* entity_id=email_cooldown_correction_cycle_2; migration_delta=none; architecture_delta=none; policy_default=zero; implementation_state=pending

VERIFICATION:
* check=VEC_01_through_VEC_03_mechanisms; expected=source_confirmation; observed=clear_equality_and_save_slot_and_approval_identity_branches_confirmed
* check=VEC_04_and_VEC_05_coverage; expected=concrete_missing_proofs; observed=database_latch_UI_metadata_config_projection_cache_gaps_confirmed
* check=planning_scope; expected=015_only; product_edits=zero; test_execution=none
* check=completion_gate; expected=all_six_findings_closed_and_V79_preserved_and_required_gates_pass; status=pending_Coding_Verification_Validation

UNCERTAINTY:
* missing=final_test_only_SQLite_contention_observer; ambiguity=none; resolution=Architecture_verify_installed_driver_API_or_equivalent_engine_observation
* missing=fresh_correction_test_results; ambiguity=none; resolution=Coding_and_Verification_execute_required_matrix

REPETITION OF RESULT:
* entity_id=email_cooldown_correction_cycle_2; stored_in=shared_memory; status=available; next_role=Architecture; operator_questions=zero

COMMUNICATION:
* sender=planning; receiver=Orchestrator_and_Architecture; task_id=17.09.2026_09.48.08-41852b28-6585-4ed3-b0b9-54b2690414cd; message_id=93e8b3c3-ae23-423a-8a30-a67ed0728c9b; protocol=AML-HIP

PERSISTENCE:
* local_memory=true; shared_memory=true; index_keys=[task_id,email_cooldown_correction_cycle_2,VEC-01,VEC-02,VEC-03,VEC-04,VEC-05,VEC-06]; storage=015-planning.md; auto_memory=false

VALIDATION:
* format_valid=true; no_pronouns=true; entities_explicit=true; high_density=true; causal_chain_present=true; ambiguity_detected=false
```
