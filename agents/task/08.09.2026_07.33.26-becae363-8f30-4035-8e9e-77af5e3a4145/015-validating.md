Model: gpt-6-astra (HL)
Changed files: agents/task/08.09.2026_07.33.26-becae363-8f30-4035-8e9e-77af5e3a4145/015-validating.md

The assigned model is the preferred HL Validator tier. Independent review of the corrected role transition, owner lifecycle, and claimed acceptance evidence requires cross-layer reasoning. I retained caveman full for internal notes and wrote this required report directly, without nested agents. All findings and handoff text are normal prose; no machine-parsed structured blocks are used.

## Validation result: FAIL

There are zero Critical, zero High, two Medium, and zero Low findings. F1, F3, F4, and F5 from step 008 are closed. F2 and F6 are partially corrected but remain open at Medium. Their authorization, privacy, and data-integrity scope requires a Coding loop under the Orchestrator's Medium Findings Decision Rule. A Planning restart is not required because the previous High finding is closed. The remaining findings have appeared in two consecutive validation cycles, below the three-cycle automatic escalation threshold.

## Scope and execution evidence

I retained the repository/Validator instructions and full first-cycle history read during step 008, then read the complete second-cycle history through 014, including Planning 009, Architecture 010, Coding 011/013, and Verification 012/014. I used ast-index for source/test navigation and reviewed the corrective diff from 9b8f097 through 7a0487d together with the original implementation reviewed in 008. The product correction is isolated in 252f013; later commits modify only immutable step reports. The worktree was clean, all role commits carry the required coauthor trailer, and `git diff --check 7ade67e..HEAD` passed.

I read the updated ui/users and ui/adminPanel READMEs completely and reviewed the roles/email README changes against their complete previously read versions. The remaining relevant feature READMEs are unchanged. Operator Notes remain untouched, root-only administration is retained, and the test dependencies plus Android local-unit-test settings are within the corrective test scope. No source, tests, README, build files, PROMPT, or earlier reports were changed by this role. No AST rebuild was needed for report-only work.

Step 014 records one tracked non-quiet full Gradle build with tool-provided exit zero, `BUILD SUCCESSFUL in 1m 19s`, and 4530 actionable tasks. Product source is unchanged since that run. I independently totaled the current relevant JVM XML suites: 157 cases, zero failures, zero errors. The affected UI JS Node XML suites contain another 26 passing cases, including 17 owner-email cases, five profile-save cases, two dashboard cases, and two model-facade cases. These corroborate the reported executed coverage without implying tests that do not exist. I did not rerun the successful full build. Step 013 correctly distinguished quiet-output capture from a demonstrated Gradle failure; 014 supplies the missing terminal result.

## Prior findings and remaining corrections

### F1 — Closed: Failed role grants remain retryable without granting revoked access

`features/roles/server/src/commonMain/kotlin/RolesBootstrap.kt:142` now grants direct User before removing NewUser, confirms a newly requested grant, and checks the final direct role state before success. Exceptions before/after grant or removal leave a state that a later pending-first invocation can complete. Role-less subjects still return false, and emailApproved never authorizes a role grant. The role mutex and coordinator-before-role ordering remain intact.

The four one-shot failure tests at `features/roles/server/src/commonTest/kotlin/RolesBootstrapTest.kt:195`, line 212, line 229, and line 246 exercise both sides of both mutations and subsequent convergence. The unconfirmed-grant case at line 263 retains NewUser; the revoked-access test at line 182 prevents reopening an old link from restoring User. `features/email/server/src/commonTest/kotlin/services/EmailDeepLinkIntegrationTest.kt:115` exercises a failed grant after durable approval and a successful retry of the same minted link. The documented possibility of valid direct User access before a failed pending-marker cleanup matches Planning 009 and does not invent cross-repository atomicity.

### F2 — Open, Medium: Production lifecycle state lacks the confinement assumed by its guards

The original missing guards are substantially implemented: `features/ui/users/src/commonMain/kotlin/ui/UserEditViewModel.kt:302` invalidates generations and cancels jobs, line 334 checks the active owner token, and lines 635–689 guard save/resend continuations. The deterministic tests at `features/ui/users/src/commonTest/kotlin/ui/UserEditViewModelEmailTest.kt:111`, line 136, line 177, line 216, line 257, and line 292 prove queued-owner denial, invalidation during requests, leave/return behavior, late refresh suppression, and obsolete finally protection under a serial test scheduler.

However, the production default at `UserEditViewModel.kt:65` is Dispatchers.Default, while the generation/token/job fields at lines 213–225 and their multi-field transitions are unsynchronized. Public callbacks run on the UI thread, and independent collectors/continuations run on Default workers on JVM/Android. A worker can pass the token/owner check at line 462, another worker can invalidate and clear state at lines 302–314, and the first worker can then publish the obsolete private profile at line 472. The check and publication have no common confinement or lock. Concurrent refresh starters can also race the plain increment and job assignment at lines 354–361. StateFlow makes individual value accesses safe; it does not make these compound operations atomic.

The dashboard has the same issue at `features/ui/adminPanel/src/commonMain/kotlin/ui/AdminPanelViewModel.kt:37`: a successful authorization/version check at line 135 can race logout clearing at line 104 before line 136 republishes the old list. Both initial collectors can concurrently replace the unsynchronized job/version state at lines 114–130. The new tests pass StandardTestDispatcher, which prevents precisely these parallel check/clear/publish interleavings.

Architecture 010 explicitly specifies UI-dispatcher confinement while retaining the inherited lifecycle Job. Coding 011 at line 9 substitutes Default to preserve the existing navigation scheduler, but retaining the Job does not require retaining Default. Confine the complete lifecycle transitions and public admissions to the same UI dispatcher, as planned, or provide equivalent synchronization. Retain deterministic tests with a production-equivalent serial execution contract. This is a remaining concurrency guarantee gap affecting private state and permission-sensitive operations, not evidence of an arbitrary-user server authorization bypass.

### F3 — Closed: Profile save honors Boolean results

`features/ui/users/src/commonMain/kotlin/ui/UserEditViewModel.kt:578` handles username false/exception and stops before password mutation; line 590 handles password failure separately; navigation occurs only at line 602 after requested writes succeed. Inputs remain available and loading clears in finally. `features/ui/users/src/commonTest/kotlin/ui/UserEditViewModelSaveTest.kt:25`, line 56, line 85, line 117, and line 158 cover username false/throw, password false, successful sequences, and cancellation cleanup. All three platform views render the two localized profile-save errors. Existing safe username-only delegation and the already-correct administrator editor remain intact.

### F4 — Closed: Storage uncertainty and delivery failure are distinct

The PUT boundary at `features/ui/users/src/commonMain/kotlin/ui/UserEditViewModel.kt:634` assigns SaveFailed for an unconfirmed save and never posts afterward. The later POST boundary assigns DeliveryFailed only after confirmed persistence. Guarded reconciliation at line 458 can recover a committed pending address without automatically sending, and its independent load-failure flag preserves the primary error. The cases at `features/ui/users/src/commonTest/kotlin/ui/UserEditViewModelEmailTest.kt:353`, line 380, line 414, line 437, and line 463 exercise these outcomes, including a throwing PUT after fake commit and subsequent resend without another PUT. Updated copy accurately says persistence could not be confirmed. F2 remains the separate concurrency qualification on late publication.

### F5 — Closed: Initial capability errors expose an owner-only retry

`features/ui/users/src/commonMain/kotlin/ui/UserEditViewModel.kt:192` separates section visibility from the confirmed-capability/profile mutation gate at line 198. Probe or initial private-read failure produces Failed with visible feedback and blocked writes. `features/ui/users/src/commonTest/kotlin/ui/UserEditViewModelEmailTest.kt:494`, line 530, and line 568 prove recovery, draft preservation, and confirmed-disabled hiding. `features/email/client/src/commonMain/kotlin/KtorEmailFeature.kt:55` now distinguishes unsuccessful HTTP responses from a successful false capability result; its MockEngine suite tests failures and the normal Boolean response.

The actual JS view at `features/ui/users/src/jsMain/kotlin/ui/UserEditView.kt:148` renders load/error status and the Refresh action under owner visibility, while save/resend use canMutateOwnEmail. JVM/Android use the same gates and feedback. The original failure where the first probe hid both its error and retry is corrected.

### F6 — Open, Medium: Important required cases are still absent from the coverage claimed as complete

The migration, durability, bulk-update, cache, transport, route, profile, and navigation suites are real improvements. `features/users/common/src/jvmTest/kotlin/repo/ExposedUsersRepoSqliteTest.kt:160` exercises an old table; line 188 proves approval survives reopening; lines 218 and 255 cover bulk reset/preservation and conditional events. `features/users/common/src/jvmTest/kotlin/repo/CacheUsersRepoSqliteTest.kt:21` exercises the real cache over SQLite and immediate visibility. The actual admin route suite proves 401/403, root rename with preserved approval, malformed/missing targets, and 409. The actual email route suite proves bearer extraction and ignored caller-id injection; the client suites exercise the dedicated paths and result/status handling.

The remaining omissions are narrower than in 008 but still material. `features/email/server/src/commonTest/kotlin/services/EmailFeatureServiceTest.kt:161` and line 191 are the only new production request-service cases. They cover the pre-delivery result matrix and immediate successful/failed sends. They never suspend SMTP and replace, clear, delete, or approve the current user, never verify ownership of the removed stale link, and never exercise a failed-send/same-address-success retry or cancellation through the production request service. Consequently, the asynchronous branches at `features/email/server/src/commonMain/kotlin/services/EmailFeatureService.kt:107` remain untested. Existing sender and coordinator tests cover adjacent boundaries, not this new post-delivery recheck and cleanup.

`features/ui/adminPanel/src/commonTest/kotlin/ui/AdminPanelViewModelTest.kt:40` and line 64 are the entire dashboard suite: initial rows despite a failed probe, and logout during a suspended request. There is no resume/change refresh, failed-load retry, empty-success distinction, old/new refresh ordering, or SMTP busy/result test. Its callback implementations at line 126 are no-ops, so it cannot prove ViewModel callback forwarding. `client/src/commonTest/kotlin/AdminPanelNavigationTest.kt:25` separately proves that the real interactor pushes the selected user, but does not fill those lifecycle cases. Coding 011 at line 11 claims retry, SMTP state, and callbacks were tested; the current suites and XML names do not support that claim. Verification 014's blanket closure of F1–F6 also exceeds its execution evidence.

Complete the deterministic production request-service race/retry/cancellation cases and dashboard resume/retry/ordering/SMTP/callback cases already specified in Architecture 010. Also make the legacy migration fixture representative of the immediately preceding schema: the current fixture omits email entirely and inserts only a null-address user, so add a pre-existing non-null email with preserved id/username/address and false approval. No live SMTP or browser installation is needed for these cases. Carry accurate executed-test names and results in the next immutable report. This remaining finding concerns data integrity and permission-sensitive lifecycle behavior, and independently mandates a Coding loop.

## Original behavior, privacy, and the rendering limitation

The original persisted false-by-default field, exact current-address conditional approval/reset, private auth/admin DTOs, email-free public projection, bearer-derived owner request, root-gated username-only path, SMTP-independent storage, and process-local coordinator remain unchanged by the correction. I found no new issue in those server contracts. Historical same-address link reuse and the lack of distributed atomicity remain explicit compatibility limitations.

All three dashboard views show real username/id rows, the existing selected-user action, preserved Users/Wishlists navigation, and separately labeled SMTP purpose/recipient controls. JVM/Android scrolling remains present. The profile views wire the actual ViewModel actions for missing, pending, and approved states; error feedback and Refresh are outside the enabled-only mutation gate. EN/RU copy is present and consistent across platforms.

Coding's reported Compose Web/jsdom disposal failure is a rendering-harness limitation, not successful DOM-control evidence. No retained DOM test, live SMTP, manual browser interaction, or pixel/layout verification is claimed. For the original simple UI wiring, direct inspection of actual views, shared behavior tests running on JVM and JS, the real navigation-chain test, and platform compilation provide useful and proportionate evidence. I do not assign an additional finding solely because the jsdom harness could not dispose its composition. The requested labels/actions are directly wired and do not depend on layout calculations. That bounded acceptance of the rendering limitation does not close the independent missing service/dashboard behavioral tests or the production-dispatcher race above.

## Handoff

Return to Coding for the two Medium findings, preserve the corrected role transition and the closed F3–F5 behavior, then run Verification and Validating with new monotonic step files. Do not restart Planning solely for these remaining Medium findings and do not escalate to the operator at this second recurrence. No external message, push, or deployment is part of this role. Only this report is committed.
