Model: gpt-6-astra (HL)
Changed files: agents/task/08.09.2026_07.33.26-becae363-8f30-4035-8e9e-77af5e3a4145/008-validating.md

The Orchestrator assigned gpt-6-astra for the preferred HL Validator tier. Independent validation requires reasoning across persisted ownership evidence, authorization, concurrent operations, and platform presentation. I wrote the mandatory report directly because role agents cannot spawn nested agents. Caveman full applied only to internal notes. The findings and handoff below are normal prose, not machine-parsed structured blocks.

## Validation result: FAIL

There are zero Critical, one High, five Medium, and zero Low findings. The High finding requires restarting from Planning. Independently, the Medium findings concerning identity, persistence verification, and permission-sensitive behavior meet the Orchestrator's mandatory Coding-loop rule; they cannot be accepted without correction merely because the build passed. This is the first validation cycle, so repeat-problem escalation does not apply.

## Scope and evidence

I read AGENTS.md, SHORTCUTS, ALL and local.ALL, VALIDATOR and checked for its absent local override, MODELS, PROTOCOL, GIT, TOOLS, AST_INDEX, ORCHESTRATOR, PROMPT, and the complete task history from 001 through 007. I read the complete feature READMEs for users, auth, email, roles, deeplinks, admin, ui/users, and ui/adminPanel. The relevant populated Operator Notes require root-only administration and reuse of existing functionality. The implementation preserves those notes and the existing server role gates.

The reviewed range is task base 7ade67e through 2680f85. Product changes are entirely in 17803c7; the subsequent commits modify only step reports. The worktree was clean before this report, and `git diff --check 7ade67e..HEAD` passed. I checked the role commits and their required coauthor trailers. Code navigation used ast-index; access to its existing external cache required approved escalation. I also inspected the indexed local MicroUtils Exposed and cache implementations to check inherited bulk-update and event/locking behavior. No source, test, feature README, PROMPT, or earlier report was edited, and no index rebuild was necessary for this report-only role.

Step 007's build evidence is credible and independently corroborated by `/tmp/007-verification-build-output.txt:8427`: `BUILD SUCCESSFUL in 1m 35s`, followed by 4477 actionable tasks, 182 executed and 4295 up-to-date. The log contains no failure marker. Step 007 records exit zero for `./gradlew build --no-daemon --no-parallel --max-workers=1`; its product source is still current. Step 005's focused 130-case JVM evidence and the JS/Android compilation results therefore remain applicable. I did not repeat the successful full build or claim new runtime tests. No live SMTP or manual browser scenario was performed.

## Findings

### F1 — High: A failed role grant permanently prevents verification retry

At `features/roles/server/src/commonMain/kotlin/RolesBootstrap.kt:142`, promotion removes the direct NewUser role before adding User at line 143. If removal succeeds and the User insertion throws an ordinary transient repository exception, the account has neither role. On the next link attempt, the new guard at line 139 reaches the false branch at line 147, so the recovered repository is never asked to grant User. `features/email/server/src/commonMain/kotlin/services/EmailVerificationAccountCoordinator.kt:105` has already persisted email approval, but line 106 propagates the failed role outcome; repeated links cannot complete required-email registration and normal login remains unavailable.

This is a regression from the base helper, which unconditionally retried the User insertion on a later call. Architecture 002 at line 64 explicitly requires retry after promotion failure, while also disclaiming crash atomicity. An ordinary caught/retried repository failure does not require a crash-atomic or distributed redesign, and treating the failed transition as an intentional revocation defeats the required retry behavior. Planning and Architecture must reconcile the pending-only authorization guard with a recoverable transition; Coding must add a deterministic one-time User-insertion failure followed by successful retry, while retaining the revoked-account denial test. Do not solve recovery by granting User to arbitrary role-less accounts or trusting the email approval Boolean as an access role.

### F2 — Medium: Owner-email mutations do not retain caller ownership across suspension

`features/ui/users/src/commonMain/kotlin/ui/UserEditViewModel.kt:396` checks the derived owner flag only before launching work. The coroutine at line 407 does not capture/recheck the caller or an operation generation before `setMyEmail`, after that suspension, before the verification POST at line 415, or before writing results. Resend has the same gap at line 437. By contrast, the refresh path has an explicit caller/version check at line 238. `features/ui/users/src/commonMain/kotlin/Plugin.kt:106` simply forwards to the shared authenticated client, so no model-layer binding closes the gap.

If identity changes during an awaited save, the old operation continues issuing its next owner-scoped request and updating state after the ownership state was cleared. A queued operation also lacks a final owner check immediately before its first mutation. Server bearer checks still restrict each HTTP call to its actual caller; the defect is the missing binding between the editor's initiating owner and the multi-request operation, not a demonstrated arbitrary-user server endpoint. Capture the initiating identity and operation generation, reject stale continuations and results, and cancel/invalidate outstanding email mutations on identity loss. Add deterministic identity-change tests at each suspension, as required by Architecture 002 at lines 79 and 123. This finding touches authorization and data integrity.

### F3 — Medium: Root profile saves still report success when requests return false

`features/ui/users/src/commonMain/kotlin/ui/UserEditViewModel.kt:368` ignores `updateUsername`'s Boolean result, and line 371 ignores `setPassword`'s result before calling `interactor.onSaved` at line 373. For example, a duplicate username or missing user can return false through the new Ktor username endpoint, yet the editor closes as though saved. A supplied password may also be changed after the username request failed.

Architecture 002 at line 76 explicitly requires both affected editors to check successful saves. `AdminUserEditViewModel` was corrected, but the public profile editor was not. Keep the editor open on false, stop dependent writes after failure, and provide failure feedback. Add tests for false username/password results and successful navigation. The affected function predates the task, but correcting its behavior is an explicit acceptance condition of the changed username-only workflow.

### F4 — Medium: Storage exceptions are presented as verification-delivery failures

At `features/ui/users/src/commonMain/kotlin/ui/UserEditViewModel.kt:418`, one catch surrounds both the storage PUT and verification POST. A network exception from `setMyEmail` at line 409 is therefore assigned `DeliveryFailed` at line 419 even though storage never returned success and no verification request ran. The view then renders the delivery-specific message through `features/ui/users/src/jsMain/kotlin/ui/UserEditView.kt:244`; JVM and Android use the same result state. Only an explicit false PUT result reaches SaveFailed.

Architecture distinguishes storage failure from a successfully saved pending address whose delivery failed. Track whether storage succeeded or handle the two calls separately: a failed/unknown storage operation must not be labeled as failed SMTP delivery. Continue refreshing the private record so an ambiguous transport failure can reconcile with server state. Add throwing-PUT and throwing-POST cases; the existing false-PUT test does not exercise this branch.

### F5 — Medium: An initial capability-probe failure hides the error and its retry action

`features/ui/users/src/commonMain/kotlin/ui/UserEditViewModel.kt:251` probes capability while the enabled flag initially remains false. If that request throws, lines 275–278 set LoadFailed but do not expose an owner-visible load-error state independently of capability. `canManageOwnEmailState` at line 154 consequently remains false. Every platform places both load-error feedback and Refresh inside that flag; the JS gate is `features/ui/users/src/jsMain/kotlin/ui/UserEditView.kt:145`.

With SMTP actually enabled, a transient first-probe failure leaves the owner with no email section, explanation, or retry control until navigation/resume creates another opportunity. Preserve the fail-closed form behavior, but allow the owner to see the probe/load error and retry without enabling mutations. Test an initial throwing probe followed by a successful retry. This implements the planned loading/failure/recovery behavior rather than introducing a new capability policy.

### F6 — Medium: Required acceptance tests were omitted at the new risk boundaries

Architecture's test specifications at `agents/task/08.09.2026_07.33.26-becae363-8f30-4035-8e9e-77af5e3a4145/002-architecturing.md:101` are required production-seam tests, not an optional live-service matrix. The only new SQLite approval test is `features/users/common/src/jvmTest/kotlin/repo/ExposedUsersRepoSqliteTest.kt:127`; its helper at line 184 always creates a fresh in-memory schema. It does not exercise an old table lacking the column, durable reopen, inherited bulk updates, or the production cache. No CacheUsersRepo approval suite exists.

`features/email/server/src/commonTest/kotlin/services/EmailFeatureServiceTest.kt:41` still constructs the service without an invite sender, and the entire suite tests only enabled, test-mail, and storage behavior. No test invokes the new production requestMyEmailVerification method, so its result matrix, caller/expected-address binding, retry, and stale delivery cleanup at `features/email/server/src/commonMain/kotlin/services/EmailFeatureService.kt:94` are untested. The existing coordinator ordering and real Koin tests are useful but do not cover the new delivery request. No AdminPanelViewModel test exists for the new lifecycle/versioning logic at `features/ui/adminPanel/src/commonMain/kotlin/ui/AdminPanelViewModel.kt:72`. The new owner ViewModel suite at `features/ui/users/src/commonTest/kotlin/ui/UserEditViewModelEmailTest.kt:94` has only three fixed-owner save/false-save/resend tests; it does not cover the failed or stale operations above, owner denial, or refresh lifecycle. New protected routes also lack the specified caller/status tests.

Implement focused deterministic tests for the migration/cache, owner request service and route authorization, profile identity/error handling, and dashboard load/resume/retry/navigation seams. Existing test doubles and a working repository browser build make these possible without a live SMTP account. Coding 003 at line 23 describes the broader test matrix as optional, contradicting Architecture's explicit specifications. Successful compilation and adjacent tests do not replace evidence for persistence, authorization, or the primary new dashboard behavior. This finding touches auth and data integrity.

## Confirmed behavior and bounded acceptance

The persisted field is non-null/default-false, inserts cannot accept approval from NewUser, unchanged non-null email retains approval, and replacement/clear resets it. The Exposed builder is used by both inherited update forms. Conditional approval matches id and exact stored email in SQL and emits after the transaction. Cache approval delegates outside its lock, then mirrors the successful record. This establishes the intended implementation shape; F6 identifies the missing migration/cache execution proof rather than asserting that the migration is broken.

The public UsersFeatureUser still contains only id and username, while private auth/admin mappers preserve email and approval. The request DTO cannot nominate a caller id, its route obtains bearer identity, and the admin username-only route calls the existing root guard. Full admin replacements and both editor rename paths use the shared coordinator, preserving current email values under the declared process-local boundary. SMTP is outside that lock. Current-address checks and request-local stale-link cleanup are present. Exact-address historical link reuse remains an explicitly accepted compatibility choice, and no distributed or generation-bound guarantee is inferred.

All three dashboard views display actual username/id rows, independent loading/error/empty states, and the existing user-detail navigation. Users and Wishlists actions remain available. JVM/Android scrolling keeps the SMTP section reachable. The heading, recipient label, and purpose explanation clarify the former unexplained field, and the shared busy/capability gates are present. English and Russian copy exists. The normal missing-email, pending, and approved owner controls are implemented on JS/JVM/Android and remain private to the owner under stable identity; the findings concern failure and identity-transition cases.

Planning and Architecture remained aligned with both prompt requirements and Operator Notes, apart from the recovery ambiguity identified in F1. Verification correctly blocked on the full-build failure, Coding 006 cleaned only generated outputs, and Verification 007 independently established a successful full build. The product is not ready for completion because the implementation and acceptance-test omissions above remain.

## Handoff

Restart at Planning because F1 is High, then revise Architecture, implement the corrections and focused regression tests, rerun Verification, and return to Validating with monotonically increasing step numbers. The five Medium findings must travel with the restart; even without F1, their count and authorization/data-integrity scope require a Coding loop. No Critical escalation, operator choice, external message, push, or deployment is required by this validation. Only this report is committed.
