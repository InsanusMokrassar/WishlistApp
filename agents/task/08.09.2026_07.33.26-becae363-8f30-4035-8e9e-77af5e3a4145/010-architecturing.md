Model: gpt-6-astra (HL)
Changed files: agents/task/08.09.2026_07.33.26-becae363-8f30-4035-8e9e-77af5e3a4145/010-architecturing.md

The Orchestrator assigned gpt-6-astra at Architecture's preferred HL tier. The remaining work requires precise reasoning about recoverable authorization transitions, ownership across suspension, and execution evidence across several boundaries. The report is written directly because ORCHESTRATOR prohibits nested agents. Caveman full applies only to internal notes; the report and narrative test specifications use normal prose. No machine-parsed handoff blocks are present.

## Scope and evidence

This architecture implements only F1–F6 from Validation 008, as resolved by Planning 009. It supersedes the insufficient failure and testing details in Architecture 002, while preserving the implemented routes, DTOs, additive approval column, exact-address link compatibility, SMTP-independent storage, and root-only administration. No new feature, production dependency, role, schema, server endpoint, global operation framework, or deployment is needed. Small constructor scheduling seams and test dependencies are justified below. Persistence, service, and dashboard changes beyond those specified are permitted only when a required regression test demonstrates a defect within F1–F6.

I reread SHORTCUTS, ALL/local.ALL, ARCHITECTURE and checked its absent local override, MODELS, GIT, PROTOCOL, TOOLS, AST_INDEX, ORCHESTRATOR, PROMPT, Validation 008, and Planning 009. I read the complete current READMEs for users, roles, email, auth, deeplinks, admin, ui/users, and ui/adminPanel. Their Operator Notes remain compatible with this correction. Source navigation used ast-index with approved external-cache access. I built only the external navigation repository's missing AST cache, then confirmed its source against the installed navigation.mvvm-jvm 0.7.7 sources artifact; no project source or project index was changed.

Important harness corrections are concrete. Navigation's ViewModel owns an immutable scope using Dispatchers.Default and a SupervisorJob, so runTest or Dispatchers.setMain alone cannot control the existing UI work. NavigationNode exposes suspend changeState; resume tests must move from STARTED to RESUMED, rather than call onResume directly. The HTTP caller utility returns principal<UserId>(); there is no UserIdPrincipal type in the current code. Both server modules lack ktor-server-test-host, while auth/client already demonstrates the available libs.ktor.client.mock alias. The existing JS browser and Node configurations both use Mocha; no actual DOM fixture exists, and no Chrome/Chromium binary was found on PATH. Consequently, the plan below supplies a DOM test fixture instead of claiming that the earlier successful browser compilation proves rendered behavior.

## Research applied

Kotlin's coroutine testing documentation explains that StandardTestDispatcher queues launched work until the shared scheduler runs it. Use a passed test dispatcher and explicit deferred barriers; the existing polling loop in UserEditViewModelEmailTest cannot deterministically place an identity change before a queued mutation. Cancellation checks remain necessary after a dependency returns, in addition to generation validation. These practices are adapted inside the existing ViewModels without changing navigation itself. [Kotlin StandardTestDispatcher](https://kotlinlang.org/api/kotlinx.coroutines/kotlinx-coroutines-test/kotlinx.coroutines.test/-standard-test-dispatcher.html), [Kotlin coroutine launch and cancellation](https://kotlinlang.org/api/kotlinx.coroutines/kotlinx-coroutines-core/kotlinx.coroutines/launch.html)

Ktor recommends testApplication for real routing/plugin behavior and MockEngine for client transport assertions. Add only their test dependencies at the existing catalog Ktor version 3.5.2. Route tests must install real Authentication and ContentNegotiation, not call extracted handlers directly. [Ktor server testing](https://ktor.io/docs/server-testing.html), [Ktor client testing](https://ktor.io/docs/client-testing.html)

jsdom supplies DOM APIs and animation-frame support for Node tests, while explicitly not implementing visual layout or browser navigation. Use it to render the actual Compose HTML controls and dispatch input/click events. Verify navigation through the application's NavigationChain rather than window.location. This proves the requested labels, controls, and interactions; it does not claim pixel/layout or live-mail delivery proof. [jsdom documentation](https://github.com/jsdom/jsdom/blob/main/README.md)

## F1: Exact recoverable role transition

Modify only promoteNewUserToUser in `features/roles/server/src/commonMain/kotlin/RolesBootstrap.kt` and its KDoc. Retain the Boolean return and roleTransitionMutex. Under that lock, read direct roles for the existing roleSubject(userId). Evaluate NewUser first, including when User also exists. If NewUser exists and User is absent, call includeDirect(User). Regardless of includeDirect's Boolean return, reread direct roles and require User membership before attempting any NewUser removal: the Boolean describes whether a change occurred, not the resulting authorization state. If User cannot be confirmed, return false and leave NewUser untouched. Then excludeDirect(NewUser), reread, and return true only when User exists and NewUser is absent. Preserve SuperAdmin and unrelated roles.

If the initial snapshot lacks NewUser, return true without writes for direct User or direct SuperAdmin; otherwise return false without writes. Inherited-only roles and emailApproved never authorize a grant. Existing SuperAdmin-only behavior confirms the address but does not invent direct User membership or change Auth's direct-User policy.

Repository exceptions and cancellation propagate. A grant exception before mutation leaves NewUser; an exception after grant leaves NewUser plus User. A removal exception before mutation leaves both; an exception after removal leaves User. Every surviving state is retryable using the same ordinary helper: pending takes precedence and finishes cleanup; User-only succeeds without writes. Do not compensate by deleting User, create a recovery table, or grant access to a fully revoked subject. A failed role-state confirmation returns false without confirmation email or success redirect.

The coordinator already persists emailApproved before invoking this helper and still invokes the helper when approval is already true. Keep that behavior and coordinator-then-role lock order. A committed grant can make login possible before a failed NewUser cleanup is retried, because Auth checks direct User. That is the accepted partial-success behavior from Planning 009: the verified pending account earned User, even if the callback failed later. Do not claim cross-repository atomicity. The first failed callback produces no success redirect/confirmation; reopening the exact link converges. If the operator revoked qualifying roles between attempts, the retry remains denied.

Implement the F1 cases in existing `features/roles/server/src/commonTest/kotlin/RolesBootstrapTest.kt`, using a RolesRepo delegate around the existing FakeRolesRepo with an operation log and one-shot faults before or after includeDirect/excludeDirect. Explicit test names should include `grantFailureBeforeMutationRetainsPendingAndRetries`, `grantFailureAfterMutationRetriesPendingCleanup`, `removalFailureBeforeMutationRetriesCleanup`, `removalFailureAfterMutationRetriesIdempotently`, and `unconfirmedGrantDoesNotRemovePending`. Seed NewUser, NewUser+User, NewUser+SuperAdmin, User, SuperAdmin, inherited-only, and revoked subjects in a compact parameterized/helper-driven matrix. Assert intermediate roles, call order, preserved unrelated roles, return values, and no grants after revocation. Include a cancellation variant before grant completion. Keep existing delayed-bootstrap and pending-registration tests.

Extend `features/email/server/src/commonTest/kotlin/services/EmailDeepLinkIntegrationTest.kt` with a real DeepLinksService and EmailVerificationDeepLinkHandler plus the faulting role delegate. The same minted link must first throw after approval was persisted, emit no confirmation, and later return the fixed redirect with true approval and only User remaining. Add the false-confirmation branch at the coordinator/handler seam and retain stale-recipient rejection. These tests cover the changed helper and the existing integration contract; no new production integration service is needed.

## F2: Owner operation identity, generation, and cancellation

The responsible production layer is `features/ui/users/src/commonMain/kotlin/ui/UserEditViewModel.kt`. Add a final defaulted constructor parameter `dispatcher: CoroutineDispatcher = Dispatchers.Main.immediate` and create a private work scope from the inherited scope's coroutineContext plus that dispatcher. Retain the inherited Job so node destruction and scope.cancel still cancel work. Use the work scope for this ViewModel's launches, collectors, and stateIn operations. Public UI callbacks remain synchronous on the UI dispatcher; document that convention. Tests pass StandardTestDispatcher(testScheduler). This also confines counters/state mutations to the UI dispatcher instead of relying on unsynchronized Long fields across Dispatchers.Default workers. Existing Koin factories continue using the default; no new DI registration is required.

Maintain an owner generation, a monotonically increasing mutation id, the active mutation token, its Job, and the existing separate refresh version/job. A token records the captured caller id, owner generation, and mutation id. Register the identity collector without awaiting any network operation inside its collection body. Observe the pair of currentUserIdFlow and userAuthorisedState. On each observed identity/session change, increment the owner generation and refresh version, detach the active token, cancel active mutation and refresh jobs, clear private profile/input/errors/results, reset capability and busy/loading, then start an owner refresh only if the new pair is authorized and matches the edited user. In particular, do not suspend the identity collector inside refreshOwnedEmailProfile as the current implementation does; that delays observation of logout while a probe is blocked.

Admission for save/resend checks the raw model flow values, known Enabled capability, a successfully loaded private profile whose id equals the editor id, no refresh, and no active mutation. Save additionally requires a missing stored email and valid input; resend requires a non-null pending email. Capture the owner and token, set busy, and clear only operation feedback before launching. Use a lazy job assigned to the active job field before starting it, so immediate dispatch cannot complete before ownership is recorded. Immediately inside the coroutine, recheck owner, authorization, generation, token identity, and currentCoroutineContext().ensureActive before the first HTTP call.

Repeat those checks before every subsequent owner-scoped request and immediately after every suspension before publishing state or deciding to issue another request. Save performs PUT, optionally verification POST, then private reconciliation; resend performs POST then reconciliation. Invalidating during PUT forbids that old operation's POST and reconciliation. Invalidating during POST forbids its result and reconciliation. A refresh result must also pass refresh-version and owner-generation checks and contain the expected profile id. A failed check returns without side effects; cancellation is rethrown.

Only the active token may publish errors/results, clear its draft, or clear busy. Finally performs guarded local cleanup only; do not place a network refresh in finally. Run necessary reconciliation inside the guarded normal/failure path before releasing the active token. Keep busy through reconciliation. An obsolete finally must never clear a newer operation's busy flag. Cancelling a client job cannot undo a request already accepted server-side, and no caller id is added to the wire DTO.

Ordinary refresh increments only refresh version, not owner generation. The Refresh button is disabled while mutating. A resume during a mutation records a pending refresh and lets the still-current operation perform at most one refresh after its reconciliation; it must not reset capability or replace ownership in mid-sequence. Starting a new refresh cancels the older refresh and guards late non-cooperative responses. Observed owner leave-and-return creates a new generation; unobserved state changes are not claimed to be detectable by a conflated StateFlow.

Expand `features/ui/users/src/commonTest/kotlin/ui/UserEditViewModelEmailTest.kt`. Move its reusable fake model/node/recording interactor into `src/commonTest/kotlin/ui/UserEditTestFixtures.kt`, with mutable identity/authorization/root flows, suspend hooks for probe, getMyProfile, PUT, POST, username, and password, and request logs. Remove waitUntil/delay polling. Use runCurrent and CompletableDeferred entry/release barriers. Test `queuedSaveRejectsChangedOwner`, `identityLossDuringPutPreventsPostAndRefresh`, `logoutDuringPostSuppressesOldResult`, `ownerLeaveAndReturnDoesNotReviveOperation`, `lateCancelledRefreshCannotPublish`, and `oldFinallyCannotClearNewBusy`. Include save and resend, changed id and authorized=false, anonymous/non-owner/root-editing-other denial, initial null-to-owner resolution, duplicate clicks, and stable-owner success.

To place a change exactly between PUT and POST without adding a production suspension, have the fake PUT update the model's raw identity flow just before returning; the caller-side raw check must reject the next request even before the collector is scheduled. For late completion, use a test-only non-cooperative continuation or a bounded NonCancellable barrier and then release it; the generation and ensureActive checks must still prevent publication. Do not leave unbounded non-cancellable test jobs. Cancel the inherited ViewModel scope and release every barrier in finally.

## F3: Boolean profile-save sequencing

Correct UserEditViewModel.onSave without changing endpoints. Capture username and optional password for the admitted save and mark loading synchronously to prevent duplicate admission. Clear a separate profileSaveErrorState. Await updateUsername; false or an ordinary exception records failure, retains inputs/dirty state, leaves the editor open, and skips setPassword. Only a successful username result allows a nonblank password request. False/ordinary exception from that request leaves the editor open. Invoke onSaved only after all requested operations returned true. Cancellation propagates; loading clears in finally. A successful username with failed password is a partial commit, not a rollback.

Use a small local profile-save error enum distinguishing `UsernameSaveFailed` and `PasswordSaveFailed`, separate from email errors. Render feedback near the root Save controls in all three `features/ui/users/src/{jsMain,jvmMain,androidMain}/kotlin/ui/UserEditView.kt` files. Add English/Russian UsersListStrings copy: the first says the profile save could not be confirmed and no password request was attempted; the second says the username was saved but the password change could not be confirmed. Clear stale feedback on the next save attempt. Do not broaden the avatar/delete flows.

Add `features/ui/users/src/commonTest/kotlin/ui/UserEditViewModelSaveTest.kt` using the shared fixture: username false/throw means zero password calls and zero navigation; password false/throw after username true means two ordered requests and zero navigation; username-only true and username+password true navigate once; cancellation clears loading without failure feedback; inputs remain on failure. Add `features/ui/adminPanel/src/commonTest/kotlin/ui/AdminUserEditViewModelTest.kt` to retain the already-correct existing-user false/success behavior and create-null/create-success distinction. If deterministic scheduling is needed there, use the same defaulted local dispatcher parameter and lifecycle-preserving work scope, without changing save semantics.

## F4: Separate save uncertainty, delivery failure, and reconciliation

In onSaveEmailAndRequestVerification, isolate PUT and POST catches. Until PUT returns true, false/ordinary exception sets EmailEditorError.SaveFailed and leaves verification-result state null. Never call POST after an unconfirmed PUT. Once PUT returns true, clear the saved draft and allow POST; ordinary POST exceptions set DeliveryFailed, while successful domain results remain verbatim. Resend has only the delivery boundary. Cancellation is never translated into an ordinary failure.

For the same active owner token, reconcile with getMyProfile after either phase succeeds or fails. Reconciliation reads the private record directly; it does not need another capability probe whose failure could prevent finding an ambiguously committed address. If the returned owner has a stored address, use the server record and clear the unsaved missing-email draft. If no address was committed, keep the draft. Do not automatically send merely because reconciliation found a pending address after an unconfirmed PUT; the next deliberate action is resend. If the private refresh also fails, preserve the primary SaveFailed or DeliveryFailed and set a separate load-failure indicator. The owner may see both the primary operation outcome and a Refresh recovery action. No secondary catch may replace a storage error with delivery error or erase the primary error.

Update emailSaveFailed wording to “The email save could not be confirmed. Refresh to check the saved address.” with a Russian equivalent. This accurately covers a transport exception after server commit. Render operation feedback independently of the missing/pending/approved branch, so SaveFailed remains visible even when reconciliation discovers a pending record. Only the server's private profile determines approval; a Sent result or query marker never sets emailApproved locally.

Add F4 cases to UserEditViewModelEmailTest: `putThrowBeforeCommitKeepsDraftAndNeverPosts`, `putThrowAfterCommitReconcilesPendingWithoutPosting`, `falsePutReportsSaveFailure`, `postThrowAfterConfirmedSaveReportsDeliveryFailure`, and `secondaryRefreshFailurePreservesPrimaryError`. Parameterize secondary failure over both primary error classes. After ambiguous committed PUT, assert the next resend issues no PUT. Retain normal Sent/AlreadyApproved/explicit DeliveryFailed behavior. Test identity invalidation at reconciliation using the F2 fixture.

## F5: Recoverable capability state without enabling writes

Replace the Boolean-only capability source inside UserEditViewModel with a local enum `Unknown`, `Loading`, `Enabled`, `Disabled`, `Failed`. Derive the old emailFeatureEnabledState if needed by callers, rather than keeping conflicting mutable copies. Owner section visibility requires the raw authorized owner and capability other than confirmed Disabled. Mutation permission requires Enabled plus a successfully loaded matching private profile, no load failure, no load, and no active mutation. The outer section therefore displays Loading or failure+Refresh while the inner input/save/resend controls stay disabled or absent. Root viewing another profile sees neither private errors nor controls.

At normal refresh start set Loading and invalidate mutation permission immediately; do not preserve stale Enabled. A successful false probe sets Disabled and hides the section. A successful true probe proceeds to private loading. A throwing probe sets Failed, clears private readiness, and exposes Refresh. An enabled probe followed by a missing/mismatched/throwing private response also clears readiness and exposes the independent load failure. Keep an unsaved draft within the same owner generation through probe/profile retry. Successful retry restores the form without leaving the screen. Logout clears every private state and cancels the refresh under F2.

Reconcile the transport seam as part of F5: `features/email/client/src/commonMain/kotlin/KtorEmailFeature.kt:isFeatureEnabled` currently collapses non-success HTTP responses into false. Return false only from a successful decoded false body; make non-success responses throw using request-local expectSuccess=true, and propagate network/decode/cancellation failures. Otherwise a 500 can never appear as Failed in the ViewModel. Existing dashboard and owner consumers already catch probe exceptions; test them. For the new requestMyEmailVerification transport, likewise validate status before decoding so a non-success body cannot be accepted as Sent. Keep setMyEmail/updateUsername Boolean status contracts intact.

Change all three UserEditView outer gates to the new owner section visibility; render load/primary operation errors independently, and keep Refresh outside the Enabled-only form gate. Do not add a new screen. Extend UserEditViewModelEmailTest with `initialProbeFailureExposesRetryAndRecovers`, `enabledRefreshFailureDisablesMutation`, `disabledProbeHidesSection`, `privateReadFailureExposesRetry`, `retryPreservesDraft`, and `logoutDuringProbeClearsPrivateState`. Assert no PUT/POST while Unknown/Loading/Failed or private readiness is absent. Add rendered-control proof below.

## F6: Required production-boundary test files and fixtures

The following suites are mandatory evidence, not an optional matrix. Combine related cases through helpers; assertions must execute the named production seam. Existing serialization, Koin graph, registration-compensation, and approval-marker tests remain required and should not be duplicated unnecessarily.

For persistence, extend `features/users/common/src/jvmTest/kotlin/repo/ExposedUsersRepoSqliteTest.kt` and add `src/jvmTest/kotlin/repo/CacheUsersRepoSqliteTest.kt`. A shared `src/jvmTest/kotlin/repo/SqliteUsersTestFixture.kt` may hold the current keeper-connection helper and a temporary file fixture. Create the legacy table and rows with JDBC before constructing ExposedUsersRepo; omit email_approved entirely. Assert preserved ids/usernames/null and non-null emails and false defaults, then approve one row. Close keeper/transactions with TransactionManager.closeAndUnregister, reopen a new Database and repository against the same temporary file, and assert true persists. Delete only that test-owned temporary file after final close. The existing Xerial dependency is sufficient.

Test `legacySchemaAddsFalseApprovalWithoutChangingUsers`, `approvalSurvivesDatabaseReopen`, `bulkUpdatesPreserveOrResetCurrentAddressApproval`, `failedBulkUpdateLeavesRowsUnchanged`, and `conditionalApprovalEmitsOnlySuccessfulUpdates`. For bulk call the inherited update(listOf(id to NewUser(...))) explicitly; cover same address, changed address, clear, and collision. The inherited bulk path currently exposes ExposedSQLException rather than the single-update DuplicateUserFieldException wrapper: assert the existing exception contract and preserved data, not an invented translation requirement. Start update-flow collectors with UNDISTPATCHED and drain scheduled work after each call; reject-condition tests assert zero events after the completed operation, without sleeping.

Construct the production CacheUsersRepo over the real ExposedUsersRepo with a test background scope. Complete its initial fill before writes and retain active subscriptions. Assert approveEmail immediately updates getById/getAll before relying on eventual event work; mismatch and duplicate failure retain the record; later full replacement resets false; operations complete within a bounded timeout. Compare the backing SQL row and cache. No fake implementation of approveEmail substitutes for these checks, and do not hold a cache lock around an event-emitting backing call.

For delivery, extend `features/email/server/src/commonTest/kotlin/services/EmailFeatureServiceTest.kt` with a fixture using the real EmailRegistrationInviteSender, real DeepLinksService, MapKeyValueRepo-backed DeepLinksRepo, existing FakeUsersRepo/FakeRolesRepo, and controllable EmailsService. Add a small internal `EmailVerificationRequestTestFixture.kt` in the same test package if needed. Exercise Sent, AlreadyApproved, Unavailable, NoEmail, EmailChanged, and DeliveryFailed through production requestMyEmailVerification, not a replacement implementation. Check exact caller id/email payload, approved/no-email/mismatch no-send, missing sender, configured sender with missing SMTP/link infrastructure, SMTP false/throw and same-address retry, and cancellation during sender delivery with cleanup. Current semantics distinguish missing inviteSender as Unavailable; an installed sender unable to deliver returns DeliveryFailed. Preserve that contract.

Pause fake SMTP after link creation. Change, clear, delete, or approve the stored caller, then release delivery. Assert the exact result, removal of only the request-owned obsolete link, and retention of an unrelated preseeded link and newer user data. Use the coordinator for address changes. The service's cleanup path already uses NonCancellable; verify that guarantee without redesigning all post-SMTP crash handling. Keep the current enabled/disabled singleton PluginTest, EmailRegistrationInviteSenderTest, handler tests, and RegistrationCompensationIntegrationTest. F1's recovery test supplies the missing role failure integration.

For routes, add `features/email/server/src/commonTest/kotlin/configurators/EmailRoutingsConfiguratorTest.kt` and `features/admin/server/src/commonTest/kotlin/configurators/AdminRoutingsConfiguratorTest.kt`. Both server commonTest source sets are JVM-only. Add `ktor-server-test-host = { module = "io.ktor:ktor-server-test-host", version.ref = "ktor" }` to the catalog and implementation libs.ktor.server.test.host to these two commonTest dependency blocks. Install server Authentication with an unnamed bearer provider mapping controlled token strings to UserId, and ContentNegotiation JSON matching production ignoreUnknownKeys behavior. Mount the real configurator under route("/api") through its Route.invoke extension. Do not create fake HTTP routing or bypass authenticate/requireAdmin.

Email route tests must assert missing/invalid bearer 401 with no service calls; malformed expectedEmail/missing field/broken JSON 400; every normal enum is HTTP 200; throwing infrastructure never yields a Sent body; two tokens map to the corresponding caller ids. Include a real-service two-user request where A supplies B's address and gets EmailChanged without an invite. An extra callerId field is either ignored by the production JSON configuration or rejected, never used as authority. Preserve PUT duplicate 409 and disabled-service PUT success. Use a recording EmailFeature for the route result/status matrix and the real service for the ownership case.

Admin route tests reuse the internal FakeUsersRepo/FakePasswordsRepo/FakeWishlistRepo/FakeWishlistItemRepo in UsersManagementFeatureTest, a real UsersManagementFeature with coordinator, AuthFeatureService, AdminFeature, WishlistService(fakeWishlistRepo), and a RolesFeature allowing only the root token id. A delegating UsersRepo may throw DuplicateUserFieldException from update to drive the 409 route branch without requiring SQLite in admin tests. Assert anonymous/invalid 401, non-root 403, root rename 200 preserving email/approval, malformed id or JSON body 400, absent target 404, and duplicate 409 with unchanged record. A missing path segment simply misses the route and is 404; do not invent a 400 contract for it. The Username model's existing validation is unchanged; malformed JSON is a reliable body-error fixture.

Add `features/email/client/src/commonTest/kotlin/KtorEmailFeatureTest.kt` and `features/admin/client/src/commonTest/kotlin/KtorUsersManagementFeatureTest.kt`; add implementation libs.ktor.client.mock to each module's commonTest. Reuse auth/client's ContentNegotiation+MockEngine setup with the production request content type. Assert POST /email/requestMyEmailVerification body contains only expectedEmail, all enum responses decode, and non-success responses throw even with a misleading JSON Sent body. Assert GET /email/enabled successful true/false and HTTP/network/decode failure behavior. Assert username PUT /admin/users/setUsername/{id} contains the serialized Username and returns true only for success, false for 401/403/404/409/500 under the normal Boolean client contract. Do not change the global HttpClient expectSuccess setting.

For profile coverage, F2–F5's named tests and shared fixture replace the current three-case fixed-owner suite. Add `features/ui/users/src/commonTest/kotlin/UsersModelTest.kt` only for the existing Plugin facade delegation: bind fake feature interfaces, meQualifier StateFlow, credentials storage, files service, and a test scope, resolve the actual UsersModel, and assert getMyProfile, email methods, and updateUsername call their owning features with unchanged arguments. Do not reflect into private me state. The analogous `features/ui/adminPanel/src/commonTest/kotlin/AdminPanelModelTest.kt` checks getAllUsers and updateUsername delegation in the existing Plugin, with unused fake methods throwing to catch wrong-layer calls. Close Koin and cancel test scopes.

Add `features/ui/adminPanel/src/commonTest/kotlin/ui/AdminPanelViewModelTest.kt` with a test model containing controllable users/probe/send hooks and a recording AdminPanelViewInteractor. Give AdminPanelViewModel the same defaulted dispatcher/work-scope seam described for the profile, preserving its lifecycle Job. Run initial rows, empty, failure+retry, STARTED-to-RESUMED refresh, changed list after resume, overlapping older/newer completion, and logout while an authorized refresh is suspended. In the latter case assert immediate clearing before releasing the response, plus no late publication. The current authorization collector awaits refreshUsers and can delay logout observation; if reproduced, make that collector launch its refresh job rather than await network work, retaining existing request-version checks. Initial/resume/auth events may coalesce; do not freeze an accidental duplicate initial request count as an API contract.

Test dashboard row selection and Users/Wishlists callbacks with exact ids; capability failure must not prevent users loading. Test SMTP disabled/no call, busy duplicate suppression, true/false/throw feedback, and cancellation clearing the send flag. Use active barrier jobs and scheduler progress, not sleeps. This proves F6's dashboard lifecycle boundary without adding search, pagination, or changing the rendered design.

For real navigation, add `client/src/commonTest/kotlin/AdminPanelNavigationTest.kt`. Register ClientPlugin.setupDI in a local Koin application without calling startPlugin, resolve its actual AdminPanelViewInteractor, and provide a NavigationChain whose nodeFactory creates NavigationNode.Empty for supplied configs. Invoke the selected-user action on a dashboard node and assert the last stack config is AdminUserViewConfig for that id; check existing section destinations. The relevant ClientPlugin definitions are lazy and the selected interactor requires no server or platform startup. No production interactor extraction is necessary.

For rendered JS behavior, add `features/ui/users/src/jsTest/kotlin/ui/UserEditViewDomTest.kt` and `features/ui/adminPanel/src/jsTest/kotlin/ui/AdminPanelViewDomTest.kt`. Add implementation npm("jsdom", "26.1.0") to only those modules' jsTest dependencies. Use a small test-local DomTestEnvironment fixture to initialize a JSDOM document at a fixed localhost origin with pretendToBeVisual=true when document is absent. Provide window/document/DOM constructors/requestAnimationFrame globals before constructing views or mounting composition; restore them and close the JSDOM window after disposal. When already running in a real browser, use its document instead. The pinned DOM-only dependency supports the Node runtime and avoids installing a system browser or changing the shared JS template. No runtime application dependency is introduced.

Register the real ViewModel in test Koin with a fake Model and interactor, instantiate the actual view and render its public onDraw through Compose Web renderComposable into a test-owned element. Assert labels and enabled/disabled controls from DOM, dispatch input/click events, and flush coroutines plus composition frames using event/frame barriers. Use mutation observation or a bounded condition wait tied to DOM updates instead of arbitrary timing delays. Profile tests prove the initial probe-failure error and Refresh exist outside the enabled-only form; click Refresh, resolve a successful probe/profile, enter and save a missing email, and assert ordered PUT/POST. Include non-owner privacy and profile-save error feedback. Dashboard tests assert actual username/id text, row click forwarding, and the SMTP section heading, recipient label, and existing purpose explanation appear together only when enabled. Check EN/RU new resources through explicit language selection. No snapshots of CSS or invented rendering substitutes are needed.

The current harness cannot perform a real-browser pixel/layout check without installing a browser, and jsdom cannot perform that check. None of F1–F6 requires a pixel assertion: DOM behavior, navigation-chain state, shared model tests, and platform compilation cover the specified changes. The required rendered-control tests remain mandatory using the supplied DOM fixture. No acceptance test is waived. If the fixture exposes an actual unsupported Compose DOM API, Coding must record the exact failure and use a scoped real-browser runner instead of silently skipping the suite or weakening it to source-text checks. Live SMTP remains unnecessary because the production sender is exercised with the existing external-service port.

## README updates

Coding should update roles/README and email/README Architecture Notes with grant-before-removal, the observed direct-state checks, retryable intermediate states, false/throw behavior, and the accepted possibility of login after a committed grant but failed pending cleanup. Clarify that no role is inferred from emailApproved and that revoked users stay denied.

Update ui/users README with observed owner-generation invalidation, cancellation plus stale-result checks, serialized UI work, save-versus-delivery uncertainty, independent reconciliation error, and unknown/failed capability recovery. Document Boolean profile-save sequencing and partial username/password success. Update ui/adminPanel README only if its suspended authorization collector is corrected by the required test, documenting prompt logout clearing. Preserve every Operator Notes section verbatim. Do not rewrite unrelated README sections.

## Execution and exit condition

Coding first adds focused F1–F5 reproductions, implements the bounded production corrections, then closes F6 with the named production-boundary suites and test dependencies. Rebuild the project ast-index after source changes. Run affected JVM tests for users.common, roles.server, email.server/client, admin.server/client, ui.users, ui.adminPanel, and the client navigation test; retain auth/admin/users serialization, Koin, registration compensation, and approval-marker tests. Run the two actual DOM suites using the configured Mocha JS tasks with the DOM fixture, and compile JS/JVM/Android views. Do not label Node+jsdom execution a real-browser layout test.

Then run the required serialized full gate `./gradlew build --no-daemon --no-parallel --max-workers=1` with no concurrent Gradle invocation. Earlier successful build results are historical evidence only. Coding's report must map F1–F6 to implemented files and executed test names; Verification must report exact passing tasks and any environmental failure. Completion requires all six findings closed, required gates passing, and independent Validation. This report does not execute source tests or claim the corrections are implemented. There is no operator choice or inherently untestable acceptance condition blocking Coding.
