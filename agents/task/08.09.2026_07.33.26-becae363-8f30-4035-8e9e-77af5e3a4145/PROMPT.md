# User request

Treat this work like a GitHub issue and implement both requirements:

1. When server email support is enabled and a user has not set an email address, allow the user to set an email address. Add a handler for approving the user's email address and persist an explicit flag showing that the address has not been approved yet.
2. Show a list of users on the main page of the admin panel. Replace or clarify the current unexplained input field so administrators understand its purpose.

The GitHub issue could not be created at task bootstrap because the configured `gh` authentication token is invalid. Continue as a prompt-driven task.
