Model: gpt-6-astra (HL)
Changed files: agents/task/08.09.2026_07.33.26-becae363-8f30-4035-8e9e-77af5e3a4145/018-validating.md

The assigned model is the preferred HL Validator tier. The remaining authorization-sensitive lifecycle and asynchronous delivery boundaries require independent cross-layer review. I retained caveman full for internal notes and used the verification-only skill to reuse current execution evidence without expanding implementation scope. This report and its commit use normal prose; no machine-parsed structured findings or handoff blocks are used. No nested agents were created.

## Validation result: PASS

There are zero Critical, zero High, zero Medium, and zero Low findings. All six findings from step 008 are closed. Coding 016 resolves the two remaining Medium findings from step 015, and Verification 017 supplies successful execution evidence for the corrected source. No Coding loop, Planning restart, repeat-problem escalation, or operator escalation is required.

## Scope and evidence

I retained the repository instructions, original prompt, Planning and Architecture acceptance, complete relevant feature READMEs, and earlier validation history already read in the preceding passes. I read steps 015–017 and independently inspected the final corrective diff and its relevant production/test seams using ast-index and direct source reads. The task base remains 7ade67e. Product correction 65f29a8 changes only the two ViewModel dispatchers and their confinement documentation, the affected regression tests, the migration fixture, and its Coding report. HEAD 977dfb0 adds only Verification 017. The prior server contracts, DTOs, routes, role transition, actual platform views, localized strings, and build configuration are unchanged by this correction.

Both affected README additions accurately describe Main.immediate confinement and the retained lifecycle Job. Their Operator Notes remain empty and untouched. The relevant previously reviewed READMEs outside these two files are unchanged. The worktree was clean before this report, both new commits have the required coauthor trailer, and `git diff --check 7ade67e..HEAD` passed. This role changes only this immutable report.

Verification 017 records the tracked, non-quiet, serialized full Gradle build with tool-provided exit code zero, `BUILD SUCCESSFUL in 1m 12s`, and 4,530 actionable tasks: 182 executed and 4,348 up-to-date. The source remains identical to the verified source. I did not rerun that full build. I independently read the current XML headers: UserEditViewModelEmailTest has 18 cases, AdminPanelViewModelTest has nine, EmailFeatureServiceTest has 15, and ExposedUsersRepoSqliteTest has 12; all have zero skipped cases, failures, and errors. The owner-email and dashboard suites also report the same 18 and nine passing cases in JS Node, JS Browser, Android debug-unit, and Android release-unit XML. Some suites were legitimately up-to-date in the final build; the XML results do not imply every test was re-executed by that invocation.

## Findings disposition

### F1 — Closed: Role promotion remains retryable and revocation-safe

The previously corrected implementation remains intact at `features/roles/server/src/commonMain/kotlin/RolesBootstrap.kt:138`. Pending accounts receive and confirm direct User membership before NewUser removal; the final role state is checked. Role-less subjects still return false at line 153, and durable email approval is not used as permission to restore access. The role mutex and coordinator-before-role ordering are unchanged. The failure-injection tests at `features/roles/server/src/commonTest/kotlin/RolesBootstrapTest.kt:195`, line 212, line 229, and line 246 still cover retry after both sides of grant/removal failures; line 182 retains revoked-access protection. The real-link retry test at `features/email/server/src/commonTest/kotlin/services/EmailDeepLinkIntegrationTest.kt:115` remains present. The final diff does not modify any of these files.

### F2 — Closed: Production lifecycle transitions have the required UI confinement

`features/ui/users/src/commonMain/kotlin/ui/UserEditViewModel.kt:67` and `features/ui/adminPanel/src/commonMain/kotlin/ui/AdminPanelViewModel.kt:39` now default to Dispatchers.Main.immediate. Their work scopes at lines 76 and 46 respectively retain the original scope context, including its lifecycle Job, and replace only the dispatcher. Actual view callbacks already enter on the UI dispatcher; collectors and continuations now share that execution context. No worker dispatcher remains between the compound lifecycle checks and publication.

The owner invalidation at `UserEditViewModel.kt:310`, request-generation checks at line 332, mutation-token checks at line 342, and publication at line 480 are therefore serialized relative to lifecycle invalidation. Cancellation is checked before mutation continuation at line 348; lazy job registration at line 443 and token-specific finally cleanup at line 455 remain intact. The dashboard's clear/version transition at `AdminPanelViewModel.kt:110` and check/publication at line 141 similarly cannot interleave as independent Default workers. This closes the precise production race reported in 015 without claiming thread safety for arbitrary off-UI callers outside the documented ViewModel contract.

The default-constructor regression at `features/ui/users/src/commonTest/kotlin/ui/UserEditViewModelEmailTest.kt:299` installs a serial Main dispatcher and proves a non-cooperative late response cannot publish after identity invalidation. The dashboard default-constructor logout case at `features/ui/adminPanel/src/commonTest/kotlin/ui/AdminPanelViewModelTest.kt:72` likewise verifies immediate clearing and suppressed late rows. Existing owner tests at lines 114, 139, 180, 219, 260, and 337 retain queued-owner denial, suspended PUT/POST invalidation, leave/return protection, late-refresh rejection, and obsolete-finally protection. These tests plus the production confinement inspection establish the intended execution contract rather than relying only on an explicitly injected test dispatcher.

### F3 — Closed: Profile-save failure handling is preserved

`features/ui/users/src/commonMain/kotlin/ui/UserEditViewModel.kt:586` still checks username failure before attempting a password write; line 598 checks password success; navigation at line 610 occurs only after the requested writes succeed. The five tests in `features/ui/users/src/commonTest/kotlin/ui/UserEditViewModelSaveTest.kt:25`, line 56, line 85, line 117, and line 158 retain false/throw, partial-success, successful-save, and cancellation coverage. The final change only moves this work onto the same lifecycle-preserving UI scope.

### F4 — Closed: Save uncertainty remains distinct from delivery failure

The PUT exception/false branches at `features/ui/users/src/commonMain/kotlin/ui/UserEditViewModel.kt:644` report SaveFailed and return without posting. Only confirmed persistence reaches the later verification boundary at line 664. Reconciliation at line 466 retains the primary error while checking the current owner before publishing. The tests at `features/ui/users/src/commonTest/kotlin/ui/UserEditViewModelEmailTest.kt:398`, line 425, line 459, line 482, and line 508 cover the separate boundaries, committed-but-unconfirmed persistence, and secondary refresh failure. F2's corrected confinement now supplies the previously missing production serialization guarantee.

### F5 — Closed: Owner-only capability retry remains visible

The owner visibility gate at `features/ui/users/src/commonMain/kotlin/ui/UserEditViewModel.kt:200` remains separate from mutation eligibility at line 206. Failed capability/private-profile reads expose retry and feedback while preventing mutation; confirmed-disabled capability hides the section. The cases at `features/ui/users/src/commonTest/kotlin/ui/UserEditViewModelEmailTest.kt:539`, line 575, and line 613 retain recovery, draft preservation, and disabled behavior. The HTTP capability error distinction and actual JS/JVM/Android Refresh/error wiring previously inspected in 015 are unchanged.

### F6 — Closed: The previously missing boundary tests now exist and pass

The fixture at `features/email/server/src/commonTest/kotlin/services/EmailFeatureServiceTest.kt:100` uses the production EmailFeatureService, real EmailRegistrationInviteSender, and real DeepLinksService over a controlled repository and suspended SMTP transport. The four delayed-delivery cases at lines 285, 316, 338, and 360 replace the address, clear the address, delete the user, or approve the address while delivery is pending. Each verifies the resulting EmailChanged, NoEmail, or AlreadyApproved outcome and removal of only the request-owned link, retaining an unrelated link. The replacement case additionally checks the exact original user/address payload and preservation of the newer pending address. These exercise the actual post-delivery recheck at `features/email/server/src/commonMain/kotlin/services/EmailFeatureService.kt:107` and stale cleanup at line 126.

The service test at line 382 proves a failed send removes its link and a same-address retry can subsequently succeed. The cancellation case at line 408 cancels during the real sender's suspended delivery, awaits propagated CancellationException, and checks request-owned cleanup before completion. These are deterministic service-boundary proofs, not claims of live SMTP delivery.

The nine dashboard tests now cover initial rows despite probe failure, default-dispatcher logout with a late response, resume with changed rows and successful empty results, failed-load retry, newest-refresh wins against a non-cooperative older response, disabled SMTP, duplicate suppression and true/false/throw send outcomes, cancellation clearing busy state, and exact callback forwarding. The new cases begin at `features/ui/adminPanel/src/commonTest/kotlin/ui/AdminPanelViewModelTest.kt:110`, line 145, line 177, line 216, line 239, line 287, and line 320. The callback recording complements the retained real navigation-chain test at `client/src/commonTest/kotlin/AdminPanelNavigationTest.kt:25`. The test-only logger override restores the original logger in finally at line 400 and does not change production logging.

Finally, `features/users/common/src/jvmTest/kotlin/repo/ExposedUsersRepoSqliteTest.kt:161` now creates the immediately preceding users schema with an existing email column and a non-null legacy address. The migrated record preserves id 41, username, and address while approval defaults false. Reopen durability at line 190, bulk preservation/reset at line 220, rollback at line 241, conditional events at line 257, and the existing real-cache SQLite test remain intact. The execution evidence now supports the specific coverage previously missing from the reports.

## Original acceptance and remaining limitations

The original false-by-default persisted approval, exact-address conditional approval/reset, authenticated owner endpoints, root-only administration, username-only preservation, private DTO separation, and email-free public projection remain unchanged from the reviewed implementation. The completed race tests strengthen the stale-link and data-preservation evidence without broadening the process-local coordinator into a distributed guarantee. Historical same-address link reuse and non-atomic transitions across separate repositories remain documented compatibility boundaries, not newly introduced behavior.

The actual profile UI still supports missing, pending, and approved owner states with separate save/verification feedback. The dashboard retains real user rows, selected-user navigation, Users/Wishlists actions, and clearly separated SMTP-purpose and recipient labels on all three platforms. No new evidence contradicts the previous bounded acceptance of the Compose Web/jsdom disposal limitation. Shared behavioral tests execute on JVM and JS, the real navigation chain is tested, actual view wiring was inspected, and configured platform builds succeed. JS Browser XML is not evidence of a retained DOM-control test. Manual browser/pixel checks, a working composition-disposal harness, and live SMTP were not performed and are not claimed. These limits remain nonblocking for the original UI acceptance.

## Handoff

Validation passes with all F1–F6 closed and no outstanding severity findings. The Orchestrator may continue the normal completion workflow without another corrective cycle. Only this report is committed by this role; no push, external message, deployment, or product modification is authorized or performed by this validation.
