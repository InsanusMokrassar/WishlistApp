Model: OpenAI Sol (HL)
Changed files: agents/task/08.09.2026_07.33.26-becae363-8f30-4035-8e9e-77af5e3a4145/002-architecturing.md

Sol is the preferred available HL model for Architecture under MODELS.md and SHORTCUTS.md. The role requires reasoning across persistence, authorization, asynchronous delivery, and three UI targets. ORCHESTRATOR.md requires one role agent per stage and prohibits nested agents; consequently, the Architecturing agent writes its own required report instead of delegating documentation to LL. Caveman full applies only to internal notes. This report and its test specifications are normal prose, not machine-parsed handoff blocks.

## Scope and acceptance

Implement the two outcomes in PROMPT.md: an authenticated owner can add a missing email when delivery is enabled, request and retry verification, and see persisted approval state; the admin landing page displays actual users and clearly identifies the test-email input. Existing required-email registration, bearer authorization, profile/avatar administration, and SMTP-disabled email storage remain usable. Approval describes the current email address, independently of account access roles.

Use the existing users repository, email coordinator, registration invite sender, deeplink handler, UsersModel, and AdminPanelModel. No feature or scenario scaffold, new mail framework, global UI state framework, search/pagination subsystem, or deployment is needed. The additional username-only operation below is necessary to preserve email during the existing editors' saves, including concurrent self-service changes. Do not add recovery, login by email, account revocation on email changes, expiry scheduling, rate-limit infrastructure, or a new provider.

## Inspected evidence and corrections to Planning

I read SHORTCUTS, ALL and its available local override, ARCHITECTURE and checked for its local override, MODELS, PROTOCOL, GIT, TOOLS, AST_INDEX, ORCHESTRATOR, PROMPT, and step 001. I read the complete READMEs for email, auth, users, admin, ui/users, ui/adminPanel, deeplinks, and roles. Relevant Operator Notes require root-only administration and reuse of existing capabilities; the plan preserves both. I used lean-build to select existing seams and define a concrete stop condition. Source navigation used ast-index. The sandbox could not see the cache; an approved escalated ast-index read found the existing index at `/home/aleksey/.cache/ast-index/1ee39e0b2f2e367d/index.db`, including the local MicroUtils source root. No rebuild or source change was needed in this role.

`features/users/common/src/commonMain/kotlin/models/User.kt` gives NewUser and RegisteredUser only username/email. `ExposedUsersRepo.kt` uses additive initTable, nullable unique email, and both inherited single and bulk updates. Its library base exposes `_updatedObjectsFlow`; the local MicroUtils FullCRUDCacheRepo exposes protected `kvCache` and `locker`, and its write delegate emits repository changes before acquiring the cache lock. The approval extension must follow that pattern and must not hold the cache lock while calling a repository method that emits events.

`features/email/server/src/commonMain/kotlin/services/EmailVerificationAccountCoordinator.kt` currently serializes self email updates with exact-address comparison and role promotion. Admin updates bypass that coordinator. `EmailRegistrationInviteSender.kt` already accepts any RegisteredUser, binds the exact id/email in a persisted opaque deeplink, validates the public origin, and owns failure/cancellation cleanup. Its compensation handle removes exactly one request's link. Reusing that sender is sufficient for owner verification; no second link handler is necessary.

Planning described promotion as pending-only, but `features/roles/server/src/commonMain/kotlin/RolesBootstrap.kt:134` currently unconditionally removes NewUser and includes User. The updated email path must not grant User to an account whose access was revoked. Add a direct-NewUser guard inside the existing role-transition lock before changing either role. Existing User/SuperAdmin accounts retain their roles; successful address verification is still allowed independently of roles.

Both `features/ui/users/src/commonMain/kotlin/Plugin.kt` and `features/ui/adminPanel/src/commonMain/kotlin/ui/AdminUserEditViewModel.kt` construct NewUser with just a username, clearing email. Fix both through the username-only operation below. Fetching an email on the client and echoing it in a full replacement is insufficient because a concurrent self-service email save can be overwritten.

The shared authenticated-user flow is internally writable only in auth/client. UsersModel may fetch a fresh private AuthFeatureUser through ClientAuthFeature.getMe for the editor without exposing that internal mutable flow or adding a global refresh service. Existing `UserEditViewModel` initial profile loading stops after its first success; add separate email refresh on resume and caller identity changes so email approval does not inherit that one-shot behavior.

Actual build files use dotted Gradle project names from settings.gradle. ui/users needs an explicit email/client dependency; ui/adminPanel already has one. Admin/server does not currently depend on email/server and will need that dependency to reuse the coordinator. Email/server already depends on auth and roles; neither introduces an admin dependency, so the new edge is acyclic. Contrary to old README descriptions, the JS editor/dashboard already use Calm Studio components. Server commonMain is JVM-only through mppJavaProject, while both UI modules use mppJvmJsAndroidWithCompose. The existing commonTest template already supplies kotlin.test and coroutines-test.

## Research adapted to the repository

The OWASP Email Validation and Verification guidance recommends a consistent comparison policy and verifying ownership of the destination address. Retain the existing validated Email type and exact stored-address equality throughout creation, resend, and handling. Do not introduce provider-specific normalization or broaden address acceptance in this task. An explicit false-by-default approval flag records ownership evidence. [OWASP Email Validation and Verification](https://cheatsheetseries.owasp.org/cheatsheets/Email_Validation_and_Verification_Cheat_Sheet.html)

OWASP Authentication guidance treats sensitive email changes as an authenticated workflow and discusses reauthentication and confirmation. The new UI adds an absent address and can retry an existing pending address; existing address replacement remains the established storage/admin API behavior. The email field is not currently a login or recovery identifier, so introducing MFA, dual-address confirmation, or changing account-access policy would exceed the request. Preserve bearer-derived ownership and keep proof of email possession separate from roles. [OWASP Authentication](https://cheatsheetseries.owasp.org/cheatsheets/Authentication_Cheat_Sheet.html)

OWASP recommends random, account-bound, expiring, single-use tokens for reset flows. Reuse the existing server-minted opaque deeplink and exact user/email binding, fixed redirect, and failed-delivery cleanup. Expiry and global single-use semantics are deferred because this is not a password-reset flow and existing verification deliberately supports repeated opens and confirmation retries. A link cannot approve a different current address; with the existing payload format, restoring the same address later makes its earlier link valid again. No stronger generation-bound guarantee is claimed. [OWASP Forgot Password](https://cheatsheetseries.owasp.org/cheatsheets/Forgot_Password_Cheat_Sheet.html)

## Persistence and compatibility

Add `emailApproved: Boolean = false` to RegisteredUser in users/common models/User.kt. Keep approval out of NewUser and do not make a caller-writable property on the shared User interface. Add the same defaulted field to AuthFeatureUser and AdminUser, updating both directions of their mappers. Public UsersFeatureUser must still serialize exactly id and username. Its reverse mapper must require explicit approval as well as explicit email, because neither can be recovered from the public projection; update its current test callers.

In users/common JVM ExposedUsersRepo, add a non-null Boolean column named `email_approved` with SQL default false. Let existing initTable/createMissingTablesAndColumns add it to old databases. All historic rows start false, including accounts with User/SuperAdmin roles; do not infer mailbox ownership from a role or downgrade any role. A reopened database must retain newly approved values. Source defaults and serialization defaults allow old records and older response shapes to decode in the new client. Existing clients use the application's tolerant JSON configuration; retain the current wire shapes and add only the defaulted output field.

Creation always stores false. A full replacement update preserves approval only when the existing non-null stored email exactly equals the new non-null email. Changed or cleared email stores false, including null-to-null cleanup of any inconsistent row. Implement that invariant in the Exposed update-builder path using a database expression based on the old email/approval columns, so inherited single and bulk updates cannot bypass it. Inserts must explicitly store false and must not use a self-referencing update expression. On reads, malformed legacy email values remain null and must project approval as false.

Add `approveEmail(id, expectedEmail): RegisteredUser?` to WriteUsersRepo and implement it in ExposedUsersRepo and CacheUsersRepo. This is a server repository capability, never an HTTP request model. Exposed performs a conditional update on both id and exact email within a transaction, sets only approval, reads the resulting row in the same transaction, and emits the updated record after commit. Missing/mismatched users return null without an event. Repeated exact approval returns the approved record and remains safe. Do not implement approval as an externally supplied NewUser value or a read followed by an unconditional full replacement.

CacheUsersRepo delegates approval to its original repository and synchronously mirrors a successful returned record under its inherited cache locker, following the existing write-delegate pattern. Repository call and event emission occur outside that cache lock. Failed writes leave the cache intact; immediate own-record reads must see the approval without waiting for a background subscription. Update repository test doubles faithfully, including copy-preservation and reset rules; locate all implementations with ast-index before changing the interface.

The migration is additive and needs no data rewrite or VersionsRepo role migration. A rollback binary can ignore the added column, but an old writer does not maintain approval-reset invariants. Do not run mixed old/new writers against the database. If an operational rollback later writes emails, conservatively reset approval before re-enabling the new approval-consuming version; this task does not execute rollback or alter deployments.

## Email service, verification, and concurrency

Keep `PUT /api/email/myEmail` and setMyEmail's Boolean storage-success contract unchanged. SMTP-disabled storage still works, and PUT never claims an invite was sent. Add `POST /api/email/requestMyEmailVerification`, with a new validated request carrying `expectedEmail: Email` and no caller id. Add the matching constant, common serializable result enum, server EmailFeature method accepting callerId, and client EmailFeature/KtorEmailFeature method without callerId. Ktor remains HTTP-only.

Use result values Sent, AlreadyApproved, Unavailable, NoEmail, EmailChanged, and DeliveryFailed. Authenticated domain results return HTTP 200 with the enum body; missing/invalid bearer remains 401, and malformed email JSON remains a bad request. Infrastructure exceptions retain a failure response rather than being mislabeled Sent. The existing PUT keeps its 409 duplicate response. The new endpoint sends only to the caller's current stored address after comparing it with expectedEmail, so a forged body cannot target another user's stored address. Missing caller records fail closed using NoEmail; the response does not identify another user.

Extend EmailVerificationAccountCoordinator with a narrow snapshot/check method for verification requests, `updateUser(id, NewUser)` for full admin replacements, and `updateUsername(id, Username)` for username-only saves. All three use the same existing mutex as updateStoredEmail and verifyInvitedEmailAndPromote. The username-only method reads the current record under the mutex and passes its existing email to the ordinary repository update. Admin full updates delegate to updateUser, closing the current bypass. No arbitrary callback execution API or new coordination registry is necessary.

For requestMyEmailVerification, EmailFeatureService checks enabled infrastructure and obtains the caller's exact pending record through the coordinator. Approved returns AlreadyApproved without sending; absent email returns NoEmail; mismatch returns EmailChanged. Invoke the existing EmailRegistrationInviteSender compensation-capable method with that snapshot outside the mutex. Bind the concrete sender once and alias its existing RegistrationEmailSender interface to the same instance; inject that concrete existing sender into the enabled email service. DisabledEmailFeature returns Unavailable without minting or sending. Required registration retains its existing interface and compensation behavior.

After delivery succeeds, check the current id/email again through the coordinator. If the address changed or disappeared during delivery, non-cancellably remove only the returned request-local link and return EmailChanged/NoEmail. If the address was approved meanwhile, return AlreadyApproved; removing the redundant request-local link is safe. Otherwise return Sent. An ordinary false/failure preserves the saved address with false approval and permits the same expectedEmail to be retried. Sender cancellation retains its current link cleanup and propagates; never roll back the user's saved address or delete the user after a profile delivery failure. No SMTP, deeplink cleanup, or network request runs under the coordinator mutex.

In verifyInvitedEmailAndPromote, reject legacy-null payloads, missing users, null addresses, and exact-address mismatches. Persist approval through approveEmail before invoking the role helper, all under the coordinator mutex. A null repository result rejects without promotion. Change promoteNewUserToUser in roles/server so its existing roleTransitionMutex checks direct NewUser membership before granting User. A normal User or SuperAdmin keeps all current roles; an account with neither User nor NewUser must not regain User through an old link. This corrects the helper's current unconditional grant rather than introducing another role service.

The approval and role writes live in different repositories and are not a database-wide transaction. If approval persistence fails, do not promote. If promotion fails after approval was persisted, do not return the success redirect; the exact link remains retryable and reattempts pending promotion even when approval is already true. An interrupted role mutation retains the pre-existing role-service limitation; do not claim crash-atomic or distributed behavior. Preserve ordering between the coordinator mutex and the existing roleTransitionMutex, and never acquire them in reverse order.

EmailVerificationDeepLinkHandler retains the persisted handler id, nullable legacy payload compatibility, fixed root approval marker, and best-effort confirmation. Update confirmation/invite wording to describe email approval accurately for already logged-in accounts; do not promise new sign-in capability to a revoked account. Confirmation failure never undoes successful persistence/promotion, ordinary failure still redirects, and cancellation propagates. Repeated valid links retain the existing confirmation-retry behavior. The JS approval toast remains feedback only; the private record, never the query marker, determines displayed approval.

Process-local coordination covers self email edits, both admin edit forms, generic admin replacements, and verification in one server process. Conditional SQL also prevents directly approving a different database address. The existing cache architecture and independent role store do not provide multi-process linearizability. Test the stated boundaries and document them rather than implying a distributed lock. Existing account-deletion compensation and role cleanup continue through their existing paths; no user resurrection is permitted by approval.

## Username-only compatibility path

Add a root-guarded `PUT /api/admin/users/setUsername/{id}` accepting the existing validated Username value, parallel to setPassword. Add the constant in admin/common, the method in admin client UsersManagementFeature and KtorUsersManagementFeature, server UsersManagementFeature, and AdminRoutingsConfigurator. The server feature delegates to coordinator.updateUsername. Preserve existing 401/403 authorization, 400 invalid id/body, 404 missing user, and 409 duplicate behavior. Existing full update routes and NewUser semantics remain intact.

Register the coordinator dependency in admin/server Plugin and declare the email/server module dependency. admin UsersManagementFeature.update must delegate its full update to the same coordinator; keep get/create/password/delete and wishlist behavior otherwise unchanged. Bind dependencies through the existing feature Plugin, with no cross-feature setupDI calls.

UsersModel.updateUsername delegates to the new client method. Add corresponding AdminPanelModel.updateUsername delegation and change AdminUserEditViewModel's existing-user save to use it. Account creation still uses NewUserWithPassword. Check the Boolean save result before signaling navigation success in the two affected editors; failed saves stay open. This prevents a failed username request from appearing successful while preserving established password and avatar workflows.

## Owner profile behavior and localization

Extend the existing UsersModel with `isEmailFeatureEnabled`, `getMyProfile`, `setMyEmail`, and `requestMyEmailVerification`. Bind the EmailFeature and ClientAuthFeature in ui/users Plugin; add email/client to ui/users build.gradle. Private email is fetched only for the authenticated owner's editor. Keep public user/profile list data email-free. Guard the model/VM sequence by the captured current user id and discard or cancel results if the caller changes or logs out between requests.

Extend UserEditViewModel with private-email status, input, busy/error/result state, onEmailChanged, onSaveEmail, onResendEmailVerification, and an explicit refresh action. Derive visibility from currentUserIdFlow equaling the edited id and enabled capability; root editing another user does not see or invoke self-email controls. An unavailable or failed capability probe must not enable the form. For a missing address, show a labeled Email address input and a Save and verify button, validating with the existing Email.parse after trimming surrounding form whitespace. A blank/invalid address is rejected locally and never sent as a clear request by this UI.

Saving first performs the existing PUT. Only a confirmed storage success triggers the request-verification POST. Refresh the private record after the attempt even when delivery fails, so the saved pending address remains visible and retryable. A pending address displays Not approved yet with Send verification email/Resend verification email. An approved address displays Approved without the resend action. Keep existing-address replacement/clear UI outside this task; existing APIs still reset approval correctly. Disable duplicate submissions synchronously before launching the coroutine, and release busy state in finally. Retry after network errors must not require editing the address, and successful SMTP acceptance must be described as a verification email sent rather than an address approved.

Refresh private email on initial owner resolution, every editor resume, and the explicit refresh action, independently of one-time username/avatar initialization. A response for another identity must never populate the current editor. Do not overwrite an unsaved missing-email draft during an incidental refresh; after a confirmed stored address appears, use that server address as the pending display. Existing logout navigation bypasses dirty confirmation, and pending email input must be considered by ordinary discard navigation until saved. Approved status after following a link comes from fresh getMe, including desktop/Android when returning from an external browser.

Update UserEditView in jsMain, jvmMain, and androidMain with the same shared ViewModel decisions. Use current Calm Studio FieldSet/CalmTextField/FormHint/CalmButton on JS and the existing Material versions on JVM/Android. Keep normal users' username read-only while adding the separate owner email section. Replace the inaccurate no-editable-fields note with wording specific to username/password permissions when email setup is visible. Provide English and Russian strings in UsersListStrings for email label, save-and-verify, pending, approved, resend, refresh, validation, storage failure, delivery failure, and sent feedback. Errors must distinguish a saved pending address from a failed save.

## Admin landing page

Extend AdminPanelViewModel using existing AdminPanelModel.getAllUsers. Load users initially and on resume; expose users, loading, and recoverable load-error state, and add onRetryUsers and onUserSelected. Clear displayed user rows on authorization loss or denied refresh so a previously authorized screen cannot retain private admin data after logout. Use serialized refresh or cancellation/generation checks so an older request cannot overwrite a newer result. An error must not render as an empty successful list.

Add onUserSelected to AdminPanelViewInteractor and implement it in client/src/commonMain/kotlin/ClientPlugin.kt by pushing AdminUserViewConfig(userId), exactly as the existing user-list interactor does. Keep Users and Wishlists buttons, including the existing Users screen's Add User route. A new direct create action on the dashboard is unnecessary.

In all three AdminPanelView implementations, render the Users heading and real username/id rows using the existing list components and user-list visual conventions, with distinct loading, empty, error, and retry states. On JVM/Android provide scrollable content so a real user list does not make the SMTP section unreachable.

Render the SMTP section separately below the users list with the existing Send test email heading, a Recipient email address label, and the explanation “Send a test message to check the server email configuration.” Add the Russian equivalent “Отправьте тестовое письмо, чтобы проверить настройки электронной почты сервера.” Use the existing capability probe in the shared ViewModel and hide the complete SMTP form when disabled; a probe failure must not block user-list loading. Keep list errors and SMTP results independent. Add a sending state so the test button cannot be submitted repeatedly during a request and report caught ordinary transport errors as failure. Preserve server-side SuperAdmin enforcement.

## Test specifications for Coding

The following are narrative test specifications for every planned production seam. Implement them alongside production code using existing suites and small focused new suites. No test requires a live SMTP account: EmailsService and DeepLinksService/repository fakes exercise the external boundary. All required behavior has an automated test seam; platform appearance can additionally receive a manual smoke check, but no requested functionality is classified as inherently untestable.

Persistence tests in ExposedUsersRepoSqliteTest must construct a legacy users table without email_approved, initialize the new repository, and assert old null/non-null emails both read false with ids/usernames preserved. Reopen the same database after approval and assert true survives. NewUser create cannot request true. Exact same non-null email updates preserve true, changed/cleared/null email writes yield false, and username-only updates preserve both email and approval. Cover inherited single and bulk update forms. approveEmail must cover matching false/true, wrong email, missing id, cleared email, and unrelated username preservation. Duplicate username/email failures must keep stored approval and cache unchanged. Malformed legacy email reads null/false. Verify approval produces an updated event and a failed conditional update does not.

Add focused CacheUsersRepo approval tests, preferably against the real SQLite repository: immediate getById/getAll after approval returns true; a failed approve keeps the prior value; later full replacement returns false; cache subscriptions complete without deadlocking. Update all FakeUsersRepo implementations so fake approval, full updates, and events reflect production semantics. Existing fake-based role/auth tests must not silently discard approval on unrelated copies.

Extend AuthFeatureUserTest and AdminUserTest for false/true round trips and JSON without emailApproved decoding false. Extend UsersFeatureUserTest to assert serialized public output contains neither email nor emailApproved, and reverse conversion requires explicitly supplied dropped fields. Add serialization tests for the verification request/result, including malformed expectedEmail and stable round trips of every result.

Extend EmailVerificationAccountCoordinatorTest for updateStoredEmail/updateUser/updateUsername preservation and resetting; verify null/missing/stale payload rejection without roles or approval changes; verify matching pending, existing User, SuperAdmin, repeated link, and revoked-role records. Deterministically pause repository/role operations and test both orders of self/admin replacement versus verification: replacement first rejects the old recipient; verification first sets true and completes allowed promotion before the later replacement resets false. Test username-only edits during email setup preserve the latest address. Test persistence failure prevents role promotion; promotion failure leaves durable approval retryable and does not falsely report success.

Extend RolesBootstrapTest around promoteNewUserToUser: direct NewUser becomes User, existing User is unchanged, SuperAdmin is preserved, repeated promotion is idempotent, and no direct NewUser/User does not gain access. Existing delayed creation callback and required-registration pending tests must remain passing. Retain the role helper's lock and exercise its direct-membership guard inside that lock.

Extend EmailFeatureServiceTest for requestMyEmailVerification: pending caller/expectedEmail sends one exact bound invite and returns Sent; already approved sends none; absent email/user, mismatched expected email, disabled infrastructure, SMTP false/exception, and duplicate save all fail without approval. Send failure followed by same-address retry succeeds. Suspend SMTP, perform a concurrent replacement, finish SMTP, and assert only the obsolete request's link is removed and the new address survives. Cancellation during delivery propagates after sender cleanup. Concurrent approval returns AlreadyApproved without reversing approval. setMyEmail remains storage-only and works in DisabledEmailFeature.

Extend PluginTest to resolve both SMTP-enabled and disabled graphs, asserting one coordinator shared by feature and handler, one concrete sender aliased as RegistrationEmailSender, and no SMTP construction when disabled. Add or extend admin Plugin graph coverage for the reused coordinator. The graph must not create an admin/email cycle. Existing registration sender and RegistrationCompensationIntegrationTest must retain exact-link ownership and account rollback contracts.

Add Ktor route tests for requestMyEmailVerification: anonymous/invalid bearer 401, valid caller identity derived from bearer, malformed body 400, and every enum domain result encoded correctly. A body cannot choose a user id; use two users to prove the authenticated caller is the only candidate. Retain PUT duplicate 409 and disabled-storage success. Use Ktor MockEngine tests in email/client for paths, body, result decoding, and error propagation; no business state may enter KtorEmailFeature.

Extend EmailVerificationDeepLinkHandlerTest and EmailDeepLinkIntegrationTest: exact recipient persists true and redirects to the fixed marker; legacy-null, missing, changed, and cleared recipients remain unhandled; repeated links remain compatible; wrong payload types are untouched; logged-in ordinary accounts verify without new credentials or role changes. Confirmation false/ordinary exception preserves approval and redirect; cancellation propagates. Retain EmailRegistrationInviteSenderTest URL validation and cleanup cases while asserting the revised email-approval copy. Keep the existing client EmailApprovalNotificationTest passing and ensure forged markers cannot modify private approval state.

Extend admin UsersManagementFeatureTest and add protected routing/client tests for setUsername: root success preserves a concurrently changed email and its approval, non-root 403, unauthenticated 401, missing user 404, invalid body/id 400, duplicate username 409. Test the existing full update path resets approval on email replacement and uses the coordinator. Assert getAll/getById AdminUser mappings carry approval only behind the existing root guard. Do not add alternate public listing endpoints.

Add UserEditViewModelTest with fake UsersModel and deterministic coroutine scheduling. Cover cold-start owner identity resolution, SMTP enabled/disabled/probe failure, anonymous/non-owner/root-editing-other denial, invalid/blank input with zero calls, successful PUT then verification POST in order, failed PUT with no POST, saved-but-unsent state, same pending-address resend, AlreadyApproved, resume/manual refresh observing true, logout/identity change during each awaited operation, duplicate clicks, and busy cleanup on error/cancellation. Check draft/discard behavior and that email actions do not trigger root username/password saves. Existing root onSave must navigate only after successful operations.

Add focused UsersModel delegation tests for owner snapshot retrieval, email method forwarding, and username-only forwarding. Add AdminUserEditViewModel tests proving existing-user save calls updateUsername, creation still calls createUser, failed save does not navigate, and loading clears after error. Add AdminPanelModel delegation tests for its new username method and retain existing users/mail methods.

Add AdminPanelViewModelTest covering initial rows, resume after create/update/delete, empty success, load failure and retry, out-of-order refresh protection, authorization loss clearing rows, and SMTP capability failure independent of users. Test onUserSelected forwards the exact id, old section actions still navigate, disabled SMTP sends nothing, duplicate send suppression, true/false/exception feedback, and cancellation clearing sending state. Test the concrete ClientPlugin interactor on a navigation chain: selecting a landing row pushes AdminUserViewConfig with the selected id while Users/Wishlists destinations remain unchanged.

Add lightweight JS DOM behavior tests for the changed UserEditView and AdminPanelView with fake models/DI: missing-email owner sees a labeled input and working save action; pending/approved states and resend visibility follow server-backed state; non-owner sees no private email; dashboard displays username/id rows whose click selects the matching user; SMTP heading, recipient label, and purpose explanation are visible together and disappear when disabled. Verify EN/RU resources contain the new copy and no empty fallbacks. JVM/Android views share those tested decisions; compile both platform bindings and, where the existing Compose harness permits, assert labels, enablement, and scrolling through semantic UI tests. A browser/SDK unavailable in a particular environment is an execution limitation to report in Verification, not evidence that the functionality cannot be automated.

## README updates

Coding must update users README models and Architecture Notes with persisted approval, conservative additive migration, field-owned writes, conditional approval, cache event propagation, and public privacy. Update auth/admin DTO descriptions for the defaulted private field. Admin README must document the username-only route, coordinator reuse, reset behavior, and preserved root gate without touching Operator Notes. Roles README must clarify pending-only promotion and that email approval is not an access role.

Update email README routes/models with requestMyEmailVerification, its result enum, storage-versus-send distinction, self identity, retry behavior, shared coordinator, and separate persistence/role failure semantics. Explain that historical addresses start pending, exact-address old-link compatibility remains, and guarantees are process-local. Update deeplinks README's email-handler description to include persisted address approval while keeping the core generic. Update ui/users README for owner setup/pending/approved states, private refresh, disabled behavior, the email dependency, and username-only saves. Update ui/adminPanel README for landing rows, loading/retry/navigation, capability-aware labeled SMTP section, and username-only edit preservation. Preserve every Operator Notes section verbatim.

## Verification and handoff

Coding should implement persistence/mappers first, then coordinator/roles/admin mutations, email request flow, shared UI state, platform views, and tests/README deltas. Rebuild ast-index after source changes. Run the relevant tests using the actual dotted module names: users.common, auth.common, admin.common, email.common/client/server, admin.client/server, roles.server, auth.server, ui.users, and ui.adminPanel JVM tests; retain deeplinks and registration integration coverage. Compile both UI modules and the client shell for JS/JVM and compile Android through available SDK tasks. Run the focused JS DOM tests with the configured browser test environment and existing approval-marker tests. The build templates configure both browser and Node tests, so DOM-dependent tests must run in the browser task rather than Node.

Do not claim browser, Android, or SMTP delivery success from compilation alone. Verification must report the exact successful gates and any environmental execution limits. No production test or build was run by Architecture because this role changed only its step report. Architecture is complete when this report is committed; product completion still requires Coding, Verification, and Validating. There are no unresolved operator choices or inherently untestable requirements blocking Coding.
