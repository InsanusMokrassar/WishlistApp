Model: GPT-6 (inherited high-capability Planning session)
Changed files: agents/task/10.09.2026_16.32.33-2f63e0e4-5fa6-4bb0-819c-3c99ea86af3d/010-planning.md

# Correction plan for issue #79 after failed Validation

Planning restarts from clean `af7c075c69723c79230e7033e5291d1ca0d5e178` on `feat/issue-79-user-email-change`. The six findings in `009-validating.md` are accepted for correction. The existing replacement implementation and its earlier acceptance contract remain the baseline. This cycle repairs live rendering privacy, feedback consistency, ordinary input validation, partial-draft protection, desktop renderer evidence, and confirmed Disabled guidance. No product or authorization question remains open.

The inherited high-capability model satisfies Planning's HL-first preference because the corrections cross authoritative profile snapshots, asynchronous state collection, and the actual native semantics boundary. The explicit assignment prohibits nested agents and requires this agent to write the planning report, controlling the general documentation-delegation instruction. The caveman skill was read and applies only to internal notes. The report and commit use normal prose. Planning changed only this report and ran no Gradle invocation, implementation test, renderer probe, or production edit.

## Finding history and required disposition

V79-01 remains Open and High, with occurrence count 1 and consecutive unresolved validation-cycle count 1. JS `UserEditView.kt:78,153,266`, JVM `UserEditView.kt:70,170,297`, and Android `UserEditView.kt:72,171,298` derive email visibility and notice placement from collected `canManageOwnEmailState` alone. Architecture 002 required a direct live-target comparison at the rendering boundary. Correct all three platform paths and execute a renderer regression while the ViewModel owner flow remains deliberately stale.

V79-02 remains Open and High, with occurrence count 1 and consecutive unresolved validation-cycle count 1. `UserEditViewModel.applyOwnedEmailProfile` at line 503 invalidates verification feedback only when the recipient address differs. A later approved-to-pending refresh for the same address retains `AlreadyApproved`; a remote A-to-B-to-A replacement is one concrete cause. Bind completed feedback to the authoritative email and approval snapshot, and prove both later manual and resume transitions after previously valid feedback.

V79-03 remains Open and Medium, with occurrence count 1 and consecutive unresolved validation-cycle count 1. `canSaveEmailState` rejects malformed input, JS disables Save, and `onEmailChanged` clears the error that only direct submission currently creates. Publish the existing `InvalidEmail` error for an ordinary nonblank invalid edit. Test typing before any save or IME callback, then test recovery through valid typing.

V79-04 remains Open and Medium, with occurrence count 1 and consecutive unresolved validation-cycle count 1. `isEmailDraftDirty` at line 488 conflates an invalid parsed value with an absent saved email. Use trimmed raw-string comparison independently of parsing, retain partial drafts through refresh/resume, and require Back confirmation for unsaved partial input.

V79-05 remains Open and Medium, with occurrence count 1 and consecutive unresolved validation-cycle count 1. Stage 4's nonexistent `UserEditEmailRenderTest` filter proved only test absence. The correction requires an actual test of the existing JVM email panel using the declared desktop Compose dependency, or a real compiled/attempted host probe with an exact causal incompatibility. A missing test, absent browser, or absent Android host cannot satisfy the JVM host attempt.

V79-06 remains Open and Low, with occurrence count 1 and consecutive unresolved validation-cycle count 1. The three views display delivery-unavailable copy only for a POST `Unavailable` result, although confirmed Disabled storage issues no POST. Show the existing explanation directly from `EmailCapabilityState.Disabled` while retaining editable storage controls and zero verification calls.

The Planning restart does not count as a second validation occurrence, resolve a finding, or escalate severity. All six IDs and counts must be carried through Architecture, Coding, Verification, and independent Validation. Prior reports remain unchanged; the incorrect renderer-guard and nonexistent-host conclusions in 006–008 are historical statements corrected by the new evidence, not rewritten history. No Critical finding or repeat-problem escalation is established.

## Read-only evidence and responsible boundaries

Planning read `AGENTS.md`, `agents/SHORTCUTS.md`, `agents/ALL.md`, the available `agents/local.ALL.md`, the routed `agents/PLAN.md`, `agents/MODELS.md`, `agents/PROTOCOL.md`, `agents/GIT.md`, `agents/TOOLS.md`, `agents/AST_INDEX.md`, `agents/VERIFICATION.md`, and `agents/VALIDATOR.md`. Local Planning/model/protocol/git/tool/AST overrides were checked and absent. The complete task prompt and reports 001–009, including the full failed Validation, were read. The users feature README and Operator Notes, all three complete editor renderers, the complete shared ViewModel, the existing email test class and fixture, email/copy/component sources, and relevant Gradle templates were inspected directly.

An initial AST query without the task cache reported no index. Subsequent navigation used the existing `XDG_CACHE_HOME=/tmp/wishlist-issue79-ast-cache`, which contains 785 files, 6,570 symbols, 26,866 references, and 49 modules. `search UserEdit`, `usages configState`, and file/symbol queries for the build templates, `Email.kt`, `CalmForms`, `emailVerificationUnavailable`, and `ExposedUsersRepo` located the exact sources. The current index reports `configState` usages only in the ViewModel; direct complete platform reads confirm the missing renderer observation. No index rebuild was needed for this report-only stage.

`Email.parse` in `features/email/common/src/commonMain/kotlin/models/Email.kt` trims whitespace and preserves exact case. Its validation is appropriate for save admission and live validation. Parsing is not an appropriate representation of a user's unfinished draft. The narrow dirty comparison is `input.trim() != savedEmail?.string.orEmpty()`. Empty/whitespace input with no stored email is clean; nonblank invalid input with no stored email is dirty; blank input against stored A is dirty; surrounding whitespace around exact A is clean; case changes retain current exact equality.

`CalmTextField` in `features/common/client/src/jsMain/kotlin/ui/components/CalmForms.kt` forwards every native input event through `onValueChange`; `CalmForm` is a `Div`, with no hidden form submission. The JS editor already forwards that event directly to `onEmailChanged` and already renders `emailInvalid`. Shared live validation therefore supplies the missing reachable path without changing shared form components or adding browser infrastructure.

The current refresh implementation clears `_ownEmailProfileState` before probing. A correction that compares only the old value of that flow inside `applyOwnedEmailProfile` would lose the previous approval snapshot before the checked GET arrives. Feedback needs its own small private snapshot binding. The existing public saved/result flows, mutation token, owner generation, raw callback checks, and exact GET/POST ordering remain useful and must be preserved.

Dependency investigation began under `/home/aleksey/projects/own/navigation`. AST navigation and direct source reads established that `NavigationNode.config` reads `configState.value`, `ComposeNode.config` writes its `MutableStateFlow`, and `ComposeView` adds no private-target rendering guard. The relevant `NavigationNode.kt`, `ComposeNode.kt`, `compose/ComposeView.kt`, and `ViewModel.kt` sources have no diff against selected tag `v0.7.7`. The existing `UserEditTestNode` exposes the same mutable config surface and real resume/destroy transitions; no navigation dependency change or new navigation host is needed.

`features/ui/users/build.gradle` applies `gradle/templates/mppJvmJsAndroidWithCompose.gradle`, which applies `addComposeForDesktop.gradle`. The latter already declares `kotlin('test-junit')` and `compose.uiTest` in `jvmTest`; desktop main already depends on `compose.desktop.currentOs`. The selected Compose version is 1.11.1. Its cached sources are available at `/home/aleksey/.gradle/caches/modules-2/files-2.1/org.jetbrains.compose.ui/ui-test-desktop/1.11.1/bfd3df32a8ba135ec42aeb7e8eda1b7730410821/ui-test-desktop-1.11.1-sources.jar`.

Direct reads of `desktopMain/androidx/compose/ui/test/v2/ComposeUiTest.desktop.kt`, the desktop implementation, both relevant Skiko implementations, and `commonMain/androidx/compose/ui/test/TextActions.kt` establish the real host path. `androidx.compose.ui.test.v2.runDesktopComposeUiTest` accepts composition and test contexts, creates `DesktopComposeUiTest`, and executes its suspendable test body. `SkikoComposeUiTest` creates `Surface.makeRasterN32Premul` and `CanvasLayersComposeScene`, provides `setContent`, `runOnUiThread`, `waitForIdle`/`awaitIdle`, semantics queries, and disposes its scene. The composition and test contexts must not share a `TestCoroutineScheduler`. `performTextReplacement` invokes the real `SetText` semantics action; `performImeAction` invokes `OnImeAction` and requires an enabled focusable editor. These are direct source facts, not proof that the host already ran. The raster surface is a concrete reason to attempt the existing desktop host even without a browser or emulator.

The repository approval-reset rule remains in `features/users/common/src/jvmMain/kotlin/repo/ExposedUsersRepo.kt:98`: a changed or cleared address resets approval, and an unchanged non-null address preserves current approval. The earlier HTTP and SQLite proof remains regression evidence. Planning does not require another backend implementation or a new result taxonomy.

## Correction ownership and precise behavior

### V79-01: Read the exact live node at each rendering boundary

Each JS/JVM/Android `UserEditView.onDraw` must observe the actual view node's `configState`, or invoke a narrowly extracted email panel that observes that exact node. Require the collected current selected user ID to match `viewModel.userId`, and perform a raw `node.config.userId == viewModel.userId` comparison during drawing so an unrelated recomposition cannot admit stale private state before the direct collector catches up. Combine that target condition with existing `canManageOwnEmail`; the ViewModel continues to own authenticated-owner business policy. Do not replace either check with another asynchronously derived ViewModel Boolean.

Apply the resulting predicate to the whole private email subtree: authoritative address/status, draft, loading and recovery content, buttons, validation/errors, saved confirmation, verification results, and capability guidance. Keep interruption placement explicit: the existing address-free notice must not allow any private subtree to escape the gate, and the notice's current owner-context placement receives the same live-target condition. Initial root-other views remain empty of private email state and interruption text. Existing ViewModel invalidation and raw callback checks continue to clear state and suppress requests independently.

For JVM, extract only the existing email section and its notice into an internal, documented `OwnerEmailEditor(viewModel, node)` composable in the existing `src/jvmMain/kotlin/ui/UserEditView.kt`. The production call passes `this@UserEditView`; the test passes the same `UserEditTestNode` used to construct its real ViewModel. The panel collects its own existing email state and the supplied node's real config, and contains the guard internally. This makes the actual widgets and callbacks testable without Koin, avatar, admin fields, dialogs, or a parallel fake form. Do not accept an independently supplied permission Boolean or new presentation-policy model as a substitute. JS and Android can keep their existing inline sections and collect their own view config directly; a matching extraction is optional only if it reduces the same email-section change.

The key renderer regression, `liveRetargetHidesPrivateEmailBeforeOwnerCollectorRuns`, must first load self-owner A and draft B with the real ViewModel, confirm `canManageOwnEmailState` is true, then stop advancing the ViewModel scheduler. Retarget the same node to another ID on the host UI thread. Allow the composition scheduler to process config and redraw, while asserting the ViewModel's collected owner eligibility and old private profile are still stale. Every address, draft, private result, and email action must be absent from real semantics. Assert absence before advancing ViewModel invalidation; afterward the existing common tests still establish clearing and cancellation. A helper-only Boolean assertion or a test that waits for both schedulers before checking absence cannot close V79-01.

### V79-02: Bind completed feedback to a checked email and approval snapshot

Keep `emailSavedState: StateFlow<Email?>` and `emailVerificationResultState` as the public surfaces. Add only the private metadata needed to bind each completed publication to the exact checked email and `emailApproved` Boolean. A small private `EmailFeedbackSnapshot(email, emailApproved)` value reused for saved and verification bindings is sufficient; it is not a new API, status enum, timestamp, token epoch, or cross-client version. Replace the address-only `emailVerificationRecipient` metadata with that snapshot or extend its private equivalent consistently.

`applyOwnedEmailProfile` compares each already-published feedback binding with the successful authoritative profile before retaining feedback. Any different email, missing email, or changed approval Boolean clears the corresponding saved/result publication and its binding. Consequently an approved-to-pending transition for A clears `AlreadyApproved`, `Sent`, and saved confirmation when those belong to the prior snapshot. A pending-to-approved transition also retires earlier completed delivery/save banners; the authoritative verified status becomes the current explanation. An unchanged exact email and approval snapshot retains supported completed feedback. Username/avatar changes do not invalidate email feedback. Once cleared on an observed A-to-B transition, feedback must not reappear merely because a later GET returns A.

At positive publication, preserve the existing final-read gates: saved confirmation requires acknowledged PUT plus the operation's matching checked final record, `AlreadyApproved` requires matching approved final A, and `Sent` requires a matching final recipient. Record the approval Boolean from that final record. A freshly completed `Sent` with a concurrently approved matching final record may remain as the actual just-completed delivery result, consistent with the current workflow; a later approval transition retires that banner. If `AlreadyApproved` contradicts the current pending final record, keep the existing `EmailChanged` failure and publish no success. Do not infer approval from delivery.

Apply snapshot invalidation only to feedback already published before a subsequent authoritative transition. A fresh negative delivery outcome from the active operation remains visible after its own successful matching final reconciliation, even when approval changed during that operation. Record that final snapshot when publishing the result. If the final GET throws, returns null, or cannot provide a usable owner profile, retain the actual primary negative outcome and load failure as the current code does; bind the negative to the most recent checked matching recipient snapshot captured by that operation. Successful recovery for an unchanged snapshot keeps the negative visible. A later checked address or approval change retires the old negative as superseded. A failed refresh alone is not evidence of a changed snapshot and must not erase `DeliveryFailed`, `Unavailable`, `NoEmail`, or `EmailChanged` results.

Do not call `clearEmailOperationFeedback` wholesale from profile application: that would erase legitimate `SaveFailed` uncertainty, retained validation for an unfinished draft, or a current primary delivery failure. Preserve those existing local errors according to their current edit/new-operation recovery rules. Clear snapshot metadata alongside publications in `clearPrivateEmailState`, eligible new edits, and admitted mutations. Merely setting the private profile flow to null at refresh start must not discard the independent snapshot binding. No delayed completion may repopulate a binding after owner/generation/token invalidation.

Extend `UserEditViewModelEmailTest` with `laterPendingRefreshClearsAlreadyApprovedFeedback`, parameterized for manual refresh and real node resume. Start with pending A, obtain a legitimate `AlreadyApproved` outcome and approved final A, then return authoritative pending A on a later GET; require pending status, no prior verification success, and no prior saved confirmation where the save variant created one. `emailRoundTripDoesNotRestoreOldFeedback` covers both an observed A-to-B-to-A sequence and remote B-to-A changes occurring before the next GET, with approved A becoming pending A. `approvalSnapshotChangesRetireCompletedFeedback` covers `Sent`, `AlreadyApproved`, and saved confirmation with relevant pending/approved transitions, plus unchanged snapshot preservation.

Add `laterSnapshotChangesRetireOldNegativeDeliveryFeedback` and `currentDeliveryFailureSurvivesReconciliationAndUnchangedRecovery`. Cover every existing negative outcome through a table of scenario parameters, with identifiable assertions; include POST exception mapping, failed final GET, successful unchanged recovery, same-address approval change on a later refresh, and a newly completed failure whose matching final profile changed approval during the active operation. These cases prevent an overbroad correction that hides useful current failure feedback. Existing final-result mismatch and uncertain PUT tests remain mandatory.

### V79-03 and V79-04: Validate typing and protect raw drafts independently

`onEmailChanged` retains its raw owner/target/capability/profile/idle admission check. For an accepted edit, store the raw input and calculate dirtiness from the trimmed raw string. Clear previous completed operation feedback as an intentional new edit already does, then publish `EmailEditorError.InvalidEmail` when the input is nonblank and `Email.parse(input)` fails. Valid accepted typing clears the validation error. Empty/whitespace typing does not introduce a validation hint before submission, remains unsaveable, and still receives `InvalidEmail` if the guarded direct save/IME callback is invoked. No blank clear-email mutation is introduced.

Input callbacks rejected because loading, mutation, stale caller/target, or failed profile admission is active must not modify draft or primary feedback. Manual refresh and final reconciliation must not call the typing handler or fabricate a new edit. Preserve `SaveFailed` and current negative delivery feedback through refresh; clear completed feedback only on an actual admitted edit/new operation or the snapshot rules above. Keep canonical `Email.parse` and exact parsed equality for save admission, and retain the imperative invalid/unchanged/busy save guards.

Change `isEmailDraftDirty` and its KDoc to compare `input.trim()` with `savedEmail?.string.orEmpty()`, without converting invalid input to null. Continue preserving raw draft bytes during failed/ordinary refresh and recompute dirtiness after each successful checked profile. No saved email plus `partial-address` is dirty before and after manual refresh, resume, failed refresh, and recovery. Existing A plus blank or invalid input is dirty; absent email plus blank/whitespace is clean; trimmed exact A is clean; a case-only valid change is dirty. A newly external-cleared saved address must not erase a pre-existing nonblank invalid draft.

Back must honor accepted dirty input even before the combined `isDirtyState` collector runs. At the existing imperative `onBack` boundary, consult the current private dirty flags with the same OR meaning as `isDirtyState`, rather than relying on a potentially delayed collected Boolean. This is confined to the discard admission check. Keep the public combined state and existing confirmation/cancel callbacks; independently dirty administrator fields still require confirmation, and logout continues to exit without that dialog.

Add `invalidDraftPublishesFeedbackDuringTyping` with missing and saved profiles and malformed/overlength nonblank variants. Assert the error directly after `onEmailChanged`, without calling `onSaveEmail`; after collection, Save remains disabled and request lists remain empty. Then enter a valid replacement and assert error recovery and Save eligibility. Extend busy input coverage to require retained primary/result state when a typing callback is rejected.

Add `missingEmailPartialDraftSurvivesRefreshAndResume`, `partialDraftSurvivesFailedRefreshAndRecovery`, `externalEmailClearPreservesInvalidDraft`, and `rawDraftDirtinessControlsBackConfirmation`. Cover partial invalid input with absent storage, stored A plus blank, absent storage plus whitespace, whitespace-wrapped unchanged A, exact-case changes, and the immediate edit-then-Back path. Require raw input retention, correct dirty/discard state, zero accidental navigation, and no PUT/POST from invalid input. Use the existing deferred hooks, mutable profile, live node, and test scheduler; avoid sleeps or another fixture framework.

### V79-06: Explain confirmed Disabled without inventing a POST result

Each renderer collects `emailCapabilityState` directly and shows `UsersListStrings.emailVerificationUnavailable` inside the private gate when the value is exactly `Disabled`, including before any save. Preserve the existing `Unavailable` result path for enabled-server delivery responses. When Disabled and a retained `Unavailable` response coincide, show the same guidance once. Unknown, Loading, and Failed must not acquire the Disabled explanation from a negated enabled Boolean.

The draft remains editable after the matching profile load, Save uses the existing save-only label, a valid changed draft enables storage, and resend is absent. A successful Disabled save still performs only PUT plus checked GET and publishes truthful saved feedback without a fabricated verification result. Reuse the existing EN/RU text; only its KDoc in `UsersListStrings.kt` needs adjustment to describe both a confirmed capability and a server outcome if Coding changes that declaration documentation.

## Real desktop renderer proof and bounded host handling

Create `features/ui/users/src/jvmTest/kotlin/ui/UserEditEmailRenderTest.kt` and test the extracted production `OwnerEmailEditor` under Material v2 `MaterialTheme` and `runDesktopComposeUiTest`. Reuse `UserEditTestUsersModel`, `RecordingUserEditInteractor`, and `UserEditTestNode` from common tests. No new dependency, Gradle/plugin configuration, Koin wiring, or copied form is planned. Add a minimal stable test tag to the real saved/draft fields only if label/editable semantics are insufficient to distinguish them; assertions must still inspect actual editable text, action enabled state, status/copy, and callbacks.

Provide an explicit composition `StandardTestDispatcher`/scheduler and a distinct test-body scheduler as required by the inspected API. The live-retarget case also needs a separately controlled ViewModel dispatcher so host synchronization cannot drain the owner collector being held stale. Drive that ViewModel scheduler on `runOnUiThread`, and perform callbacks/state changes on the same UI thread, preserving the ViewModel's serialization assumption. For ordinary cases, drain the ViewModel queue at defined barriers and then let the Compose host process collection/layout before semantics assertions. A small test-local helper may perform those two explicit operations; do not call a blanket scheduler drain in the stale-window assertion. Cancel each ViewModel scope and release any deferred request in `finally`; the host owns composition/scene teardown.

The suite must contain a minimal `ownerEmailPanelRendersOnDesktop` host smoke case plus focused interaction cases. `savedApprovedAddressRemainsSeparateFromReplacementDraft` and its pending variant use real text replacement to enter B while saved A/status remains authoritative, assert the editable field and Save state, then click the real Save action and observe B pending and the recorded PUT/GET/POST ordering. `disabledDeliveryExplainsSaveOnlyStorage` covers missing/pending/approved storage, a visible unavailable explanation before POST, editable draft, save-only label, absent resend, and successful PUT/GET with zero POST. A failed-probe/loading case confirms that Disabled guidance is not inferred from `emailFeatureEnabled=false`.

`typingInvalidEmailShowsFeedbackWithoutSubmitting` sends a malformed draft through the real field semantics, requires visible invalid copy and disabled Save with zero requests, then types valid B and requires recovery. `failedSaveKeepsDraftAndVisibleFailure` makes the real Save return false or throw, retains draft B beside saved A, and checks visible unconfirmed-storage copy with no saved/Sent success. `approvedSavedEmailHasNoResend` and a pending saved-A/draft-B resend case verify actual button presence and that the production callback sends A, not B. `laterPendingRefreshRemovesAlreadyVerifiedCopy` executes the real Refresh callback after a legitimate AlreadyApproved result, checks the latest pending copy, and requires old approval-success copy to be absent.

`rootEditingAnotherUserHasNoPrivateEmailPanel` uses a root model whose caller differs from the node/ViewModel target and requires private fields/status/actions to be absent. `liveRetargetHidesPrivateEmailBeforeOwnerCollectorRuns` supplies the decisive V79-01 stale-owner-flow proof described above, including private result removal. Keep shared raw callback/retarget race tests as additional request-safety proof, not a replacement for semantics absence.

`imeDoneUsesGuardedEmailSave` invokes the real field's `performImeAction` for valid changed B and proves one PUT. Invalid and unchanged enabled fields receive the same semantics action and produce zero PUT/POST. For the busy case, do not mistake the test library's refusal to focus a disabled field for proof of the callback guard: capture the real field's `OnImeAction` callback while enabled, start and suspend one mutation, invoke that captured callback on the UI thread, and prove no second request. Assert that the current busy field/button are disabled. If an equivalent actual semantics action can be invoked without bypassing the production callback, use that boundary. Calling `viewModel.onSaveEmail` directly is shared-state proof, not renderer IME evidence. This proves desktop semantics wiring, not a physical Android keyboard.

Begin with the real smoke case after the extraction exists. If it fails, inspect the exact compilation or runtime cause and correct routine imports, opt-ins, scheduler misuse, selectors, dimensions, or fixture lifecycle within the existing test/source scope. A Kotlin test error or missing test tag is not an unavailable-host conclusion. If a suspected environment/dependency failure prevents rendering, make an actual minimal temporary/probe test calling `runDesktopComposeUiTest` and a real Material text node to isolate the host from ViewModel/panel behavior. Record the probe source, exact command/filter, resolved dependency, whether test compilation ran, actual executed test name where available, full causal exception chain, real exit status, and log path. A real panel failure with a passing host probe must be fixed; it cannot justify fallback.

Only an established host incompatibility that cannot be resolved within the authorized existing environment permits the previously accepted bounded fallback: compile all three renderer sources, execute the new common behavior and render-boundary policy evidence that can actually run, document the exact missing native semantics evidence, and route the host limitation for Architecture/Validation disposition. Do not retain a knowingly failing or silently skipped test and call the full gate green. A temporary probe may be removed only as an explicitly recorded probe after its exact source/failure evidence is preserved in the Coding report. The preferred completion is the real runnable JVM suite, and no current host failure is established in Planning.

JS DOM and Android device/widget harness creation remain out of scope. Existing JS Node/browser logic tests and Android unit tests still run, but must not be labeled DOM, device, or physical IME proof. Review JS `onInput` through `CalmTextField` to the corrected shared typing callback and compile the actual JS branch. Use the existing Android Material3/resource translation path and compile it; add platform renderer tests only if an already executable host is found without dependencies or new infrastructure.

## Exact file scope, staged red/green work, and documentation

Production corrections belong in `features/ui/users/src/commonMain/kotlin/ui/UserEditViewModel.kt` at `isEmailDraftDirty`, `onEmailChanged`, `onBack`, `applyOwnedEmailProfile`, feedback publication/clear helpers, and private feedback metadata; in `features/ui/users/src/jsMain/kotlin/ui/UserEditView.kt`, `features/ui/users/src/jvmMain/kotlin/ui/UserEditView.kt`, and `features/ui/users/src/androidMain/kotlin/ui/UserEditView.kt` at email state collection, live-target gating, capability guidance, and the narrow JVM panel extraction. `features/ui/users/src/commonMain/kotlin/UsersListStrings.kt` may receive only the unavailable-copy KDoc correction. Existing strings and status enums remain the source of copy.

Tests belong in `features/ui/users/src/commonTest/kotlin/ui/UserEditViewModelEmailTest.kt` and the new `features/ui/users/src/jvmTest/kotlin/ui/UserEditEmailRenderTest.kt`. `features/ui/users/src/commonTest/kotlin/ui/UserEditTestFixtures.kt` may receive a minimal necessary hook, but its current mutable node, profile handlers, events, and recorded calls already appear sufficient. Do not alter client/server/repository tests or production files to accommodate these UI corrections. `features/ui/users/README.md` receives a focused account of live-node rendering guards, snapshot feedback retirement, typing validation, raw partial-draft preservation, confirmed Disabled copy, and the actual desktop evidence/limits. Update only the affected ViewModel/panel KDocs. Preserve the entire Operator Notes section byte-for-byte against `af7c075c69723c79230e7033e5291d1ca0d5e178` and the original `cc8b63934086a41fb4c5965ce30b68775a67b67e` baseline.

First, Coding adds the new common regressions against current production behavior. Run the focused existing email test class and record behavioral red assertions for later approval reset, invalid typing, missing-email partial draft, and immediate Back protection. Existing green cases remain intact. A compilation failure is repaired before claiming red behavior. Then implement only the shared corrections and rerun the same class to green.

Next, make the mechanical JVM email-section extraction while preserving the existing business behavior. Add and run the real desktop smoke case and the renderer matrix. Before adding the renderer guards/capability guidance, require failing semantics assertions for live retarget and confirmed Disabled copy on the extracted production panel. Shared live-validation tests are already green at this point; the desktop typing case now proves the actual event/copy connection. Then implement the three renderer guards and Disabled guidance, and run the same desktop suite green. Any necessary small selector tags belong on production controls; the test must never gain its own rendering implementation.

Finally, update focused documentation to match actual results and run aggregate preservation gates. Do not broaden tests after the specified matrix and required gates pass unless new code or a demonstrated failure justifies another run. Rebuild the AST index after Kotlin edits with `XDG_CACHE_HOME=/tmp/wishlist-issue79-ast-cache ast-index rebuild`; do not rebuild for report/README-only changes. Each Coding stage must report source files, exact failed/passing test names, observed counts, and any host-limit evidence without inheriting 006's nonexistent-test conclusion.

## Serial commands and acceptance gates

Run every Gradle invocation serially with `--no-parallel`, the standard project cache, `--console=plain`, and a real process preflight that attributes active Java/Gradle work before starting another invocation. Do not redirect or copy the operator's cache, kill unrelated processes, mutate ignored lock state speculatively, or run a lock-upgrade task. When piping to `tee`, use shell pipefail and retain Gradle's real exit status. Use fresh XML modification times tied to the invocation log start; report suite/test counts and failures from executed results rather than task names or stale XML.

The shared red and green command is `./gradlew --no-parallel --rerun-tasks --console=plain :wishlist.features.ui.users:jvmTest --tests '*UserEditViewModelEmailTest'`. The desktop red/green command is `./gradlew --no-parallel --rerun-tasks --console=plain :wishlist.features.ui.users:jvmTest --tests '*UserEditEmailRenderTest'`, only after that source class exists. The host smoke/probe may use the exact existing method filter after its real source is present. Use separate logs for red, host diagnosis, and final green. Planned outcomes are specifications; no execution counts are claimed in this report.

The final Coding aggregate is `./gradlew --no-parallel --rerun-tasks --console=plain :wishlist.features.ui.users:jvmTest :wishlist.features.ui.users:jsNodeTest :wishlist.features.ui.users:testDebugUnitTest :wishlist.features.ui.users:compileKotlinJs :wishlist.features.ui.users:compileKotlinJvm :wishlist.features.ui.users:compileDebugKotlinAndroid :wishlist.features.email.client:jvmTest :wishlist.features.email.client:jsNodeTest :wishlist.features.email.server:jvmTest :wishlist.features.users.common:jvmTest`. It must execute the new desktop suite as part of users JVM when the host is established. Explicitly count the added shared and renderer cases; 273 prior-cycle tests alone cannot demonstrate the corrections.

Verification then runs the required full `./gradlew --no-parallel --rerun-tasks --console=plain build` with pipefail and a fresh captured log. Run `allTests` only if the full build executed no test tasks. Any failing required build or test is Verification FAIL and returns to Coding. Record JS browser tasks as common logic unless real DOM tests ran. Independent Validation must inspect reports from this restart together with the six carried findings, reconstruct source/call-site evidence, and independently exercise the focused aggregate appropriate to the final change. It must specifically inspect the stale-owner-flow renderer assertion and later same-address approval tests, rather than accepting nominal green shared counts.

Regression preservation includes approved/pending/missing owner replacement; root self versus root other; Disabled storage and enabled PUT/GET/POST/GET ordering; first checked GET before sending; exact saved-recipient resend with draft B; normalized unchanged and invalid no-dispatch; busy/double-submit exclusion; false/409/throw/lost-response uncertainty with retained draft and no automatic POST; matching authoritative saved status and separate current delivery failure; failed/null/wrong-owner/mismatched reconciliation; refresh/resume coalescing; raw caller/authorization/target checks; all suspended retarget boundaries; owner/target leave-return; old-finally isolation; real destruction continuation suppression; logout exit; root save/password failure and dirty-admin preservation. Retain all 48 earlier common email cases, the unchanged five root-save cases and model delegation test, existing client HTTP payload/status cases, bearer/409 route cases, and repository/coordinator/deeplink invariants. Do not weaken those assertions to obtain green.

Finish with `git diff --check`, AST callback/config usage inspection, direct comparison of all three renderer gates and EN/RU copy reachability, and scope/Operator Notes checks. Preserve the PR #80 removal of the `Подтверждение email` subtitle. Do not add issue #78 or PR #81 code, APIs, schemas, server changes, dependency/build/lock changes, status taxonomy, generic forms, navigation infrastructure, or auth credential epochs. Each role commits only its authorized files and never pushes; this Planning commit contains only `010-planning.md`.

## Stop condition, questions, and limits

Architecture may proceed with the concrete corrections above; there are no questions for the operator and no present blocker. The only unresolved execution fact is whether the real desktop host works in this environment, which Coding must establish through the specified existing dependency and actual test source. The raster implementation and available sources support attempting it; they do not predeclare success or failure.

Completion requires correction and independent disposition of all six existing findings, behavioral red-to-green evidence for the new shared defects and renderer omissions, a passing real desktop panel suite or an exact justified host failure with explicitly accepted bounded evidence, all required serial aggregate/full-build gates, accurate docs, unchanged Operator Notes, and no scope expansion. V79-05 remains open until the real attempt is established. No other finding is closed solely because a compiler or prior-cycle aggregate passes. Planning stops after committing this report with a clean worktree and handing the plan to Architecture.

Previously accepted limits remain: no live SMTP/deployed-service/device guarantee; cancellation cannot undo an accepted PUT; Boolean false/transport loss cannot identify the persistence cause; the client does not create a cross-client transaction; and same-owner credential epochs are unobservable. Likewise, an A-to-B-to-A sequence that returns to the same exact address and approval snapshot without any observation cannot be detected without a server version, which this issue does not add. The concrete approved-A-to-pending-A reset is observable and must be fixed. Desktop semantics proves actual native callback wiring and rendering, not a physical IME or another platform's renderer.

## Structured planning handoff

```aml-hip
ENTITY:
entity_id=issue_79_planning_cycle_2; type=correction_plan; state=ready_for_architecture

CONTEXT:
task_id=10.09.2026_16.32.33-2f63e0e4-5fa6-4bb0-819c-3c99ea86af3d; agent_id=issue79_planning_cycle2; memory_ref=[PROMPT,001,002,003,004,005,006,007,008,009,010]
constraints=[six_findings_only,report_only_planning,no_nested_agents,no_push,serial_Gradle,Operator_Notes_preserved]; starting_head=af7c075c69723c79230e7033e5291d1ca0d5e178; branch=feat/issue-79-user-email-change
exclusions=[API,schema,server,dependencies,build_configuration,lockfiles,status_taxonomy,navigation_framework,issue_78,PR_81]; source_scope=users_UI

ACTION:
action=plan_live_render_guard; target=V79-01; params={platforms:[JS,JVM,Android],observation:exact_node_configState,synchronous_comparison:raw_node_config_userId_equals_bound_userId,test_boundary:real_desktop_semantics}
action=plan_feedback_snapshot_binding; target=V79-02; params={snapshot:[exact_email,emailApproved],later_changed_snapshot:clear_completed_feedback,current_negative_outcome:preserve,tests:[same_address_reset,A_B_A,unchanged_recovery]}
action=plan_live_input_validation; target=V79-03; params={nonblank_invalid_typing:InvalidEmail,valid_typing:clear_validation,invalid_dispatch:zero,busy_edit:ignored}
action=plan_raw_draft_protection; target=V79-04; params={comparison:trimmed_raw_draft_versus_saved_string,missing_invalid:dirty,refresh_resume:preserve,Back:raw_dirty_flags}
action=plan_real_desktop_host_attempt; target=V79-05; params={test:UserEditEmailRenderTest,host:runDesktopComposeUiTest,production_panel:OwnerEmailEditor,dependency_change:false,nonexistent_filter_accepted:false}
action=plan_disabled_guidance; target=V79-06; params={source:EmailCapabilityState_Disabled,copy:emailVerificationUnavailable,storage:enabled,verification_POST:zero}

REASON:
condition=live_target_mismatch_with_lagging_owner_flow; requirement=private_render_absence; causal_chain=direct_node_observation_and_raw_comparison→guard_real_panel→V79-01_regression
condition=same_address_approval_reset; requirement=authoritative_feedback_consistency; causal_chain=checked_snapshot_change→retire_completed_banners→V79-02_regression
condition=invalid_input_collapsed_to_null; requirement=reachable_validation_and_partial_draft_retention; causal_chain=live_validation_plus_raw_comparison→visible_hint_and_discard_protection→V79-03_and_V79-04_regressions
condition=declared_desktop_host_without_actual_attempt; requirement=honest_renderer_evidence; causal_chain=real_test_source→compile_and_execute_host→V79-05_disposition
condition=Disabled_save_without_POST; requirement=capability_guidance; causal_chain=confirmed_Disabled→existing_unavailable_copy→V79-06_regression

EXPECTED RESULT:
entity_id=issue_79_planning_cycle_2; new_state=corrections_specified; location=agents/task/10.09.2026_16.32.33-2f63e0e4-5fa6-4bb0-819c-3c99ea86af3d/010-planning.md
finding_id=V79-01; severity=High; state=Open; occurrence=1; consecutive_count=1; disposition=planned_correction
finding_id=V79-02; severity=High; state=Open; occurrence=1; consecutive_count=1; disposition=planned_correction
finding_id=V79-03; severity=Medium; state=Open; occurrence=1; consecutive_count=1; disposition=planned_correction
finding_id=V79-04; severity=Medium; state=Open; occurrence=1; consecutive_count=1; disposition=planned_correction
finding_id=V79-05; severity=Medium; state=Open; occurrence=1; consecutive_count=1; disposition=planned_real_host_proof
finding_id=V79-06; severity=Low; state=Open; occurrence=1; consecutive_count=1; disposition=planned_correction

VERIFICATION:
check=planning_execution_scope; expected=read_only_inspection_plus_current_report; Gradle_runs=0; implementation_edits=0; test_runs=0
check=planned_red_green; expected=[shared_behavioral_failures,shared_green,real_renderer_failures,renderer_green]; owner=Coding; ordering=serial
check=planned_final_gates; expected=[ten_target_aggregate,full_build,independent_validation,scope_check,Operator_Notes_comparison]; owner=Coding_Verification_Validation
check=desktop_dependency_sources; expected=existing_host_API; observed=Compose_1.11.1_v2_API_and_raster_scene; executed=false

UNCERTAINTY:
missing=desktop_host_execution; ambiguity=runtime_viability_unproven; resolution=real_panel_test_or_exact_isolated_probe_failure; owner=Coding
missing=JS_DOM_and_Android_device_hosts; ambiguity=platform_widgets_unexecuted; accepted_scope=shared_tests_actual_source_compile_and_explicit_limits
missing=server_revision_and_credential_epoch; ambiguity=unobserved_equal_snapshot_round_trip; accepted_scope=observable_email_approval_and_session_transitions
missing=operator_answers; ambiguity=none; questions=0; blockers=0

REPETITION OF RESULT:
entity_id=issue_79_planning_cycle_2; stored_in=shared_memory; status=available; result=ready_for_architecture; open_findings=6; resolved_findings=0

COMMUNICATION:
sender=issue79_planning_cycle2; receiver=Orchestrator_and_Architecture; task_id=10.09.2026_16.32.33-2f63e0e4-5fa6-4bb0-819c-3c99ea86af3d; message_id=2f64b0ac-5f62-4325-9632-f36890aa5349; protocol=AML-HIP; next_role=Architecture

PERSISTENCE:
local_memory=true; shared_memory=true; index_keys=[task_id,issue_79_planning_cycle_2,six_findings,correction_plan,desktop_host]; storage=010-planning.md; auto_memory=false

VALIDATION:
format_valid=true; no_pronouns=true; entities_explicit=true; high_density=true; causal_chain_present=true; ambiguity_detected=true; ambiguity_resolution=host_execution_and_equal_snapshot_limits_explicit
```
