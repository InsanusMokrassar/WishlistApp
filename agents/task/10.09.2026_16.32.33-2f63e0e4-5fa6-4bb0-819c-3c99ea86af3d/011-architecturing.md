Model: GPT-6 (inherited high-capability Architecture session)
Changed files: agents/task/10.09.2026_16.32.33-2f63e0e4-5fa6-4bb0-819c-3c99ea86af3d/011-architecturing.md

# Correction architecture for issue #79

The correction is ready for Coding. Keep the existing replacement workflow and repair exactly the six findings accepted in Planning 010. Architecture began at clean `6a79d72624817f264acfd937b290bdfd6c75c5ad` on `feat/issue-79-user-email-change`. No implementation, test, feature documentation, dependency, or configuration file changed in this stage. No Gradle invocation or implementation test ran; the dependency and task-template inspection below establishes available APIs, not execution success.

The inherited high-capability model satisfies Architecture's HL-first preference: the decisions cross asynchronous profile reconciliation, private rendering, and independent test schedulers. The explicit assignment to write this report without nested agents controls the general documentation-delegation instruction. The caveman skill applies only to internal notes. The report and commit use normal prose.

V79-01 and V79-02 remain High and Open. V79-03, V79-04, and V79-05 remain Medium and Open. V79-06 remains Low and Open. Every finding retains occurrence count 1 and consecutive unresolved validation-cycle count 1 until independent Validation establishes resolution or another occurrence. Architecture and Coding must not count their own corrections as independent resolution. No severity escalation or new finding is introduced here.

## Evidence and applicable research

Architecture read the complete `AGENTS.md`, `agents/SHORTCUTS.md`, `agents/ALL.md`, available `agents/local.ALL.md`, `agents/ARCHITECTURE.md`, `agents/MODELS.md`, `agents/PROTOCOL.md`, `agents/GIT.md`, `agents/TOOLS.md`, `agents/AST_INDEX.md`, Verification and Validator instructions, task PROMPT, and reports 001–010. Other local instruction overrides were absent. The complete users feature README, including Operator Notes, shared editor ViewModel, three platform renderers, 48-case common email suite, root-save suite, and shared fixtures were read. Exact component, string, parser, delegation, repository, navigation, and Compose dependency sources were inspected at the responsible boundaries.

Code navigation began with `XDG_CACHE_HOME=/tmp/wishlist-issue79-ast-cache ast-index search UserEdit` and used AST searches/outlines for the ViewModel tests, `CalmTextField`, `emailVerificationUnavailable`, `Email.kt`, desktop Compose templates, and `ExposedUsersRepo`. The existing index contains 785 files, 6,570 symbols, 26,866 references, and 49 modules. Markdown and already located source files were read directly. No rebuild was needed for this report-only change.

Dependency investigation began under `/home/aleksey/projects/own/navigation`, using its existing temporary AST cache. `NavigationNode.config` reads `configState.value`; `ComposeNode.config` updates the mutable flow; `ComposeView` supplies no email or selected-target guard. The inspected `core/src/commonMain/kotlin/NavigationNode.kt`, `compose/src/commonMain/kotlin/ComposeNode.kt`, `mvvm/src/commonMain/kotlin/compose/ComposeView.kt`, and `mvvm/src/commonMain/kotlin/ViewModel.kt` have an empty diff against selected tag `v0.7.7`. The existing test node models the actual mutable config and resume/destroy surface sufficiently.

`features/ui/users/build.gradle` already applies `mppJvmJsAndroidWithCompose.gradle`, which applies `addComposeForDesktop.gradle`; the latter declares both `kotlin('test-junit')` and `compose.uiTest` in `jvmTest`, with `compose.desktop.currentOs` in main. Selected versions are Compose 1.11.1 and coroutines 1.11.0. The sources archive `/home/aleksey/.gradle/caches/modules-2/files-2.1/org.jetbrains.compose.ui/ui-test-desktop/1.11.1/bfd3df32a8ba135ec42aeb7e8eda1b7730410821/ui-test-desktop-1.11.1-sources.jar` contains the actual v2 desktop entry point, desktop implementation, both Skiko implementations, and `TextActions.kt`; those relevant files were read completely. The host creates a raster `Surface` and `CanvasLayersComposeScene`, supports real semantics/actions and explicit UI-thread execution, and owns scene disposal. `performTextReplacement` invokes `SetText`; `performImeAction` focuses an enabled field and invokes `OnImeAction`. None of these source facts claims that the host has run successfully here.

Current primary-source research supports keeping business state and events in the existing ViewModel while observing presentation state where rendered. Apply that principle to a guarded email section without creating another form-policy model or lifecycle dependency. Source: [Compose state hoisting](https://developer.android.com/develop/ui/compose/state-hoisting).

`StandardTestDispatcher` queues work until its scheduler is driven, and an omitted scheduler can inherit a mocked Main scheduler. Explicitly allocate the independent schedulers described below; keep one shared scheduler inside pure common tests. This adapts the documented scheduling behavior to the privacy counterexample. Source: [Kotlin StandardTestDispatcher API](https://kotlinlang.org/api/kotlinx.coroutines/kotlinx-coroutines-test/kotlinx.coroutines.test/-standard-test-dispatcher.html).

JetBrains identifies the v2 Compose test APIs as the current replacements. Use the already selected v2 desktop API; neither a version migration nor a dependency addition is justified. Exact signatures and scheduler restrictions come from the selected local source archive, not an assumed newer API. Source: [Compose Multiplatform 1.11.0 release](https://blog.jetbrains.com/kotlin/2026/05/compose-multiplatform-1-11-0/).

## Source ownership and preserved invariants

Shared changes belong only in `features/ui/users/src/commonMain/kotlin/ui/UserEditViewModel.kt`: private feedback bindings and clear/publication helpers; `applyOwnedEmailProfile`; failed-read handling in refresh/reconciliation; `isEmailDraftDirty`; `onEmailChanged`; the discard check in `onBack`; and the existing save/resend publication sites. Preserve public state types and callback signatures. No public API addition is required for renderer correctness because `userId`, capability, profile, and the passed node already exist.

Renderer changes belong in the existing `features/ui/users/src/jsMain/kotlin/ui/UserEditView.kt`, `features/ui/users/src/jvmMain/kotlin/ui/UserEditView.kt`, and `features/ui/users/src/androidMain/kotlin/ui/UserEditView.kt`. JVM gains one internal `OwnerEmailEditor` composable in that same file. Only `features/ui/users/src/commonMain/kotlin/UsersListStrings.kt` may additionally change, and only the unavailable-copy KDoc needs correction. Reuse all EN/RU strings and existing status enums.

Tests belong in `features/ui/users/src/commonTest/kotlin/ui/UserEditViewModelEmailTest.kt` and new `features/ui/users/src/jvmTest/kotlin/ui/UserEditEmailRenderTest.kt`. Reuse `UserEditTestUsersModel`, `UserEditTestNode`, and `RecordingUserEditInteractor`; their present handlers, ordered events, config flow, and lifecycle methods suffice. Change `UserEditTestFixtures.kt` only if a concrete missing fixture hook appears, with a narrowly documented purpose. Do not create a generic test host or an alternate email form.

Keep the acknowledged enabled save sequence PUT B, checked private GET B, optional POST B while pending, and checked final GET B. Disabled remains PUT B plus checked GET B, with zero POST. Keep exact case-sensitive `Email` equality and trimming, self-only bearer storage, repository approval reset, Boolean false/409/throw uncertainty, distinct storage and delivery outcomes, saved-A resend while draft B remains unsaved, existing owner generation/mutation identity/refresh version/raw session checks, deferred refresh coalescing, lifecycle cancellation, old-finally isolation, logout exit, and independent admin dirtiness. No API, schema, server production, transport, dependency, build, lock, navigation-framework, status-taxonomy, password, issue-78, or PR-81 change belongs in this correction.

## V79-01: Guard the actual renderer target

Every platform must independently collect the actual rendered node's `configState`. In JS and Android `onDraw`, collect `this@UserEditView.configState` into a local `currentConfig`. In the same recomposition compute eligibility from `canManageOwnEmail`, `currentConfig.userId == viewModel.userId`, and the immediate raw `this@UserEditView.config.userId == viewModel.userId`. The raw comparison also rejects stale collected config on an unrelated recomposition. Do not substitute another derived ViewModel Boolean, `remember` a target equality without current inputs, or compare against a newly rebound user ID.

The predicate encloses every private email node: saved address and approval, draft, missing/loading/recovery text, buttons, validation and operation errors, saved confirmation, verification outcome, delivery capability explanation, and email interruption placement. Keep the generic interruption notice address-free and inside the same current owner/target boundary. Initial root-other views have neither email content nor interruption copy. The ViewModel's eventual invalidation and imperative raw checks remain independently necessary.

In JVM extract the existing email section and its interruption notice into `@Composable internal fun OwnerEmailEditor(viewModel: UserEditViewModel, node: NavigationNode<UserEditViewConfig, ViewConfig>)` in `UserEditView.kt`. Move the email-related collections and the original Material widgets/callbacks into that function. Production `onDraw` invokes `OwnerEmailEditor(viewModel, this@UserEditView)` at the current email-section position. The helper itself collects `node.configState`, collects the owner visibility state, and checks both collected and raw target equality against the immutable `viewModel.userId` before emitting any private content. Tests pass the exact same node used to construct the ViewModel. No overload accepting only a ViewModel or a permission Boolean may bypass this guard.

Keep the existing parent Column layout: the extracted function emits the same email children and requires no new layout wrapper, theme, DI setup, avatar rendering, admin controls, or navigation destination. The JVM helper is the actual production rendering boundary used by tests. JS and Android keep inline email sections for the smallest correction. All save, resend, refresh, and Done callbacks still call the existing guarded ViewModel methods.

Add `Modifier.testTag("settings-email-saved")` and `Modifier.testTag("settings-email")` to the two real JVM fields to distinguish their editable text reliably. Find Save, Refresh, Resend, and feedback by the actual translated `UsersListStrings` values. Add another tag only if a demonstrated semantics ambiguity prevents selecting the real action; never add test-only state, synthetic results, or copied controls.

## V79-02: Bind feedback to the checked email and approval snapshot

Use one small private value `EmailFeedbackSnapshot(email: Email, emailApproved: Boolean)`, plus nullable `emailSavedSnapshot` and `emailVerificationSnapshot` fields. Replace `emailVerificationRecipient` with the verification snapshot. These are metadata for the existing public flows, not another policy model or a public status. A nullable metadata field means no bound publication; a non-null value contains a validated non-null recipient and its checked approval flag. The current generation/token already binds owner identity, so another epoch, timestamp, server revision, or owner-policy object is unnecessary.

At each successful same-owner profile application, compare both existing publication bindings against the incoming email and approval before exposing the replacement profile. Clear each superseded publication and its binding together. An incoming missing email matches neither non-null binding. Username and avatar changes are irrelevant. Comparing only the previous `_ownEmailProfileState.value` is incorrect because ordinary refresh sets that flow to null before GET. Independent metadata must survive refresh startup until checked reconciliation or explicit clearing.

Keep `clearEmailOperationFeedback` for an admitted new operation or an intentional materially changed draft. Extend it and `clearPrivateEmailState` to clear both bindings. Do not call it wholesale from profile application, failed GET handling, or ordinary refresh: `SaveFailed`, `InvalidEmail`, and a current negative delivery result require separate treatment. A narrow private success-clear helper may clear saved confirmation and positive verification feedback on unusable reads while retaining negative outcomes and local errors. Private helper names may follow repository conventions; the state and invalidation rules below are required.

Change `publishVerificationResult` privately to receive the exact checked profile used for the binding, in addition to the token, captured recipient, and result. Require current token/session and a matching binding recipient. For normal completed operations pass the successful matching final profile. If final reconciliation is unusable, publish only a real negative POST outcome, bound to the last checked matching profile captured by that operation: `firstProfile` for save, or the admission `profile` for resend. Never read a later mutable profile implicitly to construct that fallback binding.

Publish saved confirmation with the operation's actual last successful matching profile: the Disabled first GET, or the Enabled final GET. Preserve the final local profile in the save block so the publication cannot accidentally bind to the pending first GET when the final GET is approved. An acknowledged PUT alone is insufficient. `AlreadyApproved` still requires approved final recipient equality. `Sent` requires matching final recipient equality but does not itself assert approval. A newly returned `Sent` may coexist with a matching independently approved final record; its binding uses that final approval value.

The following paragraphs are the exact invalidation table. “Old” means published before the current checked transition; a local operation outcome is published only after its own final reconciliation and therefore is not an old banner at that application step.

An unchanged same-owner email/approval snapshot retains its old saved marker and verification outcome. Manual Refresh and node resume behave identically. Failed refresh startup is not evidence of a changed email: setting the profile flow to null does not itself compare against or destroy the binding. No successful recovery recreates a banner already cleared.

For same-email approved-to-pending, clear all old saved and verification publications, including `AlreadyApproved`, `Sent`, and earlier negative delivery outcomes. Show authoritative pending status. This covers an unobserved remote A-to-B-to-A mutation when the next observed A has reset approval. For same-email pending-to-approved, also clear old bound banners and show authoritative approved status. These transitions invalidate a historical snapshot even when the address string is unchanged.

For an observed A-to-B-to-A sequence, the first checked B clears A's old bindings; later A cannot restore them. For a checked different email or a same-owner record with no email, clear all old bound publications. Preserve the raw unsaved draft and recompute dirtiness. No attempt is made to detect a wholly unobserved round trip ending with the exact same email and approval; that would require a server revision outside scope.

For null or wrong-owner private records, never publish that record. Clear old completed success claims, set existing load failure, and preserve the current primary local error or negative delivery outcome with its last checked recipient binding. A transport/probe failure likewise must not retain a new claim of confirmed completion; clear old positive banners while preserving negative outcomes and the draft. Unknown or loading alone is not Disabled. On later usable recovery, compare retained negative metadata against the checked snapshot; equal preserves the failure, changed retires it. No positive banner is reconstructed from a GET alone.

For save final refresh, the new operation has already cleared prior operation feedback. Apply and validate the final record first, then publish the fresh result and saved binding. If approval changed during the active POST but the final recipient matches, retain the newly completed honest `DeliveryFailed` or `Unavailable` with the final snapshot; do not erase it by comparing against the pre-POST pending snapshot. The same rule covers the existing `NoEmail` and `EmailChanged` negative outcomes. A matching final record may support saved confirmation together with a real negative delivery result.

For resend final refresh, use the same ordering and final-snapshot binding, perform no PUT, and preserve the independent draft. If the final GET throws, returns null, or returns another owner, retain a newly completed negative result with the captured last matching snapshot and the existing load failure. Suppress `Sent`, `AlreadyApproved`, and saved success. Existing successful-final-read recipient mismatch behavior remains: set local `EmailChanged`, publish no success, and do not associate the old recipient's outcome with the replacement. An `AlreadyApproved` response with final pending email also remains local `EmailChanged` with no positive result.

For false or thrown PUT, keep `SaveFailed` through its own reconciliation and later refresh/recovery regardless of whether the refreshed address proves that an uncertain write committed. Snapshot invalidation must not clear this local operation error or invent automatic verification. An admitted new operation, a materially changed draft, or genuine owner/target invalidation is the existing explicit recovery boundary. Rejected busy/loading/unauthorized callbacks cannot clear any current primary error. All delayed publications retain the existing cancellation, token, generation, lifecycle, caller, authorization, and raw target checks.

## V79-03 and V79-04: Validate input and protect raw drafts

`isEmailDraftDirty(input, savedEmail)` becomes the direct comparison `input.trim() != savedEmail?.string.orEmpty()`. Parsing remains exclusively the canonical validation/save-admission mechanism. Missing storage plus `partial-address` is dirty; missing storage plus blank/whitespace is clean; stored A plus blank or invalid input is dirty; whitespace-wrapped exact A is clean; case-only changes retain current exact equality. No blank input clears a stored email because invalid dispatch still returns before PUT.

`onEmailChanged` first keeps `currentProfileForEmailMutation(false)` admission. For an ordinary admitted owner edit, store the raw input, recompute raw dirtiness immediately, and publish `InvalidEmail` immediately if the input is nonblank and `Email.parse` fails. Valid input and blank input clear an existing validation error; blank typing does not introduce an error until an attempted submit. `onSaveEmail` retains its imperative blank/invalid/unchanged guards, including defensive IME behavior.

Avoid turning a redundant field event into failure dismissal. Compare the previous and new trimmed raw drafts before clearing operation feedback. A materially different draft is an intentional new edit and clears completed feedback and the previous operation error, then installs the new validation state. An identical or whitespace-equivalent draft event may update raw bytes/dirtiness and clear only an obsolete `InvalidEmail`; it preserves an unrelated current `SaveFailed` or negative result. This is a narrow refinement of Planning's eligible-new-edit rule. No new public error channel is required because changing to malformed text makes the prior operation's draft obsolete before `InvalidEmail` is assigned. Normal refresh must not invoke the typing handler or simulate an edit.

An accepted invalid draft survives successful manual refresh, resume, failed refresh, recovery, resend, and external clearing of the saved address. Profile application preserves existing dirty input and recomputes the raw comparison against the new authoritative email. Existing valid normalization and uncertain-save reconciliation remain unchanged. Callbacks rejected due to busy/loading/failed capability/profile/raw owner mismatch leave draft and primary feedback untouched.

`onBack` must check `_profileDirtyState.value || _emailDraftDirtyState.value` directly. The public combined `isDirtyState` remains the same observable presentation state, but its collector may lag an immediate edit-then-Back event. Keep the existing dialog, confirm/cancel callbacks, root admin behavior, and logout bypass. This change only repairs discard admission; it does not change navigation policy.

JS already sends every `CalmTextField` input event to `onEmailChanged`, and the existing `InvalidEmail` branch renders `emailInvalid`. The `CalmForm` is a Div, so no implicit submit can be assumed. The live shared state correction makes the ordinary JS input path reach existing copy while Save stays disabled. No blur handler, Enter behavior, generic-form edit, DOM library, or second validator is needed.

## V79-06: Show confirmed Disabled guidance

Each renderer collects `emailCapabilityState` and derives unavailable guidance from exact `EmailCapabilityState.Disabled` while the self-owner private profile is loaded and current. Display existing `emailVerificationUnavailable` before any action as well as after a Disabled save. Do not infer Disabled from `!emailFeatureEnabled`, because Unknown/Loading/Failed share that Boolean value. The capability check lives inside the live-target private guard.

Keep the enabled server's real `Unavailable` result path. Render the unavailable text once when either the loaded Disabled predicate or the current `Unavailable` outcome requires it; do not duplicate it when both hold. The outcome can remain visible with a current load failure under the negative-outcome rules above. Use the existing EN/RU resource and Android resource-aware translation. Save remains enabled for a valid changed draft under confirmed Disabled, uses the save-only label, and performs PUT/GET; resend remains absent and the verification result stays null when no POST occurred. The renderer may derive the enabled label/presence directly from the collected capability to avoid collecting an additional lagging delivery Boolean; shared business eligibility still controls actions.

## Common regression specifications

Implement all additions in `UserEditViewModelEmailTest.kt` using fresh fixtures, `runTest`, the existing injected `StandardTestDispatcher(testScheduler)`, explicit `runCurrent`/`advanceUntilIdle`, and deferred barriers. Cancel `viewModel.scope` and release barriers in `finally`. Parameter loops must add scenario names to assertions. Assert public state, exact calls/events, and navigation rather than private metadata. Keep all 48 existing email tests, five root-save tests, and the model-delegation test.

`laterPendingRefreshClearsAlreadyApprovedFeedback` begins with pending A, legitimately obtains `AlreadyApproved` by having the request handler set approved A before final GET, then supplies pending A to a later manual refresh or real node resume. Assert initial positive publication, later pending status, null verification result, and no accidental requests. Include a save variant beginning at C and saving A so an old saved marker is also proved cleared.

`emailRoundTripDoesNotRestoreOldFeedback` covers two independent variants: checked A-approved, checked B-pending, checked A-pending; and remote B then A without an intervening GET, followed by checked A-pending. Begin with legitimately completed A feedback. Assert absence after the first observable superseding snapshot and after return. `approvalSnapshotChangesRetireCompletedFeedback` covers saved markers, `Sent`, and valid `AlreadyApproved` across relevant approval transitions and preserves publications across an unchanged exact snapshot, including a username-only change. Exercise both manual and resume refresh sources with fresh fixtures.

`laterSnapshotChangesRetireOldNegativeDeliveryFeedback` covers `DeliveryFailed`, `Unavailable`, `NoEmail`, and `EmailChanged` produced by save and resend. After a matching completed result, require unchanged recovery to preserve it, then a later checked address/approval change or missing email to retire it. `currentDeliveryFailureSurvivesReconciliationAndUnchangedRecovery` covers the same negative outcomes, POST exception mapping, matching final email with approval changed during the active request, and failed/null/wrong-owner final GET followed by unchanged recovery. Assert no premature publication while final GET is suspended, fresh negative retained afterward, no false positive on failed GET, and actual call ordering.

`unusableRefreshClearsOldSuccessWithoutErasingCurrentFailure` establishes legitimate saved/positive feedback, then separately supplies null profile, wrong owner, thrown private GET, and failed probe on later refresh. Require cleared success and existing load failure; negative-result variants retain their real failure until checked recovery retires or preserves the binding. `saveFailureSurvivesApprovalReconciliationAndRecovery` exercises false, throw-before-commit, and lost-response-after-commit variants with later approval changes and failed/recovered reads; `SaveFailed` persists, raw draft remains available, and no automatic POST or saved marker appears.

`invalidDraftPublishesFeedbackDuringTyping` covers missing and stored profiles with malformed and overlength nonblank drafts. Immediately after `onEmailChanged`, before any submit or scheduler drain, assert `InvalidEmail` and retained raw text. After collection, assert disabled Save and empty PUT/POST lists. Type valid changed B and then valid unchanged A where applicable; validation clears and eligibility follows the saved comparison. This method also runs on JS Node, closing the shared ordinary-input policy gap without claiming DOM interaction.

`equivalentDraftEventsPreserveCurrentOperationFailure` creates `SaveFailed` or a real negative resend result, sends identical and whitespace-equivalent draft events, and checks retained primary feedback and zero new requests. A materially changed valid draft clears prior failure; a materially changed invalid draft publishes `InvalidEmail`. Extend `emailChangeWhileBusyDoesNotReplaceSubmittedDraft` with error/result assertions and add `rejectedLoadingEditKeepsDraftAndPrimaryFailure` after a completed failure while a manual refresh is suspended. These tests cover both rejection and explicit-edit boundaries.

`missingEmailPartialDraftSurvivesRefreshAndResume` types `partial-address` with no saved email, verifies dirty state, then drives successful manual refresh and real resume; raw bytes, validation, and dirtiness survive. `partialDraftSurvivesFailedRefreshAndRecovery` repeats with thrown GET and successful recovery. `externalEmailClearPreservesInvalidDraft` begins with saved A, types invalid text, receives a same-owner profile with no email, refreshes again, and requires the partial text and dirty state throughout.

`rawDraftDirtinessControlsBackConfirmation` uses independent cases for absent-plus-invalid, saved-plus-blank, saved-plus-invalid, absent-plus-whitespace, whitespace-wrapped unchanged A, case-only changes, and independently dirty admin fields. Call `onBack` directly after the edit without advancing the combined-state collector. Dirty cases show the dialog and never navigate before confirmation; clean cases navigate once after driving the scheduler. Exercise cancel then confirm, and retain existing logout-with-dirty-input coverage. All invalid variants make zero PUT/POST even after defensive direct Save.

These cases exercise every modified shared callback/helper through its public behavior. Metadata clearing on genuine owner/target invalidation remains covered by all existing suspended retarget and leave-return cases; strengthen their saved/result absence assertions if needed, without replacing their current request-safety assertions.

## V79-05: Real desktop setup and renderer matrix

Create the actual `UserEditEmailRenderTest` class before running its filter. Use `androidx.compose.ui.test.v2.runDesktopComposeUiTest`, `ExperimentalTestApi`, the existing coroutines opt-in, and Material v2 `MaterialTheme`. The host content invokes the production `OwnerEmailEditor(viewModel, node)` inside an ordinary Column large enough to lay out the section. A 1024 by 1200 raster surface is sufficient initial sizing; layout or selector mistakes are test defects to fix, not host incompatibilities. Resolve expected text using the same existing translations; do not hardcode an environment-dependent locale assumption.

Allocate three explicit `TestCoroutineScheduler` instances per case: composition, test body, and ViewModel. Pass `StandardTestDispatcher(compositionScheduler)` as `effectContext` and `StandardTestDispatcher(testBodyScheduler)` as `runTestContext`. Inject `StandardTestDispatcher(viewModelScheduler)` into the real ViewModel. Do not wrap this API in another project `runTest`, install `Dispatchers.setMain` for these renderer cases, share the host scheduler with the ViewModel, or let an omitted scheduler inherit one accidentally. The dependency source explicitly forbids sharing composition and test-body schedulers.

Create/mutate the fixture and drive ViewModel work through `runOnUiThread`. For ordinary barriers, run `viewModelScheduler.advanceUntilIdle()` on that thread, then `awaitIdle()` or the supported Compose clock/idle mechanism so composition collects the public states and renders. For suspended mutations, use `viewModelScheduler.runCurrent()` and an entered `CompletableDeferred` assertion. A test-local synchronization helper may combine these explicit operations, but the stale-owner test must not invoke it during the critical interval. Do not directly drain the composition scheduler with `advanceUntilIdle` while its recurring render loop is active; let the host clock/idle API drive composition. Release deferred work and cancel the ViewModel scope in `finally`; the host handles composition/scene teardown.

`ownerEmailPanelRendersOnDesktop` is the first real host smoke test. Load a self-owner profile, set content with the production panel, and assert actual saved text, editable draft semantics, status, and Refresh. This proves the dependency can render the extracted panel; a ViewModel-only assertion is insufficient.

`savedApprovedAddressRemainsSeparateFromReplacementDraft` and `savedPendingAddressRemainsSeparateFromReplacementDraft` render saved A/status, use `performTextReplacement` to type B into the real draft field, and require A to remain in the saved field while B is editable and Save enables. Click the actual Save button, drive the ViewModel and host at barriers, then require B pending, old approval absent, saved feedback, and exact PUT/GET/POST/GET. Cover enabled and disabled capability through the focused Disabled case below. Preserve independently approved first/final refresh behavior through the existing common cases.

`disabledDeliveryExplainsSaveOnlyStorage` covers missing, pending, and approved owner profiles. Before POST or Save, assert unavailable copy exactly once, editable input, save-only text, and absent resend. Type valid B, click Save, and assert PUT/GET only, saved feedback, null verification outcome, and persistent unavailable explanation. `loadingAndFailedCapabilityDoNotClaimDisabled` suspends the probe, fails it, then recovers; Unknown/Loading/Failed never display the Disabled explanation merely because enabled is false. A real `Unavailable` result still displays its outcome under Enabled and does not duplicate the explanation if later Disabled is confirmed.

`typingInvalidEmailShowsFeedbackWithoutSubmitting` uses actual text semantics to enter malformed input, then asserts visible `emailInvalid`, disabled Save, and zero requests without invoking Save/IME. Valid B removes invalid copy and enables Save. `failedSaveKeepsDraftAndVisibleFailure` clicks Save with false and thrown handlers, requires saved A plus raw draft B, visible unconfirmed-storage copy, and no saved/Sent success. Repeat with a later refresh to prove error retention in actual widgets.

`approvedSavedEmailHasNoResend` requires absent resend for approved A. `resendUsesSavedAddressBesideReplacementDraft` types B beside pending A, invokes the production Resend button, and requires only POST A/GET, retained B, and actual result copy. `laterPendingRefreshRemovesAlreadyVerifiedCopy` creates legitimate `AlreadyApproved` through the real resend/save callback, then makes the real Refresh callback read pending A; require pending status and absence of old already-verified/saved copy.

`rootEditingAnotherUserHasNoPrivateEmailPanel` sets root true, caller different from the bound target, and renders the real panel with the same actual fixture node. Assert both field tags, all email actions/status/results, and interruption copy absent; private GET/PUT/POST remain empty. This tests renderer output in addition to the shared authorization suite.

`liveRetargetHidesPrivateEmailBeforeOwnerCollectorRuns` is mandatory V79-01 proof. First load self A, type draft B, and use real resend to obtain a current result for saved A while B remains dirty. Verify the real address, draft, result, and actions exist and `canManageOwnEmailState.value` is true. Record the stale profile/result/draft. On the host UI thread retarget that exact node to another ID, then drive only Compose config collection and rendering. Leave the ViewModel scheduler deliberately unadvanced. Assert that its owner eligibility remains true and old private profile/result/draft remain in the ViewModel, yet both field tags, all address text, all private status/error/result copy, and all email actions are absent from real semantics. Assert absence before any ViewModel drain. Additional variants establish saved confirmation/AlreadyApproved and SaveFailed before the retarget so those branches are also removed. Afterward drive the ViewModel and retain the existing private-clearing and request-suppression checks. A second fake node, helper-only Boolean assertion, or draining all schedulers before absence cannot close this finding.

`imeDoneUsesGuardedEmailSave` performs the real draft field's `performImeAction` with `ImeAction.Done`. Valid changed B issues exactly one PUT; invalid, blank, and unchanged enabled fields issue no PUT/POST. Use fresh fixture cases to avoid feedback from earlier actions. For busy proof, capture the real enabled field's `SemanticsActions.OnImeAction` action, start one save and suspend PUT, assert current field/Save disabled, then invoke the captured production semantics action on the UI thread. Require no second request. A test-library refusal to focus a disabled field proves disabled semantics only; it does not prove the production callback guard. Do not replace IME wiring proof with a direct `viewModel.onSaveEmail()` call.

If the smoke test fails, first fix routine imports, opt-ins, source-set visibility, scheduler separation, field selectors, sizing, fixture lifetime, and test timeouts within this scope. If an environmental or selected-dependency incompatibility remains plausible, add an actual minimal `desktopHostRendersMaterialText` probe in the same test class invoking v2 `runDesktopComposeUiTest` and real Material `Text`. Compile and run its exact existing filter. Record source, selected artifact, compilation result, actual executed name, full causal exception chain, command/log/exit, and why the failure cannot be corrected within the existing environment. A passing host probe means the panel/test defect must be fixed. A nonexistent filter is never a host attempt.

No host failure is established at Architecture time. A concrete irreducible host failure may use the previously authorized bounded fallback only with the exact evidence and missing semantics coverage explicitly carried to Orchestrator/Validation. Do not silently skip the renderer suite, keep a knowingly failing test while calling full Verification green, or introduce dependency/browser/device infrastructure. If a temporary diagnostic probe is removed, preserve its exact source and causal evidence in the Coding report. All ordinary shared behavior remains automatable. Existing authorization already accepts JS/Android compile plus state/static wiring evidence where no executable DOM/device host exists; no new operator permission is required for the specified desktop attempt.

## Serial red, green, and preservation gates

Before every Gradle invocation inspect actual host Java/Gradle processes and attribute any active work. Run invocations serially with `--no-parallel`, the standard project cache, and `--console=plain`. Do not run a second Gradle process while one is active, copy/redirect the operator cache, kill unrelated work, or alter lock state speculatively. Capture separate logs with shell pipefail and the real exit code. Count only fresh XML tied to that invocation's start; distinguish compilation failure, behavioral red assertions, host failure, and green execution. Names below are specifications, not reported executions or counts.

Stage 1 adds the new common regressions against unchanged production. Run `./gradlew --no-parallel --rerun-tasks --console=plain :wishlist.features.ui.users:jvmTest --tests '*UserEditViewModelEmailTest'`. Require behavioral red assertions in `laterPendingRefreshClearsAlreadyApprovedFeedback`, `approvalSnapshotChangesRetireCompletedFeedback`, `invalidDraftPublishesFeedbackDuringTyping`, `missingEmailPartialDraftSurvivesRefreshAndResume`, and `rawDraftDirtinessControlsBackConfirmation`, plus the related new cases that fail on current behavior. Resolve test compilation errors before recording red. Existing passing acceptance cases remain intact.

Stage 2 applies only the shared corrections and reruns that exact focused command to green. Every added variant must pass along with the preserved 48 cases. Do not proceed to renderer evidence with unresolved shared failures.

Stage 3 mechanically extracts the actual JVM panel and adds its two field tags and real renderer tests while retaining the existing missing target/capability behavior. Run smoke only after the method exists: `./gradlew --no-parallel --rerun-tasks --console=plain :wishlist.features.ui.users:jvmTest --tests '*UserEditEmailRenderTest.ownerEmailPanelRendersOnDesktop'`. Then run `./gradlew --no-parallel --rerun-tasks --console=plain :wishlist.features.ui.users:jvmTest --tests '*UserEditEmailRenderTest'`. Require real semantics red for `liveRetargetHidesPrivateEmailBeforeOwnerCollectorRuns` and `disabledDeliveryExplainsSaveOnlyStorage`; record exact failed assertions. The other renderer cases now prove shared corrections through actual widgets. A required diagnostic filter, only after source exists, is `*UserEditEmailRenderTest.desktopHostRendersMaterialText` with the same task/options.

Stage 4 applies all three direct renderer guards and Disabled guidance, then reruns the exact full `UserEditEmailRenderTest` focused command green. Complete the README/KDoc delta. Rebuild after Kotlin edits with `XDG_CACHE_HOME=/tmp/wishlist-issue79-ast-cache ast-index rebuild`; a final rebuild covers the final source tree. No rebuild is needed for subsequent markdown-only edits.

Stage 5 runs the platform compile/state gate serially: `./gradlew --no-parallel --rerun-tasks --console=plain :wishlist.features.ui.users:compileKotlinJs :wishlist.features.ui.users:compileKotlinJvm :wishlist.features.ui.users:compileDebugKotlinAndroid :wishlist.features.ui.users:jsNodeTest :wishlist.features.ui.users:testDebugUnitTest :wishlist.features.email.client:jsNodeTest`. The new ordinary typing and partial-draft cases must execute on JS and Android common-test targets. Review the real JS `CalmTextField.onInput` to `onEmailChanged` to `emailInvalid` branch and each actual platform config guard. Record that evidence as shared execution plus source wiring and compilation, not DOM/device interaction.

Stage 6 runs the ten-target aggregate: `./gradlew --no-parallel --rerun-tasks --console=plain :wishlist.features.ui.users:jvmTest :wishlist.features.ui.users:jsNodeTest :wishlist.features.ui.users:testDebugUnitTest :wishlist.features.ui.users:compileKotlinJs :wishlist.features.ui.users:compileKotlinJvm :wishlist.features.ui.users:compileDebugKotlinAndroid :wishlist.features.email.client:jvmTest :wishlist.features.email.client:jsNodeTest :wishlist.features.email.server:jvmTest :wishlist.features.users.common:jvmTest`. Users JVM must discover the real desktop suite when the host is established. Report new shared and renderer names/counts separately from the 273 earlier tests. Keep direct PUT-body/status/409, bearer caller/forged selector/duplicate route, repository reset/duplicate rollback/conditional approval, service, coordinator, and deeplink suites unchanged and passing.

Verification next runs the required full `./gradlew --no-parallel --rerun-tasks --console=plain build` under pipefail with a fresh log. Only if no test tasks execute may Verification run `./gradlew --no-parallel --rerun-tasks --console=plain allTests`. A failing required build/test is FAIL and returns to Coding; focused passes cannot replace it. JS browser execution counts as common logic unless real DOM interactions actually ran.

Independent Validation starts from reports after 009, carries all six original IDs/counts and their exact counterexamples, and inspects earlier reports as necessary for finding history and preserved acceptance. Independently rerun the ten-target aggregate above on the final candidate, inspect fresh XML and source names, and specifically reconstruct the stale-ViewModel scheduler interval, later same-address approval reset, real invalid input path, raw draft Back check, actual host attempt, and Disabled copy. Correctness requires independent disposition of each finding, not merely the absence of compiler failures.

Finish with `git diff --check`, AST usages of `configState`, `OwnerEmailEditor`, `onSaveEmail`, and feedback publication helpers; direct comparison of the three complete private guards and EN/RU reachability; exact source-scope review; and an Operator Notes byte comparison against both `6a79d72624817f264acfd937b290bdfd6c75c5ad` and original `cc8b63934086a41fb4c5965ce30b68775a67b67e`. Preserve PR #80's removed `Подтверждение email` subtitle. Once the required gates pass, do not broaden the scope or repeat unrelated checks without a new change/failure.

## README updates and correction of prior evidence

Coding updates only the affected Overview/Architecture Notes/test-coverage prose in `features/ui/users/README.md`. Preserve the complete Operator Notes section byte-for-byte. Document direct observation of the actual rendered node plus raw target comparison, snapshot-bound saved/result retirement including approval changes, immediate invalid typing feedback, parse-independent raw dirtiness and Back protection, confirmed Disabled delivery explanation, and the new production desktop panel test. State that pure ViewModel tests use one controlled scheduler, while the desktop privacy test deliberately separates ViewModel and host schedulers. Do not generalize desktop semantics evidence into Android keyboard or JS DOM proof.

Update affected ViewModel KDocs: immutable bound `userId`, result/saved snapshot binding, raw dirtiness, accepted/redundant/rejected edit behavior, current failure preservation, private profile application, and final publication rules. Document the internal JVM panel's mandatory passed-node guard and production/test reuse. Correct `emailVerificationUnavailable` KDoc to cover both confirmed Disabled capability and a real server outcome. No other declaration or feature README needs modification.

This report explicitly retracts the live-renderer guard claims in 006–008: the earlier compiled views did not directly observe their node's config or compare the target at the rendering boundary. It also retracts 006's claimed JVM host attempt and the unsupported unavailable-host conclusion carried through 007–008: “No tests found” for a nonexistent class proved only missing tests. Preserve the historical files unchanged. Coding and Verification must refer forward to actual correction/test evidence and avoid copying those claims.

The earlier 48 shared cases, HTTP/route/repository assertions, three-platform compiles, 273-test aggregate, and 898-test full build remain valid evidence for the behaviors actually executed. Their results do not establish the six missing behaviors, desktop host failure, widget interaction, or immediate rendering privacy. Fresh reports must distinguish retained proof from new proof with exact executed test names and limits.

## Stop condition, questions, and limits

Architecture stops after committing only this report, with no push and a clean worktree. No operator question or present blocker remains. Coding is authorized to implement and test the bounded correction. Completion requires all six findings independently disposed, shared and renderer behavioral red-to-green evidence, a real desktop panel suite or exact established host failure handled under the already accepted bounded limits, platform/aggregate/full Verification gates, accurate documentation, unchanged Operator Notes, and preserved issue-79 behavior.

The change does not guarantee live SMTP delivery, deployed-server behavior, physical Android IME behavior, browser DOM behavior, rollback of an accepted PUT, same-owner credential epochs, or atomic cross-client transactions. Equal email-and-approval round trips entirely unobserved by the client remain undetectable without an out-of-scope server version. An observed approval reset is detectable and must invalidate old claims. A real raster host test proves the production JVM widgets and callbacks, with the explicitly controlled scheduling described above.

## Structured architecture handoff

```aml-hip
ENTITY:
entity_id=issue_79_architecture_cycle_2; type=correction_architecture; state=ready_for_coding

CONTEXT:
task_id=10.09.2026_16.32.33-2f63e0e4-5fa6-4bb0-819c-3c99ea86af3d; agent_id=issue79_architecture_cycle2; memory_ref=[PROMPT,001,002,003,004,005,006,007,008,009,010,011]
constraints=[six_findings_only,report_only_architecture,no_nested_agents,no_push,serial_Gradle,Operator_Notes_preserved]; starting_head=6a79d72624817f264acfd937b290bdfd6c75c5ad; branch=feat/issue-79-user-email-change
exclusions=[API,schema,server,dependencies,build_configuration,lockfiles,status_taxonomy,navigation_framework,issue_78,PR_81]; implementation_scope=users_UI

ACTION:
action=specify_direct_renderer_guard; target=V79-01; params={nodes:actual_passed_configState,comparisons:[collected_userId,raw_userId,immutable_bound_userId],JVM_boundary:OwnerEmailEditor_with_node,private_markup:guarded}
action=specify_feedback_binding; target=V79-02; params={metadata:[email,emailApproved],public_flows:unchanged,later_snapshot_change:retire,current_negative:publish_after_reconciliation,SaveFailed:preserve}
action=specify_live_validation; target=V79-03; params={nonblank_invalid:immediate_InvalidEmail,valid_input:validation_recovery,equivalent_draft:preserve_primary_failure,invalid_dispatch:zero}
action=specify_raw_draft_protection; target=V79-04; params={comparison:trimmed_raw_versus_saved_string,refresh_resume:preserve_partial,Back:raw_dirty_flags,parse:save_admission_only}
action=specify_real_desktop_proof; target=V79-05; params={API:androidx_compose_ui_test_v2_runDesktopComposeUiTest,test_class:UserEditEmailRenderTest,schedulers:three_distinct,nonexistent_filter:forbidden}
action=specify_disabled_guidance; target=V79-06; params={source:confirmed_Disabled,copy:emailVerificationUnavailable,duplicates:zero,storage:enabled,synthetic_POST_result:forbidden}

REASON:
condition=lagging_owner_flow_after_retarget; requirement=private_semantics_absence; causal_chain=actual_node_observation_and_raw_comparison→guard_production_panel→V79-01_regression
condition=same_address_approval_reset; requirement=authoritative_feedback_consistency; causal_chain=checked_snapshot_transition→clear_old_bindings→V79-02_regression
condition=invalid_input_without_submit; requirement=visible_error_and_draft_preservation; causal_chain=live_parse_validation_and_raw_dirty_comparison→existing_error_and_discard_paths→V79-03_and_V79-04_regressions
condition=unestablished_desktop_host_claim; requirement=real_compiled_renderer_attempt; causal_chain=production_panel_test_source→existing_raster_host→V79-05_evidence
condition=Disabled_without_POST; requirement=visible_delivery_guidance; causal_chain=confirmed_capability→existing_copy_once→V79-06_regression

EXPECTED RESULT:
entity_id=issue_79_architecture_cycle_2; new_state=implementation_specified; location=agents/task/10.09.2026_16.32.33-2f63e0e4-5fa6-4bb0-819c-3c99ea86af3d/011-architecturing.md
finding_id=V79-01; severity=High; state=Open; occurrence=1; consecutive_count=1; resolution_owner=independent_Validation
finding_id=V79-02; severity=High; state=Open; occurrence=1; consecutive_count=1; resolution_owner=independent_Validation
finding_id=V79-03; severity=Medium; state=Open; occurrence=1; consecutive_count=1; resolution_owner=independent_Validation
finding_id=V79-04; severity=Medium; state=Open; occurrence=1; consecutive_count=1; resolution_owner=independent_Validation
finding_id=V79-05; severity=Medium; state=Open; occurrence=1; consecutive_count=1; resolution_owner=independent_Validation
finding_id=V79-06; severity=Low; state=Open; occurrence=1; consecutive_count=1; resolution_owner=independent_Validation

VERIFICATION:
check=architecture_execution; expected=inspection_and_report_only; Gradle_runs=0; implementation_test_runs=0; production_edits=0
check=planned_red_green; expected=[shared_red,shared_green,real_renderer_red,renderer_green]; test_files=[UserEditViewModelEmailTest.kt,UserEditEmailRenderTest.kt]; execution=serial
check=planned_preservation_gates; expected=[platform_compile_state,ten_target_aggregate,full_build,independent_Validation]; existing_email_cases=48; weakening_allowed=false
check=planned_render_race; expected=private_semantics_absent_with_stale_ViewModel; held_scheduler=ViewModel; driven_scheduler=composition
check=prior_report_correction; expected=forward_retraction_of_006_008_guard_and_host_claims; preserved_evidence=[compilation,shared_tests,HTTP,repository,aggregate,full_build]

UNCERTAINTY:
missing=desktop_host_execution; ambiguity=runtime_viability_unproven; resolution=real_panel_then_exact_host_probe_if_required; owner=Coding
missing=JS_DOM_and_Android_device_execution; ambiguity=platform_widgets_unexecuted; accepted_evidence=[common_state,actual_source_wiring,platform_compile]
missing=server_revision_and_credential_epoch; ambiguity=wholly_unobserved_equal_snapshot_round_trip; accepted_scope=observable_snapshot_and_session_changes
missing=operator_answers; ambiguity=none; questions=0; blockers=0

REPETITION OF RESULT:
entity_id=issue_79_architecture_cycle_2; stored_in=shared_memory; status=available; result=ready_for_coding; open_findings=6; independently_resolved_findings=0

COMMUNICATION:
sender=issue79_architecture_cycle2; receiver=Orchestrator_and_Coding; task_id=10.09.2026_16.32.33-2f63e0e4-5fa6-4bb0-819c-3c99ea86af3d; message_id=da8a88bc-7199-4350-8aa7-1e4b52c78ff6; protocol=AML-HIP; next_role=Coding

PERSISTENCE:
local_memory=true; shared_memory=true; index_keys=[task_id,issue_79_architecture_cycle_2,six_findings,correction_architecture,real_desktop_tests]; storage=011-architecturing.md; auto_memory=false

VALIDATION:
format_valid=true; no_pronouns=true; entities_explicit=true; high_density=true; causal_chain_present=true; ambiguity_detected=true; ambiguity_resolution=host_execution_and_unobserved_equal_snapshot_limits_explicit
```
