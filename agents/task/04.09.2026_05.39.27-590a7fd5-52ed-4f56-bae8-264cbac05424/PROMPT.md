# Task Prompt

Add an architecture rule requiring utility declarations—stateless, reasonably short, and serving one clear purpose—to live in a `utils` package. Then move `isUniqueViolation` into the `utils` package.
