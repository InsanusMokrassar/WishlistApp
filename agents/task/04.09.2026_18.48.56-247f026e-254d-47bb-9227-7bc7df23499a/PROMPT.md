# Task Prompt

Check the latest commit where the `gradle.properties` version was edited, compare that release with current `master`, change the version to `0.2.0`, increment the Android code version, and fill the `CHANGELOG.md` 0.2.0 section with changes made since the latest version. Do not create a separate version for these actions.
