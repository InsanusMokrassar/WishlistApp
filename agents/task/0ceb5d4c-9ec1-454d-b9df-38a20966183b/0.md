# STEP_0 PLAN

task_id=0ceb5d4c-9ec1-454d-b9df-38a20966183b; issue=23; title="Replace modes selectors in line if possible"
role=root/orchestrator; branch=issue/23-selectors-in-row; protocol=AML-HIP

## ENTITY
entity_id=selectors_layout; type=js-compose-html-ui; state=stacked_column
constraint=do_not_modify_selector_components; constraint=bootstrap_responsive_flex_only(local.CODING.md)

## CONTEXT
selectors=[WishlistSortSelector, CurrencySelector(conditional), ViewModeSelector]
container_files=2:
- file=features/ui/wishlist/src/jsMain/kotlin/ui/WishlistView.kt; selectors_at_lines=76..92; parent=Div(classes="container","py-3")
- file=features/ui/wishlist/src/jsMain/kotlin/ui/UserWishlistsView.kt; selectors_at_lines=78..93; parent=Div(classes="container","py-3")
root_cause: each selector renders own block-level Div(classes="d-flex","align-items-center","mb-3","gap-2","flex-wrap") → siblings stack vertically = column.

## PROBLEM_INVESTIGATION
- selector components forbidden to change → must add wrapping parent Div only around the 3 selector calls.
- CurrencySelector conditional (currencyEnabled && currencies.isNotEmpty()) → conditional must stay INSIDE new wrapper.
- child mb-3 margin: keep for column fallback; row spacing via gap on wrapper.

## PLAN
1. Wrap the 3 selector invocations (incl. conditional CurrencySelector) in WishlistView.kt with one parent Div.
2. Same in UserWishlistsView.kt.
3. Parent classes: d-flex flex-column flex-md-row flex-wrap gap-md-3 align-items-md-start mb-3
   - narrow(<md): flex-column → stacked (original behavior, wrap graceful)
   - >=md: flex-md-row + flex-wrap → row, wraps when no horizontal fit
4. Build module :wishlist.features.ui.wishlist:build (or jsBrowser compile). Fix max 1 cycle.
5. ast-index rebuild after source change.

## UNCERTAINTY
- none blocking. Breakpoint choice md is convention (issue: "if fit screen"). md=768px reasonable for 3 selectors.

## EXPECTED_RESULT
entity_id=selectors_layout; new_state=row_when_fit_else_wrap_column; files_changed=2
