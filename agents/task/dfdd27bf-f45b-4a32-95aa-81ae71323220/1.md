# STEP_1 — ARCHITECTURE

task_id=dfdd27bf-f45b-4a32-95aa-81ae71323220; issue=#44; prev=0.md(plan)
scaffold=DONE via ./generate_feature.sh email -> features/email/{common,server,client}

## MODULE: features/email/common (mppJvmJsAndroid)
deps: api features.common.common; +api features.users.common (for UserId in setEmail wire types)
files:
- models/Email.kt : `@Serializable(EmailSerializer::class) @JvmInline value class Email private constructor(val raw)`
  companion: `parse(String): Result<Email>` (non-throwing), `operator fun invoke(String): Email` (throws), `isValid(String): Boolean`
  validation: trim; reject blank; regex `^[A-Za-z0-9!#$%&'*+/=?^_\`{|}~.-]+@[A-Za-z0-9-]+(\.[A-Za-z0-9-]+)+$`; max length 254
  EmailSerializer: KSerializer<Email> serializing as primitive String (mirror WishlistItemLink custom serializer precedent)
- models/TestEmailRequest.kt : `@Serializable data class TestEmailRequest(val to: Email)`
- models/SetEmailRequest.kt : `@Serializable data class SetEmailRequest(val email: Email?)`
- Constants.kt : prefix "email"; sendTest "sendTest"; myEmail "myEmail" (self set)

## MODULE: features/email/server (mppJavaProject, JVM-only)
deps: +api features.users.common (UsersRepo for self email update), +api features.auth.server (caller id util), +api features.admin... NO. Root check via usersRepo username=="root" inline (admin server NOT depended to avoid coupling; replicate requireAdmin like AdminRoutingsConfigurator).
   -> simpler: depend api features.auth.server for getCallerUserIdOrAnswerUnauthorized + api features.users.common for ReadUsersRepo/WriteUsersRepo
dependency added: angus-mail (Jakarta Mail impl) -> catalog `angusMail`. coordinate org.eclipse.angus:angus-mail
files:
- EmailConfig.kt : @Serializable data class EmailConfig(smtp: SmtpConfig? = null) decoded from root JSON (mirror CurrencyConfig pattern). SmtpConfig(host, port, username, password, from: Email or String, useTls, useSsl). null/blank host => feature no-op disabled.
- EmailFeature.kt (commonMain server) : interface { suspend fun sendEmail(to: Email, subject: String, body: String): Boolean }
- services/JavaMailEmailService.kt : implements EmailFeature using jakarta.mail Session/Transport. Disabled when config null -> returns false + logs warn.
- configurators/EmailRoutingsConfigurator.kt :
  - POST /email/sendTest (root-only, requireAdmin via usersRepo) body TestEmailRequest -> sendEmail -> 200/500/403
  - PUT  /email/myEmail (authenticated, any logged user) body SetEmailRequest -> update own user's email via WriteUsersRepo -> 200/401
- Plugin.kt : decode EmailConfig, single JavaMailEmailService bound EmailFeature, register routing configurator singleWithRandomQualifier
- JVMPlugin.kt : delegate common.JVMPlugin + Plugin (scaffold already correct)

## MODEL CHANGE: features/users (persistence of optional user email)
- features/users/common/build.gradle : +api features.email.common (for Email type)  [users.common -> email.common dependency; email.common -> users.common ALSO (TestEmail uses UserId)]
  CYCLE RISK: users.common<->email.common. RESOLUTION: keep Email in email.common; users.common depends email.common. email.common must NOT depend users.common. So SetEmailRequest uses no UserId (self-service from caller token). TestEmailRequest uses only Email. => email.common deps only common.common. No cycle.
- models/User.kt : add `val email: Email?` to User sealed iface + NewUser + RegisteredUser (nullable default null -> existing callers compile? NewUser(username) call sites exist -> add default = null to keep source compat)
- repo/ExposedUsersRepo.kt : + nullable text column "email"; asObject maps Email?(parse->getOrNull); update sets email?.raw; insert asObject carries value.email
- nullable column => Exposed createMissingTablesAndColumns adds it; existing rows get NULL. Safe.

## CLIENT: features/email/client (mppJvmJsAndroidWithCompose scaffold)
deps: +api features.common.client
- EmailFeature.kt : interface { suspend fun sendTestEmail(to: Email): Boolean; suspend fun setMyEmail(email: Email?): Boolean }
- KtorEmailFeature.kt : HTTP POST /email/sendTest, PUT /email/myEmail (Ktor-only, no extra logic)
- Plugin.kt : single KtorEmailFeature bound EmailFeature

## UI: features/ui/adminPanel (root test-email action)
- build.gradle : +api features.email.client
- AdminPanelModel : + suspend fun sendTestEmail(to: String): Boolean (parse Email, delegate)
- Plugin.kt model impl : inject EmailFeature, implement sendTestEmail
- AdminPanelViewModel : + testEmailAddressState, + sendingState, + testEmailResultState, onTestEmailAddressChanged, onSendTestEmail
- AdminPanelView (js/jvm/android) : input + button + result text
- AdminPanelStrings : + testEmailSection/label/sendButton/success/failure/invalid

## WIRING
settings.gradle: +":features:email:common", ":features:email:server", ":features:email:client"
server/build.gradle: +api features.email.server
sample.config.json: +"...email.server.JVMPlugin"; +"smtp": {host,port,username,password,from,useTls,useSsl} block (nullable/sample)
client/build.gradle commonMain: +api features.email.client
client/android/build.gradle commonMain: +api features.email.client
client Main.kt x3 (js/jvm/android): + email.client J*Plugin
README.md for features/email + update adminPanel/users README Architecture Notes (haiku rule noted; doing inline to keep build correct)

VERIFY tasks:
- ./gradlew :wishlist.features.email.common:build :wishlist.features.email.server:build
- ./gradlew :wishlist.features.users.common:build
- ./gradlew :wishlist.server:build
- email.client + adminPanel compile (metadata/jvm). JS full build heavy -> compile commonMain/jvm at least.
