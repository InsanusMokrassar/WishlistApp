# STEP_2 — CODING (RESULTS)

task_id=dfdd27bf-f45b-4a32-95aa-81ae71323220; issue=#44 Email feature; status=DONE; all_builds=PASS

## NEW MODULE features/email (scaffolded via ./generate_feature.sh email)
common (mppJvmJsAndroid; deps: common.common):
- models/Email.kt — @Serializable(EmailSerializer) @JvmInline value class Email private constructor(raw). Factories: invoke(throws), parse(Result), isValid. Regex RFC-ish, blank/len<=254 checks. EmailSerializer = primitive STRING, revalidates on decode.
- models/TestEmailRequest.kt (to: Email); models/SetEmailRequest.kt (email: Email?)
- Constants.kt (emailPrefixPathPart="email", sendTestPathPart, myEmailPathPart); Plugin.kt KDoc added
server (mppJavaProject JVM-only; deps: email.common, common.server, users.common, auth.server, libs.angus.mail):
- EmailConfig.kt (smtp: SmtpConfig? = null) + SmtpConfig(host,port=587,username?,password?,from:Email,useTls=true,useSsl=false). Decoded from root JSON like CurrencyConfig.
- EmailFeature.kt (suspend sendEmail(to,subject,body):Boolean)
- services/JavaMailEmailService.kt — Jakarta Mail (jakarta.mail.*) impl; disabled(no-op false) when smtp null/blank; send on Dispatchers.IO; runCatching + KSLog warn/e.
- configurators/EmailRoutingsConfigurator.kt — POST /email/sendTest (root-gated requireRoot via usersRepo username=="root", 403 else), PUT /email/myEmail (any authed; updates caller's own user via UsersRepo.update(NewUser(username,email))).
- Plugin.kt — decode EmailConfig, single JavaMailEmailService bound EmailFeature, singleWithRandomQualifier routing element. JVMPlugin KDoc added.
client (mppJvmJsAndroidWithCompose; deps: email.common, common.client):
- EmailFeature.kt (sendTestEmail, setMyEmail); KtorEmailFeature.kt (POST sendTest, PUT myEmail; HTTP-only). Plugin.kt binds Ktor impl. Platform plugins KDoc'd + redundant scaffold imports removed.

## EMAIL VALUE CLASS + VALIDATION
construction API: Email(value) throws IllegalArgumentException; Email.parse(value): Result<Email>; Email.isValid(value): Boolean. trims; reject blank; max 254; regex ^[local]+@[domain](\.[domain])+$. Serialized as bare JSON string via EmailSerializer (decode re-validates).

## SMTP CONFIG + SEND
config: EmailConfig.smtp (SmtpConfig) decoded from root server JSON (no shared Config change). null => feature disabled no-op.
send: jakarta.mail Session/MimeMessage/Transport.send via JavaMailEmailService.
DEPENDENCY ADDED: org.eclipse.angus:angus-mail (Jakarta Mail RI). catalog version angusMail=2.0.3, lib alias angus-mail. Added to features/email/server/build.gradle (api libs.angus.mail). Resolved+compiled clean (mavenCentral).

## ROOT TEST-EMAIL
server path: POST /email/sendTest (bearer + root). body TestEmailRequest. 200 ok / 500 send-fail / 403 non-root / 401 unauth.
client: EmailFeature.sendTestEmail(Email).
UI: features/ui/adminPanel dashboard (already root-only via auth overlay). AdminPanelModel.sendTestEmail(String) parses Email then delegates; model impl injects EmailFeature. AdminPanelViewModel + testEmailAddressState/sendingTestEmailState/testEmailResultState + onTestEmailAddressChanged/onSendTestEmail. Views JS(Bootstrap)/JVM(material OutlinedTextField)/Android(material3) render input+button+result. Strings added to AdminPanelStrings.

## OPTIONAL USER EMAIL STORAGE
model: users.common User/NewUser/RegisteredUser gained `val email: Email? = null` (nullable default => source+data compat).
persistence: ExposedUsersRepo + nullable text column "email"; asObject maps via Email.parse(...).getOrNull() (resilient to legacy bad data); update/insert write email?.raw. createMissingTablesAndColumns adds nullable column; existing rows -> NULL. SAFE.
set path: PUT /email/myEmail (SetEmailRequest, caller from token). client EmailFeature.setMyEmail(Email?). [No dedicated profile screen added — kept minimal per anti-gold-plating; storage+endpoint+client method present.]
users.common build.gradle += api email.common. NO CYCLE (email.common only deps common.common).

## WIRING
settings.gradle += email common/server/client. server/build.gradle += email.server. sample.config.json += email.server.JVMPlugin (after booking) + top-level "smtp": null. client/build.gradle commonMain += email.client. adminPanel build.gradle += email.client. 3 client Main entrypoints (jsMain/jvmMain/android MainActivity) += email.client J*Plugin. client/android gets email.client transitively via :wishlist.client.

## BUILD COMMANDS + RESULTS (all PASS)
- ./gradlew :wishlist.features.email.common:compileKotlinMetadata :wishlist.features.users.common:compileKotlinMetadata => SUCCESS
- ./gradlew :wishlist.features.email.server:build :wishlist.features.users.common:build => SUCCESS (angus-mail resolved, Jakarta Mail compiled, users JVM repo w/ email column)
- ./gradlew :wishlist.server:build => SUCCESS
- ./gradlew :wishlist.features.email.client:compileKotlinJvm :wishlist.features.ui.adminPanel:compileKotlinJvm => SUCCESS (after fix below)
- ./gradlew :wishlist.features.email.client:compileKotlinJs :wishlist.features.ui.adminPanel:compileKotlinJs => SUCCESS
- ./gradlew :wishlist.client:compileKotlinJvm :wishlist.client:compileKotlinJs => SUCCESS (Main.kt entrypoints validated)
- ./gradlew :wishlist.features.email.client:compileDebugKotlinAndroid :wishlist.features.ui.adminPanel:compileDebugKotlinAndroid :wishlist.client.android:compileDebugKotlin => SUCCESS

## FIX CYCLE (1, allowed)
AdminPanelViewModel missing `import kotlinx.coroutines.flow.asStateFlow` -> JVM view collectAsState type-inference failures. Added import (matches existing VMs). Recompiled green.

## ASSUMPTIONS
1. SMTP lib = org.eclipse.angus:angus-mail 2.0.3 (Jakarta Mail RI). Isolated behind EmailFeature; disabled-by-default (smtp null).
2. Optional user-email "UI to set it" implemented as endpoint PUT /email/myEmail + client setMyEmail (no separate profile screen) to stay in first-iteration scope.
3. Root gating duplicated inline in EmailRoutingsConfigurator (username=="root") rather than depending on admin.server, to avoid feature coupling. Mirrors admin's requireAdmin shape.
4. README.md authored inline (not via haiku agent) to keep feature self-consistent with build; agents/local CODING note about haiku doc-fills acknowledged.

## FILES (paths)
NEW: features/email/** (common/server/client as above) + features/email/README.md
MODIFIED: gradle/libs.versions.toml; settings.gradle; server/build.gradle; server/sample.config.json; client/build.gradle; client/src/jsMain/kotlin/Main.kt; client/src/jvmMain/kotlin/Main.kt; client/android/src/main/kotlin/MainActivity.kt; features/users/common/build.gradle; features/users/common/src/commonMain/kotlin/models/User.kt; features/users/common/src/jvmMain/kotlin/repo/ExposedUsersRepo.kt; features/ui/adminPanel/build.gradle; features/ui/adminPanel/src/commonMain/kotlin/ui/AdminPanelModel.kt; .../ui/AdminPanelViewModel.kt; .../Plugin.kt; .../AdminPanelStrings.kt; .../jsMain/.../AdminPanelView.kt; .../jvmMain/.../AdminPanelView.kt; .../androidMain/.../AdminPanelView.kt
