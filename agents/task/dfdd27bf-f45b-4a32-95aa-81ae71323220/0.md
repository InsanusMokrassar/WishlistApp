# STEP_0 — PLAN

task_id=dfdd27bf-f45b-4a32-95aa-81ae71323220; issue=#44 "Email feature"; role=root/orchestrator
constraints=[first_iteration_only, match_existing_style, no_git, no_PR, build_to_verify]

## ENTITY scope
entity=feature/email; layout=common+server+client (full-stack feature, mirror features/currency + features/admin + features/auth)
entity=Email; type=@JvmInline value class; location=features/email/common; validation=RFC-ish, reject blank/malformed
entity=SMTP_config; location=features/email/server (EmailConfig decoded from root JSON, mirror CurrencyConfig)
entity=test_email; auth=root-only; route=POST /email/sendTest; gating=mirror AdminRoutingsConfigurator.requireAdmin()
entity=user_email; type=optional/nullable; storage=add nullable email column to ExposedUsersRepo + RegisteredUser/NewUser model

## DECISIONS
1. Email value class -> features/email/common/commonMain/models/Email.kt
   - `@Serializable(with=EmailSerializer) @JvmInline value class Email private constructor(val raw: String)`
   - companion `operator fun invoke(String): Email` (throws IllegalArgumentException on invalid) + `parse(String): Result<Email>` (non-throwing)
   - validation: not blank, matches simple RFC-ish regex `^[^@\s]+@[^@\s]+\.[^@\s]+$`, length bound
   - custom KSerializer so wire form stays a plain string (mirror WishlistItemLink custom serializer precedent, commit 41fbf97)
2. SMTP send: add Jakarta Mail (Angus Mail) dependency to version catalog. Risk assessment:
   - dependency clean-add via TOML catalog == low risk; server module is JVM-only (mppJavaProject) so jakarta.mail import is safe
   - candidate: org.eclipse.angus:angus-mail (Jakarta Mail 2.x reference impl, single coordinate pulls jakarta.mail-api)
   - isolate behind EmailFeature interface; if dependency resolution fails at build -> fall back to no-op + report
3. user_email storage: add `email: Email?` to User model (NewUser, RegisteredUser) + nullable text column in ExposedUsersRepo.
   - nullable keeps existing rows/data unaffected (Exposed createMissingTablesAndColumns adds nullable column)
   - endpoint+UI: minimal — users feature already has UsersFeature; add setEmail route OR include in admin user edit. SCOPE: store + admin can set via existing admin update path by extending NewUser. Keep optional.
4. test-email UI: add action to features/ui/adminPanel AdminPanel dashboard (root-only screen already gated by auth overlay). Add EmailFeature client + AdminPanelModel.sendTestEmail(to) + button on AdminPanelView (js/jvm/android).

## REGISTRATION POINTS
- settings.gradle: +3 includes (email common/server/client)
- server/build.gradle: +api project(email.server); sample.config.json: +EmailFeature server JVMPlugin + smtp config block
- client/build.gradle (jvm+js+android commonMain) + client/android: +api project(email.client)
- 3 client Main entrypoints: +email.client JS/JVM/Android plugins
- adminPanel build.gradle: +api project(email.client)

## RISKS / OPEN ITEMS
- angus-mail availability in repo set (mavenCentral expected) — verify at build; fallback documented
- user-email "endpoint/UI to set it": minimal first-iteration = extend NewUser with optional email so admin user-edit + self update paths carry it; no separate dedicated screen to avoid gold-plating. Documented as assumption.

VERIFICATION: ./gradlew compile of email common/server/client + server + client commonMain (js full build heavy; compile common+server mandatory)
