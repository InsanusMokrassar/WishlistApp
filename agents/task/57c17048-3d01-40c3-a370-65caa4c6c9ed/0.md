# STEP_0 Planning — Issue #39 placeholder images

ENTITY: task_id=57c17048-3d01-40c3-a370-65caa4c6c9ed; issue=#39; state=planned
SCOPE: default(placeholder) images for user/wishlist/wishlist-item, all 3 client targets (js/jvm/android), shown whenever no image set.

## Task restatement
- user placeholder = gray silhouette profile photo.
- wishlist-item placeholder = present/gift box look.
- wishlist placeholder = several wishlist-items stacked, each offset by small x/y.
- placeholders MUST render as default whenever image NOT set for the entity.

## Verified facts (ast-index + reads)
- KMP, 3 UI targets: jsMain(Compose HTML/Bootstrap, `Img`), jvmMain(Compose Desktop, `androidx.compose.material`), androidMain(Compose `material3`).
- Image render helpers:
  - JS: `Img(src = viewModel.imageUrl(id))`.
  - JVM/Android: `RemoteImage(key,loader,contentDescription,modifier)` — `features/ui/users/...ui/RemoteImage.kt` and `features/ui/wishlist/...ui/RemoteImage.kt` (4 copies). `RemoteImage` renders NOTHING on null/loading/failure.
- USER avatar (`features/files` `UserAvatarsRepo.getAvatar(userId): FileId?`, null=unset):
  - JS: `UsersListView` (list leading) already renders gray-circle Div fallback; `UserView`, `UserEditView` render `Img` only inside `avatarId?.let`.
  - JVM/Android: `UsersListView` list leading already renders gray `Box` fallback; `UserView`/`UserEditView` render `RemoteImage` only inside `avatarId?.let`.
- WISHLIST-ITEM image (`RegisteredWishlistItem.imageIds: List<FileId>`, empty=none):
  - JS: `WishlistItemView` (no-image → text "no images"), `WishlistItemCard` (media only when firstImage!=null), `UserWishlistsView.ItemRow` (gray Div fallback), `WishlistItemEditView` (preview list).
  - JVM/Android: same set; `WishlistItemCard` media only when firstImage!=null; `UserWishlistsView.ItemRow` gray `Box` fallback; `WishlistItemView`/`EditView` preview lists.
- WISHLIST entity: `RegisteredWishlist` has NO image field (only id,userId,title,defaultPriceUnits). `WishlistsListView` (all 3 platforms) renders wishlists as PLAIN text rows via `ListRow(text=...)` — NO leading thumbnail at all currently.
- `ListRow` (common/client `ui/components/ListComponents.kt`, all 3 platforms) supports a `leading: @Composable` slot → safe injection point for the wishlist stacked placeholder thumbnail in `WishlistsListView`.

## Product interpretation (resolved from issue text, NOT blocking)
- Wishlist has no image field → wishlist placeholder is ALWAYS shown (every wishlist) as the leading thumbnail in `WishlistsListView`. Issue text ("looks like several wishlist items stacked") makes the intent unambiguous. No new persisted wishlist-image feature is in scope; only the visual placeholder.
- Existing gray `Box`/`Div` fallbacks for user-avatar & item-thumbnail are upgraded to the proper silhouette / gift-box drawn placeholder. Detail screens (`UserView`, `WishlistItemView`) currently render NOTHING when empty → add placeholder there too.

## Planned changes (high level; details in STEP_1)
1. Shared cross-platform placeholder source per entity type (silhouette / gift box / stacked) — strategy to be fixed in STEP_1 (candidate (a): Compose-drawn vectors in commonMain; candidate (b): static assets). Strong lean toward (a) Compose-drawn `ImageVector`/`Canvas` in each feature's `commonMain` so one source covers all 3 targets with no asset-serving infra.
2. users feature: render user placeholder when avatar FileId == null in `UsersListView`, `UserView`, `UserEditView` (×3 platforms).
3. wishlist feature: render item placeholder when no image in `WishlistItemView`, `WishlistItemCard`, `UserWishlistsView.ItemRow` (×3); render wishlist stacked placeholder leading in `WishlistsListView` (×3).
4. Add localized alt/contentDescription strings (EN+RU) via buildStringResource.
5. KDocs on all new .kt symbols; JS stylesheet object if custom CSS needed.
6. Build `:wishlist.features.ui.users:build` + `:wishlist.features.ui.wishlist:build` (+ any common module touched). One fix cycle.
7. `ast-index update`.

## Open questions
- none blocking. Proceeding to STEP_1 Architecturing.

## Verification
- check=all-3-entities-covered; expected=true
- check=all-3-platforms-covered; expected=true
- check=placeholder-shown-when-image-null; expected=true
