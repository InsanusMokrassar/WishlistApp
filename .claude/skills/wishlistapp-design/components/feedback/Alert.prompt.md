One-line: Compact Bootstrap alert banner — success confirms queued copies, danger flags failed actions.

```jsx
<Alert variant="success">Copy queued. It will appear in your profile shortly.</Alert>
<Alert variant="danger">Copy failed. Please try again.</Alert>
```

Defaults to the compact `py-2` inline-banner style the app uses above lists.
