One-line: Generic Bootstrap badge for small counts and labels (for item priority use PriorityBadge instead).

```jsx
<Badge bg="primary">New</Badge>
<Badge bg="success-subtle" pill>3</Badge>
```

`bg` takes a Bootstrap color or a `*-subtle` name (which auto-pairs the matching emphasis text color). `pill` gives the rounded shape.
